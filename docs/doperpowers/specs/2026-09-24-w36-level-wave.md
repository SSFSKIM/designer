# W36 — the level wave: the body's tone at black, its curve's middle, and its chroma (2026-09-24)

**Status: CHARTERED 2026-09-24, v1 — sent to adversarial review before any child opens.** Chartered
by the parent on the user's "W36 level wave (Recommended)" after W35 closed at its qualified negative
(main `5d0fedf0`), under the standing "rest on your judgement" and the routing the user set on
2026-09-22 (X9, with the Codex-limit fallback). Grounded on a read-only memo taken off W34's bed and
archive through W35's guarded readers, the canonical solids at a checked generation, and the
runtime's sources (`/Users/new/.claude/jobs/17c7ce02/tmp/w36-grounding.md`, a default-worker
product; every number it carries is reproduced by G0 into this wave's evidence before anything is
fitted).

## Purpose

W35 G0a (§5.177) read, on W34's uniform greys and chromatic solids, that vitrea's deep body — well
inside the edge, where no rim, lens or scatter acts — misses Apple's LEVEL by more than the edge
line it was looking for: **+49 codes on light black, +28 on dark black**, +3…+8 on light mid-greys,
−1…−7 on dark mid-greys, and **up to 111 (active) and 144 (inactive) codes on saturated channels**.
The memo found three mechanisms, not one wrong curve:

1. **Black is a fallback, not a curve point.** The tone solve's authority is
   `smoothstep(anchor/2, anchor, x)` with the first knot's abscissa 0.004 (`wgsl/optics.ts` ~1168;
   mirrored on the CSS tier at `platform-web/src/optics.ts` ~616). At a pure-black backdrop it is
   exactly 0, the solve is off, and the body falls back to `sizedAlpha · neutral` — **181 codes on
   light, 60 on dark**, which is the web reading to the code and the whole of the black miss. The
   fade was never a fitted law; the first knot was fitted on `impulse` (a structured backdrop) or
   floored at 0 (the dark receded document, whose unexplained +0.0475 floor in §5.154 §6 is, by
   arithmetic, probably this same fallback). Restoring authority alone would render −4 / −20 / +21 /
   −20 codes against native (light active / light inactive / dark active / dark inactive): the knot
   itself must be fitted on uniform black.
2. **The curve's middle sits where `photo` put it.** The shipped response reproduces all 48
   non-black web medians on the bed within 0.5 code — the web body's luma IS the curve — and its
   third knot (0.425, fitted on `photo`, a chromatic structured backdrop, W29 G3) sits +0.05 linear
   above Apple's uniform-grey curve at the same input. Structure alone barely moves Apple's level
   (the neutral checkerboard reads within 1–3 codes of grey-128), so the excess is either photo's
   chroma coupling into luma or a knot in the wrong place; the greys decide.
3. **The chromatic miss is a chroma SCALE, pose-independent on Apple's side.** Apple keeps the
   backdrop's linear chromaticity whole on the dark scheme (implied retention 0.95–1.03) and at
   0.86–0.93 on the unclipped light cells, the same in both poses and holding hue within 0–2°;
   vitrea's implied retention is 0.17–0.60, differs by pose, and its mixing toward grey at low
   retention is what rotates hue. The receded pose's abscissa (the silhouette's per-channel
   encoded sum, W28) reads saturated colours about 0.13 low and compounds the dark inactive misses.
   Two limits bound what can be identified: light red, blue and magenta, dark-active blue and every
   macOS 27 `mid-chroma-solid` cell are **censored at 255**, and on the dark scheme even full
   retention leaves Apple's chromatic cells **5–28 codes brighter** than any luminance-keyed curve
   predicts — a form nobody has identified. And the likeliest stop: the solids want retention
   0.9–1.0, which at M1's measured slopes (§5.164 §4) pushes its chroma-to-structure ratio to about
   2.8 (light) and 4.9 (dark), far outside [0.8, 1.2]; M1 reads chroma VARIATION over `photo`, the
   solids read MEAN chroma, so the missing operator may act on the plate's mean colour rather than
   being more per-pixel retention.

Scale and shape drop out (all 104 solid cells read identical medians at 1x and 2x; every span-44
geometry agrees within a code; the thick row rests on the canonical `rrect-md` solids and W34's
`continuous-160`). The frozen macOS 26.5 documents share the chroma defect (their receded
`mid-chroma-solid` misses −67 / +16 / −79 on dark) and cannot be checked at black; they do not move.

The purpose is Apple's body level on macOS 27 — at black, across the greys, and in chroma — closed on
the WebGPU tier under every stop the ledger carries with a level row of its own adopted, the CSS tier
deriving what it can, the frozen bed untouched, and every gap left written down.

## Parent-Level Acceptance

1. **The cut precedes the fit, with the mechanisms reproduced.** G0 reproduces the memo into
   committed scripts through W35's guarded readers: web − native at the deep body per scheme × pose ×
   scale × level and colour, per channel, encoded and linear; the shipped response re-evaluated on
   the bed (48 of 48 non-black medians within 0.5 code) and the authority fallback reproduced at black
   (181 / 60); Apple's tone curve against vitrea's per scheme and pose; the chroma decomposition
   (implied retention per cell, hue rotation, the receded abscissa's shortfall); the censoring rule
   (a channel at 255 on either side is excluded from every fit and named); the dark chromatic luma
   excess tabled; the M1-versus-solids arithmetic; the canonical solids at a checked generation
   beside the bed. G0 declares every stop with its expected post-fit value and **adopts-in-
   declaration the wave's own row L1**: W31's declared level stop (|Δlevel| ≤ 0.055, growth ≤ 0.005;
   tracker ~4929, never adopted) as the interior level's bound on the macOS 27 standard profiles,
   WebGPU tier, re-derived at the adopting gate with the amendment beside the header.
2. **Part A, the tone curve, is a form change plus a refit, identity-pinned.** The authority fade at
   black is replaced by a uniform-black knot: a new leaf through the identity table whose identity
   reproduces today's behaviour exactly (both 26.5 digests and the four 27 digests unmoved; the
   goldens byte-identical at identity), then set on the four macOS 27 documents. The knots are refit
   per scheme and pose on W34's calibration greys (grey-96 validation) and the canonical `rrect-md`
   solids for the thick row; **`photo`, `impulse`, the checkerboards and `hc-text` are read cells
   with stops declared in G0, never fit targets** (the uniform-versus-structured question is open
   and a refit that moves the photo knot moves every photo cell). The two abscissae (active
   "source", receded "silhouette") are unified or the difference justified on the numbers
   (Decision Log 2).
3. **Part B, chroma, starts with a decision and fits nothing until it is made.** G0 decides between
   per-pixel retention (W31's operator, raised) and a plate-mean chroma term (a new operator) by
   reading the solids (r ≈ 1 dark, ≈ 0.9 light, pose-independent), `photo`'s M1 and the dark
   chroma-to-luma excess TOGETHER, censored channels excluded by rule; the draft goes to the parent
   as Decision Log 1. Only an operator that the joint read identifies enters G1's seal, as a leaf at
   identity under W31 Decision Log 1 (a); if none does, Part B is recorded as unidentified with the
   form of the work and G1 lands Part A alone.
4. **Bounds before reads; a miss is recorded, not widened; holdout once by artifact.** The canonical
   holdout is read once at the sealed configuration by `configuration.py record` (its fifth read;
   the current source hash equals the §5.172 record, so a W36 change is a new configuration); W34's
   holdout is spent and its cells serve calibration and validation only through W35's guarded
   native reader and shared web reader; the canonical native reader admits calibration, validation
   and probe and refuses holdout and recorded. The per-cell tables, `MISSED_27_ROWS` (which must
   equal the machine's misses, and today names a dark photo miss "the achromatic solve cannot
   close"), M1 in [0.8, 1.2], M2 re-baselined per W32 Decision Log 4 against the superseded
   generation, the conditioning predicate as the machine says (a level change on dark cells moves
   the extractor's silhouettes — W29 G3 went 32 → 67 lines), C1 ≤ 0.0042, X1 = 0 and B1's windows
   are all read, and **zero change on the exterior rows is shown, not assumed**. If Part A's refit
   and M1 conflict — the solids wanting a retention M1 forbids — the conflict goes to the user as
   Decision Log 3 before any bound moves.
5. **The fit's reach.** The tone knots and the black knot on the four macOS 27 documents; the
   receded abscissa as Decision Log 2 rules; the chroma leaf Part B identifies, if any, on the same
   four documents. Nothing else: not the rim, the lens, the outer shadow, the scatter, the size law,
   the tint shade (read as a check on the canonical tinted cells, §5.154 §6's shade gap recorded).
   The frozen 26.5 documents' values do not move (W29 Decision Log 1 (i)); their chroma defect is
   recorded beside, not fixed.
6. **The CSS tier derives or declines by measurement.** It carries the same response and the same
   fade (`platform-web/src/optics.ts` ~597–646), so it derives the black knot from the same leaf; its
   chroma decline (`BODY_CHROMA_RETENTION` 0, `saturate(1.8)` / 1.4, W31's measured decline) is
   re-read against the shipped documents, and `tier-coherence.test.ts`'s pins (the tone constants,
   the adaptation curve, the response with its fourth knot, the four retentions against the CSS 0,
   `interiorLevelRatioGpuOverCss` 0.8–1.25, cross-tier ΔE ≤ 0.05) hold or the decline is re-pinned
   with the reason.
7. **The read follows the seal, once, and lands with its captures.** G1 seals the four documents
   (rule 2, every digest site, `macos27-profile.ts` regenerated, the goldens attributed with the
   isolation proof's hashes moved for the named reason), runs the canonical read on the WebGPU
   tier, the ladder, the holdout once, the split (`--read-claims "c9a §5.179"`), pins
   `PREDICATE_EXCLUDES` as the machine says and the gated count, copies the read's capture tree to
   the canonical path with the replaced generation aside, runs `check-capture-tree`; M2 re-baselined
   at G1; the sheets and the eye with the LSB check beside every ×8 panel — and the eye on the
   photo cells, because a level change is visible where SSIM is not.
8. **Landing and every gap recorded.** G2 adopts L1 as declared, re-derived at the gate; prepares
   0.24.0; writes Outcomes, Deferred-at-close and the Tracking Map. The dark chromatic luma excess,
   the censored channels, the CSS chroma residual and the 26.5 chroma defect are recorded, not
   chartered.

## Grounding Baseline (main at `5d0fedf0`, W35 closed)

- **The miss** (memo §1; §5.177's level table): 1x and 2x identical; light active black +49, dark
  active black +28, dark inactive black +40; light greys +3…+8, dark greys −1…−7 (grey-128: +8 / −7);
  saturated channels up to 111 active (yellow's blue) and 144 inactive (blue's blue); the canonical
  `rrect-md` solids at the checked generation (1,900 captures, 1,893 match, 7 no-row, 0 mismatch)
  read the same directions at span 96.
- **The pipeline on a uniform backdrop** (memo §2; `wgsl/optics.ts`): backdrop = mix(blur, scatter)
  is the identity; the tone abscissa is the mean of encode(Y) for the active pose (`analysis.ts`
  ~89–97) and the silhouette's per-channel encoded sum for the receded (`silhouette-tone.ts`, W28);
  `toneAdapt` collapses (W7's low/high 0 / 0.0001 with the size bias put x above the band);
  `sizedAlpha = tintAlpha + sizeOcclusionGain · sizeK · (1 − tintAlpha)` (sizeK 0.0923 at span 44,
  1 at 96); the W9 response solve shifts the neutral's luma so the mean lands on R(x, sizeK)
  (~1163–1200; achromatic by construction) under **authority = smoothstep(anchor/2, anchor, x)**
  with anchor 0.004 (~1168–1169; `material.ts` ~4159; CSS ~616) — 0 at x = 0; `presentAlpha =
  adaptedAlpha · mat`, `colour = mix(backdrop, adapted, presentAlpha)` (~1256–1257);
  `bodyChromaRetention` restores chromaticity toward the backdrop's at held luma (~652–675, call
  ~1307), the only chroma lever, r = 1 being the backdrop's chromaticity; the tint shade only with
  an author tint.
- **The leaves** (memo §2): `tintAlpha · neutral` 0.46 · 1 light, 0.90 · 0.05 dark; `anchorX`
  0.004 / 0.11 / 0.425 / 0.95 (light) and 0.004 / 0.11 / 0.47 / 0.95 (dark) on 27, 0.1104 / 0.2706 /
  0.9505 on 26.5; the thin and thick rows per document; `bodyChromaRetention` 0.282 / 0.336 light /
  dark active, 0.349 / 0.142 receded, 0 on 26.5; the dark receded first knot a floor at 0.
- **What each was fitted on** (memo §2): W7's collapse curve on checkerboard, photo, dark-solid and
  impulse (§5.8); W9's three knots on the 56-cell probe at the neutral solids (§5.30–§5.35); W21's
  dark profile on W9's grid; W29 G3's four 27 knots with knot 1 moved to `impulse` and knot 3 at
  `photo` (§5.153); W29 G3b's receded knots re-anchored to impulse with the dark first knot floored
  (§5.154 §6); W31's retention on untinted `photo` only through M1 (§5.164 §4). **No uniform black
  and no saturated solid was ever in a level or chroma fit set.**
- **The CSS tier** (memo §2): the same R and authority fade per source; one `backdrop-filter` with
  `saturate(1.8)` regular / 1.4 clear under a solved `rgba()` plate; `BODY_CHROMA_RETENTION` 0 with
  the decline recorded; `tier-coherence.test.ts` pins the tone constants (~1603), the adaptation
  curve (~1624), the response with its fourth knot and `sizeToneLevelFar` (~837, ~873), the four
  retentions against the CSS 0 and the saturate values (~3330–3377).
- **The gates** (memo §4): the per-cell tables (ΔE mean ≤ 0.07 light / 0.09 dark, P95 ≤ 0.17–0.18,
  edge-weighted, SSIM); `MISSED_27_ROWS` 8; coherence `interiorLevelRatioGpuOverCss` 0.8–1.25 and
  cross-tier ΔE ≤ 0.05; the predicate and `PREDICATE_EXCLUDES` 67; M1 median [0.8, 1.2] with R rising
  with retention at slopes 2.894 / 2.716 / 5.956 / 5.029 (§5.164 §4) and R ∝ level^(−2/3) (tracker
  ~4933); M2 ±2 % re-baselined; C1, X1, B1 exterior; the identity rule; the holdout at four records
  with the current source hash equal to the last; W31's declared level stop never adopted.
- **The readers**: W35 G0a's canonical native reader (calibration, validation, probe admitted;
  holdout, recorded refused) and shared web reader over W34's split, both with negative tests
  (`results/2026-09-24-w35-g0-edge-cut/`).
- **The machine**: the original harness bundle holds its grant; no native capture is planned; the
  Codex accounts' usage limit refused the GPT rungs on 2026-09-24 and X9 carries the fallback.

## Design (advisory unless marked)

**Part A.** A uniform-black knot: the cleanest form is a first knot at x = 0 with its own row values
and the authority fade removed or floored — expressed as one new leaf (`toneBlackAuthority` or the
knot itself) whose identity value reproduces today's smoothstep exactly so the 26.5 documents and
the goldens do not move at identity; G0 chooses the form and proves the identity. The refit: per
scheme and pose, the four (or five) knots on W34's calibration greys and the canonical solids' thick
row, least squares in linear light with the censoring rule, grey-96 as validation, the photo /
impulse / checkerboard / hc-text cells read against declared stops. The abscissae: unify on the
active "source" mean unless the numbers say the silhouette sum is the receded pose's actual law.

**Part B.** Three readings decide the operator: the solids' implied retention per cell (pose-
independent on Apple's side), M1's chroma-to-structure ratio on photo (what a raised retention does
to it, by §5.164 §4's slopes), and the dark chromatic luma excess. If the solids' mean chroma and
photo's chroma variation cannot both be met by one retention value, the operator is a plate-mean
chroma term (the plate's mean colour restored toward the backdrop's mean chromaticity, per-pixel
variation left to retention) — a new leaf at identity; if they can, retention is raised. The dark
excess is a separate finding unless the same operator explains it.

**The stops** (declared in G0 with expected values): L1 on every macOS 27 interior; the per-cell
tables and `MISSED_27_ROWS`; M1; M2; the predicate's movement counted; C1, X1, B1 unchanged and
shown; tier coherence; the photo / impulse / checkerboard / hc-text stops; the tinted cells' shade
read; the receded pose flat where it is flat today.

## Children

### G0: The cut, the mechanisms, the stops and Part B's decision — no material change, no capture

Ledger **§5.178**; evidence `packages/calibration/results/2026-09-24-w36-g0-level-cut/`. Owns its dir,
`test/w36-*.test.ts`, and any read-only extension of W35's readers. Does: clause 1 in full; the
black-knot form chosen with its identity proved (both 26.5 digests and the four 27 digests unmoved
by a script in W33 G0's `identity-proof.ts` shape; the `inertLawCase` and the literal identity
named); the abscissa reading for Decision Log 2; Part B's joint read and the Decision Log 1 draft;
L1's declared form; the stops with expected post-fit values in `bounds-declaration.md`; the CSS
derivation plan and the chroma-decline re-read plan; a scratch tune-path render of a Part A
candidate on the bed (existing leaves cannot express the black knot — say so; the refit of the
existing knots CAN be rendered, and is, to show the mid-curve correction and its photo cost before
G1). Acceptance: every memo number reproduced or corrected beside; the declaration reviewed;
nothing under `src/`, `profiles/`, `fixtures/`, `results/matrix.json` or the canonical `scenes.json`
touched; freeze 1,818. Stop conditions: the photo stop cannot be met by any uniform refit (the
uniform-versus-structured question closes the wave for a ruling); the canonical tree at a refused
generation.

### G1: Part A (and Part B if identified), the seal, the read — one merge

Ledger **§5.179**; evidence `packages/calibration/results/2026-09-24-w36-g1-level-fit/`. Owns the
black-knot leaf and its identity-table entry and tests, the WGSL change (the fade → the knot), the
knot values on the four documents, the chroma leaf if Decision Log 1 identified one, the CSS
mirror or re-pinned decline, the four documents, the generated profile, `adopted-thresholds.test.ts`'s
`PREDICATE_EXCLUDES` and `MISSED_27_ROWS`, `window-activation.spec.ts`'s hashes if they move. Does:
the refit; the goldens attributed; the seal; `configuration.py record` before the passes; the
canonical read, the ladder, the holdout once; the split; the gated count; M2 re-baselined; the
sheets and the eye; every stop read with its expected value beside. Acceptance: clauses 2–7; a miss
recorded and put to the user, never widened. Stop conditions: a stop breaks; M1 leaves [0.8, 1.2]
(Decision Log 3); any 26.5 digest moves; the photo stop breaks.

### G2: The landing — L1 adopted, 0.24.0 prepared

Ledger **§5.180**; evidence `packages/calibration/results/2026-09-24-w36-g2-landing/`. L1 adopted and
re-derived; M2 per W32 Decision Log 4; `CLAUDE.md`, both READMEs, CHANGELOG, the coverage matrix;
the `/laws/` stage re-checked; the chain with X6's four facts; `pnpm changeset version` to 0.24.0;
Outcomes & Retrospective; Deferred-at-close; the Tracking Map row.

## Cross-Child Contracts

- **X1 — the freeze, intact.** No macOS 26.5-keyed path, row, bound, floor or document changes;
  `freeze.py verify` 1,818 at every merge; the goldens byte-identical until G1's attributed change;
  the 26.5 digests the live pin with no exemption spent (the black knot and any chroma leaf through
  the identity table, X13).
- **X2 — the cut precedes the fit.** G0's tables, mechanisms, stops and Decision Log drafts are
  committed and reviewed before G1 opens; G1 fits against them and no other.
- **X3 — the fit's reach** as clause 5; a fit that moves a row outside the interior level and chroma
  axes is a warning, not a result; the photo / impulse / structured cells are never fit targets.
- **X4 — bounds before reads; a miss is recorded, not widened; holdout once by artifact.**
- **X5 — no native capture; no bundle built, launched or granted; W34's bed read-only through the
  guarded readers; W34's holdout never read.**
- **X6 — RT and IC 0, the slider 0.5, Show Borders 0, one capture process, ≥ 60 s idle and the
  foreign-process count** before every browser run, every run in `browser-runs.txt`; a foreign
  browser present is recorded and the run's own determinism checks are the detector (the parent's
  W35 ruling carries).
- **X7 — the evidence layout by the one rule**: the split after the read, the capture tree to the
  canonical path at G1's merge with the replaced generation aside, `check-capture-tree` run.
- **X8 — padding untouched.**
- **X9 — routing** as W35's: `astra-medium` / `astra-high` or `sol-xhigh`; the default Claude
  worker when the Codex limit refuses them, each such dispatch recorded; the charter's review on
  `doperpowers:adversarial-reviewer`; reviews read-only; merges `--no-ff -F <file>` with the freeze
  verified; no attribution trailers; corrections beside.
- **X10 — the 27 bed never empties between merges.**
- **X11 — file ownership** as the children state; nobody touches `receded-profile.ts`'s 26.5 block
  or `DEFAULT_MATERIAL_PROFILE`'s existing values; W34's spent receipt and W35's readers' code paths
  are extended beside, not edited.
- **X12 — censored channels excluded by rule** (a channel at 255 on either side), named per cell,
  never fitted.
- **X13 — new leaves through the identity table**, flat, at an identity reproducing today's
  behaviour, gate-group if they gate others, the rule-2 version recorded in every sealed document.
- **X14 — M2 re-baselined per W32 Decision Log 4 at G1** against the superseded generation, the
  cumulative drift tabled.

## Ordering & Dependency Map

G0 → adversarial review of its declaration → merge → Decision Logs 1 and 2 (the parent) → G1 → review
→ merge with the capture tree → G2 → review → merge → `pnpm release` (the user's hand) → tag.

## Risks & Mitigations

- **A uniform refit breaks photo.** The photo knot is where the middle sits today; G0's scratch render
  of the refit prices its photo cost before G1, and the photo stop is declared; if no uniform refit
  meets it, the wave stops for a ruling on uniform-versus-structured.
- **M1 forbids the retention the solids want.** Decision Log 3 to the user before any bound moves;
  Part B's operator decision is what avoids the conflict if the plate-mean term is the answer.
- **The dark chromatic luma excess** is unexplained by any luminance-keyed curve: recorded as a
  finding, not fitted.
- **Censoring** leaves the light chroma law on three colours: stated, the rule applied.
- **The predicate moves** on dark cells: counted, the cells named, the count pinned.
- **The 26.5 digests move** with the form change: the identity value reproduces the smoothstep
  exactly and the proof runs before any WGSL edit.

## Deferred / Out of Scope

- The edge (W35 Deferred-at-close 2): after this wave.
- The dark chromatic luma excess; the censored channels' chroma law; the 26.5 chroma defect.
- The accessibility documents' level.
- W34's and W35's Deferred-at-close lists carry.

## Tracking Map

| child | status |
| --- | --- |
| G0 | CHARTERED |
| G1 | CHARTERED, opens after G0's merge and Decision Logs 1 and 2 |
| G2 | CHARTERED, opens after G1's merge |

## Decision Log

### Decision Log 1 — Part B's operator (after G0; the parent's, under the standing "rest on your judgement")

Open. G0 drafts: per-pixel retention raised, or a plate-mean chroma term, on the joint read of the
solids, photo's M1 and the dark excess; whether it joins G1's seal.

### Decision Log 2 — the receded abscissa (after G0; the parent's)

Open. Unify on the active mean, or keep the silhouette sum with the difference justified.

### Decision Log 3 — M1 against the solids (if they conflict; the user's)

Open.

### Decision Log 4 — L1's adopted form (after G1's read; the parent's)

Open.

## Surprises & Discoveries

(none yet)

## Revision Notes

- 2026-09-24 (the parent): v1 chartered from the grounding memo; sent to adversarial review before
  G0 opens. The memo ran on the default Claude worker (X9).
