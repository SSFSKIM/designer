# vitrea Post-v1 Wave 1 (2026-08-28)

> **Parent:** [the vitrea composite spec](./2026-08-24-vitrea-liquid-glass-design.md)
> §Outcomes & Retrospective → What remains — this wave is the cut that section
> seeds. **Consumes:** [the coverage matrix](./2026-08-25-coverage-matrix.md)
> (the enumeration this wave is ranked against) and
> [the claims doc](./c9a-fidelity-claims.md) (the thresholds W1 extends).
> Children dispatch per their track hint; each child's work cites this document
> (path + child id).

## Purpose

v1 shipped the material with its fidelity claim held to exactly what was
measured: 1× cells, two profiles, a size channel without its laws, no
author-facing tint, and a layer rule stated in prose but enforced nowhere. This
wave widens what the claim covers (the measurement bed first — scale and
accessibility profiles), then closes the three highest-value absences the
coverage matrix ruled as the round's openers (#33(a)), and clears the recorded
seed backlog so the next cut starts clean. The ultimate-goal frame: every child
either widens the measured-fidelity moat (the thing no competitor in the
verified prior art has) or removes a trap a real adopter would hit.

## Parent-Level Acceptance

Observable state that closes the wave as a whole, verified at recomposition in
the shipped artifacts:

1. The fidelity claim covers **2× and both accessibility profiles**: profile
   files and capture matrices exist for the new keys, thresholds are adopted
   and enforced in-suite for them, and the committed 1× matrix is re-baselined
   under recorded capture conditions (its accepted ≤1.5e-4 staleness resolved).
2. A thick and a thin surface over the same backdrop differ the way Apple's
   do: the **size law** couples the material's thickness-derived facets, with
   the coupling fitted against the matrix and holdout-checked.
3. An author can **tint** a surface through a supported API and the result
   remains glass (not flat fill), with a measured claim of its own.
4. Putting glass where the layer model forbids it (glass nested inside
   glass's content) produces a **dev-mode diagnostic naming the rule**, and
   the layer model is documented where an adopter reads.
5. The seed backlog items W6 carries are closed or explicitly re-recorded with
   reasons; `pnpm run ci` and all Playwright suites green; the wave ships as a
   minor release with the demo showing the new capabilities.

## Design

Thin by intake shape — this is an uncoupled bundle; each child designs its own
means. What the joint view settles is graded binding below and in the
contracts.

- **[binding — user ruling 2026-08-28] The reference stays macOS 26.5.** The
  capture machine is this Mac mini (macOS 26.5), driven into a HiDPI scaled
  mode for the 2× keys — the user confirmed the match and chose this over
  updating the Retina MacBook (which runs macOS 15, pre-glass). Every existing
  threshold and fitted constant therefore stays valid; OS 27 reference
  migration stays Deferred.
- **[binding — cut-level ruling] W1 is the bed; W2 and W3 tune only against
  W1's matrix.** Calibration-tuned work against the stale or 1×-only matrix
  would re-create exactly the blind-spot class that produced the corner-hook
  defect (#40).
- **[binding — cut-level ruling] Motion metrics are out of this wave.** The
  native side has no declarative pressed pose; frame sequences need an
  input-driving capture harness that is its own future initiative (Deferred).
- Advisory: the wave probably ships as **0.2.0** at recomposition (new public
  API in W3/W5); a mid-wave patch release is legitimate if W6 lands long
  before the calibration children.

## Children

### W1: The controlled recapture — controlled

- **Purpose:** Re-establish and widen the measurement bed: the 2× profile keys
  (unreachable at v1 on this panel at 1×), both accessibility profiles
  (reduce-transparency, increased-contrast), and a re-baselined 1× matrix
  captured under recorded conditions — so every later calibration claim stands
  on a current, reproducible reference.
- **Acceptance:** G1 (required): `backingScaleFactor == 2` capture path proven
  on this machine (HiDPI mode) and `-2x-` profile keys captured for light and
  dark standard. G2 (required): both accessibility profiles captured with the
  matching System Settings state (the harness refuses mislabeled keys — that
  guard stays). G3 (required): the full matrix re-measured, thresholds adopted
  (amended only with reasons) and enforced in-suite for every new profile;
  cross-tier ΔE tripwire resolved by real captures. G4 (required): capture
  conditions (display mode, OS build, settings) recorded in the profile files
  so a future session can reproduce them. Folded in from #41(k): the
  accessibility-axis capture partition guard, and the native color-space
  binding check.
- **Edges:** blocked-by: external:<HiDPI mode verified on this display —
  the wave's first experiment>; blocks: W2, W3.
- **Contracts:** X1, X2.
- **Design inheritance:** the reference ruling and motion exclusion (binding,
  above); capture.sh/README corrections from #42(j) (advisory).
- **Required:** yes — parent acceptance 1 is its gate set.
- **Status:** in-flight. G1's display half is PROVEN (2026-08-29): the physical
  panel advertises exactly one 1x mode, so the path is a BetterDisplay virtual
  HiDPI display (user-created; "가상 16:9", 2560×1440 points @2x) — the
  harness's own probe reports `window backingScaleFactor: 2.0`. Machine-side
  prep landed: the harness's scale gate now skips-and-records mismatched
  profiles like the a11y gate (a mixed-scale scenes.json used to fail every
  run), the two `-2x-` standard profiles are declared, the 2x backgrounds are
  rendered byte-stable, and selection verified live at 30 fixtures / 2.0x.
  **G1 COMPLETE (2026-08-29):** the TCC grant applies to
  `VitreaReference.app`'s own identity, so capture launches through
  LaunchServices (`open`), never the shell symlink — a shell launch inherits
  the terminal's recorded denial (env travels via `launchctl setenv`,
  unset after). 30 fixtures captured byte-stable at actualBackingScale 2
  (24 light + 6 dark, 640×400 device px, observed kCGColorSpaceSRGB),
  committed with the manifest.
  **G2 + G4 COMPLETE (2026-08-29; commit decac41).** The capture bed exists
  in full: all six declared profiles captured and described by ONE merged
  manifest (schema 2 — wholesale per-run replacement was erasing every other
  run's condition records; profiles now merge by key, background keys carry
  their scale, both calibration consumers read scaled-first with a schema-1
  fallback). First-ever accessibility captures (4 + 4, eyeballed correct),
  the 2x pair re-attested, and reference reproducibility measured: 1x
  standard byte-identical 30/30 against the committed matrix across
  independent runs, days apart. Harness rebuilds invalidate the TCC grant
  (ad-hoc signature keys on the build hash), so harness changes batch into
  single rebuilds.
  **G3 measurement half COMPLETE (2026-08-29; commits a3b4d00, 5205017):**
  all six profiles measured on both web tiers — 136 cells, every one
  deterministic over two independent page loads, every texture cell on the
  real apple/metal-3 adapter, v1's frozen material profiles applied
  unchanged (nothing fitted, nothing tuned; X1 held). Staged as
  `results/w1-matrix.json` beside the byte-unchanged committed matrix
  (a six-profile write would break the adopted gate's partition assertion).
  The cross-tier ΔE tripwire is answered by real captures: every profile
  passes the adopted bound in the mean AND on its worst single cell (worst
  anywhere 0.031 against ≤ 0.05); the 1× light interior range reproduces §5's
  0.844…1.067 exactly. Enforcing the row from the matrix instead of prose
  needs a schema-4 coherence axis — a design decision, left to the human
  gate. Watch item for W2/W3: both dark profiles sit within 0.9% of the
  interior-ratio ceiling (1.241/1.238 vs 1.25); any retune that lightens the
  CSS tier in dark crosses it. The adopted light-standard gate now also
  holds over the recapture (strengthen-only test block; 131 tests green).
  **W1 COMPLETE (2026-08-30; a1ad7a8, ca207ba).** The settled 168-cell
  schema-4 matrix is the committed bed; adopted gates enforced for both
  light-standard profiles (both tiers, coherence included) and both
  accessibility profiles; dark pair provisional pending post-glow-fix
  contour sanity (the extractor's ring-hole artifact inflates their
  contour proposals ~4x — recompute before adoption if the extractor
  learns glowing interiors); the seven tone-adaptation rows are excluded
  as a self-enforcing labeled defect class that W7's landing dissolves
  by construction (entries must still fail, measured figures must match
  the matrix, the class cannot grow). Historical record below:
  **G3 adoption half OPEN — the human gate (superseded by the above):** proposed holdout-bounded
  threshold tables for `2x-light-standard` (both tiers) are in
  `2026-08-29-w1-g3-measurement.md` (this directory);
  colour bounds are the 1× bounds unchanged (measurements are), contour
  bounds doubled (device-pixel quantity), SSIM bounds set from 2× numbers
  (+0.014 systematic). The three remaining profiles (`2x-dark-standard`,
  both accessibility) should NOT be gated yet: calibration cells only, no
  holdout column to bind a bound — v1's dark-provisional reasoning transfers
  unchanged. Cheapest close: the split is declared per scene, so extending
  those profiles' native scene sets to one validation + one holdout scene
  gives them a split for free — one accessibility-toggle session and one
  dark session on the existing harness.

### W2: The size law — controlled

- **Purpose:** The coverage matrix's largest behavioral gap (§3.2): Apple
  derives five facets from thickness/size; vitrea implements one (lensing
  depth). Couple the remaining thickness-derived facets so a large surface
  reads deeper — the way the reference measures, not the way prose describes
  it.
- **Acceptance:** the coupling implemented behind the material profile seam;
  constants fitted against W1's matrix with the holdout discipline (fit once,
  measure holdout once, against a frozen configuration); demo shows a
  thickness sweep; claims doc gains the size-law section with its own numbers.
- **Edges:** blocked-by: W1; blocks: —.
- **Contracts:** X1, X2.
- **Design inheritance:** coverage matrix §3.2 rows (advisory — re-read before
  design; the matrix is enumeration, not design).
- **Required:** yes — parent acceptance 2.
- **Status:** not-dispatched (blocked-by W1).

### W3: Author-facing tint — controlled

- **Purpose:** Flat fill is currently the only coloring option, and Apple
  names exactly that a character-breaking failure. A supported tint API whose
  result stays glass: tint participates in the optical stack (both tiers),
  with accessibility policies still winning.
- **Acceptance:** public tint API on the material/variant surface; measured
  against tinted native captures (its own capture extension run under W1's
  recorded conditions); CSS tier converts coherently; contrast machinery
  accounts for tint; README documents it with the claim held to what was
  measured.
- **Edges:** blocked-by: W1 (conditions + harness currency); blocks: —.
- **Contracts:** X1, X2, X3.
- **Design inheritance:** coverage matrix §3.4 (advisory); OS 27's user tint
  slider exists — design the API so a future reference migration doesn't
  break it (advisory).
- **Required:** yes — parent acceptance 3.
- **Status:** not-dispatched (blocked-by W1).

### W4: The content-layer contract — autonomous

- **Purpose:** Apple's layer model makes glass a controls-layer material and
  names glass-on-glass a failure; vitrea currently enforces nothing. The
  cheapest opener with the widest protection: make the rule a checkable
  contract instead of prose.
- **Acceptance:** dev-mode diagnostics fire on the prohibited compositions
  (at minimum: a registered glass host nested inside another glass host's
  subtree), with tests proving fail-before/pass-after and zero false
  positives across the demo and playground; the layer model documented in the
  READMEs where hosts are introduced; no production-path cost.
- **Edges:** blocked-by: —; blocks: —. Parallel-safe.
- **Contracts:** X3.
- **Design inheritance:** coverage matrix §3.9 (advisory); vitrea's
  diagnostics doctrine — honest state, dev-mode advisories, never silent
  (binding by parent canon, not by this wave).
- **Required:** yes — parent acceptance 4.
- **Status:** landed 2026-08-28 (da0e532; Decision Log 6).

### W5: The deferred API batch — decomposing run at dispatch

- **Purpose:** The recorded API debts whose batching was ruled cheaper than
  drip-feeding core churn: the framework-agnostic host entry (`vitrea` alone
  cannot mount a root — #30(c)), per-corner radii, the #23(c) core batch
  (corner reference + concentric parent link as scene-model fields, per-group
  probe setter, generic diagnostics channel), the refraction-ladder dedup
  (#23(d)), the GlassMorph pre-pin polish (#28(d)), and the GlassNode clip
  chain (#41(k) — producer and consumer, the overflow-scroller scenario;
  moved here from the hygiene sweep because it is a two-sided feature, not a
  seed).
- **Acceptance:** coarse by design — precise gates emerge from its own cut at
  dispatch.
- **Edges:** blocked-by: — (no calibration dependency); blocks: —.
- **Contracts:** X3.
- **Design inheritance:** W4's `layer-model.ts` is a pure function taking a report callback, written to port unchanged onto the generic diagnostics channel if this batch lands it (advisory).
- **Required:** yes for parent acceptance 5's "closed or re-recorded" clause;
  individual items may be re-deferred with reasons at its cut.
- **Status:** cut concluded 2026-08-30 — the gate says LEAF: all six items are
  core/platform-web/react API work, no calibration or capture dependency,
  coherent under one owner (each of W2/W3/W7 was larger). Dispatched as one
  child with per-item gates in the dispatch brief; re-deferral of individual
  items with reasons remains legitimate at the worker's discretion per the
  acceptance. In-flight.

### W6: The hygiene sweep — autonomous

- **Purpose:** Close the small recorded seeds that each trap a real adopter:
  (a) the inline-style `color` wrinkle (#34(c)) — an app rule on a glass host
  parses and silently never applies; (b) the engine-versioned advisory row
  (#39) — now with concrete content: Chromium ≥152 no-ops `backdrop-filter`
  on `clip-path: path()` elements under a rounded-overflow ancestor
  (`spikes/s1-proxy-topology/chrome152-regression/`), which is exactly
  vitrea's proxy shape under a custom container mount; (c) the
  texture-before-declare edge (#41(k)) — a texture handed over before its
  source is declared is never auto-marked; (d) the 3σ-under-
  `reducedTransparency` wrinkle — the library's own defaults trip its own
  floor when one accessibility prop flips; (e) the GlassMorph tab-order
  surprise — the hoisted closed platter holds the toolbar's first tab stop.
- **Acceptance:** each item closed with a fail-before test, or explicitly
  re-recorded with a reason at the wave spec; suites green; no assertion
  loosened.
- **Edges:** blocked-by: —; blocks: —. Parallel-safe.
- **Contracts:** X3.
- **Design inheritance:** per-item context above plus the cited Decision Log
  entries (advisory — each item's mechanism is the child's to design; (d) is
  the one with real design content: prefer deriving the default padding from
  the resolved post-policy blur over warning-suppression, and prove golden
  neutrality at nominal state).
- **Required:** yes — parent acceptance 5.
- **Status:** landed 2026-08-29 (Decision Log 7); all five items closed with
  fail-before tests, one deliberate scope refusal recorded.

### W7: Backdrop tone adaptation — controlled

- **Purpose:** The recapture's biggest finding (Surprise, 2026-08-30):
  Apple's material continuously adapts its appearance to backdrop
  luminance — a light-scheme capsule over a black backdrop settles to
  near-black — and vitrea has no such axis at all (material profiles are
  discrete per-scheme). This is vitrea's largest measured fidelity gap
  (ΔE p95 0.66 against an adopted bound of 0.17) and the mechanism behind
  the settled bed's only gate failures.
- **Acceptance:** a backdrop-luminance-driven adaptation axis in the
  material pipeline, BOTH tiers, tuned against the settled bed under the
  X1 holdout discipline; the two defect-excluded scenes
  (`impulse__capsule-button__rest`, `dark-solid__capsule-button__rest` in
  the light profiles) re-enter the adopted gate and PASS at the adopted
  bounds — the exclusion dissolves rather than being widened; the
  adaptation is continuous (intermediate backdrops measured, not just the
  extremes); accessibility policies still win; claims doc updated with
  the measured behavior; no regression on any other adopted row.
- **Edges:** blocked-by: W1 (needs the settled bed — satisfied); ordered
  after W2/W3 dispatch (they are the chartered openers and tune against
  the same frozen bed; W7's retune lands last so their holdout
  consumption is not invalidated mid-flight).
- **Contracts:** X1, X2.
- **Design inheritance:** the settled-bed evidence and the visual
  polarity finding (binding — the dark state is the settled state); the
  tone-adaptation-transient session rule (advisory: capture timing is the
  instrument's concern, not the renderer's); Apple's documented behavior
  that Liquid Glass adapts between light and dark appearances by content
  behind it (advisory — re-verify the intermediate curve empirically,
  never assume the endpoint mapping).
- **Required:** yes — chartered by user decision 2026-08-30 (Decision
  Log 11); the parent acceptance's fidelity clause inherits its
  defect-exclusion dissolution.
- **Status:** not-dispatched (after W2/W3 dispatch).

### W8: The outer shadow — controlled

- **Purpose:** Stage 0's discovery (Decision Log 15): Apple's active
  material casts an outer shadow; vitrea renders zero across its entire
  footprint — the largest visible fidelity gap ever measured in the
  project, larger in canvas coverage than W7's tone adaptation.
- **Acceptance:** an outer-shadow facet on BOTH tiers (CSS: a box-shadow
  derived from the same profile constants; GPU: a shadow pass or
  field-derived occlusion outside the component), behind the material
  profile seam; the shadow's parameters (offset, extent, falloff,
  span-coupling, backdrop-multiplicativity) implemented to the damage
  report's measured description with constants left PROVISIONAL for the
  cascade's fit; composes with the size law (span-coupled) and under
  accessibility policies (measure what reduce-transparency does to the
  reference's shadow before assuming); demo shows it; tests pin the
  mechanism; no regression in any suite.
- **Edges:** blocked-by: — (the description is measured; the bed is in);
  blocks: the cascade's resumption.
- **Contracts:** X1 (constants fitted in the cascade, not here), X2.
- **Design inheritance:** the damage report's shadow characterization
  (binding as the mechanism's description — §5.11 and the stage-0
  matrix); C9a's deleted box-shadow triple (advisory — the recorded
  numbers were measured against a real facet and are a starting point).
- **Required:** yes — parent acceptance 1-3 close only through the
  post-W8 cascade (Decision Log 15).
- **Status:** landed 2026-08-31 — merged to main at ed6d265 (five commits)
  after one independent review round (two field-extent findings, fixed and
  pinned fail-before-verified; the shadow's measured cost 3.35× → 2.79×
  frame time on the mobile bench). Post-merge verification on the merged
  tree: build, lint and every unit suite green, renderer GPU e2e 34/34
  including the two new shadow-extent pins.

## Cross-Child Contracts

- **X1 — The matrix is the bed.** Owner: W1. Binds W2, W3: no constant is
  fitted and no claim is stated except against W1's matrices and conditions;
  the holdout discipline (fit once / measure holdout once / frozen
  configuration) carries over from the parent unchanged.
- **X2 — Profile key contract.** Owner: W1. The
  `apple-{platform}-{os}-{scale}x-{scheme}-{a11y}` grammar and the
  refuse-mislabeled-keys guard are binding; new axes (tint scenes for W3)
  extend the scene set, never the key grammar, unless W1 records a Revision
  Note here.
- **X3 — Diagnostics registry ownership.** *(Amended 2026-08-29 after W6's
  report: "add by name" alone did not keep attribution clean — a shared
  README section of W6's was swept into W4's commit by W4's `git add` of the
  whole file. Content correct, attribution wrong, recorded in 97cac56's
  body. Next multi-child tree: a shared file needs staging from a mine-only
  copy, not just name-scoped adds.)* W4 owns the content-layer
  diagnostic codes; W6 owns the engine-advisory code; both add codes
  additively and re-read shared union files immediately before editing (two
  children share this working tree). Naming follows the existing
  kebab-case code style; every code's message names what to do, not just
  what is wrong.

## Ordering & Dependency Map

W4 and W6 run now, in parallel — no calibration dependency. W1 starts with
the HiDPI experiment (its external gate) and proceeds through captures; W2 and
W3 dispatch only after W1's G3. W5 cuts after W4/W6 land (their flow-back
informs it) and can overlap W2/W3. Recomposition verifies the five
parent-acceptance clauses in the shipped artifacts and ships the minor
release.

## Risks & Mitigations

- **HiDPI capture ≠ Retina capture.** A scaled mode renders at 2× into a
  panel that downsamples for display; ScreenCaptureKit reads the backing
  store, so captures should be true 2× — but this is exactly what W1's G1
  experiment must prove, not assume. Mitigation: G1 verifies pixel dimensions
  and `backingScaleFactor` from the harness's own probe before any matrix
  run; if the panel's backing store disappoints, the fallback (MacBook on
  macOS 26) returns to the user with the reference-version question attached.
- **Accessibility toggles change the whole desktop.** W1's capture sessions
  announce before flipping display mode or accessibility settings and restore
  after; the harness's refuse-mislabeled-keys guard is the backstop.
- **Two children in one working tree.** X3's re-read rule, plus per-child
  file-scoped commits (`git add` by name, never `-A`).

## Deferred / Out of Scope

**Deferred (may return):** motion-metrics capture harness (needs input-driven
frame sequences; no declarative pressed pose exists natively); OS 27 /
reference-version migration (the matrix's "the reference has already moved"
flag — priced separately, interacts with W3's tint API); component-layer
expansion (1 measured row of 26 — correct v1 trade, revisit after the wave);
retail Safari 26 spot-check of the WebKit conformance row; concentric-rule
discrepancy (#33(c)); contour morphs; glow diffusion; W7's two-axis
tone-adaptation rework with new mid-band scenes in its capture plan
(Decision Log 13); a dedicated low-resolution shadow pass for the GPU tier
(W8's cost recommendation — the field pass rasterises the enlarged rect at
full resolution to feed a ~15 px Gaussian; the facet measures 2.79× frame
time on the mobile bench); the per-corner radii geometry rewrite; the
inactive-pose material as a deliberate window-focus-aware feature (its
ready-made reference bed lives in git history at the pre-DL14 fixtures).

**Explicitly out of scope:** WebGL2 tier; topology-changing morphs (standing
parent exclusions, unchanged).

**External, user-held (not children):** the Gecko manual pass (conformance
rows fail closed meanwhile); filing the drafted Chromium bug report.

## Tracking Map

| child | where | status |
| --- | --- | --- |
| W1 | this session + capture sessions; agents (a3b4d00, 5205017, d4acebe, f657cbd, 7e580b4, a1ad7a8, ca207ba) | COMPLETE 2026-08-30 |
| W2 | LANDED 2026-08-30 (21e87ed, fast-forwarded to main; composed with W3 tint in-branch per Decision Log 12) | landed |
| W3 | phase 1 LANDED 2026-08-30 (0e9ee01, a3c5699 — API + both tiers + capture plan); measurement phase awaits the batched capture session | phase 1 landed |
| W4 | landed — commit da0e532, 2026-08-28 | landed (wave integration check at recomposition) |
| W5 | — | awaiting cut |
| W6 | landed — commits 97cac56, 265b422, c550b06, 2e2c66d, 74df3e5 (+ e18aa7e catch-up), 2026-08-29 | landed (overlap follow-through: 4d17f5e spike, 647a374 + aa7d481 fixes) |
| W7 | LANDED 2026-08-30 (7eec80c, 1699998, fast-forwarded to main) — the seven-row exclusion DISSOLVED at unedited bounds | landed |

## Decision Log

1. **The cut and its ordering (2026-08-28, user-approved):** recapture first,
   then the #33(a) openers, hygiene and the content-layer contract in
   parallel from day one; the deferred API batch cuts late so flow-back
   informs it. Rejected: opening with the three absences directly (re-creates
   the #40 blind-spot class by tuning against a stale 1×-only bed); one
   mega-child for "the backlog" (fails the gate — no single owner could
   reliably carry calibration infrastructure, two calibrated features, and a
   seed sweep as one unit).
2. **The reference stays macOS 26.5 on this Mac mini via HiDPI (2026-08-28,
   user):** "on mac 26 -- it matches. We'll use this Mac Mini." Rejected:
   updating the Retina MacBook (runs macOS 15 — pre-glass; an update lands on
   current 26.6+, which keys a different reference than every committed
   fixture and threshold).
3. **Motion metrics excluded from the wave (2026-08-28):** the missing piece
   is a harness capability, not a capture session; bundling it into W1 would
   couple the wave's bed to an unbuilt instrument. Deferred with its reason.
4. **No board materialization (2026-08-28):** the wave runs on this composite
   spec's tracking map, as the v1 charter did. Rejected: GitHub-issue
   tickets — nothing about this wave needs cross-session dispatch state.
5. **Seed re-assignments at cut (2026-08-28):** the accessibility-axis
   capture guard and native color-space check fold into W1 (they are capture-
   harness items and W1 lands the axis that makes them evaluable); the
   GlassNode clip chain moves from the hygiene sweep to W5 (two-sided
   feature, not a seed).

6. **W4's detection set and its false-positive control (2026-08-28, landed
   da0e532):** two structural, registration-time, dev-mode-only checks —
   `glass-inside-glass` (error; DOM-descendant relation between registered
   hosts, caught in either registration order, cross-plane by design) and
   `glass-in-content-layer` (warning; resolved ARIA role is a list-or-table
   structure). The false-positive control: an explicit `role` beats the tag's
   implicit one, so a controls-layer container declares itself out — the
   diagnostic's message names that escape; `<menu>` is excluded from the
   implicit-role table as HTML's own command-bar element. Rejected with
   reasons: the no-scroll criterion (temporal; see Surprises),
   non-interactive-items-on-glass (fires on every legitimate glass container
   Apple itself glasses), glass/content intersection (geometric, per-frame,
   needs a notion of content vitrea cannot identify), standard-materials rows
   (a missing feature, not an unenforced rule). Verified: 18 unit tests
   (4 fail with the hooks reverted), live-browser injection check, demo 17
   and playground 6 e2e clean with a registered-surface floor so silence
   cannot come from emptiness; production path untouched (`devMode`-gated at
   `registerHost`, never per-frame).

7. **W6 landed — all five seeds closed, one deliberate refusal (2026-08-29;
   97cac56, 265b422, c550b06, 2e2c66d, 74df3e5; tree green at 1,268 unit /
   280 + 102 e2e / 17 demo / 31 goldens):** (a) the runtime's ink is now a
   single `:where([data-vitrea-node]) { color: var(--vitrea-foreground) }`
   rule in a head-PREPENDED style element — specificity 0,0,0 so any app rule
   naming the host wins, and deliberately NOT `adoptedStyleSheets` (adopted
   sheets sort after document sheets, so a zero-specificity app rule would
   lose the source-order tiebreak — the wrong way round) and NOT `@layer`
   (jsdom applies no layered contents to computed style, making the mechanism
   invisible to every jsdom test, this repo's and consumers'); measured ink
   proven unchanged on ten hosts. (b) the conformance table grew a `defects`
   axis: per-engine-version recorded defects with mandatory evidence AND
   mandatory workarounds, failing in the OPPOSITE direction from conformance
   (an unmeasured engine under-promises on conformance; an unmeasured version
   accuses nothing on defects) — first row: Chromium ≥152's
   clip-path-path()-under-rounded-clip no-op, detection riding layer 2's
   existing ancestor walk via an `observe` hook (zero extra layout reads),
   over-triggering stated in its own evidence (only overflow:hidden was
   measured; clip/auto/scroll accepted fail-safe). Row order became
   load-bearing (first match wins; the 152 row precedes 113) — commented at
   the table. (c) only the IMAGE kind was ever stuck (canvas/video re-mark
   per frame); registration now imports an already-held texture through the
   supply path's own call. (d) default samplingPadding derives from the
   resolved post-policy blur — the old constant 24 was 3σ at nominal σ=8 all
   along, so nominal-state neutrality is exact (3×8=24), proven across every
   suite; six of the consumer's seven warnings gone. **The refusal:**
   `proxy-overlap-after-enforcement` stands — two groups closer than their
   enlarged sampling regions is a fact about the page's layout, and silencing
   it under exactly the preference that enlarges the blur would invert the
   diagnostics doctrine. (e) hoisted toolbar items publish their in-flow
   placeholder via a new `PLANE_ANCHOR_ATTRIBUTE` DOM marker (a React context
   cannot work — `useToolbarItem()` runs above the morph's provider scope);
   the toolbar sorts descendants first so arrows, tab stop, and the
   `aria-owns` accessibility order agree by construction. Also: e18aa7e
   caught a WebKit-project e2e left stale by the conformance row's 18.6
   landing (13fea2f) — it now reads the closed gates off the same evidence
   the table does.

8. **W1/G3: the web side renders the COUPLED state for the increased-contrast
   profile (2026-08-29; a3b4d00, 5205017).** Measured, not assumed. The native
   `-increased-contrast` reference embodies both flags because macOS
   force-couples them, and vitrea's two axes are independent media queries, so
   both readings were measured against the same reference on both tiers.
   Rendering both flags beats contrast alone on every one of the eight cells
   and every perceptual metric: ΔE p95 roughly doubles under contrast-only
   (0.0329→0.0660, 0.0550→0.1131 on the texture tier), SSIM falls everywhere,
   edge-weighted mean rises ~30%; shape is unaffected. The mechanism is
   legible — the coupled render reaches an interior level of 0.75–0.89 against
   the reference's 0.86–0.95 where contrast-only stalls at 0.69–0.81, and the
   occlusion that closes it comes from reduced transparency.
   `compare --web-accessibility as-captured` is the default and the fidelity
   number; `contrast-only` is retained, staged in
   `results/w1-matrix-contrast-only.json`, and must be read as a bound on a
   state the reference cannot see, never as a fidelity claim. A genuine
   contrast-only claim needs a reference from a platform that decouples the
   toggles — a new capture bed, not a threshold.

9. **G3's adoption gate: all three approved (2026-08-29, user: "approved
   all").** (1) The proposed `2x-light-standard` thresholds adopted as
   written (both tiers; `2026-08-29-w1-g3-measurement.md`). (2) The cell
   schema bumps to 4 with a `coherence` axis so the cross-tier bound is
   enforced from the matrix instead of prose — dom-tier cells carry
   cross-tier ΔE and interior ratio against their texture twin. (3) The
   three provisional profiles stay ungated until they have a split; the
   extension sessions are approved to run — the split being per-scene,
   the dark extension is autonomous (appearance switches in-run) and the
   accessibility extension rides the next toggle session. G3's "for every
   new profile" clause is amended accordingly, with this entry as the
   reason: gating a profile whose binding column does not exist would
   certify overfitting (v1's own dark-provisional doctrine).

10. **The reference bed moves to the virtual-display context (2026-08-29,
    user-approved).** The extension chain's attestation gate exposed that
    captured material bytes depend on the DISPLAY COLOR CONTEXT at the
    ±4/255 level: after the physical panel power-cycled, it renegotiated
    EDID (generic "Display" 1920×1080 → "S27HG5x" 2560×1440) and 15 of 30
    1x cells stopped reproducing — diffs concentrated in photo-backed
    cells, chromatically skewed, max 4/255 — while the same bed had
    reproduced byte-exactly 45 minutes earlier on the pre-cycle identity.
    A reference bound to an EDID handshake is not reproducible (this also
    retroactively explains v1's "panel advertises exactly one mode"
    oddity — classic degraded-EDID behavior). Ruling: ALL captures move to
    the BetterDisplay virtual display ("가상 16:9"), whose identity and
    color context are software-defined — hiDPI off (2560×1440@1x) for the
    1x profiles, hiDPI on (@2x) for the 2x profiles, set as main during
    captures (window placement follows the main display; verified by
    probing 2.0 with the 1x physical still connected). The 1x bed and both
    accessibility profiles re-baseline onto this context; the 2x bed was
    already captured on it and gets a settled A/B attestation. Every
    re-baseline capture is an A/B double run — byte-identity across two
    independent process launches at the settled state — before the bytes
    become reference. Refines Decision Log 2 (the machine stays; the
    panel is no longer a condition) and subsumes the pressed-cell
    correction (the whole 1x bed re-baselines; that finding's settle
    discipline stands). Consequences: claims §5's 1x figures re-verify
    against the new bed (thresholds have ~2-3x margin over a ±4/255
    shift; figures update where they move), the matrix promotion
    regenerates once against the re-baselined bed, and capture G4
    conditions now record the virtual-display mode instead of a panel.

11. **The settled-bed gate ruling, the W7 charter, and the accessibility
    adoptions (2026-08-30, user-approved, all three as recommended).**
    (1) The settled matrix promotes WITH a documented defect-class
    exclusion: the extreme-backdrop capsule cells' perceptual rows
    (impulse and dark-solid capsule scenes, light profiles) are excluded
    from the adopted gate as a LABELED known renderer gap — the measured
    numbers stay in the claims doc as an honest limitation tracked to W7;
    nothing loosened, nothing silent. Rejected: holding the promotion
    (blocks the wave on a feature-sized fix) and loosening bounds
    (certifies the defect as acceptable fidelity). (2) Backdrop tone
    adaptation is chartered as child W7 (controlled), ordered after
    W2/W3 dispatch; its acceptance dissolves the exclusion at the
    adopted bounds. (3) The reduced-transparency and increased-contrast
    threshold tables are ADOPTED as proposed; the dark pair stays
    provisional until the dark+pressed cross-tier fix lands and its
    figures re-measure.

12. **W3 phase 1's composition contract — BINDING on W7 (2026-08-30;
    0e9ee01, a3c5699).** Research settled the axis split: Apple's
    `Glass.tint(_:)` is an author's COLOUR while the OS 26.1/27 user
    Clear-to-Tinted control is an OPACITY preference (the axis Reduce
    Transparency already lifts) — so vitrea's author tint occupies the
    colour axis only, its strength carried by the colour's own alpha, and
    a future user-slider mapping lands on the alpha axis without touching
    the API. The colour is a seed read through a tone curve against the
    sampled backdrop (per-pixel on the GPU tier), never a fill.
    Composition order, in series: colour scheme → backdrop adaptation
    (W7) → author tint; a full-strength tint is its own adaptation, a
    partial one moves with W7's. Accessibility overrides once (forced
    colours removes material and tint together) and folds otherwise
    (policies modulate how far the material may move the colour — via the
    existing `ambientTint` axis — never the colour itself). The contrast
    machinery reads the TINTED result. W7 builds against this order;
    re-litigating it is a `[parent-impact]` event, not a local choice.
    The tone curve's four constants live in the material profile,
    mirrored to the CSS tier through the same equation as the neutral
    tint (achromatic identity proven in-suite) — W3's capture phase fits
    them (plan: `2026-08-30-w3-tint-capture-plan.md`; 18 cells, tint as
    a scene axis per X2, harness diff drafted but unapplied pending the
    batched rebuild + TCC re-grant).

13. **The holdout round's two rulings (2026-08-30, user-approved, both as
    recommended).** (1) W7's curve STAYS as landed: the mid-dark holdout
    passes every adopted bound comfortably (dE 0.0104 vs <=0.07, SSIM
    0.9817 vs >=0.88) while falsifying the curve's MECHANISM at 5x
    (predicts 0.650 adaptation where the reference does 0.127; the three
    measured points — 1.000, 0.127, 0.218 over rising curve input — are
    non-monotonic, which the smoothstep form cannot express, so surface
    size is its own axis rather than a shift along the brightness one).
    The mis-shape costs nothing measurable on any bound, and the only
    transition-band holdout is now SPENT — a rework now would fit without
    validation, the doctrine's own definition of overfitting. The two-axis
    rework is chartered for the next wave WITH 2-3 new mid-band scenes in
    its capture plan (claims §5.9 carries the finding). (2) The tint
    ships MEASURED, not deferred: one re-capture session closes it after
    the colour-loss diagnosis lands its fix and attestation (the 18
    tinted cells re-captured under the same four-phase protocol; the fit
    agent re-runs unchanged against the corrected bed). Wave
    recomposition follows that session.

14. **The bed was the INACTIVE material — full re-baseline on the active
    pose (2026-08-31, user-approved).** The tint-colour diagnosis found the
    real fault beneath the wave: the capture window was borderless and
    never overrode `canBecomeKey`, so it was never key, the app never
    active, and Liquid Glass rendered its INACTIVE appearance through
    every capture the project has ever taken — all 176 committed cells,
    v1 included. The first active-pose capture (key window verified,
    attested per-cell) moves EVERY cell: deltas concentrate on the
    component footprint (means 0.3–19/255; backgrounds byte-still), and
    the tinted cells finally carry their hue (chroma 102–250/255 against
    the untinted control's 0.01) — tint is an active-material feature,
    which is why the colour was lost. Ruling: the canonical reference is
    the ACTIVE pose (what a user interacting with glass actually sees);
    the whole bed re-baselines through one four-phase session, and the
    calibration cascade re-runs against it — the C9a material constants,
    W2's size-law fit, W7's adaptation fit, the first real tint fit, and
    every adopted threshold re-verified or re-adopted with reasons.
    Every prior fitted constant and threshold is understood as describing
    the inactive pose — correct measurements of the wrong state.
    Recorded for a future wave: an inactive-pose material could become a
    deliberate vitrea feature (window-focus-aware glass); the inactive
    bed lives on in git history as its ready-made reference. The harness
    now attests `presentedActive` per cell and refuses colourless tinted
    beds outright, so neither failure class can recur silently.

15. **The shadow round: W8 chartered, the extractor redesigned, no interim
    re-adoption (2026-08-31, user-approved, all three as recommended).**
    Stage 0 of the recalibration found the active material casts an OUTER
    SHADOW the inactive never showed — a downward-offset multiplicative
    occlusion, span-coupled, ~20px above/40 below/33 beside at 1x, present
    in every profile, and vitrea renders exactly 0.0000 across its whole
    footprint (8.5-29.6% of canvas). C9a had deleted the CSS tier's
    box-shadow as "the one thing it draws that the reference does not" —
    the deleted triple was closer to the truth than what ships; the
    inactive reference simply could not show it. Rulings: (1) W8 is
    chartered — the outer shadow on BOTH tiers — and the wave finishes
    complete: extractor redesign, then the cascade re-runs to done; no
    new captures needed. (2) The instrument becomes TWO honest axes:
    shape extraction bounds its search to the declared component region
    (contour/corner fidelity keeps full meaning; area-recovery partly
    assumed, stated), and the shadow becomes its own measured axis
    (offset, extent, falloff) fitted like any facet. (3) No threshold
    re-adopts before W8 — the perceptual rows passing over a known
    zero-rendered facet is the certify-the-defect move DL11 rejected;
    the inactive-bed gates stay enforced as the historically-labeled
    suite until the one honest post-W8 pass.

16. **The adoption, the span coda, and the documented exceedance
    (2026-08-31, user-approved, all three as recommended).** The cascade
    completed stages 1-4 and spent its single holdout pass; the colour
    axis holds everywhere (worst dE mean 0.0587 against 0.07/0.08) and
    three findings routed here instead of being absorbed. Rulings:
    (1) ADOPT IN FULL - the refit's passing tables become the enforced
    suite (the gate flips from the inactive-bed schema-4 matrix.json
    to the active-bed schema-5 matrix, the one honest post-W8 pass DL15
    ruling 3 held out for); the eleven refitted constants ship
    (shadowAlpha 0.55 -> 0.05 - the rim was always there, the inner
    shadow was drowning it; tintAlpha 0.46; blurSigma 3, identifiable
    for the first time; the tone curve at identity - Apple's author tint
    IS the seed colour, exactly, in linear light); the 16 red GPU goldens
    re-record against the adopted material; the simply-connected
    conditioning predicate is built as proposed (topology, not area, is
    what breaks the contour metric - the repaired light-solid cell proved
    an area arm gates nothing) and corner curvature is never gated.
    (2) SPAN CODA NOW - the holdout falsified sizeOcclusionGain = 0
    (280-pt surfaces read 0.10-0.19 below the reference while 32-96
    match; the calibration band tops out at 96, so no honest refit exists
    against current cells). One autonomous light-standard 1x capture
    session adds ~2 calibration spans in 128-192 plus a fresh 280-class
    holdout cell; a single-constant fit with everything else frozen,
    under fresh holdout discipline, folds into the adoption - the wave
    closes complete rather than shipping a falsified constant.
    (3) W7's mid-dark holdout miss (dE p95 0.1775 against 0.17, 4.4%,
    every other row comfortable) becomes a DOCUMENTED EXCEEDANCE: the
    bound stays enforced everywhere, the one cell is annotated
    known-failing with the chartered two-axis W7 rework as its owner -
    the same documented-defect move as Decision Log 11. Also recorded
    for the tint axis's next step: the CSS tier's coherence ceiling
    break on saturated tints (1.65 against 1.25) is a gamut clip, not a
    material error - solving the CSS alpha and colour jointly under the
    sRGB gamut constraint is chartered into the backlog.

17. **Round two, declared; the outer-contour instrument; DL16's coda
    voided on arithmetic (2026-08-31, user-approved, both as
    recommended).** The cascade refused DL16's ruling 2 with arithmetic
    the parent verified: sizeOcclusionGain multiplies
    smoothstep(32, 96, span), which is exactly 1 at every span >= 96, so
    calibration captures at 128-192 would land on the same curve point
    the span-96 cells already occupy; and the 320x200 canvas cannot host
    a larger surface without clipping W8's shadow (rrect-lg is already
    the maximum, and its span is 160 by min-dimension - DL16's "280-pt"
    figure was the width, a naming error, corrected here). It also
    refused ruling 1's flip as scoped, because the passing set without
    further fits would gate the flagship profile more weakly than the
    retired suite - the certify-the-defect move this log keeps refusing.
    Both refusals upheld. Rulings: (1) ROUND TWO, DECLARED - three fits
    on calibration cells that already exist: increasedOcclusionLift and
    reducedTransparencyFrost (never refitted by any stage; they own six
    of the ten failing rows), then sizeSpanMax with sizeOcclusionGain
    refitted after it (the gain is identifiable only once the curve
    stops saturating inside the bed). Grid and objective DECLARED BEFORE
    the fit runs; then one fresh holdout read of the frozen result. The
    contamination is stated plainly in the record: the holdout column
    has been read once, so the fitter knows which cells fail - the
    declared-protocol discipline is the mitigation, not a cure.
    (2) OUTER-CONTOUR NOW - the contour metric becomes the distance
    between each mask's OUTER contour, immune to interior holes by
    construction, replacing the topology-predicate gate (built, provably
    inert on measured values, and measured to be weak: 77 cells lost to
    its arms, zero cells gated on increased contrast; it stays recorded
    as the measurement that motivated the replacement). It lands and is
    read on calibration cells first, with contour bounds declared before
    the round-two holdout read. (3) Adoption re-adjudicates on round
    two's result - the gate does not flip before it.

## Surprises & Discoveries

- **The active material is byte-reproducible per CELL, not per RUN — the
  reference state is now a per-cell plurality (2026-08-31; bed 973fd7e).**
  Three independent runs per phase: within each run every cell converges
  byte-stable under the settled instrument, but across runs a small
  rotating subset (4-6 of ~50) lands in a different micro-state (deltas up
  to 60/255 on the component footprint). The inactive material never
  showed this — an unfocused window's material is static; the active one
  evidently carries per-instance state. Doctrine: a cell's reference
  bytes are the state at least two of three runs share exactly; 120 of
  121 cells settled at 2-of-3 (most unanimously), and the one refuser —
  the 2x dark-solid capsule, the extreme-backdrop tone-adapter — resolved
  2-of-4 on a tie-break run. Zero cells unresolved: even the noisiest
  cell has a majority state, so byte-attestation survives as the bed
  doctrine with N=3. (Correction to 973fd7e's commit message: the bed is
  121 cells, not 204 — the larger number crept in from counting
  per-phase checks.)
- **W7's inactive-pose findings need re-reading against the active bed —
  flagged, not re-litigated here.** The tone-adaptation law, its 5x
  mechanism misprediction, and the mid-range transmission residual were
  all measured against the inactive material. The recalibration cascade
  re-measures each before refitting; DL13's model-shape ruling (same
  form this wave, two-axis rework next) stands regardless.

- **W1: macOS couples the accessibility toggles, and the coupled state is the
  only increased-contrast state that exists.** The user measured it directly:
  Increase Contrast force-enables Reduce Transparency and the transparency
  checkbox cannot be uncleared while contrast is on. The harness classifier
  was reordered (contrast first) with the coupling stamped on the profile's
  own manifest entry — the increased-contrast reference is, by construction,
  what a real user who enabled the mode actually sees. Web-side implication
  for W2/W3: vitrea's two accessibility axes are independent media queries,
  but the native reference for increased-contrast embodies both flags.
- **W1: Apple's material has a tone-adaptation transient over extreme
  backdrops.** One 2x impulse-scene capture (black backdrop) came out with a
  DARK capsule — byte-stable within its run — where the committed and 1x
  references are light; a re-run minutes later reproduced the committed light
  version to the byte (mean 0.0000). The trigger correlates with capturing
  too soon after a display-topology change. Session rule recorded: after
  connecting or disconnecting a display, re-verify a black-backdrop cell
  against its committed counterpart before trusting the run.
- **W1: the native reference is more reproducible than #40(c) feared** — that
  entry recorded a regeneration attempt producing "a tier flip and thousands
  of unrelated changes" (a web-side capture problem); the NATIVE side
  reproduces the committed matrix byte-identically (30/30 at 1x, twice, days
  apart).

- **W1/G3: the 2× axis found no defect and instead REPAIRED v1's worst
  cell.** Every scale-free metric at 2× agrees with its 1× twin to within
  noise (ΔE mean invariant to 0.0011 over 24 light cells; web interior levels
  to 0.004), and contour distance scales exactly with DPR because it is a
  device-pixel quantity. The one real change is a repair: claims §5 excluded
  `checkerboard__capsule-button__rest` in the dark profile as ill-conditioned,
  where the extractor recovered 88.9% of the declared capsule and produced the
  worst shape figures in the whole v1 matrix (IoU 0.8434, contour p95
  15.0 px). At 2× the same cell recovers 100.2%, passes the predicate, and
  reads IoU 0.9538 with contour p95 5.81 device px. That cell's v1 figures
  described the instrument at 1×, not the material. Corollary for threshold
  authors: SSIM reads systematically higher at 2× (+0.014 mean) because its
  window is fixed in pixels, so a 2× SSIM bound must be set from 2× numbers
  rather than inherited.
- **W1/G3: the accessibility folds under-occlude against the real reference,
  consistently.** The first measurement of vitrea's accessibility material
  against a native one. Under reduced transparency the reference's interior
  sits at 0.944–0.953 where vitrea reaches 0.827–0.883, the same direction on
  all four cells and both tiers. Under increased contrast the reference's
  interior standard deviation is two to five times vitrea's (0.19–0.30
  against 0.05–0.12) and its luminance transfer slope on the well-conditioned
  cells runs 0 to −0.22 where vitrea's is +0.14 — Apple's accessibility
  material has all but stopped transmitting the backdrop and vitrea's has
  not. The material axis is not gated, so nothing fails;
  `increasedOcclusionLift` and `reducedTransparencyFrost` are the constants
  this points at. Recorded as evidence for a fix wave, not fixed in G3.
- **W1/G3: the native reference reproduces on the WEB side too, exactly.**
  The 1× light-standard profile re-measured on 2026-08-29, through a re-keyed
  capture path, on a different day, reproduces claims §5 figure for figure on
  both tiers — worst calibration IoU 0.8489, contour p95 4.0000, SSIM 0.9114,
  ΔE 0.0247; worst holdout 0.9612, 2.8284, 0.9007, 0.0548; dom tier likewise.
  Combined with the native side's byte-identity across runs, the whole
  fidelity bed is now demonstrated reproducible end to end — the full answer
  to what #40(c) doubted.
- **W1/G3, a named candidate, not a defect claim:** at 2× the rim-intensity
  estimator reads vitrea's rim peak at 0.011–0.040 at 1 device px where the
  native reads 0.0002–0.010 at 3–11 px; at 1× both sides read ~0.001. Two
  stories fit — vitrea's ~1.4 px contour bleed (claims §4.6) finally
  resolving as a bright edge band, or the estimator locking onto the first
  interior pixel — and the native peak distance jumping between 2 and 11 px
  says the estimator is not stable enough to choose. Claims §6.2's ruling
  (no rim thresholds; below quantisation) stands; the light profile's tuning
  file predicted 2× would lift the rim clear of the code step, and the first
  thing the lifted signal shows is that the estimator needs work before it
  can be trusted. Needs a rim-specific investigation before any tuning
  touches rim constants.
- **W1/G3 in-tree, for the record:**
  `apps/reference-apple/fixtures/backgrounds/index.json` is now unread. It is
  rewritten wholesale per capture run and keyed by bare id, so after the 2×
  run it pointed every background at a 640×400 raster and would have had 1×
  web captures composite the wrong-sized backdrop — the same
  wholesale-replacement fault that moved the manifest to schema 2. The scene
  page now reads the manifest's scaled background map, the same lookup
  `compare` and `tier-delta` use. The harness still writes the index; delete
  it or scale-qualify it in the next batched harness rebuild. Also: the 2×
  holdout is SPENT for this configuration — W2/W3 must not fit to those six
  cells, and after they retune, G3's holdout numbers no longer describe the
  new configuration. W6's overlap-check experiment (S1 split-vs-single at
  2σ/3σ/4σ) was NOT run — G3's scope filled the session; still outstanding,
  still browser-side and TCC-free.

- **W1: the committed pressed cell was the anomaly — the reference must be
  captured SETTLED, and within-run stability is not reproducibility.** The
  extension chain's attestation gate tripped on
  `1x-light-standard/checkerboard__capsule-button__pressed`: six settled runs
  (robust to cursor position, scene order, process restarts) reproduce the
  ORIGINAL v1 bytes (0f83802) byte-identically, while the version committed
  at decac41 — captured by the standard-restore run minutes after the
  accessibility toggles flipped off — is a perturbed-state capture (capsule
  interior uniformly ~5–9/255 darker; the same transient class as the
  tone-adaptation Surprise, on a pressed material after an a11y flip instead
  of a black backdrop after a display change; the state survives app
  relaunches, so it lives in the WindowServer). The restore run's check was
  within-run byte-stability only; no against-committed diff ran before
  commit, which is how one anomalous cell slipped into the reference the G3
  matrix then measured against. Corrected back to the v1/settled bytes.
  Session rules hardened: (1) captures run SETTLED — five minutes after any
  WindowServer-perturbing event (display topology change, accessibility
  toggle, wake); (2) every capture run gets an against-committed diff of
  every cell before commit — modified-set exactly-as-expected is the gate;
  (3) the committed 2x bed's provenance is under the same question (every
  2x capture ever taken ran seconds after a display connect) — resolved by
  a controlled settled-2x experiment, outcome recorded below when in.
- **W1, operational: the capture rig's display topology is not stable
  overnight.** The physical panel drops OFF THE DISPLAY BUS when it sleeps;
  macOS then substitutes a headless dummy display at 2.0x, and
  `betterdisplaycli --connected=off` fails SILENTLY when the virtual display
  is the last one standing — which is how one uncontrolled capture ran at 2x
  mid-teardown and overwrote 13 committed 2x fixtures (caught by git,
  reverted; the harness's scale gate behaved correctly throughout — it
  captured exactly the profiles matching the actual backingScale). Chain
  discipline now: run under `caffeinate -d`, wake the panel before phase 0,
  and probe-verify the window's backingScaleFactor after EVERY topology
  command instead of trusting the CLI's exit status.

- **W1: the harness's "byte-stable" was measuring the wrong interval — the
  settledness defect that explains every capture anomaly of the last two
  days (fixed, b88296e).** The stability check captured twice milliseconds
  apart; the material's tone adaptation is an animation over seconds, so a
  mid-adaptation frame passes as byte-stable. A four-run convergence
  experiment on the virtual 1x context showed the signature exactly: each
  run internally "stable", but across runs individual cells flip between an
  adapted and a mid-adaptation byte state (max deltas 31–36/255) — a
  DIFFERENT one or two cells each run, correlated with concurrent system
  load (three agents were hammering CPU/GPU; the settled bytes recur
  exactly whenever a cell is not caught mid-flight). The committed pressed
  cell, the 2x impulse tone transient, and tonight's A/B failures are all
  this one defect sampled at different moments. The fix makes settledness
  the thing measured: 2s dwell after content swap, then captures at 1s
  spacing until two consecutive agree (bounded; a never-settling cell
  records NOISY). Meta-lesson for instrument builders: a determinism check
  is only as good as the interval it spans — validate it against the
  slowest process in the system under test, not the fastest.
- **W1: Decision Log 10's mechanism, isolated — the display-context
  dependence is ColorSync matching of the PHOTO background at draw time,
  and nothing else.** On the virtual display, every non-photo 1x cell
  reproduces the committed physical-panel bytes BYTE-EXACTLY (19/19,
  including checkerboard, solids, hc-text, impulse — and the settled
  pressed cell matches the v1 bytes exactly); every differing cell is
  photo-backed (11/11), while the photo background PNGs themselves are
  byte-identical to committed. So the tagged photo raster is colour-matched
  against the display profile when drawn into the scene, and numeric-colour
  backdrops pass through unchanged. Consequences: the re-baseline's
  expected diff scope is the photo-backed cells only; the manifest now
  records displayName + displayColorProfile per profile as bindable
  evidence; and a future session could pin the virtual display's ICC
  profile to make even the photo path context-free.
  **AMENDED (2026-08-30, settled instrument):** the ±4/255 photo deltas
  were mostly unsettledness too — under the convergence instrument the
  photo-backed RECT cells match the committed bytes within max 1/255 on
  <0.1% of pixels. The ColorSync mechanism was real for the EDID event
  (the 15-cell chromatically-skewed shift after the panel renegotiated)
  but it is not a standing hazard on the virtual display. The standing
  finding is simpler: the settled state is display-stable; only the EDID
  change plus mid-flight sampling ever made it look otherwise.
- **W1: the v1 committed bed was systematically unsettled on the adapting
  components — the settled re-baseline is a materially different, and now
  finally reproducible, reference (2026-08-30).** The first settled pair
  on the virtual display is byte-identical E==F across all 34 1x cells —
  the strongest attestation any capture has produced — and against the
  committed bed the diff is exactly the component footprint on exactly the
  components that tone-adapt: every capsule-button cell, toolbar-group,
  rrect-sm, glass-over-glass (means 1.3–3.2/255; the extreme-backdrop
  capsules dark-solid and impulse at mean ~14.5, max 192–198 — the
  committed cells caught DEEP mid-adaptation), while every rrect-md/lg
  cell and light-solid capsule matches committed within 1/255. The
  committed bed mixed settled and mid-flight states cell by cell, as the
  moment happened to give; the settled bed is a definable state. All v1
  fidelity figures were measured against the mixed bed and regenerate
  against the settled one; adopted thresholds re-verify (they carry
  2–3x margin over deltas of this size in the aggregate metrics, but the
  re-verification is the evidence, not this sentence).

- **W1, the recapture's biggest finding: Apple's material tone-adapts INTO
  extreme dark backdrops, and vitrea has no such axis (2026-08-30).** The
  settled regeneration STOPPED at the adopted gate, correctly: seven
  perceptual rows fail, all on the two capsule cells over extreme backdrops
  (worst ΔE p95 0.66 against ≤ 0.17). Visual confirmation is unambiguous —
  the settled light-scheme capsule over the impulse (black) backdrop is a
  near-black capsule with a faint rim (interior 0.013 where the v1 bed
  read 0.681), and over dark-solid it is within 0.02 of its own backdrop;
  vitrea renders a light capsule (0.631) in both. The v1 bed had captured
  the PRE-adaptation flash on these cells — the earlier "tone-adaptation
  transient" Surprise had the polarity backwards: the dark state IS the
  settled state; the committed light state was the transient. This is
  macOS 26's documented behavior (Liquid Glass adapts appearance to
  backdrop luminance continuously); vitrea's material profiles are
  discrete per-scheme, so the continuous backdrop-driven axis is missing
  entirely — a renderer feature gap, not a threshold problem. Every other
  adopted row HOLDS over the settled bed (all shape, all ΔE mean, all
  edge-weighted, both coherence rows). Also repaired by the settled
  instrument: the 1x dark checkerboard capsule (§5.3's exclusion) now
  recovers 100.8% / IoU 0.9516 — the exclusion was instrument-caused at
  BOTH scales. `impulse__capsule-button__rest` now correctly fails the
  conditioning predicate in all four standard profiles (the reference is
  nearly invisible — there is almost no silhouette to extract).
- **W1/coherence, first blood: the GPU tier draws the dark-scheme PRESSED
  capsule ~2x brighter than the CSS tier** (interior ratio 1.964 at 1x,
  1.985 at 2x, on `photo__capsule-button__pressed` in both dark profiles;
  the light profiles' worst on the same scene is 0.937 — dark+pressed
  specifically). Exactly the C9a-class cross-tier regression the
  coherence axis was built to catch, on a scene the split extension had
  only just added. Ungated (dark is provisional) so nothing fails; fix
  wave dispatched. The same cell carries the dark profiles' worst contour
  figures (p95 13.0/27.0), so the dark threshold proposals wait for the
  fix.

- **W7 landed: the exclusion dissolved at bounds nothing edited — and the
  dark-solid texture cells are now BYTE-IDENTICAL to the reference (dE p95
  0.0000, SSIM 1.0000; impulse cells 0.032-0.037 against <= 0.17).** The
  measured law's observable is the SEPARATION between the light and dark
  reference over the same backdrop (transmission, backdrop structure and
  both scheme tints cancel algebraically): x = backdropLuminance +
  0.09*sizeThickness(span), smoothstep down over 0.14..0.02 - the size term
  in the curve's ARGUMENT, not its amplitude (an amplitude gate fits
  calibration and mispredicts validation). The scheme sets only the neutral
  the adaptation departs from, so the dark profile runs the same constants
  without double-adapting. CSS-tier design chosen BY the coherence gate:
  backdrop tone resolves once per group host-side (hint, else linear-light
  average of the supplied backdrop, else nothing) and feeds BOTH tiers -
  per-pixel GPU vs averaged CSS read a ratio of 79 (the tiers blur in
  different colour spaces); resolved-once reads exactly 1.000. Full
  regeneration: 140/168 cells bit-identical, 28 moved, none the wrong way;
  coherence floor 0.818 untouched to the digit.
- **W7 corrects W2's occlusion reasoning - parent-impact, named:** the tone
  axis exists now and the occlusion sweep is unchanged in shape, so W2's
  "the axis that would let it fit is W7's" was wrong. The real residual is
  the material's MID-RANGE TRANSMISSION (vitrea 0.79 over a 0.5 backdrop
  where the reference reads 0.61) - a retune of C9a's fitted tintAlpha that
  would move every cell and every adopted bound. Deferred as the next
  cut's calibration item, deliberately not touched mid-wave.
- **W7's axis has NO holdout validation - a bed gap, not a discipline
  breach.** Every holdout scene's backdrop sits far above the adaptation
  curve's knee, so the unmoved holdout proves no regression but cannot
  validate the axis. A dark-backdrop holdout scene should ride the next
  capture session (batched with W3's tinted cells - one rebuild, one
  re-grant).
- **W7's class lesson: when an axis moves the material this far, every
  decision previously taken AGAINST the material needs re-checking.** Two
  runtime defects surfaced this way, both fixed in-branch: the ink was
  decided against a backdrop the runtime had measured and then ignored, and
  a live canvas/video backdrop's tone froze at first paint. The calibration
  bed structurally cannot see either (static backdrops, no ink cells).

- **W2: the coherence floor is the project's tightest margin now — 0.818
  against a gated 0.80** (cross-tier interior ratio, glass-over-glass holdout
  cell), with texture SSIM on the same scene at 0.8934 against >= 0.88. The
  mechanism is structural: the size law's two active facets (inner shadow,
  scattering) deepen a large surface on the GPU tier and the CSS tier has no
  inner shadow to gain, so the tiers diverge exactly where the law acts.
  Closing it is a CSS-tier feature (an inner shadow), not a constant.
  Anything that deepens the GPU tier further before that feature exists
  tightens this margin — W7 must re-check it.
- **W2: W7 inherits a BUILT seam.** The occlusion gain is wired on both
  tiers and fitted to zero purely because the tone axis does not exist yet
  (vitrea's interior sits 0.16-0.19 above the reference at every span, and
  the axis that would let occlusion fit is W7's). W2 also declined a band
  optimum (64, better by 4%) because that gain was entirely the
  tone-adaptation residual — fitting it would have used the size law as a
  proxy for W7's mechanism. W7 fits the existing occlusion gain and may key
  its light/dark flip off the same thickness curve; it does not build the
  plumbing.
- **W2, the good news in the same measurement: the dark interior-ratio
  ceiling watch RESOLVED** - 1.2412 -> 1.1998 at 1x and 1.2377 -> 1.1956
  at 2x, moving away from the 1.25 ceiling rather than toward it.
- **W2, discipline disclosure (recorded, not repeated):** during
  exploratory measurement a derived reference-side statistic for the
  rrect-lg holdout scene printed before the fit froze. It did not enter the
  fit (constants fitted on spans 32/44/96; the curve saturates by 96 in
  calibration data alone). The holdout was spent once, pre-rebase, and its
  validity across the tint merge was proven by 72 bit-identical re-measured
  values rather than assumed from W3's zero-strength identity claim.

- **W3: under forced colours the GPU tier's optics pass kept painting —
  invisible until tint made it visible.** Untinted, the leak rendered as
  white and coincidentally matched the platform Canvas colour; with an
  author tint it became a flat fill of the author's colour exactly where
  the platform palette is required. Fixed in phase 1 (the pass now stands
  down with the material). The class lesson: a bypass that happens to
  match the expected output is only ever one feature away from being
  visible — `glass: "none"` paths need tests that assert the MECHANISM
  (pass not running) rather than the pixels.
- **W3 in-tree:** two Firefox press/morph e2e cases fail a different
  subset each run on an unchanged tree (the #32(e) intermittency class,
  React suite) — logged to tech debt, not chased in a colour-axis child.

- **W6: the cross-group overlap check is more conservative than S1's own
  measurements support — left standing, with an experiment attached.**
  `proxy-overlap-after-enforcement` fires when padded boxes intersect (≈6σ
  separation at the derived default), but S1's leak table measured a 40px gap
  at 60px padding as Gaussian-tail noise (mean 0.03–0.05/255). The demo's
  one surviving warning sits at 4σ — past where S1 found noise. Not narrowed
  by W6 (re-deriving a normative invariant from a spike table is not a
  hygiene sweep's call); the parent-level experiment: re-run S1's
  split-vs-single comparison at 2σ/3σ/4σ separations to find where the leak
  stops mattering. Browser-side, no TCC — cheap to attach to any W1 session.
  **RESOLVED (2026-08-29; `spikes/s1-proxy-topology/overlap-experiment/`):**
  81 cells (3 blur radii × 4 backdrop classes × separations 0.25σ–8σ), all
  byte-identical across passes. The leak is byte-identical ZERO at every
  separation above 3σ, at every radius — and 3σ is the geometric bound
  (chaining needs one proxy's padded box to reach a neighbour's PAINTED
  region, which extends only to its clip; the Gaussian tail dies before the
  geometry does). Between 3σ and 6σ the boxes intersect over a region where
  neither group paints — the check's stated mechanism provably does not
  occur there. The demo's surviving warning is a measured FALSE POSITIVE
  (4σ separation under reduced transparency: 0/0/0 byte-identical). Fix
  dispatched: narrow the predicate to padded-box-vs-painted-region overlap
  (mechanism, no perceptual constant; order-aware refinement deliberately
  declined — a paint-order-dependent finding is worse diagnostics than a
  symmetric layout statement), and fix the message's stale "up to 17/255"
  magnitude. Methodology lesson for future split-vs-single comparisons:
  corner antialiasing of the clip path is a constant cost of splitting
  (~1/255 on 0.7% of pixels, up to 17/255 on the corner arcs) that never
  reaches zero and must be excluded, or the comparison cannot bottom out —
  S1's "noise floor" was two different things stacked, and "put them in one
  group" is not a byte-neutral remediation.
  **Fix LANDED (647a374):** the predicate is now padded-box-vs-clip-union in
  both directions, the clip union rides `ProxyGeometry` (it was already
  computed en route to the padded box), the message quotes magnitudes from
  separations the check actually fires at, and the demo-shaped geometry
  demonstrably goes quiet at 56px and re-fires at 28px in the platform-web
  e2e harness (the demo site itself renders adapter-less on CI-class
  runners and builds no proxies — the harness spec is the reproduction of
  record, stated in-tree). Carry-forwards, both closed same-day: core's
  `group-proxy-overlap` carried the identical defect — narrowed to the same
  mechanism predicate (aa7d481; core 264/264, the old predicate flips
  exactly the two intended cases, everything downstream green); and the
  e2e accessibility-state fragility (four platform-web specs inherit the
  machine's OS toggles; Chromium answers `prefers-reduced-transparency`
  from the OS and Playwright cannot emulate it) is recorded in the new
  `tech-debt-tracker.md`, along with a second entry the worker surfaced:
  `merge-distance-below-padding` still quotes the retired 17/255 figure
  for a WITHIN-group mechanism nothing has measured — rewording means
  measuring that case or dropping the number, deliberately left as a call
  for later.
- **W6: the tab-order defect never reproduced in vitrea's own demo — and
  that is the alarming half.** The demo's app portals the whole `<nav>`, so
  the platter mounts last and the order came out right by luck; a toolbar
  that portals itself (the consumer's shape) lands it first. The reproduction
  of record is the unit fixture; the demo e2e cases are labeled guards, not
  reproductions. Same lesson as the corner hooks: correctness that depends on
  the app's structure is only ever sampled, never proven, by one demo.
- **W6: `aria-owns` had a second, independent ordering disagreement** —
  owned elements append after DOM children, so the accessibility tree read
  the hoisted trigger last while the arrows put it first. Two orders
  disagreeing is worse than either being wrong; the descendants-first sort
  makes arrows, tab stop, and screen reader agree by construction.
- **W6 in-tree, for the record:** `packages/react/e2e/press.spec.ts` is
  intermittent on this machine independent of any change (2 of 3 clean-tree
  runs at dec7299 failed on differing motion-timing subsets) — the #32(e)
  class, now observed on Chromium as well as Firefox.

- **W4: the coverage matrix credited vitrea with an enforcement it did not
  have — and the neighbouring check's advice made the gap worse.** §3.10's
  last row read S356's rule as "enforced structurally" via X1's same-plane
  overlap check. The overlap check is geometric and same-plane only: a nested
  host promoted to the overlay plane produced no finding, a glassed `<ul>`
  overlapping nothing produced none — and the overlap finding's remediation
  ("put the upper one on the overlay plane instead"), followed on a
  glass-on-glass nesting, silences the only check that fired while leaving
  the forbidden composition in place. W4's `glass-inside-glass` is structural
  and cross-plane precisely because of this. The matrix row is annotated.
- **W4: §3.9's most actionable-looking row is the one that cannot be
  afforded.** The OS 27 "nothing scrolling underneath, nothing to refract"
  criterion needs the dirty epoch watched *over time* — per-frame work the
  child's acceptance forbids — and a page at rest is indistinguishable from a
  page that never scrolls: it would fire on vitrea's own deliberately
  non-scrolling playground. Deferred as a future initiative with a
  temporal-observation budget.

- The user's labeled WebKit manual pass ran on Safari 18.6 / macOS 15.7.7
  (their screenshots), not the Safari 26 the checklist named — the
  conformance row is keyed at the measured 18.6 claiming forward, and the
  MacBook's macOS 15 is what ruled it out as the capture machine (Decision
  Log 2).
- Re-verifying the Chrome 152 regression for the bug report sharpened it:
  the trigger is `clip-path: path()` specifically — every basic-shape value
  is unaffected (12-cell × 2-build matrix in
  `spikes/s1-proxy-topology/chrome152-regression/REPORT.md`). This gave W6's
  advisory-row item concrete, testable content.

## Outcomes & Retrospective

Pending — written when the unit closes. Closing is a RECOMPOSITION check:
verify Parent-Level Acceptance as written — all children landed is not the
same event — then retrospect.

## Revision Notes

- 2026-08-31 (W8 — the outer shadow, appended by that child; no other section of
  this spec touched). **The facet is implemented on both tiers, behind the
  material profile seam, with the mechanism measured from the active bed's own
  fixtures and the constants left PROVISIONAL for the cascade (X1).**

  **The mechanism, and how well it holds.** The reference's shadow is the
  component's OWN rounded silhouette, outset by a spread, translated down by an
  offset, blurred by a Gaussian, and applied as a multiplicative occlusion:
  `L_out = L_backdrop × (1 − occlusion · Φ(−sd/σ))`. Fitted in two dimensions
  against the outside-component region of the native fixtures, that model
  reproduces the reference to an **RMS of 0.0021 in occlusion over 142,550
  pixels** on the finest cell (2× photo capsule), and 0.0013–0.0043 on every
  other well-conditioned cell — about 1% of the facet's own amplitude.
  Multiplicativity is not assumed: mirrored pixel pairs either side of a capsule
  over the `photo` backdrop see one shadow over two different backdrop
  luminances, and the darkening's ratio tracks the backdrop's ratio to **4.5%**
  where a constant-subtraction model misses by **79%** of the signal.

  **The lengths are in points, and they are span-invariant.** σ = 15.55, offset =
  7.95, spread = +3.10 CSS px. The 2× bed measures 31.05 / 15.83 / 6.58 device
  px — exact doubling — and the amplitude is scale-free to four decimals
  (0.3259 at 1× against 0.3258 at 2×, independently captured). Across spans of
  32, 44, 96 and 160 px the fitted σ stays inside 15.4…15.9 and the offset inside
  6.9…8.1. **Only the amplitude is a candidate for span coupling**, and it ships
  uncoupled: over `photo` it falls 0.326 → 0.196 between span 44 and 96 while
  over `light-solid` it RISES 0.123 → 0.198, and the dark scheme rises 0.060 →
  0.177 → 0.274 across 44/96/160. One monotone gain on one thickness curve cannot
  be all three; the seam exists at the identity, which is `sizeOcclusionGain`'s
  own precedent and Decision Log 13's finding in a second place.

  **Both tiers paint it identically, and the CSS tier's version is not an
  approximation of the mechanism.** A `box-shadow` is a constant colour
  composited source-over — which IS a multiply when the colour is black, because
  `(1−α)·backdrop + α·0` is `backdrop·(1−α)`. So the facet is analytically zero
  over black on both tiers with no branch for it, which is what the reference's
  `dark-solid` cells measure. The GPU tier reaches the same algebra by emitting
  premultiplied black outside the contour: the readback gains ALPHA and moves no
  RGB channel at all, so what the browser composites is `page × (1 − α)`. The one
  stated residual is the colour SPACE — the reference removes linear light, a
  browser composites encoded sRGB — closed by inverting sRGB's power law
  (`α = 1 − (1−occlusion)^(1/2.4)`), whose worst departure across every backdrop
  level from 0.004 to 1.0 is **2.1 of 255**, against a bed whose own
  reproducibility is ±4/255 (Decision Log 10). The second residual is the blur
  KERNEL: a browser approximates the Gaussian, the shader evaluates it.

  **Accessibility, measured before the fold was written, as the charter
  required.** Reduce-transparency does not remove the shadow and does not deepen
  it: it scales the amplitude to **0.566** (0.1830/0.3259, 0.1884/0.3309,
  0.1882/0.3314 on three backdrops at span 44) with σ, offset and spread
  unmoved. The increased-contrast reference reproduces the reduced-transparency
  amplitude **to four decimals**, which is Decision Log 8 again — macOS
  force-couples the toggles, so the bed cannot separate them and the fold keys on
  `frost`, the axis reduced transparency alone sets. What increased contrast adds
  on top is a hard border stroke one pixel outside the contour (ring-0 occlusion
  0.60–0.80 against a shadow of ~0.10 at ring 1); that is a separate renderer
  feature and the amplitude above is measured on an annulus that excludes it.

  **A correction to the two-axis instrument's reading of the dark scheme.** The
  instrument reports the dark profile's shadow as a different SHAPE — upward
  reach collapsing 24 px → 8 px while the downward reach barely moves — and
  recommends a scheme-dependent geometry. Re-tested directly, that is not
  supported: refitting the dark cells with the LIGHT geometry and only the
  amplitude free costs 3–7% of an already-tiny residual (photo capsule RMS
  0.00177 against 0.00165 free; photo rrect-md's own free fit lands on the light
  geometry at σ 15.21, offset 7.29, spread 3.14). One geometry with one amplitude
  per profile predicts the instrument's per-direction extents in all three
  profiles from a single calibration point: light 24/40/32 against a measured
  24/39/32–34, dark 10/26/18 against 8/26/16–20, reduced-transparency 20/36/28
  against 20/35/28–29. The apparent collapse is an iso-occlusion threshold acting
  on a weaker shadow — upward is where the shadow is thinnest, so it crosses
  first. Separately, the falloff is Gaussian rather than exponential: over 585
  points on the 2× photo capsule the Gaussian CDF fits at RMS 0.00233 against the
  exponential's 0.00527. Both are the implementable choice as well as the
  measured one, since `box-shadow` blurs with a Gaussian and takes one alpha.

  **What is NOT closed here.** The amplitude has no mechanism for its
  scene-to-scene spread: over the flat near-white `light-solid` backdrop the same
  fit reads 0.123 rather than 0.33, reproducibly and at both scales, while the
  other flat backdrop (`mid-dark-solid`, linear 0.0595) reads 0.310 — so it is
  not a function of backdrop luminance, backdrop structure, or the material's own
  interior level, and no compositing model in either colour space produces both.
  The dark scheme's amplitude (0.046–0.061 at span 44) lands as a profile patch,
  not as a branch. Both are the cascade's to fit, and no threshold is proposed or
  re-adopted here — Decision Log 15's third ruling stands.

  **Two field-extent defects, found by the independent review and fixed before
  landing.** Both were on the GPU tier only, and both are the same class: the
  shadow is read from the group's field texture at an OFFSET position, so the
  rect that texture spans has to be right in two different ways.

  The first is the pad's arithmetic. It thresholded the LINEAR occlusion, where
  what the canvas writes is `page × (1 − α·falloff)` in compositing space — so it
  reached about five CSS px further on every edge than anything could be seen at,
  pure cost on a facet already measured at three times frame time. And it was
  taken from the group's BASE amplitude, before the size law amplifies it per
  surface: at a fitted gain the shader would emit more shadow than the pad
  reached and the scissor would slice roughly the last 6 px off, while the CSS
  tier, which has no scissor, went on drawing it. Both are fixed; the pad now
  solves in compositing space against the maximum effective amplitude across the
  group's members, which is an upper bound whatever sign the gain is fitted at.

  The second is a real rendering defect rather than a cost. The shift can leave
  the field texture — for a surface within the shadow's reach of the viewport's
  TOP edge, where the rect is clipped to the canvas, and in the topmost band of
  every group, since the pad is only the reach and the shift needs a further
  offset above that. Clamping repeated the edge texel, which is a distance too
  small, so the falloff went FLAT and too dark exactly where it should have been
  fading out. Reproduced before the fix as a constant alpha of 15 over the seven
  rows above a top-adjacent surface. The shader now adds back the distance it
  could not read, which is exact directly above a surface because a signed
  distance field is 1-Lipschitz, and errs toward less shadow past the corners.
  Pinned by two new GPU e2e scenes — the same surface at the top edge and clear
  of it — asserting the profile is not flat, is monotone, and agrees with the
  unclipped one within one 8-bit code.

  Three goldens re-recorded, with the delta attributed rather than accepted:
  every one of the roughly 5,000 moved pixels per scene moved in ALPHA and moved
  DOWN, which is the only direction either fix can push, at a maximum of 6 codes
  and a whole-canvas mean of 0.03. The 31–113 pixels per scene whose colour moved
  moved by exactly one code and sit inside the surfaces, which is f32 rounding
  from the changed rect origin. The isolation proof still reproduces `main` on all
  nine scenes with the shadow declined, so the attribution of the whole facet is
  unchanged.

  **The facet is not cheap, and the number is measured rather than estimated.**
  Every GPU pass is confined to the group's field rect, and the shadow's reach
  takes that rect's pad from the rim-and-bulge 3 CSS px to about 41 — several
  times the area on a small control. The benchmark now carries a row for it: the
  same scene at `occlusion: 0`, interleaved, which is the only comparison this
  benchmark trusts. On `apple / metal-3` the shadow costs about **2.8× the
  frame's GPU time** on the mobile scene (median ratio 2.79), moving it from
  roughly 40% of the ~2 ms hypothesis to roughly 115%.

  It came down from 3.35 in two measured steps, each A/B'd on that row at three
  interleaved runs a side. Scoping the highlight pass back to the surface's own
  rect took it to 3.11 — it returns on `coverage <= 0` and was paying to
  rasterise the difference — with the `highlight-press-glow` golden byte-unmoved;
  that is less than its own pass timestamp suggested, because most of that
  timestamp is the field texture's write draining into the next pass. Tightening
  the pad's threshold into compositing space took it to 2.79.

  The rest is inherent: the shadow covers 8.5–29.6% of the canvas in the
  reference and something has to write those pixels. What is still not inherent
  is the field pass rasterising the whole enlarged rect at full resolution to
  feed a term whose own detail is a 15 px Gaussian. **A dedicated low-resolution
  shadow pass is the structural fix and is recommended for the next cut**; it is
  a design change rather than a tuning one, so it was not taken here.

  **Verification.** 1,557 unit tests green (20 new pinning the mechanism: the
  falloff's shape against an independent `erf`, the multiplicative algebra, the
  zero-over-black property, the colour-space residual, the accessibility folds,
  the size seam, the field rect's pad, and the shader's own text); every
  Playwright suite green; goldens re-baselined with a byte-level attribution —
  rendering every golden scene with `outerShadow: { occlusion: 0 }` reproduces
  what `main` renders from the same patch, all nine scenes, exactly. Two
  pre-existing failures were found on `main` and are NOT W8's: the isolation
  proof's eight recorded hashes (re-recorded here, with main's own output and the
  reasoning), and one demo readout assertion on a tinted dark-solid scene (left
  alone). The field-extent pin is fail-before verified: with the reconstruction
  reverted it reports the flat profile the defect produced.

- 2026-08-30 (eleventh revision — the measurement child of W3/W7, appended by that child; no other section of this spec touched). Two results, one good and one that needs the human gate.

  **W7's axis now has its holdout, and the curve fails it.** `mid-dark-solid__capsule-button__rest` was measured once against the frozen W7 configuration, and nothing was tuned in response. On the adopted gate it passes every light-standard row at both scales and on both tiers (texture 1×: SSIM 0.9817, ΔE mean 0.0104, ΔE p95 0.1267 against ≤ 0.17; coherence 0.0031 against ≤ 0.05). On the mechanism it misses by five times: the reference's measured adaptation on the slope is **0.127** where the landed constants predict **0.650**. vitrea executes the constants correctly (0.6497 measured), so the defect is the law, not the implementation. And the miss is a *shape* error rather than a scale one — measured adaptation is 1.000 at curve argument 0.0200, 0.127 at 0.0678 and 0.218 at 0.1017, which is non-monotone in that argument, so no `1 − smoothstep(low, high, x)` reproduces all three. The two points the law was fitted on come back right (1.000 and 0.218 against a predicted 0.241); only the unseen one moves. Per Decision Log 11's own reasoning a differently-shaped curve is a design change, not a retune, so this is left for the gate. Full figures: claims §5.9.

  **W3's measurement phase is blocked by a defective bed, not by analysis.** The tinted native cells captured on 2026-08-30 carry no author-tint colour: `photo__capsule-button__rest-tint-blue` is byte-identical to its `-tint-orange` twin at both scales (as are the `checkerboard` and `dark-solid` pairs), every tinted cell's native chroma response sits inside the untinted cells' band, and the tinted interiors are their untinted twins scaled by a channel-uniform factor. The tint's *alpha* did arrive — the half-strength cell differs — so the registry was read and only the colour was lost. The Swift wiring reads correct by inspection, so separating "the seed never reached `Glass`" from "`Glass.tint(_:)` ignored it" needs a rebuild, which invalidates the TCC grant and therefore belongs to the next capture session. No tone-curve constant was fitted, no tint threshold is proposed, and the four constants stay provisional and unclaimed. The tinted cells are absent from `results/matrix.json` by derivation rather than by memory — `cli/compare.ts` finds the byte-identical pair and skips the tint axis on every profile, since one binary in one session cannot have dropped the seed for two scenes alone. Claims §5.10.

  **What did land.** A full six-profile regeneration from clean over the valid bed: **176 cells** (168 + W7's holdout scene in the four standard profiles), every adopted bound holding, the partition assertions restated per profile, suite and lint green. The tint plan (`2026-08-30-w3-tint-capture-plan.md`) needs no revision — it runs unchanged against a re-captured bed.

- 2026-08-31 (twenty-second revision — round two, executed and reported; appended by the recalibration cascade, no other section touched). Decision Log 17's two rulings, done in its order. Declaration: claims **§5.15**, committed at `8d7ff60` before any constant moved. Result: claims **§5.16**; staged matrix `packages/calibration/results/2026-08-31-round-two.json`. **Nine rows still fail, so this routes to the gate rather than to a third round.**

  **The outer-contour instrument landed first, and it works.** `contourDistance` now measures between each mask's outer contour — both silhouettes are hole-filled before their boundaries are taken. Regenerated over calibration and validation: **71 contour cells moved, 103 did not, and not one non-contour value changed.** The worst readings collapse (15.34 mean / 65.0 p95 → 0.605 / 5.00). The topology arm is withdrawn from the gate and kept in the record as the measurement that motivated the replacement; the hole counts stay on every cell, and IoU still sees a hole because a hole is a genuine set difference even when it is not an outline difference. The conditioning predicate is now two arms with no chosen number in either: area on both sides against the region floor, and bodies on both sides against the *declared region's own* body count — the second arm exists because hole-filling does not merge fragments, and on `photo__rrect-md__rest-tint-orange` at 2× the reference is one body and the CSS tier is four.

  **Fit A succeeded and it was the bigger gap.** `increasedOcclusionLift` 0.4722 → **0.75**. The old number was never a measurement — it was the pre-C9a absolute floor re-expressed as a fraction so the new form would reproduce the old behaviour — and against the active bed it under-occluded on both accessibility profiles. The two profiles disagree about the right lift exactly as §5.15 predicted, because macOS couples the toggles and one constant serves both (increased contrast wants 0.80, reduced transparency 0.70); 0.75 is the minimiser of their equal-weight sum. `reducedTransparencyFrost` swept as declared and is flat to 1.03× — unidentifiable, so unmoved. Interior levels move from 0.777–0.786 to 0.897–0.933 against references of 0.893–0.957, and `photo__rrect-md__rest` under reduced transparency is now exact. **Eight failing rows became passing rows**, and every perceptual row on both accessibility profiles now passes.

  **Fit B found nothing, and the reason is §5.14's bind one level up.** `sizeSpanMax` is monotone *against* widening (0.09346 at 96 → 0.09861 at 224, 1.06× spread) and `sizeOcclusionGain` reads a 0.05% difference at 0.05 — its fifth boundary result. The cause is structural: **the calibration set's largest span IS the band's current top**, so widening can only lower thickness at 96 and weaken a law the objective already likes. The spans that would separate a band ending at 96 from one ending at 160 — 130 and 160 — exist in this bed only in the holdout. §5.14 corrected the deficit's attribution from the gain to the band; round two shows the band is no more identifiable from calibration cells than the gain was.

  **The flagship profile is bit-identical across round two** on every perceptual row of both light-standard profiles and both tiers — the lift is reached only by an accessibility policy, and that inertness is observed rather than asserted.

  **The nine remaining rows, by mechanism.** Six are the band (SSIM and ΔE p95 on both light-standard profiles, every one on `checkerboard__rrect-lg__rest` at span 160 or a `glass-over-glass` cell at span 130 — the very holdout scenes that expose the deficit and therefore cannot be fitted to). Two are pre-registered contour bounds the holdout refuted on their first read under the new metric (`2x-light-standard` dom p95 10.00 against a declared 8.0; reduced-transparency dom p95 5.00 against 3.5, both single raster steps). **Those two are deliberately NOT amended here** — widening a bound after seeing the holdout figure that broke it is the contamination §5.15's protocol exists to prevent. The ninth misses by 0.0005 (reduced-transparency texture SSIM 0.9595 against ≥ 0.96, a validation cell, against a bound adopted on the inactive bed); it is a calibration-side figure, so **≥ 0.95 is proposed** as its honest active-bed value.

  **What the gate now owes.** The accessibility half of the adoption blocker is closed. The band half is not, and it has a sharper name: not a missing gain, not a bound that needs widening, but a bed whose calibration cells cannot see the span range where the deficit lives. Three ways out, all the gate's to choose: give the band a calibration cell at span 128 (the canvas allows it, at the cost of clipping the reference's shadow so the cell carries no usable shadow axis); enlarge the canonical canvas (which §5.7 already named for the scatter gain, and which re-beds every fixture); or accept the band at 96 and record the large-surface deficit as a measured limitation, narrowing what the flagship claim covers. `results/matrix.json` is unchanged, the gate has not flipped, and the demo's tinted-cell assertion stays red for the reason the twentieth revision gives. DL17's "280-pt" naming correction is swept through: the only remaining uses are in DL16's and DL17's own historical text, where DL17 annotates it.

  Build, lint and **1,588 unit tests** green.

- 2026-08-31 (twenty-first revision, by the parent): Decision Log 17 - the coda voided on the cascade's arithmetic (upheld), round two chartered as a declared-protocol fit on existing cells with the holdout contamination stated, the outer-contour instrument approved, the adoption held until round two reads. DL16's \"280-pt\" naming corrected (rrect-lg span is 160).
- 2026-08-31 (twentieth revision — the adoption round; appended by the recalibration cascade, no other section touched). Full figures: claims **§5.14**, with a dated correction banner on §5.13's second holdout finding. **The instrument work landed; the suite flip did not**, and the blocker is a correction to my own diagnosis rather than anything new the bed said.

  **Decision Log 16's span coda is void, and it is arithmetic rather than a measurement — so no capture session was run.** The size law's one input is `smoothstep(32, 96, span)`, exactly 1 at every span at or above 96, so `sizeOcclusionGain` takes the *same* value on `rrect-md` (span 96), `glass-over-glass` (130) and `rrect-lg` (160) and provably cannot separate them; calibration cells at 128 or 192 would land on the point the span-96 cells already occupy — more cells, no information. And the canonical 320×200 canvas cannot host the band regardless: a span of 192 needs 336 px of width at the existing aspect, and every span from 128 up leaves under 41 px of vertical margin against a reference shadow reaching 39–41 px, so it clips the very facet W8 built. Nothing larger than `rrect-lg` fits, so there is no fresh large holdout cell to add either. Spending the activation window on a fit that arithmetic says is unidentifiable would have been worse than saying so.

  **§5.13's finding (2) is corrected, and the correction is mine to own.** I attributed the large-surface deficit to `sizeOcclusionGain` and wrote that vitrea "is not too transparent at 32, 44 or 96". Both halves are wrong. The deficit is already present at span 96 in *calibration* (over `photo`, −0.086 at 96 against −0.102 at 160), and it is two other mechanisms. **The band's top:** past 96 the reference keeps changing (0.6649 → 0.6855) while vitrea is flat, because every size facet is saturated there — that is `sizeSpanMax`, and the bed's existing spans can identify it without new captures. **The accessibility occlusion constants, which this cascade never refitted at all:** Stage 0 measured the fold under-occluding, §5.11 recorded it, and no stage fitted `increasedOcclusionLift` or `reducedTransparencyFrost`. It shows as a calibration residual rather than a large-surface one — increased contrast misses by −0.173 at span 96 (calibration) and −0.186 at 160 (holdout), the same miss at both spans — and it owns six of the ten failing rows.

  **Why the gate cannot flip.** After the topology predicate the rows that still fail are SSIM and ΔE p95 on both light-standard profiles (the band top) and SSIM, ΔE and edge-weighted across both accessibility profiles (the fold). Enforcing a suite that omits its SSIM and ΔE p95 rows on the flagship profile would be a weaker gate than the one it replaces; excluding those cells as documented exceedances would certify a measured 0.17-of-interior transparency error as acceptable fidelity — the move Decision Log 11 refused and Decision Log 16 refused again for the tint. So `results/matrix.json` stays on the inactive-bed schema-4 gate for one more round. **That is not a good state** — the enforced suite currently gates the shipped material against a retired reference — and it should not survive another cut. The demo's `site.spec.ts` "every scene's figures come from the primary cell" case is red for exactly this and nothing else: it reads `matrix.json`, the tinted scenes are declared, and the inactive bed carries no tinted cells. The adopted constants do not repair it and never would; the matrix flip does.

  **What did land, all verified.** The review's single finding is fixed and pinned — `sweep.ts`'s interior-objective guard no longer discards a shadow-only sweep, with three cases pinning both directions. The topology arm is built (`silhouetteHoleCount`, plus `silhouetteHolesNative`/`Web` on every cell) and the matrix was regenerated to add it with **20,293 measured values identical and zero differing**, so the addition is provably inert and §5.13's holdout figures are the same measurement rather than a second spend. Measured, the predicate costs more than proposing it suggested: 101 of 234 shape-bearing cells carry holes, the contour rows lose 50 cells to topology and 27 to the two area arms, and on increased contrast they gate **zero** cells. It does fix its target — light-standard contour goes from 15.34 mean / 65.0 p95 to 0.50 / 4.29 — but the better answer is a contour distance measured between *outer* contours, immune to holes by construction and needing no arm at all; recommended rather than taken, because it moves every contour figure and needs its own read. Corner curvature is never gated, now with its evidence recorded (ratios of 496×, 800× and 8125× on cells reading IoU 0.96). The tint's gamut clip is recorded as a limitation beside the tinted tables per DL16, with no coherence bound proposed until it is solved.

  Build, lint and **1,588 unit tests** green. The GPU golden bed is still red and still deliberately not re-recorded: it would bake in a configuration the gate has not adopted.

  **One more round closes this:** `increasedOcclusionLift` and `reducedTransparencyFrost`, then `sizeSpanMax` with `sizeOcclusionGain` refitted after it, all on calibration cells that already exist — then one fresh holdout read, carrying the disclosure that §5.13's holdout column has been read, so the fit must be declared and gridded before it runs.

- 2026-08-31 (nineteenth revision, by the parent): Decision Log 16 - the cascade's holdout pass adjudicated. Adopt in full; the span coda chartered (the one capture session ruling 2 requires, autonomous); W7's mid-dark miss recorded as a documented exceedance owned by the next wave's rework. The cascade's independent review round runs before the adoption bakes constants into goldens.
- 2026-08-31 (eighteenth revision — the recalibration cascade resumed and completed its fits; appended by that child, no other section touched). Full figures: claims **§5.13**; staged matrix `packages/calibration/results/2026-08-31-active-bed-refit.json` (schema 5, 242 cells, six profiles × both tiers, all three sets). `results/matrix.json` is untouched and the enforced suite still runs the inactive-bed gate, per Decision Log 15 ruling 3 — **the adoption is the gate's, and this note is the proposal.**

  **Eleven constants refitted, on calibration cells only.** `tintAlpha` 0.62 → 0.46 and `blurSigma` 8 → 3 (identifiable for the first time — §6.1's "not identifiable" described a bed that passed almost no structure); `shadowAlpha` 0.55 → 0.05, the largest single gain in the cascade; `sizeShadowGainMax` 1.4 → 1; `backdropToneSizeBias` 0.09 → 0.13 and `backdropToneLow` 0.02 → 0.03; `tintToneFloor` 0.45 → 1 and `tintToneCeilMix` 0.45 → 0; `outerShadow.occlusion` 0.33 → 0.285 and its reduce-transparency fold 0.566 → 0.70, with 0.09 for the dark profile. W8's shadow **geometry survives untouched**. The size band, the occlusion and scatter gains, the lens gain, the rim constants and the CSS mapping constant all stay where they were, each with a measured reason.

  **Three results worth more than the constants.** (1) The active reference's contour is *brighter* than its body — a rim peak of 0.025…0.129 at one pixel deep — and vitrea rendered none; the rim constants were never wrong, the inner shadow was drowning them. §6.2's "the light-scheme rim is below the capture's quantisation" described the inactive pose and is withdrawn. (2) Apple's author tint **is the seed**: on three of five calibration backdrops the reference's tinted interior is the declared colour exactly, at a per-channel standard deviation of 0.000, so the tone curve fits to the identity and `tintToneLow`/`High` now describe nothing. The tint still works over dark backdrops because Decision Log 12's composition order has the *adaptation* supply the opacity and the tint supply the colour. (3) The size bias was decided by a validation cell against the estimator §5.8 was fitted on — `impulse__rrect-md__rest` reads ΔE 0.02344 at the old bias and 0.00378 at the new one — and the estimator is now known to assume a shared tint alpha the two profiles do not have.

  **The holdout was opened once and it bit.** Every ΔE mean row holds on every gated profile and both tiers (worst untinted 0.0587 against 0.07/0.08), as does every IoU row and both reduced-transparency shape tables. Three things fail and none is a number a threshold should absorb. **(a)** The contour rows fail on a measured instrument artefact: the CSS tier's checkerboard silhouette carries interior holes (72 on `checkerboard__rrect-lg__rest` at 96.5% area recovery, IoU 0.9653, contour p95 65.0) because the two tiers blur in different colour spaces and the refitted σ = 3 no longer washes the 16 px period out. **(b)** Every remaining failure is a large surface, and they all say `sizeOcclusionGain` should not be 0 — vitrea's interior sits 0.102 to 0.186 below the reference's there and matches it at 32, 44 and 96. *(Corrected in the twentieth revision and in Decision Log 17: the spans are 130 and 160, not 280 — 280 is `rrect-lg`'s width — and the mechanism is the band's top plus the accessibility fold, not the occlusion gain.)* **This is a holdout falsification of a fitted constant, and it cannot be repaired here**: the calibration set has no span above the band's top, so the gain has no leverage anywhere in the fit, and refitting against the cells that exposed it is what the holdout exists to prevent. It needs calibration spans above the band's top — later shown to be unreachable on this canvas, see the twentieth revision. **(c)** W7's mid-dark holdout misses OKLab ΔE p95 by 4.4% (0.1775 against ≤ 0.17) at both scales, with every other row on that cell comfortable. It is the first bound the tone curve has ever failed, and Decision Log 13's chartered two-axis rework is its natural home.

  **The predicate ruling §5.12 asked for: neither an exclusion nor an area arm.** The cell that motivated it is *repaired* by the refit (area recovery 0.888 → 1.000, corner curvature 8.65 → 0.056, because the inner shadow was what pushed vitrea's rim across the backdrop). An area arm would not catch what now mis-measures — those cells recover 94.8…96.2%, above any usable floor, while a 0.95 arm would exclude eleven cells reading IoU 0.94…0.98. What breaks the metric is **topology**, so the proposed arm is a simply-connected test on both sides' silhouettes. Proposed, not built. Also recorded: `cornerCurvature*` must never be gated — it reaches 496×, 800× and 8125× ratios on cells whose IoU is 0.96.

  **Proposed for the gate:** every ΔE mean row, every IoU row, both reduced-transparency shape tables, both increased-contrast shape tables and the cross-tier ΔE coherence row re-verify at their adopted numbers and are proposed **unchanged**; the contour and large-surface rows are **referred rather than loosened**, because the honest fix is a topology arm and a capture session rather than a bigger number; and the tinted cells get their own holdout-bounded table (claims §5.13). The tinted **coherence ceiling** breaks at 1.6493 against 1.25 on a fully saturated tint, mechanism named and measured: the CSS tier would have to declare an `rgba()` colour outside sRGB, so it clips and renders a weaker tint. Solving the CSS alpha and colour jointly under the gamut constraint is the next parent-impact item on that axis.

  Suite state: build, lint and all 1,579 unit tests green. The renderer's GPU golden bed is red (16 of 34) and **deliberately not re-recorded** — the refitted material is a proposal, and baking it into the golden bed would make the bed assert a claim nobody has approved; the regeneration should ride the adoption. Recorded in `tech-debt-tracker.md`, together with the note that `isolation.spec.ts`'s two-constant patch can no longer isolate an eleven-constant delta.

- 2026-08-31 (fourteenth revision — the recalibration cascade, appended by that child; no other section of this spec touched). **The cascade ran Stage 0 and stopped there, at a stop condition its own brief names.** Full figures: claims §5.11; the staged matrix is `packages/calibration/results/2026-08-31-active-bed-stage0.json` (182 cells, six profiles × both tiers, calibration + validation, every constant unchanged). The holdout column was never opened, no constant was fitted, and `results/matrix.json` is untouched.

  **The active material casts an outer shadow, and the inactive material cast none.** On the checkerboard capsule, outside the declared component, the inactive bed's largest departure from its own backdrop is 0.0003 with zero pixels past the extraction threshold; the active bed's is 0.2546 over 4740 pixels. It is a downward-offset multiplicative occlusion — strongest under a bright backdrop, analytically absent over a dark one, reaching 20–40 px at 1× and growing with span. vitrea draws exactly 0.0000 outside the component on every cell, over a shadow footprint covering 8.5–29.6% of the canvas. The same facet is already in the tree with the right number and the wrong conclusion: C9a recorded the CSS tier's `box-shadow` as "the one thing it draws that the reference material does not", and C9d removed it — against the inactive reference.

  **That falsifies the instrument, not just a claim.** The silhouette extractor's rule is "anything differing from the known background is the surface", so it now returns component *plus shadow* (native areas about 2× the declared component), and `interiorMean`/`interiorStdDev` — sampled under that silhouette on both sides — are averaged over a region half of which is shadowed backdrop. Those three quantities are the whole of the declared sweep objective, so every chartered refit would have been fitted against a corrupted target. Damage: **every shape row fails on every gated profile and both tiers** (worst IoU 0.092 against ≥ 0.82; 20–23 of 26 cells failing in light-standard, 7/7 and 8/8 in the accessibility profiles). The perceptual axis is sound as a measurement and mostly passes — worst ΔE mean anywhere 0.0417 against 0.07/0.08, every edge-weighted row clear — with SSIM missing on one or two `checkerboard__rrect-md` cells and **every** ΔE p95 failure being a tinted cell against provisional constants.

  **What was measured but not fitted.** The mid-range transmission residual W7 named is *unchanged* by the pose (+0.191 active against +0.186 inactive at a 44 px span over a 0.5 backdrop) and is now visibly size-dependent (+0.105 at 96 px); matching it needs an effective `tintAlpha` near 0.23 against the shipped 0.62 — C9a raised it from an advisory 0.28, and the advisory was the closer number. `blurSigma` may finally be identifiable (the reference retains eight times more backdrop structure than vitrea). `sizeOcclusionGain` still fits to its boundary, now for a third and different reason. The accessibility folds are *worse* than W1 measured (increased-contrast reference interior 0.982–0.992 against vitrea's 0.831–0.893). W7's axis was not measured at all: its only transition-band scene is holdout. And the tint bed is finally good — the reference's tinted interior is the declared seed *exactly* (sd 0.000 on three of five calibration backdrops), which the existing four constants can express by degenerating to the seed, so no tone-curve shape change is implied; what is wrong is the tinted material's opacity, which rides `tintAlpha` and therefore waits on Stage 1.

  **Three rulings owed, none of them a constant:** (1) the outer shadow as a both-tiers renderer feature — measured, mechanism-named, larger in footprint than the tone adaptation W7 was chartered for; (2) the extractor's rule, which needs a decision before any shape claim can exist again, every alternative having a real cost; (3) whether any bound may be re-adopted before (1) — recommended no, because the perceptual rows would pass over a reference facet vitrea renders as zero across a fifth of the canvas, which is the certify-the-defect move Decision Log 11 rejected at a smaller size. `KNOWN_RENDERER_GAP_EXCLUSIONS` stays empty: the gap clears the measured-and-named bar but fails the class's own cannot-grow property, since it would have to swallow every shape row in the claims doc.

- 2026-08-30 (sixteenth revision — the instrument's fix wave, appended by the same child; only `packages/calibration` and claims §5.12 touched). Three verified review findings and two corrections measured by W8, with the staged matrix regenerated on the same tree state (W8's branch still unmerged, so the lineage stays comparable to the fifteenth revision's).

  **The falloff was fitted in the wrong family, and the axis now fits two.** A shadow is a filled shape convolved with a blur kernel, so its profile is a blurred *edge* rather than an exponential — which is also what `box-shadow` and a GPU shadow pass implement, and what W8's mechanism fit found independently (Gaussian RMS 0.00233 against the exponential's 0.00527 over 585 points). Both families are now fitted over the same rings, with the same objective and two free parameters each, and both residuals are reported so the family question stays answerable from the record. The blurred edge wins on **all 142 normalised cells** (residual 0.001…0.019 against 0.005…0.037), and the geometry restates as **σ ≈ 17.8 pt** in place of the fifteenth revision's λ ≈ 12 pt: 16.2…18.4 device px on every 1× profile, 32.9…37.2 on every 2× one, moving under 15% across backdrop, span, colour scheme and accessibility state. The amplitude is now reported separately from σ, which is what makes "same shadow, dimmer" a readable result rather than an inference.

  **The dark-scheme shape reading is withdrawn.** W8 refitted the dark cells with the light geometry and amplitude-only free at a cost of 3–7% of an already small residual, and one geometry with one amplitude per profile predicts our own per-direction extents in all three profiles from a single calibration point. This axis, refitted in the right family, now says the same thing on its own: dark σ 16.2…18.4 against light's 17.3…18.2 — the same number — with the amplitude falling from 0.361 to 0.069 on the same cell. The apparent collapse of the upward reach was the iso-occlusion threshold acting on a weaker shadow, since upward is where a downward-offset shadow is thinnest and therefore crosses first. **The extents stand as measurements; the scheme-dependent-geometry interpretation does not.** W8 needs an amplitude per colour scheme, not a shadow per colour scheme.

  **Three review findings, fixed.** (1) The extent rule credited ring 0 with a phantom qualifying predecessor, so a lone edge-halo ring reported a one-pixel reach in every direction and a *defined* offset of (0, 0) on a side whose offset must be absent — seven cells of the staged matrix carried that; the walk now starts unqualified and an unmeasurable ring breaks adjacency. (2) With the schema at 5, the documented default `compare` path (and `diff --matrix`) deserialised the deliberately frozen schema-4 `results/matrix.json`, and failed only *after* the capture work; both CLIs now refuse an incompatible target before doing anything, by name and citing ruling 3. The frozen matrix stays byte-untouched — the interregnum has no sanctioned default target, and saying so in a message is cheaper than an hour of capture. (3) The caveat block counted absent-axis *scenes* while labelling them cells, announcing 40 unnormalised cells as six; absences are now tallied per cell and named per scene.

  **State.** Same 182 cells, schema 5, `results/matrix.json` untouched, no threshold proposed or adopted on either axis; suite (16 files, 182 tests), eslint and all four tsconfigs green. New pins: the lone-ring rejection, the blur-radius recovery, and the family comparison in both directions — each family must win on its own synthetic, so the estimator cannot simply always answer "Gaussian".

- 2026-08-31 (fifteenth revision — the calibration-instrument redesign, appended by that child; only `packages/calibration` and claims §5.12 touched). **Decision Log 15's ruling 2, built.** Full figures: claims §5.12; the staged matrix is `packages/calibration/results/2026-08-31-active-bed-bounded-instrument.json` (182 cells, six profiles × both tiers, calibration + validation, every constant unchanged, holdout never opened). `results/matrix.json` is untouched per ruling 3, and no threshold is proposed or adopted on either axis.

  **The bound is the declaration, and its margin is zero — measured, not assumed.** Extraction now searches only inside the component's own geometry from `scenes.json`, placed by the same function the web page lays the scene out with (`src/component-region.ts`; `web/scenes.ts` now imports it rather than keeping a second copy). The brief allowed a small margin for edge softness; there is none to allow. The reference's occlusion field begins at the contour with no gap — the first pixel ring outside it already carries 0.05…0.19 occlusion — so every outward pixel of margin admits shadow into the reference's silhouette and almost none into a renderer that draws none. Measured cost: `photo__capsule-button__rest` IoU 1.000 / 0.978 / 0.970 / 0.942 at margins 0 / 0.5 / 1 / 2 px. The margin stays a per-cell recorded parameter, at 0. Silhouettes are back at declared area (native/region median 1.000, against ~2× before), so `interiorMean`, `interiorStdDev` and the rim — the whole sweep objective — are interior statistics again.

  **What the shape axis gives up, in writing.** Both sides are clipped to the declaration, so over-fill is unmeasurable and area recovery is *assumed*: `componentRegionArea` ships in every cell as the ceiling the bound imposes, and §5's conditioning predicate is now explicitly a floor with an assumed ceiling. A new consequence for the post-W8 gate: with the reference filling the region everywhere, the badly-conditioned cell is now a web-side one — over `light-solid` vitrea's rim crosses the backdrop level, its silhouette comes back at 4328 px of 4872 with a ring of holes, and the contour traced round that reads a corner curvature of 8.65 1/px against the reference's 0.056. The predicate needs a web-side arm; the number for it is the cascade's.

  **The shadow is an axis (schema 5), and the reference's description is now data.** Measured outside the declared region on both sides as *occlusion* — `(backdrop − rendered)/backdrop` — with per-direction extent, a reach-implied offset, a mass centroid, an exponential falloff with its own residual, and strict absent-not-zeroed discipline (over `dark-solid` and `impulse` there is no light to remove, so every ratio is absent with the support recorded). The reference: a **7.5 pt downward offset with a ~12 pt falloff**, reaching about 20 pt above, 37 pt below and 30 pt beside a light-standard surface — every one of those doubling exactly between 1× and 2×, so the shadow is specified in points. Two findings W8 should have before it builds: the dark scheme's shadow is a different shadow, not a dimmed one (strength 0.171 → 0.035 on the same cell, reach above 24 px → 8 px while below barely moves) — **[WITHDRAWN by the sixteenth revision: same geometry, one fifth the amplitude]** — and **increased contrast adds a hard border stroke** (ring-0 occlusion 0.560 against a shadow of 0.096 at ring 1) that is a renderer feature in its own right. The multiplicative model describes the shape well and the amplitude only approximately: normalised strength moves by 1.7× with span over `light-solid` and by 0.6× over `photo`, which is where W8's backdrop-multiplicativity parameter has to land. **[The "~12 pt falloff" above is superseded: refitted as a blurred edge, the geometry is σ ≈ 17.8 pt — sixteenth revision.]**

  **vitrea reads zero, and it is recorded as zero rather than as an error.** Extents are 0 in all four directions on 39 of 71 texture cells and 26 of 71 dom cells, `offsetY` is *absent* on exactly those (undefined displacement, not (0, 0)), and its mean departure is slightly *negative* on most cells — it brightens where the reference darkens. Where it reads 2…7 px of reach that is its own antialiased edge: peak at ring 0, decay length 0.7…3.7 px against the reference's 10.8…27.7.

  **State.** Schema 5; `results/matrix.json` deliberately left at schema 4 with `test/adopted-thresholds.test.ts` pinning both numbers so the divergence stays a decision; `KNOWN_RENDERER_GAP_EXCLUSIONS` still empty; suite (16 files, 176 tests), eslint and all four tsconfigs green in `packages/calibration`. New pins: the bound (a shadow-like halo outside the declaration stays out of the silhouette, a departure of the same magnitude inside it is found) and the shadow axis (a synthetic multiplicative halo recovered at its built-in offset, reach and decay length; a zero-shadow render read as zero reach and an undefined offset; a dark backdrop read as unmeasurable rather than as zero).

- 2026-08-31 (seventeenth revision, by the parent): the shadow round closed. W8 landed and merged (ed6d265; five commits) after one independent review round — the field-rect pad now solves in compositing space against the group's maximum effective amplitude, and the top-edge field read reconstructs the distance it cannot sample (exact directly above a surface by the field's 1-Lipschitz property; past the corners it errs toward less shadow), both pinned fail-before-verified, three goldens re-recorded with the delta attributed (alpha-only, downward, ≤ 6 codes). The instrument's fix wave (dde1954) closed its round the same way. The two agents' corrections cross-confirm: the falloff is a blurred edge in both the mechanism fit and the axis fit, and the dark scheme is one geometry at one-fifth amplitude. Post-merge verification on main: `pnpm run ci` green across all packages; renderer GPU e2e 34/34 with the two new shadow-extent pins. The recalibration cascade resumes with stages 1–4 plus the shadow-constants fit, one holdout pass, threshold re-adoption, and the claims rewrite.
- 2026-08-31 (fourteenth revision by the parent; the cascade's own note is fifteenth-dated): Decision Log 15 and the W8 charter — the outer-shadow discovery ends the cascade at stage 0 by design; extractor redesign approved (bounded shape + shadow axis); no interim re-adoption; W8 + instrument agents dispatched in parallel.
- 2026-08-31 (thirteenth revision): the active bed landed (973fd7e; 121 cells, plurality doctrine, tint chroma present) and the recalibration cascade dispatched — stages: damage report, base material refit, W2 refit, W7 refit (same form), first tint fit, one holdout pass, threshold re-adoption, claims rewrite.
- 2026-08-31 (twelfth revision): Decision Log 14 — the whole bed was the inactive material (never-key capture window); user approved the full active-pose re-baseline + recalibration cascade; the tint hue confirmed present in the active pose (chroma 102-250/255).
- 2026-08-30 (eleventh revision): the holdout round — W7's curve falsified in shape yet green on every bound (kept; two-axis rework chartered for the next wave with new mid-band scenes), the tinted bed refused for colour loss (diagnosis dispatched; one re-capture session then recomposition); Decision Log 13.
- 2026-08-30 (tenth revision): W5 landed (five of six; per-corner radii re-deferred honestly), the golden bed re-recorded against the post-W2/W7 material, the harness extension for the batched capture session committed (tint axis + the mid-band holdout backdrop, scene matrix 24 -> 37); the capture chain armed on the re-grant.
- 2026-08-30 (ninth revision): W7 landed and fast-forwarded - the adaptation axis on both tiers, the defect exclusion dissolved at unedited bounds, the mid-range-transmission residual named as the next cut's parent-impact item, the dark-backdrop holdout gap recorded for the batched capture session.
- 2026-08-30 (eighth revision): W2 landed and fast-forwarded to main - the size law as one thickness curve with four facet gains, fitted under the holdout discipline, composed with the tint in-branch; the coherence-floor margin, the built W7 seam, and the resolved dark ceiling recorded. (This and the two notes above were restored 2026-08-31: a quoting bug made their original insertions fail silently while the same commits' Surprise and status edits landed.)
- 2026-08-30 (W5 — one item re-deferred with reasons; appended by the child under
  the acceptance's "closed or explicitly re-recorded" clause, with no other section
  of this spec touched). Five of the six items landed. **Per-corner radii are
  re-recorded as deferred**, and the reason is a dependency this batch cannot
  satisfy rather than a scope preference.

  Two halves. The first is that the item is a *geometry* change, not an API one:
  X8 rider 3's refusal (`assertUniformRadii`) exists because v1's corner algebra
  is built on `abs(p)` — one corner reach and one coefficient set, four quadrants
  folded onto one — so four radii need the quadrant selected before the algebra
  runs, which puts 4×(reach, k[5]) on the instance and takes the packing from 18
  floats to 36, past the 72-byte stride it is written around. `cornerBudget`'s
  `min(halfW, halfH)` is likewise a *consequence* of uniformity (Figma splits a
  side between its two corners in proportion to their radii), so the general rule
  comes back with it, and with it `smoothingCeiling`, `resolveCornerConstruction`,
  `resolveConcentric`'s single offset and floor, and both contour builders. The
  declared 0.17 px / 2.91° error bound and the committed f32 cross-check fixture
  are stated over the symmetric family and would need re-running — affordable,
  since both are CPU sweeps.

  The second half is the one that decides it: **the pixel gate is unavailable.**
  `packages/renderer-webgpu`'s eight GPU goldens are red on main and have been
  since W2's size law, verified at the base commit `b6dcbac` (`field-mask` max
  channel delta 8 against an allowed 4) — the material moved twice, deliberately,
  and nothing re-recorded the bed because `pnpm run ci` runs no Playwright. A
  change to the project's hardest-won geometry cannot be landed against a bed
  that cannot say whether it moved, and re-recording the bed is a fidelity
  judgment against the calibration cells (#30(d)) belonging to whoever owns the
  material change — not to an API round. Logged in `tech-debt-tracker.md` with
  the fix shape.

  What landed in its place is the honesty fix the gap was actually costing
  adopters: `CornerRadii` is a Vec4 in every type along the path and the CSS tier
  renders four radii correctly through `border-radius`, so non-uniform radii look
  supported right up until the WebGPU tier resolves the shape against `radii[0]`
  or geometry throws from inside a frame. The new `non-uniform-radii` diagnostic
  names it at registration, with both tiers' answers. Geometry's own refusal is
  untouched and its tests are unweakened.

  Two notes for recomposition. (a) The wave's X3 amendment held: every commit in
  this child staged mine-only paths, and the three agents working this tree
  concurrently produced no attribution bleed. (b) The refraction-ladder dedup
  found the duplication at **five** sites rather than the two #23(d) named — the
  regime map was re-inlined twice inside `platform-web`'s own `optics.ts`, and
  the rung-scale table written out three times — and proved byte-identity by
  running the (already red) golden bed before and after and showing every scene's
  failure figures *and* rendered-byte md5s unchanged, which is the workaround the
  stale bed forces.

- 2026-08-30 (W7 — advisory inheritance overturned in two places; appended by the
  child under its brief's flow-back rule, with no other section of this spec
  touched). W7's design inheritance carried Apple's documented behaviour as
  advisory, with the instruction to "re-verify the intermediate curve empirically,
  never assume the endpoint mapping". Re-verified, the endpoint mapping is not
  what the documentation's wording implies and not what the coverage matrix §3.3
  box assumed: the material does **not** cross between a light-scheme tint and a
  dark-scheme one. Over the two extreme-dark backdrops the light and dark
  references are the *same pixels*, so what the material converges on is the
  backdrop's own tone, and the colour scheme sets only the neutral it departs
  from. W7 built the axis that way — one law, applied inside whichever scheme is
  active — which is also what makes it compose with the discrete scheme split
  without double-adapting. Coverage matrix §3.3's standing contradiction (the
  "one place where vitrea's measurement contradicts Apple's documentation")
  resolves with it: explanation (c) was right, (a) and (b) are refuted, and the
  box is annotated in place with its before-state kept.
  The second overturn is W2's: `sizeOcclusionGain` was fitted to a boundary with
  the reason recorded as "the axis that would let it fit is W7's". The axis exists
  now and the sweep is unchanged in shape — 0 remains the optimum, monotone, a
  1.34× spread — so the diagnosis was wrong even though the decision was right.
  The residual is the material's mid-range transmission, which this bed says
  plainly and which is a retune of C9a's `tintAlpha` rather than a size or tone
  question; named as the next parent-impact item in the claims doc's §5.8. W2's
  declined band optimum (64) also survives and is declined again, now for a second
  reason: the tone axis independently requires 32…96. Both documents are annotated
  in place with their before-states kept; the measurement, the fit and the full
  re-verification are in the claims doc's new §5.8 and its rewritten §5.4. W7's own
  outcome stays in its report and at recomposition.
- 2026-08-30 (W2 — advisory inheritance overturned; appended by the child under
  its brief's flow-back rule, with no other section of this spec touched): W2's
  design inheritance was "coverage matrix §3.2 rows (advisory — re-read before
  design; the matrix is enumeration, not design)", and re-reading them against
  the settled bed overturned the part of them that was not enumeration. §3.2's
  opacity row and the C9a §4.1 table it cites have the reference's transmission
  *rising* with span, and prescribe "a size term on the tint alpha… the inverse
  of the lens gain — a larger surface is more transparent, not less". The settled
  bed reverses that direction — and, more importantly, the estimator both rows
  rest on is not a clean measurement of size at all: it regresses interior level
  *across* backdrops, which is exactly where W7's tone adaptation lives, and an
  adapting level is indistinguishable from a small transmission in it. W2
  measured the law *within* a fixed backdrop instead, where it is unambiguous
  across six profile × span comparisons, and coupled the facet that evidence
  supports. Both documents are annotated in place with their before-state kept;
  the measurement, the fit and the fit's declined optima are in the claims doc's
  new §5.7. W2's own outcome stays in its report and at recomposition.
- 2026-08-30 (seventh revision): W1 CLOSED — the settled 168-cell matrix promoted with the self-enforcing defect-class exclusion, accessibility tables adopted, dark provisional with the ring-hole caveat; W2 and W3 dispatched. X3 lesson extended: packages/calibration/web-captures/ is shared mutable state — concurrent agents must not both write it (the press-glow fix's re-measure and the promotion regeneration collided; recovered by a full fresh-capture sweep).
- 2026-08-30 (sixth revision): the settled re-baseline round — the settledness defect found and fixed, the whole bed re-captured attested on the virtual display, the tone-adaptation gap discovered (W7 chartered, Decision Log 11), the coherence axis's first catch (dark+pressed tier split, fix in flight), accessibility thresholds adopted, the overlap experiment resolved through both fixes (647a374, aa7d481).
- 2026-08-29 (fifth revision): W1 G3's measurement half complete — the six-profile matrix (136 cells), the coupled-state ruling (Decision Log 8), four Surprises (the 2× repair, the accessibility occlusion gap, web-side reproducibility, the rim candidate), the tripwire answered; threshold adoption presented to the human gate.
- 2026-08-29 (fourth revision): W1's capture bed complete (G1+G2+G4) — the merged-manifest model, the toggle-coupling ruling, the tone-transient session rule; three Surprises added.
- 2026-08-29 (third revision): W6 landed — Decision Log 7, four Surprises, the X3 amendment; W6's overlap-check experiment recorded for a W1 session.
- 2026-08-28 (second revision): W4 landed same-day — Decision Log 6, two Surprises, the matrix §3.10 row annotated, W5 inherits the port note.
- 2026-08-28: v1 of this document — the cut authored from the parent's What
  remains, approved by the user in session, W4/W6 dispatched and W1's
  experiment started the same day.
