# vitrea — Liquid Glass Material Runtime for the Web — Design Spec

> **Parent:** root — chartered 2026-08-24 by the user as a new project in this
> monorepo; the repo's standing purpose (the designer plugin) gains vitrea as a
> sibling artifact, and the plugin's archived SVG-filter direction named this
> project's ground ("material simulation moves to WebGL/GPU", commit a923a7c).
> This document is the composite spec: design up top, roadmap below, one
> document. Children dispatch per their track hint; each child spec opens by
> citing this document (path + child id).

## Purpose

Replicate Apple's Liquid Glass material and interaction system (WWDC25's `glassEffect`, `GlassEffectContainer`, `interactive()`, `glassEffectID`) on the web, as an open-source TypeScript library named **vitrea**. Apple's material is not a blur preset: it is real-time, size-parameterized lensing; per-element adaptation of tint, shadow, and foreground to the live backdrop; container-scoped sampling; and shape-to-shape morphing — a system no CSS filter chain can express and no existing web library ships as a production-integrable whole.

vitrea serves four goals at once — a standalone OSS product, the material engine the designer skill's archived SVG-filter direction pointed toward, a technical showpiece, and a commercially credible asset. When those goals conflict on a v1 decision, **fidelity wins** (Decision Log #2).

Positioning: not "the first WebGPU glass demo" — verified prior art exists (liquid-glass-studio, liquidGL, glass-effect-webgpu, liquid-glass-js). vitrea is **the production-oriented, reference-calibrated material compositor for semantic web controls**: explicit backdrop contracts, shared sampling groups, coherent cross-element morphing, adaptive accessibility, progressive fidelity tiers, and fidelity claims backed by a versioned native calibration harness. That combination is what each verified competitor lacks.

## Locked design decisions (from the 2026-08-24 grill and review round)

See the Decision Log for rationale and rejected alternatives. In brief: hybrid-first backdrop promise; WebGPU + CSS-only renderer ladder (no WebGL2 in v1); framework-agnostic core + React bindings only; monorepo restructure of this repo (plugin untouched at `skills/`); native SwiftUI calibration harness from the start; v1 = primitives + core controls + one cross-layer morph pair; finish line = public demo site + npm publish of `vitrea` and `vitrea-react` (Decision Log #27); the Chromium-only SVG-displacement tier gets a reserved capability seam but no v1 implementation.

## What "done" looks like (acceptance, phrased as behavior)

1. **Arbitrary DOM, zero setup.** A React app wraps its tree in `GlassRoot` and places `GlassButton`s in a `GlassToolbar` over ordinary page content. Glass renders via the dom-backdrop path (CSS backdrop proxy + GPU optics). Label text remains real DOM: selectable, focusable, IME-capable, announced by VoiceOver as a button.
2. **Texture upgrade.** Registering an image or video as a group's backdrop upgrades that group to true refraction: edge lensing visibly bends the backdrop, and a larger surface shows deeper shadow and stronger lensing than a small button over the same backdrop.
3. **Interruptible press.** Pointer-down produces press-point glow and ~1–2% compression on a spring; releasing mid-press redirects the animation from its current position and velocity with no snap or restart.
4. **The morph.** Activating a toolbar button morphs it into a menu platter as one continuous material transition on the overlay plane — geometry, radius, and material thickness interpolate; no crossfade of two separate surfaces. The menu's glass body correctly occludes the toolbar's DOM content beneath it.
5. **Honest degradation.** In a browser without WebGPU (e.g. flagged Firefox on Linux), the same app renders presentable CSS-tier glass with no console errors, and `useGlassCapabilities()` reports the resolved state — `activeRenderer: "css"`, `demotionReason: "no-webgpu"` — while preserving `configuredSource`. No state ever silently pretends to a capability it lacks.
6. **Accessibility modes.** `prefers-reduced-motion` removes overshoot and deformation while preserving spatial continuity; `prefers-contrast: more` strengthens borders and forces near-monochrome foregrounds; `forced-colors: active` renders system colors with no glass. Each is overridable via `GlassRoot` props.
7. **Calibrated fidelity.** The calibration suite diffs demo renders against versioned `apple-macos-26.5-*` fixtures across canonical scenes (backgrounds × components × states × sizes) with shape, material, and motion metrics inside declared thresholds — per supported engine/backend cell, with texture-tier and dom-tier claims stated separately, including holdout scenes not used for tuning. **Status after C9a: substantially met for the texture tier, and open on three counts that are not code.** Met: the suite diffs all 30 cells of both captured profiles, the split is honoured with holdout measured once against a frozen configuration, and the two tiers are claimed separately (`c9a-fidelity-claims.md`). Open: **thresholds are proposed, not declared** — adopting them is the human gate this acceptance implies; **motion metrics are absent**, since no frame sequences were captured and the still pressed cells cannot substitute (the native side has no press pose); and **two of the four declared profiles have no captures at all** (both accessibility modes, plus the canonical 2× keys, which are unreachable on this display). The dom tier carries a Chromium-only figure by S1's constraint, not a cross-engine one.
8. **Shipped.** `npm install vitrea vitrea-react` works (names per Decision Log #27); the public demo site shows the showpiece with side-by-side native captures; the README's fidelity claim reads "reference-calibrated against macOS 26.5 captures," nothing broader.

## Architecture

### Repo layout & packages

This repo becomes a dual-artifact monorepo (pnpm workspaces). The existing plugin is untouched: the marketplace installs from `./` and reads `skills/`; the root `package.json` is `private: true`.

```
packages/
  core/            scene model, instance registry, capability/tier resolution,
                   material policy, scheduler interfaces, accessibility policy
  geometry/        ShapeSpec → ContourIR → compiled shapes; continuous corners,
                   concentric offset, parametric morph solver, group union
  motion/          MotionDriver family (springs, attack/decay, low-pass,
                   crossfade, step), interaction state machine, interruption
  platform-web/    DOM host registration, GeometrySync, GlassLayerManager,
                   backdrop proxies, CSS-tier renderer, media-query policy,
                   WebGPU lifecycle (capability probe, device loss, resize)
  renderer-webgpu/ GPU resource graph, backdrop import, blur/analysis pyramids,
                   group field pass, optics pass, highlight pass, texture pool
  react/           thin declarative bindings (GlassRoot, GlassGroup,
                   GlassSurface, controls); asChild composition seam
  calibration/     fixture schema, diff metrics, profile-fitting CLI
apps/
  demo/            Vite + React public showpiece; also the dev playground
  reference-apple/ SwiftUI capture harness (Xcode project) + capture scripts
```

`core`, `geometry`, `motion` never import `window`, `document`, or `HTMLElement` — pure, unit-testable math and state. `platform-web` owns the browser. `react` maps lifecycle and JSX to `platform-web` registration and stays thin enough that Vue/Svelte/WC adapters later duplicate nothing.

**Published packages: `vitrea` (core) and `vitrea-react` only (renamed from `@vitrea/*` — Decision Log #27).** Internal packages are bundled at publish; the WebGPU renderer loads behind a dynamic import so CSS-tier users never download WGSL.

### The rendering contract

One compositor — one `GPUDevice`, scheduler, pipeline cache, texture pool, instance registry — serving **one canvas pair per managed stacking plane**. v1 ships exactly two planes: **base** and **overlay** (menus/popovers). Per plane, paint order is:

```
page content
  ↓
backdrop-proxy layer     (dom-backdrop mode only: ONE pointer-events:none
                          proxy per sampling group — border box padded by
                          samplingPadding, mask clipped to the exact union
                          of member shapes — carrying backdrop-filter,
                          portaled to the plane root below the optics canvas)
  ↓
optics canvas            (glass bodies: tint, lensing, rim — pointer-events:none)
  ↓
semantic host DOM        (real <button> etc.: text, icons, focus, IME, a11y tree)
  ↓
highlight canvas         (specular sweeps, press glow — pointer-events:none)
```

Arbitrary interleaving with foreign stacking contexts is **documented out of contract** for v1: all glass lives in GlassRoot's managed planes; menus portal internally to the overlay plane. Two further constraints are **checked, not assumed**: (a) glass surfaces within one plane must not overlap — a dev-mode error, because the sandwich cannot place one surface's body above another surface's DOM label (v1's components never overlap in-plane; the overlapping case *is* the cross-plane case); (b) v1 supports exactly one overlay plane — multiple simultaneous overlays are out of scope. During a cross-plane morph the surface is promoted **as a unit** — body, semantic host, and highlight together — to the destination plane so the transition renders on one canvas pair, seam-free; promotion under scroll and focus is a required integration-test scenario. Backdrop sampling context is a documented constraint: glass hosts must not sit beneath ancestors whose filter, opacity, mask, or clip changes the backdrop root — [Filter Effects 2's Backdrop Root definition explicitly lacks WG consensus](https://drafts.csswg.org/filter-effects-2/), so engines may legitimately differ — and a runtime conformance probe validates the proxy topology at startup, demoting a group to the CSS tier when sampling proves non-equivalent. The full proxy topology across Safari/Chrome/Firefox was **gating spike S1** (§Gating spikes) — landed 2026-08-24, findings at docs/doperpowers/spikes/2026-08-24-s1-proxy-topology-findings.md. Verdicts: **Chromium confirmed byte-exact** (122 capture variants, retail Chrome and headless); **Gecko and WebKit unmeasurable by any automated capture path** — backdrop-filter renders as a no-op in every screenshot/snapshot route while sibling filters render correctly in the same images — so their verdicts come from the spike's self-scoring manual page (`spikes/s1-proxy-topology/pages/manual-check.html`), pending. Three normative corrections now bind X1: (a) the proxy's *border box* carries the padding while the *mask* clips to the exact member-shape union — Filter Effects 2 clips a filter's input to the element's own border box, so an unpadded box starves its blur and a padded mask paints a blurred halo; (b) `samplingPadding ≥ 3σ` of the group's blur radius (verified byte-exact at three radii spanning 5×); (c) `mergeDistance ≥ samplingPadding` — overlapping group proxies apply the filter exactly twice (measured 1.5625× for brightness(1.25)²), paint-order-dependently, drifting up to 17/255 even in legal 8px-gap geometry; core additionally checks neighbouring-group proxy-rect overlap, since mergeDistance alone cannot see two one-node groups 8px apart. Enforcement split: core checks what its own data can see (mergeDistance, proxy-rect overlap); the 3σ floor lives in platform-web, which alone knows the blur radius; `samplingPadding` defaults to an advisory 24px (3σ at σ=8), calibration-delegated. The startup conformance probe is three layers: a support gate (necessary, not sufficient — `CSS.supports` answers true in builds that render nothing), a structural backdrop-root ancestor audit (measured: no false negatives, two benign false positives), and a CI-generated per-engine conformance table shipped as data — because no pixel oracle for backdrop-filter output exists in any engine and one failure class remains undetectable at runtime. No public layer API ships in v1.

### Core model

```
BackdropSource            GlassGroup                GlassNode
├─ kind: texture | dom    ├─ backdropSourceId       ├─ shape family params (IR-authored)
├─ raw texture/source     ├─ morph namespace        ├─ viewport bounds, clip chain
├─ blur pyramid           ├─ material profile       ├─ z-slot (plane + order)
├─ analysis maps          ├─ adaptation policy      ├─ variant: regular | clear
├─ dirty epoch            ├─ mergeDistance          ├─ interaction state
└─ resolution policy      └─ samplingPadding        └─ foreground policy
```

**Invariant:** blur/analysis pyramids belong to `BackdropSource`, rebuilt **at most once per dirty source per frame** — never per group. Static backdrops rebuild nothing. In dom-backdrop mode the browser compositor does the blur; the GPU builds no pyramid at all.

### Backdrop & analysis contracts (the honesty core)

Capability is **not a free tuple** — a free product of axes admits undefined and misleading states (a registered texture with no WebGPU; a dom group in an engine whose `backdrop-filter` probe fails; a CORS-tainted video). Configuration and resolution are separate: the app *configures* a source; the runtime *resolves* one of an enumerated set of states per group, reported by `useGlassCapabilities()`:

```ts
interface GlassGroupState {
  configuredSource: "texture" | "dom";     // what the app declared — never mutated
  activeRenderer:  "webgpu" | "css";       // what is actually drawing
  samplingBackend: "gpu-texture" | "css-backdrop" | "none";
  refraction:      "true" | "approximate" | "none";
  analysis:        "exact" | "hint" | "none";
  health:          "ok" | "demoted";
  demotionReason?: "no-webgpu" | "no-backdrop-filter" | "tainted-source"
                 | "incompatible-texture" | "device-lost" | "probe-failed"
                 | "governor";
}
```

A root that never requested WebGPU is **not a fault**: the capability inputs distinguish "WebGPU unavailable" from "WebGPU not requested," and a CSS-by-choice root resolves its groups to `activeRenderer: "css"` with `health: "ok"` and no demotion reason — labeling intent as fault would be the inverse of the honesty doctrine (amendment ruled 2026-08-25, Decision Log #21; implemented as corrective task K1). Only enumerated states are legal. Every demotion names its reason and its recovery transition (device restored, source replaced, probe re-passed); `configuredSource` survives demotion so an app can always see what it asked for versus what it got. Ratified with C4's landing (Decision Log #19): `no-webgpu` names its recovery `"none"` — honestly unrecoverable within a session; `no-backdrop-filter` keeps the WebGPU renderer with `samplingBackend: "none"` (no tier can blur when the engine lacks backdrop-filter, and the GPU still draws rim/tint/glow) while `probe-failed` demotes to the CSS tier (the proxy path that failed is not on the CSS tier's path); a texture-configured group demoted to the CSS renderer reports `samplingBackend: "css-backdrop"` — it samples the DOM behind it, not the registered texture; the CSS renderer always reports `refraction: "none"` (backdrop-filter blurs, it never bends); governor pressure is per-group with a scene-wide default, and intra-tier degradation is not a state change. Where the accessibility policy caps refraction alongside the state's refraction level, **renderers honour the lower of the two caps** — binding on C5 and C6. The three healthy primary states:

- `texture` + `exact`: the app registers a GPU-ownable source (image, video, canvas, procedural gradient, app-rendered texture). Full refraction; luminance/variance/edge-density reduction runs on-GPU with temporal hysteresis.
- `dom` + `hint`: CSS backdrop proxy does blur/saturation; GPU renders rim-lensing approximation, tint, glow, morph. Adaptation comes from **one** hint contract: a `backdrop` prop on `GlassGroup` (`{ tone, luminance?, complexity? }`) or an estimator provider. A built-in best-effort DOM estimator (known background colors/images, CORS permitting) may implement the provider — always documented as an estimator, never implied to be pixel analysis.
- `dom` + `none` (default for arbitrary DOM): fixed regular material, geometry-driven rim/specular, foreground from explicit tokens or `color-scheme`.

A capability seam is **reserved** (not implemented) for two future backends: Chromium's `backdrop-filter: url()` displacement tier and HTML-in-Canvas DOM-to-texture (origin trial, Chrome 148–150).

### Foreground adaptation (GPU → DOM data path)

`ForegroundAdaptation = fixed | author-hint | sampled-async { rateHz, hysteresis }`. GPU classification results reach DOM `color` via **low-frequency async readback** (never per-frame) updating a CSS variable, with threshold + hysteresis so scrolling never pumps the foreground. `sampled-async` is available only where `analysis: exact` (an illegal combination resolves to the nearest legal mode with a diagnostics warning); its readback rate is capped at an advisory 15Hz default, calibration-delegated. Dom-backdrop groups use hints or fixed tones; the CSS tier uses explicit light/dark tokens.

### GPU device ownership

Default: **vitrea owns the device** (`createGlassRoot({ powerPreference })`); apps hand external sources (image/video/canvas → `copyExternalImageToTexture` / `importExternalTexture`). Alternative: **app owns the device** — the app passes its `GPUDevice` and provides same-device texture views per frame, or accepts a render-target callback from vitrea. There is no cross-device texture sharing in WebGPU, so the frame contract is operational, not aspirational: every texture source is normalized through one **BackdropFrame acquisition protocol** — `acquire(frameInfo)` at frame start, `release()` after submit, the frame carrying view, size epoch, color space, and alpha mode. `importExternalTexture` handles expire at task end, so video is re-acquired on every frame that samples it, with `VideoFrame` close ownership held by the provider; imported video arrives unpremultiplied while copied images arrive premultiplied, and the compositor normalizes at import. App-supplied views must satisfy declared usage/format/dimension requirements — validated at registration with a typed error, never discovered at draw time. Resizes bump a size epoch that invalidates dependent pyramid allocations. Device loss demotes affected groups (`demotionReason: "device-lost"`) while recovery runs: vitrea-owned devices re-request and re-register automatically; app-owned devices require a replacement-device callback plus a resource re-registration handshake, and groups stay demoted until it completes.

### Geometry

Shapes are authored as a **Contour IR** (cubic Bézier/arc segments + corner metadata + winding) — the interchange representation for export, tessellation, and future arbitrary shapes. The IR is **not** the per-pixel render form: exact distance to a cubic Bézier requires quintic root-finding, so no practical closed-form SDF of the raw contour exists. v1 renders a small set of **parametric pseudo-SDF families** evaluated in-shader with a declared, measured error bound against the true contour — **gating spike S2 landed 2026-08-24** (docs/doperpowers/spikes/2026-08-24-s2-geometry-field-findings.md): the bound is met, no atlas promotion. The chosen family is **rsupn** — the analytic rounded-rect SDF with the corner radius replaced by a degree-5 polynomial in corner angle, plus first-order gradient normalization: value error ≤ 0.170px max / 0.156px p95 and gradient error ≤ 2.91° max / 2.55° p95 in the |d| ≤ 8px optical band over the full 324-shape matrix; capsules and circular corners exact to machine precision; cost 0.0264 ns/eval/px (≈5% of the 2ms mobile budget with field passes scoped to group bounds). The un-normalized variant (family C, "rsup", 0.0097 ns/eval/px) is the quality governor's first within-tier degradation step, **conditional on C6 running the f32 cross-check on its WGSL** — it is priced but numerically inspection-verified only. S2 also re-derived the reference: the smoothing seed fits **directly to Apple's measured `.continuous` curve** (edge-reach 1.528665 — Apple's published `cornerCurveExpansionFactor` — circular mid-arc, and a real 2.4532° tangent break at the shoulder joins), because routing through the Figma-squircle family costs 6.4× the value error (0.670px vs 0.104px); the widely cited Figma smoothing 0.6 is wrong for Apple-matching — 0.66 measures nearly 2× closer, answering that delegated unknown ahead of C7. Shape families in v1 — vitrea's supported set, not "Apple's taxonomy":

- **fixed** rounded rectangle with a **continuous numeric corner profile** (`smoothing ∈ [0,1]`, 0 = circular arc, clamped by a continuous budget derived from size and radii so morphs stay safe; the Apple-matching value is calibration-determined and internal, seeded from S2's direct fit to Apple's measured curve — the public API exposes `profile: "continuous" | "circular"` sugar over the number). **v1 restriction:** the corner algebra is mirror-symmetric and validated for uniform radii only — `radii` keeps its Vec4 shape in X8, but v1's evaluator requires all four equal (dev-mode error otherwise); per-corner algebra is post-v1 (no v1 component needs it),
- **capsule**,
- **concentric** rounded rectangle: radius derived from the parent's field as a **level-set offset** (inset, floored at a minimum radius) — exact inward offsets of continuous-corner cubics leave the cubic family, so the level-set approximation under the same error budget is the honest contract; concentricity still governs radii, not the curve profile (the research refuted that conflation).

Morphs in v1 are **parametric only**: interpolation over `{ center, size, radii, smoothing, thickness }` — every channel numeric, so every channel interpolates (capsule ↔ rounded rect, button ↔ menu platter, indicator slides). Contour resampling and topology-changing morphs are post-v1. If S2 shows the error budget unachievable, the fallback is promoting distance-mask atlases into v1 — a renderer change behind the same IR, not an API change. Group rendering goes through a **per-group field pass** (instances → group SDF/coverage field → one optical pass), which makes bounded smooth-min proximity union within a group nearly free; union aesthetics (neck width, max bulge, separation threshold) are capped and calibration-tuned so nothing reads as jelly.

### Motion

Per-channel drivers, not springs-everywhere:

| Channel | Driver |
| --- | --- |
| position, size, radius, press compression | velocity-preserving interruptible spring |
| lens strength | spring or critically damped |
| glow/illumination | fast attack + slow exponential decay |
| backdrop-adaptation values | exponential low-pass + hysteresis |
| foreground light/dark | threshold + hysteresis + crossfade |
| opacity/materialization, disabled | monotonic ease or step — no overshoot |
| quality tier | long hysteresis + cooldown |

Springs integrate closed-form (or capped fixed-step) so 60Hz, 120Hz, and dropped frames produce the same response. Interaction states in v1: idle, hover, pressed, focused, disabled, morphing. The neighbor glow-diffusion field is post-v1.

Reduced Motion removes elastic overshoot, deformation, and shimmer travel; keeps direct-manipulation positional continuity; shortens morphs to non-elastic interpolation; reserves crossfade for large plane shifts.

### Material variants

`variant: "regular" | "clear"` is first-class. Regular is the adaptive default. Clear is persistently more transparent with constrained adaptation and requires a dimming policy; a clear surface without one resolves to regular and raises a dev-mode error — the runtime refuses rather than inventing a scrim (a one-line default, `DEFAULT_CLEAR_DIMMING`, satisfies it). Mixing variants inside one `GlassGroup` raises a dev-mode warning, mirroring Apple's guidance.

### Accessibility policy

Lives in `core` as policy, applied by `platform-web` via media queries with `GlassRoot` prop overrides (`reducedTransparency | reducedMotion | increasedContrast: "system" | boolean`). Reduced transparency → more frosted, less refraction, higher occlusion. Increased contrast → stronger borders, near-monochrome foregrounds, reduced ambient tint. `forced-colors` → system colors, borders, no glass. `prefers-reduced-transparency` is not Baseline; the explicit override is therefore load-bearing, not a courtesy.

### Color pipeline

Internal optical math in linear-light textures; compositing premultiplied-alpha; output sRGB. v1 calibration is locked to sRGB; Display-P3 and HDR/extended-range are future profiles. Without this lock, golden images destabilize across GPUs/browsers and blur/highlight energy drifts from reference.

### Performance envelope & quality governor

The GPU budget (~2ms/frame) is a **hypothesis to validate**, pinned to declared benchmark scenes — mobile: 390×844 CSS px @ DPR 3, one video backdrop, 8 surfaces, 3 groups, 1 active morph, 60Hz; plus a desktop 2× scene — measured per pass (backdrop import, blur, analysis, body, highlight, composite) alongside browser end-to-end frame time. Effect-texture resolution is decoupled from DOM DPR. The governor degrades **within** a tier first (refraction resolution, adaptation cadence, edge analysis) and switches tiers only with long hysteresis and cooldown — never mid-interaction.

### Calibration harness & methodology

`apps/reference-apple` renders canonical scenes — backgrounds (light/dark solids, photo, high-contrast text, video) × components × states (rest/hover/press/release/disabled/selected/focused/menu-open) × sizes — captured on this machine into versioned fixture profiles keyed like `apple-macos-26.5-2x-light-standard` (OS, hardware, scale, color scheme, a11y mode, capture method, refresh rate). Native and web renders use **identical pre-rendered raster backgrounds** so font-rasterization differences never pollute the diff. Metrics are separated by axis: shape (contour distance, corner curvature, silhouette IoU), material (blur edge-spread, luminance transfer, tint response, rim intensity, shadow falloff), motion (response latency, peak compression, overshoot, settling, redirect continuity, morph silhouette trajectory), perceptual (edge-weighted diff, SSIM/OKLab ΔE, human A/B). Fixtures split into calibration / validation / **holdout** sets to prevent overfitting to specific scenes. Fixtures record the native side; **results** are versioned by the full render cell: native profile × web cell (browser engine + version, backend tuple, GPU adapter class, canvas color configuration, capture path). Claims and thresholds are **per tier** — the texture tier (vitrea's own shader math) and the dom tier (each engine's own backdrop-filter blur) are calibrated and stated separately, and holdout passes are required per supported engine/backend cell, not once. Canonical scenes include impulse and checkerboard backgrounds, which expose blur kernels and displacement fields directly for system identification, and a glass-over-glass scene (menu over toolbar): S1 established that an overlay-plane proxy necessarily samples the base plane's rendered output, so overlay material calibrates against a glassed backdrop, not raw content. All fidelity claims cite the profile and cell, never "pixel-identical to Apple."

Spring constants, corner smoothing values, tint thresholds, hysteresis rates, merge-distance defaults, and clear-variant dimming are **delegated unknowns**: named here, answered by the harness, recorded into calibration profiles. Two further unknowns gate architecture itself — see §Gating spikes.

### Testing

`geometry` and `motion`: vitest unit tests, TDD. WGSL passes: headless golden-image tests (sRGB-locked). Integration: Playwright against the demo app, including the CSS tier and capability resolution. Fidelity: the calibration diff suite (capture is machine-local; fixtures and thresholds are committed and CI-diffable).

## Gating spikes

Two prototypes run **before their contracts are built upon**; their outcomes land in the Decision Log. They are the first children of the decomposition, not afterthoughts:

- **S1 — backdrop proxy topology.** LANDED 2026-08-24 (docs/doperpowers/spikes/2026-08-24-s1-proxy-topology-findings.md): Chromium confirmed; Gecko/WebKit pending the manual page (automation is blind to backdrop-filter there); three X1 corrections adopted (§rendering contract); probe designed and measured.
- **S2 — geometry field error and cost.** LANDED 2026-08-24 (docs/doperpowers/spikes/2026-08-24-s2-geometry-field-findings.md): bound met with the rsupn family, no atlas promotion, X8 unchanged, Apple-direct reference adopted; the spike's harness is C3's regression base.

## v1 scope

Components: `GlassRoot`, `GlassGroup`, `GlassSurface` (with `asChild`), `GlassButton`, `GlassIconButton`, `GlassToolbar`, `GlassSegmentedControl`, and a menu built by composing `GlassSurface asChild` over an external accessible menu primitive (React Aria / Base UI class; `@vitrea/react` takes no hard dependency — the demo picks one). Transitions: press/release, segmented-indicator slide, button → menu matched morph. Backdrops: image, video, canvas/app texture, procedural gradient, dom-hybrid. Geometry: the three shape families. Planes: base + internal overlay. Profile: macOS 26.5 first.

## Out of scope (v1 cut list)

WebGL2 backend; Vue/Svelte/Web-Components adapters; Sheet, Slider, Toggle, TabBar, Popover, SearchField; neighbor glow diffusion; scroll-edge effects; arbitrary-contour/resampled morphs; public layer API and arbitrary stacking interleave; same-plane overlapping glass surfaces (checked dev-mode error) and more than one simultaneous overlay plane; automatic pixel analysis of arbitrary DOM (never promised); HTML-in-Canvas backend (seam only); Chromium SVG-displacement tier (seam only — Decision Log #11); Display-P3/HDR profiles; iOS/iPadOS calibration profiles.

## Parent-Level Acceptance

The eight behaviors in §What "done" looks like. Recomposition verifies those — end-to-end, in the shipped demo and published packages — not the sum of child gates.

## Children

### S1: Backdrop proxy topology spike — spike (findings, never a merge)

- **Purpose:** De-risk the one contract only real browsers can confirm: the per-plane sandwich with one masked backdrop proxy per sampling group over arbitrary DOM.
- **Acceptance:** A findings document recording, per stable engine (Safari, Chrome, Firefox): sampling equivalence versus in-place `backdrop-filter`, masked-proxy and paint-order behavior, scroll/fixed/zoom behavior, and whether the startup conformance probe can detect failure. Parent Decision Log updated: proxy topology confirmed, or the arbitrary-DOM promise narrowed per engine.
- **Edges:** blocked-by: — ; blocks: C5.
- **Contracts:** X1, X2 (the `probe-failed` demotion path).
- **Design inheritance:** §rendering contract, §Gating spikes (S1) — binding.
- **Required:** yes. **Status:** not-dispatched (dispatchable now).

### S2: Geometry field error & shader cost spike — spike (findings, never a merge)

- **Purpose:** Validate that parametric pseudo-SDF families hold a declared error bound against ground-truth contour distance at acceptable shader cost — or trigger the named fallback (distance-mask atlases) before the geometry API freezes.
- **Acceptance:** Error curves across `smoothing ∈ [0,1]` and a size sweep; shader cost on the benchmark scenes; a recommendation (bound met | atlas promotion) recorded in the parent Decision Log.
- **Edges:** blocked-by: — ; blocks: C3, C6.
- **Contracts:** X8.
- **Design inheritance:** §Geometry, §Gating spikes (S2) — binding.
- **Required:** yes. **Status:** not-dispatched (dispatchable now).

### C1: Monorepo foundation — autonomous

- **Purpose:** Turn the repo dual-artifact: pnpm workspaces, the seven package skeletons, strict TypeScript, vitest, CI, changesets, purity lint (X4), lazy-renderer bundling shape (X7) — with the plugin untouched and verified.
- **Acceptance:** `pnpm install && pnpm -r build && pnpm -r test` green on skeletons; the plugin still validates and its manifest paths resolve; CI runs on push; root `package.json` private.
- **Edges:** blocked-by: — ; blocks: C2, C3, C4 (and transitively all package work).
- **Contracts:** X4 (enforces), X7 (establishes).
- **Design inheritance:** §Repo layout & packages — binding for the package boundary law and publish surface; advisory for tooling details.
- **Required:** yes. **Status:** not-dispatched (dispatchable now).

### C2: Motion kernel — autonomous

- **Purpose:** The pure-math motion package: the MotionDriver family, closed-form/capped-step springs, the interaction state machine, interruption semantics.
- **Acceptance:** TDD unit suite: velocity-preserving redirects; frame-rate invariance (60Hz/120Hz/dropped-frame equivalence within tolerance); each driver family's behavior; zero DOM imports (lint-enforced). Constants ship as advisory defaults; calibration (C7) replaces them later.
- **Edges:** blocked-by: C1; blocks: C8.
- **Contracts:** X4.
- **Design inheritance:** §Motion — the driver-per-channel table is binding; integrator choice within the stability requirement is advisory.
- **Required:** yes. **Status:** not-dispatched (blocked-by C1).

### C3: Geometry kernel — autonomous

- **Purpose:** The pure-math geometry package: ShapeSpec/Contour IR, the pseudo-SDF families as S2 landed them, the level-set concentric resolver, parametric morph interpolation, group union math.
- **Acceptance:** Unit suite including an error-bound regression against S2's ground-truth harness; concentric derivation properties; morph channel continuity; zero DOM imports.
- **Edges:** blocked-by: S2, C1; blocks: C6.
- **Contracts:** X4, X8.
- **Design inheritance:** §Geometry — shape families, IR role, level-set concentric are binding; internal algorithms advisory.
- **Required:** yes. **Status:** not-dispatched (blocked-by S2, C1).

### C4: Core runtime — autonomous

- **Purpose:** The platform-free heart: scene model, the three registries (BackdropSource/GlassGroup/GlassNode), the resolved-state capability model with every demotion and recovery transition, material policy (regular/clear + mixing warning), accessibility policy, scheduler interfaces.
- **Acceptance:** Unit-tested state transitions covering every `demotionReason` and its recovery; policy resolution (variants, accessibility overrides); zero DOM imports.
- **Edges:** blocked-by: C1; blocks: C5, C6 (C8 inherits transitively).
- **Contracts:** X2 (implements), X4, X6.
- **Design inheritance:** §Core model, §honesty core, §Material variants, §Accessibility — binding.
- **Required:** yes. **Status:** not-dispatched (blocked-by C1).

### C5: platform-web runtime — controlled

- **Purpose:** The browser layer: host registration, GeometrySync (batched dirty reads, zero steady-state reads), LayerManager (base + overlay, unit promotion, overlap dev-error), per-group masked proxies with the conformance probe, the CSS-tier renderer, media-query policy, WebGPU lifecycle (probe, device loss).
- **Acceptance** (narrowed 2026-08-24 per S1, human-approved): Playwright pixel assertions run on Chromium only — S1 proved automation cannot observe backdrop-filter output on Gecko/WebKit. All three engines run the non-pixel suite: registration/teardown, hit-testing, focus, the probe battery, the overlap dev-error, instrumented proof of zero steady-state layout reads, probe-demotion path, and promotion under scroll and focus. The S1 manual self-scoring page becomes a release-time gate (C9 owns running it).
- **Edges:** blocked-by: S1, C4; blocks: C8.
- **Contracts:** X1, X2, X4 (consumer side), X6.
- **Design inheritance:** §rendering contract (binding, as revised by S1's findings), §Foreground adaptation (binding), §Accessibility (binding).
- **Required:** yes. **Status:** not-dispatched (blocked-by S1, C4).

### C6: WebGPU renderer — controlled

- **Purpose:** The optical engine: BackdropFrame providers for every source kind, blur/analysis pyramids, the group field pass, optics and highlight passes, texture pool, the color pipeline, golden-image tests.
- **Acceptance:** Golden suite green (sRGB-locked); lensing visibly scales with surface size (acceptance #2's mechanism); instrumented dirty-epoch invariant (≤1 pyramid rebuild per dirty source per frame); device-loss recovery test for both ownership modes; benchmark scenes measured pass-by-pass with the ~2ms hypothesis evaluated and recorded.
- **Edges:** blocked-by: S2, C3, C4; blocks: C8.
- **Contracts:** X1, X2, X3 (owner), X5, X8.
- **Design inheritance:** §GPU device ownership, §Color pipeline, §Performance envelope — binding; pass structure internals advisory.
- **Required:** yes. **Status:** not-dispatched (blocked-by S2, C3, C4).

### C7: Calibration system — controlled — LANDED (2026-08-25) with one axis externally blocked

- **Delivered:** `packages/calibration` (the four metric axes as pure TypeScript over decoded PNGs, X9's per-cell result matrix, a `diff` CLI writing per-axis reports into it), `apps/reference-apple` (a SwiftUI harness on the real `glassEffect`/`GlassEffectContainer`/`interactive` surface, the six shared raster backgrounds, the fixture manifest), `scenes.json` as the geometry contract both sides read, a seed-constants profile, and one automated web→diff→matrix comparison run.
- **Blocked:** the native *material* cannot be captured here. Liquid Glass is composited by the window server; ScreenCaptureKit is the only path that can see it and its TCC grant is denied with no prompt. **This needs a human** (apps/reference-apple/README.md §"The capture wall"). Until then the committed fixtures are provisional and marked so mechanically — `materialRendered: false`, `identicalToBackground: true` on every entry — and **no fidelity claim may cite them**.
- **Consequence for C9:** the pipeline C9 needs is built and proven end to end, but the ground truth it must tune against does not exist yet. C9's tuning is gated on the Screen Recording grant, not on more code. The seed profile records which constants are `measured` (the S2-derived corner geometry) and which are `unmeasured` with the reason, so C9 can see exactly what it is missing.
- **Flow-backs C7 hands forward** (details in §Surprises, all found by building the comparison rather than by reading the design): one corrective on **C6** — the renderer bridge never forwards core's `mergeDistance` into geometry's `DEFAULT_GROUP_UNION`, so no group scene can measure the spacing it declares, and the wiring must precede the tuning. Three scoping items for **C9** — material thickness has no shared value between the two sides and needs a scene-matrix field before any thickness-sensitive metric means anything; the glass-over-glass holdout scenes are unavoidably a mixed-backend cell and cannot be stated as a clean texture-tier claim; and the pressed scenes compare two derived poses, so they are a tuning target rather than a measurement. One free win — the accessibility profiles are already reachable on the web side and merely unplumbed in the capture driver.
- **Purpose:** The fidelity ground truth: the calibration package (fixture schema, multi-axis diff metrics, fitting CLI), the SwiftUI reference harness, and the first committed `apple-macos-26.5-*` fixture profiles with the calibration/validation/holdout split.
- **Acceptance:** The harness renders the canonical scene matrix and captures versioned fixtures on this machine; the diff CLI produces per-axis metrics on sample pairs; the per-cell result-matrix schema (X9) exists and is consumed by at least one automated comparison run. **Met, with one qualification:** the harness renders the matrix and captures it, but the capture cannot include the material — so the fixtures are structurally complete and substantively empty until the TCC grant lands.
- **Edges:** external:xcode-installed (start gate — opened 2026-08-25); **new external edge discovered in flight: `external:screen-recording-tcc`**, which gates the material captures and therefore C9's tuning. No code dependencies.
- **Contracts:** X5, X9 (owner).
- **Design inheritance:** §Calibration harness & methodology — binding.
- **Required:** yes (parent acceptance #7 depends on it). **Status:** landed 2026-08-25; parent acceptance #7 remains **open** pending the grant.

### C8: React bindings & v1 components — controlled

- **Purpose:** The developer-facing surface: GlassRoot/Group/Surface (asChild), Button, IconButton, Toolbar, SegmentedControl, the menu composed over an external accessible primitive, and the press/indicator/morph wiring — thin over platform-web.
- **Acceptance:** Parent acceptance behaviors #1, #3, #4, and #6 demonstrably pass in the demo playground; a11y assertions (axe + screen-reader spot checks); the variant-mixing warning fires.
- **Edges:** blocked-by: C2, C5, C6; blocks: C9.
- **Contracts:** X1, X2, X6, X7, X8.
- **Design inheritance:** §v1 scope component list (binding), menu-composition decision (binding — Decision Log #13); component internals advisory.
- **Required:** yes. **Status:** not-dispatched (blocked-by C2, C5, C6).

### C9: Fidelity pass, demo site & release — decomposing run at dispatch

- **Purpose:** Close the charter: tune against calibration cells with per-tier claims, build the public showpiece with side-by-side native captures, write docs and the NOTICE trademark clause, publish `@vitrea/core` and `@vitrea/react`.
- **Acceptance:** Parent acceptance #5, #7, #8 as written — per-cell holdouts pass, packages install, demo public, claims honest. Precise gates emerge from its own cut.
- **Edges:** blocked-by: C7, C8.
- **Contracts:** X7, X9 (consumer).
- **Design inheritance:** §Purpose positioning (binding), §Calibration claims wording (binding).
- **Required:** yes. **Status:** not-dispatched (deliberately late — frontier rule).

## Cross-Child Contracts

- **X1 — Rendering sandwich & plane law** (§rendering contract; binds S1, C5, C6, C8): per-plane paint order, one overlay plane, checked same-plane overlap, unit promotion. Owner: this document; S1 findings may narrow it per engine.
- **X2 — GlassGroupState resolved-state model** (§honesty core; binds C4, C5, C6, C8): the enumerated state interface, demotion reasons, recovery transitions. Owner: C4 implements exactly this shape.
- **X3 — BackdropFrame protocol** (§GPU device ownership; binds C5, C6 and the public API): acquisition timing, alpha/color normalization, size epochs, ownership modes, loss recovery. Owner: C6 delivers the operational detail.
- **X4 — Purity law** (§Repo layout; binds C2, C3, C4; enforced by C1's lint): core/geometry/motion never import window, document, or HTMLElement.
- **X5 — Color pipeline** (§Color pipeline; binds C6, C7): linear-light internal, premultiplied compositing, sRGB-locked v1 output and calibration.
- **X6 — Hint contract** (§honesty core; binds C4, C5, C8): the `backdrop` prop shape and estimator-provider interface — one mechanism, never implied to be pixel analysis.
- **X7 — Publish surface** (§Repo layout; binds C1, C9): two published packages, bundled internals, lazy renderer import.
- **X8 — Shape channel set** (§Geometry; binds C3, C6, C8): `{ center, size, radii, smoothing, thickness }` — every morph channel numeric. S2 confirmed the set unchanged, with three riders: smoothing is budget-clamped (continuously) by size and radii; **rider 2 (tightened at C3's landing): the concentric error bound holds only on the level-set path — renderers MUST render a concentric child as a level set of the parent's field (`concentricField`), where the offset is the zero set by construction (< 0.18px at every inset); instantiating the child as its own resolved shape adds an offset-approximation error that grows with inset (0.105px at 1px, 0.326px at 8px) and dominates past ~4px. A concentric child that shares a group with its parent renders as a field-reference-only surface — the parent's field is referenced, never unioned in, because min(parent, parent+inset) erases the child by construction (C6's addition, ratified; this is the segmented-control-indicator case)**; radii are uniform-only in v1 (Vec4 shape preserved). Corner profiles are two separate references, not one axis: `"figma-smoothing"` is the interpolable authoring axis the declared bounds are measured on; `"apple-continuous"` is the Apple-direct fit that `profile: "continuous"` resolves to; morphs across the two throw (every v1 morph pair shares a profile; `APPLE_BEST_FIGMA_SMOOTHING = 0.66` is the documented route onto the interpolable axis). C6's instance buffer widens by six derived floats.
- **X9 — Fixture/profile/cell schema** (§Calibration; binds C6, C7, C9): native profile × web cell keying, per-tier thresholds. Authority here; content delegated to C7.

## Ordering & Dependency Map

```
Wave 0 (parallel, now):  S1   S2   C1   C7(external: Xcode)
Wave 1:                  C2(C1)   C3(S2,C1)   C4(C1)
Wave 2:                  C5(S1,C4)   C6(S2,C3,C4)
Wave 3:                  C8(C2,C5,C6)
Wave 4:                  C9(C7,C8)
```

C7 runs whenever its external gate opens — nothing downstream of code blocks on it until C9; a late C7 delays tuning, not structure (advisory defaults hold until fixtures land).

## Risks & Mitigations

- **S1 partially fails on an engine** → the arbitrary-DOM promise narrows there (dom groups demote to CSS tier); API unchanged. Carried by X1/X2.
- **S2 misses the error budget** → distance-mask atlases promote into v1 behind the same IR; carried by §Geometry's named fallback.
- **Xcode/calibration slips** → C2/C6 ship advisory constants from WWDC-footage eyeballing; fidelity *claims* wait for C7 (claims are calibration-cited by contract, so no dishonest interim state exists).
- **WebGPU engine variance** (timestamp queries, adapter quirks) → benchmark scenes record per-adapter-class results (X9); the governor absorbs variance at runtime.

## Deferred (may return)

Vue/Svelte/Web-Components adapters; Sheet, Slider, Toggle, TabBar, Popover, SearchField; neighbor glow diffusion; scroll-edge effects; HTML-in-Canvas backend; the Chromium displacement tier; Display-P3/HDR profiles; iOS/iPadOS calibration profiles; public layer API. Standing exclusions stay in §Out of scope (v1 cut list).

## Tracking Map

| Child | Spec / findings | Status |
| --- | --- | --- |
| S1 proxy-topology spike | docs/doperpowers/spikes/2026-08-24-s1-proxy-topology-findings.md | landed; ALL engine verdicts closed 2026-08-25 — Chromium measured byte-exact; Safari 26 and Firefox manually verified by the user via the self-scoring page (retail browsers on their MacBook; 8 evidence screenshots at spikes/s1-proxy-topology/manual-evidence/): sampling equivalence holds, 3σ sweep stabilizes as measured, double-filter arithmetic reproduces, mask-exact construction confirmed, canvas-above never sampled |
| S2 geometry-field spike | docs/doperpowers/spikes/2026-08-24-s2-geometry-field-findings.md | landed 2026-08-24 (bound met; rsupn; 46 spike tests; costs from the serialized bench run 2026-08-24T14:29:14.670Z) |
| C1 monorepo foundation | merge 5923258 (agent report in session; 29 tests, purity lint proven, X7 asserted on built artifact) | landed 2026-08-24, re-verified on main (chain exit 0) |
| C2 motion kernel | merge (162 package tests; frame-rate invariance ≤1e-12 asserted, ~1e-15 measured) | landed 2026-08-24, re-verified on main (chain exit 0) |
| C3 geometry kernel | merge (137 tests; full 324-shape matrix green in 125s; analytic one-pass gradient) | landed 2026-08-25, verified on main (chain exit 0) |
| C4 core runtime | merge 1475029 (241 package tests; 43-state X2 machine; codex review: 6 findings fixed) | landed 2026-08-24, verified on main (chain green, root lint clean) |
| C5 platform-web runtime | merge (86 unit + 242 Playwright tests: 88 Chromium / 77 Firefox / 77 WebKit; 4 suite-caught bugs fixed) | landed 2026-08-25, verified on main (chain exit 0) |
| K1 X2 css-by-choice amendment (corrective, core) | merge 89e4a8b (43→49 pinned states; probe axis three-valued) | landed 2026-08-25, verified on main (chain exit 0) |
| C6 WebGPU renderer | merge (198 unit + 19 GPU tests; f32 check PASSED 3.0e-5px; goldens on real headless Chromium; benchmark: mobile 1.47ms median / desktop 2.20ms vs the 2ms hypothesis) | landed 2026-08-25, verified on main (chain exit 0 after the bundle-test graph fix) |
| C7 calibration system | branch (harness + metrics + CLI + one comparison run; fixtures provisional) | **landed with a blocked axis** 2026-08-25 — the package, the harness and the pipeline are real and verified; the native *material* is not capturable on this machine (Screen Recording TCC denied, no prompt). Correction: the earlier "Xcode 26.6 licensed" note was wrong — the accepted license is 16.4, and the gate lives only in the wrapper tools, so `swiftc` builds directly. See Surprises. |
| K3 mergeDistance→union wiring (corrective, platform-web) | merge 35b92b6 (declared-vs-resolved distinction preserved the no-declaration identity; geometry 143 tests) | landed 2026-08-25, verified on main (chain exit 0) |
| C8 React bindings & components | merge (70 unit + 31×3-engine Playwright; acceptances #1/#3/#4/#6 evidenced; axe clean ×3 engines) | landed 2026-08-25, verified on main (chain exit 0) |
| K2 platform-web→renderer bridge (corrective) | merge (923 unit tests; GPU glass end to end; acceptance #2's mechanism visible: larger plate lenses harder) | landed 2026-08-25, verified on main (chain exit 0) |
| C9 fidelity, demo & release | decomposed 2026-08-25 (see §C9 closing cut) | C9a, C9b, C9c landed (rename verified across 82 files; dry-runs clean: vitrea 248kB / vitrea-react 234kB packed, self-contained dts, lazy WGSL); K5 landed — C9d's gate order now open at step 2 (golden regeneration behind the isolation proof) |
| K5 tier-coherence opacity (corrective, platform-web) | merge d82cdd4 (CSS-tier mapping seam + 30 dom-tier cells committed, matrix 30→60; tier-switch interior ratio 3.19→0.99; foreground rule corrected, demo contrast 1.24→3.53/pass) | landed 2026-08-25; 1079 unit tests, platform-web e2e 254/254 |
| K4 CSS-tier foreground hint wiring (corrective, platform-web) | merge aee60d9 (8 unit + 1 pixel test; a11y-over-hint precedence asserted) | landed 2026-08-25, verified on main (chain exit 0) |

## C9 closing cut (decomposed at dispatch, 2026-08-25 — real fixtures in hand)

The Screen Recording grant landed and real fixtures exist: 30/30 `materialRendered: true` via ScreenCaptureKit, deterministic, captured through the granted bundle via LaunchServices (a shell-spawned harness inherits the shell host's TCC identity — the bundle launch is now the documented capture path). The first real cell measures untuned vitrea vs actual Liquid Glass at rest: silhouette IoU 0.953, contour mean 0.70px / p95 2.83px, SSIM 0.975, OKLab ΔE mean 0.0076 — and the tuning target: luminance slope 0.119 native vs 0.0625 web (native frosts ~2× harder). Remaining capture caveats: 1x display only (2x canonical profiles need a Retina run — the user's MacBook is the candidate); the two accessibility profiles need their own runs with System Settings toggles (external, user).

**C9a's correction to the paragraph above, kept rather than overwritten because the mistake is instructive.** The per-pixel luminance slope is not the material's transmission: it is *attenuated by the material's own blur*, so it reads lower the higher-frequency the backdrop, and the direction it implied was backwards. On the same reference material the same estimator returns 0.045 on the impulse background and 0.419 on the photo. Regressing each side's interior level against the backdrop level across scenes is not subject to that attenuation, and it says the reference passes **0.26** of the backdrop where untuned vitrea passed **0.70** — Apple's material is markedly *more* opaque than vitrea's default, not less, and the frosting was never the headline gap. A single-cell reading of one estimator set the tuning target in the wrong direction, and only the full matrix plus a second estimator caught it.

- **C9a — fidelity tuning — LANDED (2026-08-25).** Claims: `docs/doperpowers/specs/c9a-fidelity-claims.md`. **Delivered:** the full 30-cell matrix measured in one reproducible run (`compare` now batches the matrix with opt-in fixture sets, an interleaving permutation matching the native harness's own, and one write); a material tunables seam (`MaterialProfile`/`MaterialProfilePatch`/`withMaterialOverrides` on the renderer, forwarded through platform-web, injectable into the calibration page with its hash recorded in every cell); two tuned constants with a committed profile per colour scheme and a test pinning the profile against the runtime default; a committed tuning sweep so the search is reproducible rather than asserted; per-tier claims with proposed thresholds for the human gate. **Numbers:** holdout (six scenes never fitted) IoU 0.9924/0.9612, SSIM 0.9475/0.9007, OKLab ΔE 0.0320/0.0548 — every holdout axis improved over the untuned defaults, with a mild overfitting signal reported (holdout went from 2.3× to 3.1× the fitted sets' ΔE) and its mechanism named. **Two constants moved:** `tintAlpha` 0.28 → 0.62, and the adaptive tint's backdrop crossover made inert. **Six parent-impact items** (§Decision Log #27). Inherited C7 scoping items all resolved as findings rather than fixes: thickness not added (only one side can honour it), glass-over-glass stated as the mixed-backend cell it is, and the pressed caveat sharpened — the native pressed fixtures are byte-identical to rest, so no press claim is made. External sub-gates unchanged: a11y-profile captures and the 2× run.
- **C9b — the showpiece** (opus, in-flight): restyle apps/demo into the public demo site via the repo's designer skill (stance, tokens, QA per the plugin's own doctrine), side-by-side panels with native captures, fix the demo's own overlap diagnostics, static-deployable build. Blocked-by: nothing (structure now; final numbers from C9a slot in).
- **C9c — docs & packaging**: execute the naming amendment (Decision Log #27: core → `vitrea`, react → `vitrea-react`, import sweep, X7 test updates), package READMEs, NOTICE trademark clause, changeset, publish dry-run. Blocked-by: C9a claims wording, C9b landing (rename sweeps their files). External gate: CLEARED — user authenticated as kimmi17, unscoped names need no org.
- **C9d — release gates**: labeled per-engine manual-page run feeding the conformance table, final chain + axe + goldens, the publish itself, then parent recomposition against §What "done" looks like. Blocked-by: C9a–C9c.

## Decision Log

32. **K5 rulings (2026-08-25) — tier coherence, claims amended in `docs/doperpowers/specs/c9a-fidelity-claims.md` §3:** (a) **The CSS tier no longer holds a material of its own; it converts the one the root carries.** `css-tier.ts` keeps no optical literal — tint alpha, tint and border colour, border alpha and the ambient shadow all arrive on the optics, which `optics.ts` derives from `createGlassRoot({ materialProfile })` through one mapping function (Decision Log #29b's seam, extended to the second tier). Copying C9a's 0.62 across was refused for the reason C9a refused it: the renderer lerps toward the tint in **linear light** while the page composites `rgba()` over `backdrop-filter` in **encoded sRGB**, so the same alpha is a different material. The mapping solves for the alpha that reproduces the renderer's composite at one declared backdrop level; that level is the tuned constant. (b) **The mapping was fitted against the cross-tier delta, not against the fixtures, and the two disagree monotonically in opposite directions** (cross-tier ΔE 0.0078 → 0.0115 as the CSS tier's own ΔE against Apple falls 0.0118 → 0.0094 across the swept range — they can disagree because the GPU tier is not itself exactly on the reference). Coherence chosen deliberately: an independently-fitted CSS tier is free to drift the moment either tier is retuned, which is precisely how this gap opened, while a converted one inherits the GPU tier's fidelity by construction. The price is stated, not hidden — 0.0116 against Apple where an independent fit reached 0.0094, both far inside C9a's proposed 0.07. (c) **Exact coherence is unreachable and the arithmetic says so before any capture does**: matching the two composites for the shipped tint needs a CSS alpha of 0.761 over a backdrop at 0.05 linear luminance and 0.635 at 0.8, a 1.2× spread with no single scalar in it. The measured floor is cross-tier ΔE 0.0080 mean / 0.0151 worst on the fitted sets, 0.0229 / 0.0358 on holdout, with the interior level within 1% in the mean and 11% on the worst cell — **coherence is claimable in that wording and not as identity**. (d) **`platform-web` mirrors the renderer's per-variant optics rather than importing them** — a static dependency would put the GPU tier's module on the CSS tier's critical path against X7 — and `packages/calibration/test/tier-coherence.test.ts` pins the mirror in both directions from the one package that legitimately depends on both. Before K5 the two tiers' agreement on σ = 8 was two literals that happened to match. (e) **All 30 dom-tier cells committed** (matrix 30 → 60 cells, `renderer: css` / `samplingBackend: css-backdrop` / `tier: dom`), and dom-tier perceptual + coherence thresholds are now proposable; the dom-tier **shape** threshold is not, because that axis measures the tier's own `box-shadow`. Two mapping constants swept and declined with reasons recorded as data: `borderAlphaPerRimAlpha` (1.01× grid, and zeroing it would delete the tier's contrast floor to win flatness — C9a's rim reasoning one tier along) and the shadow triple. (f) **The foreground rule had to move with the material.** K4 wired X6's hint straight through — a `tone: "dark"` group got the light ink — which was right while the material was 28% opaque and the backdrop showed through it. At the tuned opacity what a reader sees behind the glyphs is the tint, not the backdrop, and the demo measured **WCAG contrast 1.24 against a 4.5 floor**: near-white ink on a near-white surface. The CSS tier now decides against `mix(backdrop, tint, α)` using the hint's optional `luminance` where an app gives one and the tone's coarse reading otherwise. K4's mechanism and its accessibility precedence are untouched; only the arithmetic changed, and both regimes stay reachable — the clear variant over the same dark hint still resolves to the light token, because at its alpha the backdrop really does dominate. It is not a contrast calculation and does not promise a ratio; an app needing one sets its own foreground. **Whether the GPU tier's foreground path needs the same correction is the parent's** — that path is the renderer's and K5 moved nothing there. (g) **Two parent-impact items, both measured and neither forced.** The dom tier's `box-shadow` owns its entire shape axis — off, silhouette IoU goes 0.676 → 0.942 and contour p95 18.7px → 2.2px, and ΔE and coherence both improve, which would put the CSS tier marginally closer to Apple than the GPU tier; the reference has no exterior shadow, but the fallback has to read as floating, so it is a taste call with a design owner (C9b's showpiece inherits it). And **the reduced-transparency occlusion floor no longer lifts anything**: C9a moved the nominal tint alpha to 0.62 and `increasedOcclusionAlpha` is also 0.62, so `Math.max` has been inert on the GPU tier since C9a and K5 makes the CSS tier match it. The reduced-transparency reader gets more absolute occlusion than before (0.718) and no difference from nominal on that one axis; frost still moves. Re-basing the floor is the parent's — it is one number shared by both tiers, inside the set C9a froze against holdouts.

31. **C9d gate procedure (2026-08-25, user-directed — binding):** the ten pre-existing failures are two situations wearing one label. (a) The eight stale goldens regenerate only behind an **isolation proof**: re-render every golden scene with the OLD profile values injected via the `materialProfile` override — byte-identity against the old goldens attributes the entire delta to exactly the two tuned constants; any residual means something unintended moved and is investigated before re-baselining. Then an eyeball pass on the new renders, then regeneration via the documented script, committed citing the proof. (b) The two demo failures are real defects the tuned material exposed, and neither assertion loosens: the segment-label contrast (4.402 vs the 4.5 floor) is fixed at the token — the foreground re-derived over the tuned material until the pixel-measured contrast passes, the floor being the repo's own accessibility law; the reference-pair readout's dark default is a selection drift — fix the demo default to the primary light profile, keep the test. (c) **Sequencing: all ten wait for K5** — fixing contrast before K5's final CSS-tier material would mean fixing it twice. Gate order: K5 lands → golden regeneration with isolation proof → demo fixes over the final material → full suite green → labeled per-engine manual-page pass → deploy and publish.
30. **C9c rulings (2026-08-25):** (a) **The release path is `pnpm release` (changesets → pnpm publish), never bare `npm publish`** — verified by tarball unpacking: npm would ship react's dependency as the literal unresolvable `"vitrea": "workspace:^"`, while pnpm rewrites it to `^0.1.0`; binding on C9d. (b) Per-package LICENSE + NOTICE accepted (Apache-2.0 §4 requires them inside the distribution; root copies never reach a tarball). (c) **`vitrea` alone cannot mount a root** — core carries no DOM by the purity law and the browser host is bundled into `vitrea-react` only, so v1's only path to rendered glass is React; the §Purpose "framework-agnostic core" promise is narrower in practice than it reads, both READMEs say so plainly, and a framework-agnostic host entry joins the deferred post-v1 round. (d) Ten e2e failures are confirmed pre-existing on main (verified by detached re-run, byte-identical): eight renderer goldens stale since C9a's tint change and two demo assertions (contrast 4.402 vs 4.5; dark-profile default cell) — all release-blocking, all owned by C9d's gate, where golden regeneration is made as a fidelity judgment against the calibration cells, not a mechanical refresh. (e) The demo's static build distributes the native reference captures (`dist/fixtures/`) — held as measurement ground truth per NOTICE; surfaced to the user and **approved 2026-08-25** for the public site.
29. **C9a rulings (2026-08-25) — claims in `docs/doperpowers/specs/c9a-fidelity-claims.md`:** (a) **Two constants moved and every other calibration-delegated material constant was deliberately left alone, each with its reason recorded as data** in `packages/calibration/profiles/apple-macos-26.5-1x-*.json`. `tintAlpha` 0.28 → 0.62 (the reference passes 0.26 of the backdrop against untuned vitrea's 0.70), and the adaptive tint's backdrop crossover made inert. Declined: `blurSigma` (the fixtures do not identify it — the two-dimensional sweep spans 1.08×, no canonical background resolves a step edge at this σ, and σ=8 is what makes core's 24px `samplingPadding` exactly S1's 3σ floor); rim and specular in light (the reference's rim peaks there are 7–26× *below* one 8-bit code step at the interior level involved); shadow (both sides ≈0 outside the contour); the union caps (§Surprises — the reference does not union at the one spacing the matrix declares). A dark-scheme rim retune that halved the dark objective was **found and refused**: the residual bottoms out at the mean of the reference's own rim peaks, so the win came from vitrea's rim going to zero rather than matching, and the per-cell direction is not even consistent. Fitting a four-cell mean by deleting the feature it measures is fitting the estimator. (b) **The tunables seam does not read JSON at run time.** The profile is the authority and carries provenance; the renderer's `DEFAULT_MATERIAL_PROFILE` mirrors the one profile it targets; a test pins them together in both directions. A published package loading a calibration file would ship a data dependency for numbers that never change between releases, against X7. Any other profile is applied as data via `createGlassRoot({ materialProfile })`, with the applied file's SHA-256 in every cell's `capturePath`. (c) **Thresholds are bounded by the holdout numbers, not the calibration ones** — a gate that calibration passes and holdout fails certifies overfitting rather than preventing it — and the shape axis is gated only on a cell whose native silhouette is within 5% of the declared component area, because one canonical cell measures the extractor rather than the geometry. (d) **The corner question S2 left open is answered as undecidable by these fixtures**, on arithmetic before measurement: 0.012px against 0.039px at the largest canonical radius, both two orders of magnitude under one pixel. The Apple-direct fit stands on S2's geometric evidence, not on anything C9a measured. (e) **Six parent-impact items, each measured and none forced:** a size term on the tint alpha (the reference's opacity runs 0.88 at a 32px span to 0.56 at 96px; vitrea has no size term, and this is what the holdout penalty is); a multiply rather than lerp blend for the dark material (evidence weak-to-moderate — four cells, factor-two scatter, no dark holdout); colour-scheme selection of a profile (nothing in the runtime reads the scheme, so dark mode is correct only for a host that passes the dark profile itself); the bridge not forwarding the corner `reference`, which makes X8's Figma route unreachable from the web runtime; a canonical scene tight enough that the reference actually unions; and a CSS-tier tunables seam plus a Chromium-only dom-tier sweep, since the two tiers now differ in opacity by more than 2×. (f) **The pressed scenes carry no claim.** The native pressed fixtures are byte-identical to their rest counterparts (all four, SHA-256): `Glass.interactive(true)` opts the material into responding to press input rather than posing it pressed, and Apple exposes no declarative pose. They were also excluded from the tuning decision — re-running the sweep over only the non-pressed cells returns the same optimum.

28. **C9b flow-back rulings (2026-08-25):** (a) the scissor-clip renderer fix accepted — `groupFieldRect`'s 5px padding underflowed `setScissorRect` for any surface within 5px of the viewport's top/left edge (a left-pinned toolbar sufficed; every morph platter hit it on mount); fixed in renderer-webgpu with device-pixel clipping, goldens unchanged, five tests. Also accepted: the demo's vite source-alias for the renderer (dev was silently running the last build — the one unaliased package). (b) The CSS tier ignoring X6's `hint` for foreground is corrective **K4** — the honesty core's one mechanism for telling the runtime what it cannot see must reach the tier most visitors get. (c) GlassMorph's missing landmark/role seam joins the deferred post-v1 API round (documented exclusion carries the axe scan's stated reasoning). (d) The GlassMorph pre-measure registration flow (viewport-wide box at plane origin for a frame) was fixed at the demo layer with the constraint rules written into the demo's DESIGN.md as checkable law — the underlying pre-pin behavior is noted as a polish candidate for the deferred round, not a v1 defect.
27. **npm naming amendment (2026-08-25, user-decided):** the `vitrea` npm org name is taken, so the published pair becomes **unscoped `vitrea` (core) + `vitrea-react`** — verified unclaimed; publishes under the user's account (kimmi17), no org required, and `npm install vitrea` is the strongest install line the brand could have. Rejected: `@kimmi17/*` (reads personal, migration pain), a new `@vitreajs`-style org (another name lottery for no gain over unscoped). Internal workspace packages keep their `@vitrea/*` names — they never publish and are bundled per X7. Execution belongs to **C9c** (package renames, import sweep in react/demo/calibration-web, X7 test updates) after C9a/C9b land, to avoid worktree conflicts with the in-flight children.
26. **C7 rulings (2026-08-25):** (a) **The native material is not capturable on this machine, and the child landed saying so rather than working around it.** Liquid Glass is composited by the window server; every in-process path was measured failing and ScreenCaptureKit's TCC grant is denied with no prompt (§Surprises). Rejected: synthesising a "reference" from vitrea's own output (that is not ground truth, it is a mirror), and treating the material-free captures as fixtures (they are pixel-identical to their backgrounds — a fidelity claim on them would be measuring nothing). The provisional fixtures are committed with `materialRendered: false` and `identicalToBackground: true` on every entry so the emptiness is machine-checkable, not a footnote. **Parent acceptance #7 stays open**, and its remaining blocker is a human granting Screen Recording, not code. (b) **`scenes.json` is the geometry contract**, read by both the native harness and the web calibration page — the spec's identical-raster-backgrounds rule generalises to geometry, and two hand-kept copies of a rect would drift. (c) **Metrics carry no thresholds**; C9 owns what counts as close enough, per tier and per cell, decided against holdout scenes. (d) **The split is declared as data in `scenes.json`** and holdout ids are named in no tuning code; holdout took the axes tuning would most want to peek at (the legibility case, the far end of the size sweep, the stacked case S1 forced). (e) The capture order interleaves by a stable FNV-1a of the cell key rather than a stride — the spec's interleaving mandate needs a permutation, and Swift's per-process string hash seed would have made the order incomparable between runs. (f) Corner curvature reports the corner-region **median**, not the peak: Menger curvature was implemented and then measured failing (0.052 against a true 0.025 on a radius-40 disc), and its curvature-scale-space replacement rings at the arc-to-straight junction.

1. **Name: vitrea** (`@vitrea/core`, `@vitrea/react`; npm scope verified unclaimed 2026-08-24). Rejected: *lensing*, *meniscus* (weaker as brands), *glasskit* (collision-prone, bland).
2. **Fidelity-first tiebreaker** among the four purposes (OSS product, designer-skill engine, showpiece, commercial). Rejected: adoption-first, skill-integration-first — the defensible ground against verified competitors is being visibly closest to native. Carried by: calibration harness (§Calibration), v1 cut list.
3. **Hybrid-first backdrop promise** — works on arbitrary DOM, fidelity scales when the app registers GPU-ownable backdrops (§Backdrop contracts). Rejected: GPU-scene-first (shrinks the audience), arbitrary-DOM-only (caps the ceiling).
4. **WebGPU + CSS-only ladder; WebGL2 cut from v1.** Research: WebGPU default-on in all three engines (Chromium 113+/Android 121+, Firefox 141+ Windows / 145+ ARM Mac, Safari 26); Firefox-on-Linux still flagged → CSS tier is a hard requirement, not courtesy (§rendering contract axis `renderer: css`). Rejected: WebGL2 backend (roughly doubles renderer surface for a shrinking tail the CSS tier covers), WebGPU-only (breaks the OSS promise).
5. **Monorepo restructure of this repo**; plugin untouched (manifest reads `skills/`, verified). Rejected: subdirectory project, separate repo.
6. **Core + React bindings only in v1**; adapters later. Rejected: +Web Components (surface before the core API settles), React-only (retrofit cost).
7. **v1 = primitives + controls + one morph pair** — the morph pair is what proves the differentiating subsystems. Rejected: primitives-only (validates nothing distinctive), broad set (delays v1).
8. **Calibration harness from the start** — fidelity-first is unfalsifiable without ground truth. Rejected: eyeball-first, no harness.
9. **Finish line: demo site + npm publish.** Rejected: demo-only, in-repo milestone.
10. **Controlled track; design-whole-then-decompose** — the pieces share one design surface, so the interaction surface had to be generated before cutting. Rejected: single execution plan (too big to execute reliably), autonomous execplan (fidelity taste arises mid-flight).
11. **Chromium displacement tier: seam reserved, not implemented** (fork Option 1, user-decided). Verified: `feDisplacementMap` in `backdrop-filter` is Chromium-only (WebKit bug 245510; Firefox unsupported). Rejected: implement in v1 (third optical stack + Chromium-only calibration fork), never (concedes competitors' best demo permanently).
12. **Review adoptions (2026-08-24 independent reviews, both verified where checkable):** BackdropSource/analysis split with explicit `exact | hint | none` (§honesty core) — fixes the hybrid-mode contradiction; `sampled-async` low-rate readback for foreground (§Foreground) — fixes the GPU→DOM contradiction; canvas pair per managed plane (§rendering contract) — fixes the z-order wall liquidGL demonstrates; backdrop-proxy layer (§rendering contract); GPU device ownership contract (§GPU device ownership); `platform-web` package (§packages); Contour IR over canonical-SDF-as-truth (§Geometry); parametric-only v1 morphs; per-channel MotionDrivers (§Motion); Regular/Clear first-class (§Material variants); color pipeline lock (§Color); blur pyramid owned by BackdropSource (§Core model invariant); calibration rigor — profile keys, raster backgrounds, multi-metric, holdout (§Calibration); benchmark-pinned budget + intra-tier degradation (§Performance); publish two packages with bundled internals; menu via external a11y primitive; repositioned competitive claim (§Purpose).
13. **Review declines/modifications (2026-08-24 design reviews):** SegmentedControl **kept** in v1 (bounded; exercises within-group indicator morph — a distinct case from the cross-plane morph); three parallel hint mechanisms **consolidated** to one prop + provider contract; public `GlassLayer` API **deferred** (internal LayerManager only); flat three-tier naming **superseded** by orthogonal capability axes (themselves superseded by the resolved-state model in #14).
14. **Adversarial spec review adoptions (codex `gpt-5.6-sol`, 2026-08-24) — all six findings survived verification against the cited specs; none rejected:** same-plane overlap becomes a checked constraint and cross-plane promotion is specified as unit promotion (§rendering contract); backdrop proxies become one masked proxy per sampling group with a startup conformance probe and demotion path, gated by spike S1 (§rendering contract, §Gating spikes); the free capability tuple is replaced by an enumerated resolved-state model with named demotion reasons (§honesty core); the device contract becomes the operational BackdropFrame acquisition protocol (§GPU device ownership); "analytic SDF of the contour" — mathematically vacuous for cubics — is reformulated as parametric pseudo-SDF families with a measured error bound, continuous corner smoothing, and level-set concentric offsets, gated by spike S2 (§Geometry); calibration results gain the web-side cell axis with per-tier claims and per-cell holdouts (§Calibration).
25. **K2 flow-back rulings (2026-08-25) — all accepted:** (a) core's lazy seam grew `loadWebGPURendererModule` beside `loadWebGPURenderer` — a second entry edge would have split the renderer into two chunks; X7 re-verified (entry carries no WGSL). (b) `setBackdropTexture` as a GlassRoot method rather than a createGlassRoot option — the option form would have forced @vitrea/react to forward it, violating the thinness law; core's TextureBackdropSource deliberately carries no pixels, so the host must hand them somewhere. (c) The demo defaults to the GPU tier with `?renderer=css|webgpu`, and React's e2e suite pins CSS — ratified: that suite tests bindings, not tiers; tier-visual coverage lives in platform-web's real-adapter project and C7's calibration cells. (d) Two K2-caught platform truths recorded in Surprises. The demo's own pre-existing overlap diagnostics are a C9 layout-cleanup item.
24. **C8 flow-back rulings (2026-08-25):** (a) core's broken published declarations — fixed on main at merge (`dts.resolve: true`; the package-name-list form left `export *` siblings external, emitting relative imports into unpublished files; C8 had applied and test-pinned the same fix in @vitrea/react). (b) The missing platform-web→renderer assembly is corrective **K2** — the demo currently runs CSS-tier only, so acceptance #2 and the GPU half of #1 are undemonstrable until it lands; the bridge lives in platform-web per the bindings' thinness law. (c) The segmented-control indicator: C8's derivation approach (track as the only glass node, indicator radius via `resolveConcentric`) is ACCEPTED for v1 — the renderer's `fieldReferenceOnly` support stays dormant until core's node descriptor grows a parent link in the deferred core round (#23c), which also inherits the X1-overlap-exemption question for same-group concentric pairs. (d) GlassRoot change-notification and scheduler-participant APIs join the same deferred core/platform-web round (the poll-into-useSyncExternalStore shim and the second rAF loop are tolerable v1 debt, each localized to one file). (e) React Aria hooks (not components) as the demo's menu primitive — ratified with C8's placement reasoning; @vitrea/react keeps zero a11y-library dependency. (f) The landmark obligation for portalled plane content is documented API behavior, carried to C9's docs task.
23. **C6 flow-back rulings (2026-08-25):** (a) core's bundle-shape test now follows the entry's static import graph — a shared chunk extracted by the bundler is a legal X7 shape; the substance (no @vitrea imports, WGSL lazy-only, zero runtime deps) was never violated (fixed on main at merge, 6c533f3). (b) Field-reference-only surfaces ratified into X8 rider 2 (the union-erasure case). (c) Corner reference and concentric parent link stay render inputs in v1, NOT scene-model fields — deferred to a post-v1 core API round with C5's per-group probe setter and generic diagnostics channel (all three are the same "core grows a field another package already carries" shape; batching them beats drip-feeding core churn). (d) The duplicated refraction ladder (platform-web + renderer) joins that same deferred round — flagged as debt, tolerable at two call sites. (e) The local WebGPU flag-namespace declarations stand until TypeScript's DOM lib ships them; deletion note recorded in the file. Also: the family-C governor gate opens by default — Decision Log #20's condition is MET (f32 cross-check 3.042e-5px = 0.0053% of family C's bound, with the family-D control reproducing S2's own figure, making the runs comparable). Benchmark verdict recorded as measured, not tuned: the 2ms hypothesis holds at the mobile median (1.47ms), is borderline on desktop (2.20ms), and p95 exceeds it on both — C7 owns tuning; the governor's intra-tier knobs exist.
22. **C3 flow-back rulings (2026-08-25) — both ratified:** (a) the two-reference corner model with morph-across-references throwing (rejected: forcing one axis — Apple's curve has no smoothing parameter, and blending two separate L∞ fits would interpolate into a corner nobody measured); (b) X8 rider 2 tightened to bind the level-set rendering path on C6 (rejected: leaving the rider ambiguous — the instantiated-shape path's offset error dominates the field error past ~4px inset and would silently eat the declared bound). C3 also delivered the analytic gradient of the normalized field in one pass (the ≤2.92° bound at the free normal's price — an option S2 left open), finding and fixing a genuine (0,0)-gradient bug on the corner-sector boundary while deriving it.
21. **C5 flow-back rulings (2026-08-25):** (a) core's scene-wide platform probe vs S1's per-group audit — C5's workaround accepted for v1 (it calls core's own pure resolver with the group verdict folded in: same transition table, same enumerated states); a per-group probe setter on core is deferred API work. (b) Core's closed diagnostics code union — C5's separate code space fanned into one sink accepted; a generic channel is deferred. (c) CSS-by-choice roots reporting `demoted(no-webgpu)` — REJECTED as-is: with C5's (accepted) CSS-default-renderer overturn, every default root would read as faulted; X2 gains a "not requested" input resolving to `health: "ok"` (corrective task K1 on core; the 43-state pin updates deliberately). C5's other advisory overturns accepted: proxy-chain audit walk instead of LCA (a strict superset of the segment that matters), padding floor over area cap with error escalation (a starved blur is a universal artifact; an over-cap proxy is a single-rasteriser hazard), and the added post-3σ cross-group overlap check (invisible to core's authored-padding check).
20. **S2 outcomes adopted (2026-08-24):** rsupn as v1's field family with its measured bounds; family C as the governor's first within-tier step conditional on C6's f32 cross-check; the smoothing seed re-based to a direct fit against Apple's measured `.continuous` curve (rejected: routing through the Figma-squircle family — 6.4× the value error; S2 proved the reference family, not the field, was the fidelity bottleneck); radii restricted to uniform in v1 with the Vec4 API shape preserved (rejected: extending the corner algebra to per-corner now — no v1 component needs it, and the restriction is a dev-mode error, not an API change). Cost figures carry the serialized-bench provenance after a GPU-contention incident (both concurrent runs deadlocked; the clean run reproduced the verdict within 0.12%).
19. **C4 flow-back ratifications (2026-08-24) — all eight interpretations adopted:** the dual-cap rule (renderers honour the lower of the accessibility refraction cap and the state's refraction level — the one that binds C5/C6 and most needed a spec line), texture-at-CSS-tier sampling semantics, CSS-tier `refraction: "none"`, the `no-backdrop-filter`-stays-on-WebGPU / `probe-failed`-demotes-to-CSS split, `no-webgpu` recovery `"none"`, per-group governor with scene default, clear-without-dimming refusal, and the two advisory constants (15Hz readback ceiling, 24px samplingPadding default). Also accepted: C4's spec-fidelity fix (probe-failed demotion target), the neighbouring-group overlap check the codex review forced, and the root-lint scratch-directory ignores (extended on main to figma-design-workspace, .playwright-cli, and live worktrees under .claude).
18. **C5 acceptance narrowing adopted (2026-08-24, human-approved):** Chromium-only pixel assertions + all-engine non-pixel suite + the manual page as a release gate. Rejected: all-engine pixel assertions (unachievable per S1's evidence — permanently red or vacuous checks), dropping the manual gate (leaves the dom tier's visual behavior on Gecko/WebKit permanently unverified).
17. **S1 outcomes adopted (2026-08-24):** box-padded/mask-exact proxy construction, `samplingPadding ≥ 3σ`, `mergeDistance ≥ samplingPadding`, three-layer probe with a data-shipped conformance table, and the glass-over-glass calibration scene. Rejected: treating `CSS.supports` as the probe (returns true where rendering is a no-op); treating capture-path blindness as feature breakage (live rendering is presumed functional pending the manual check — captures and reality diverge on Gecko/WebKit). Pending: Gecko/WebKit manual verdicts; a proposed C5 acceptance narrowing (pixel assertions Chromium-only) awaiting the human gate.
15. **The cut (2026-08-24 decomposing run): eleven children in five waves** (§Children). Rejected: one mega-plan (fails the reliably-ownable gate); merging the pure-math kernels into one "runtime" child (different invariants and verification strategies — shape math versus time-domain drivers — and different dependency gates: geometry waits on S2, motion doesn't); cutting C9 finely now (frontier rule — distant cuts go stale; it carries a decomposing-at-dispatch hint instead); folding the spikes into their consumer children (their deliverable is findings that may *change* those children's contracts, so they must land first and separately).
16. **Track hints:** spikes for S1/S2; autonomous for C1–C4 (well-scoped, contracts binding and precise, taste-light); controlled for C5–C8 (browser-integration-heavy or fidelity-critical); decomposing-at-dispatch for C9. Rejected: autonomous for C5/C6 (fidelity judgment arises mid-flight there — the tiebreaker purpose lives in those children).

## Surprises & Discoveries

- K5: **a foreground rule can be correct at one opacity and an accessibility failure at another, with nothing in between to warn you.** K4 mapped X6's backdrop tone straight to the ink token, which is right while the material is transparent enough to see the backdrop through — and inverts once it is not. C9a's tint tune crossed that line for the GPU tier and K5 crossed it for the CSS tier, producing near-white ink on a near-white surface at WCAG 1.24 against a 4.5 floor. Neither tuning pass touched the foreground code. The lesson generalises past this rule: a policy expressed in terms of one layer of a composite silently stops describing the composite when another layer's weight changes.
- K5: **the CSS tier's ambient `box-shadow` was the whole of its shape axis, and it was hiding a good geometry.** The dom tier's silhouette IoU sat at 0.676 and its contour p95 at 18.7px, which C9a read as "not comparable to the texture tier's" and attributed to the shadow. Turning the shadow off — nothing else — gives IoU 0.942 and contour p95 2.2px, against the texture tier's 0.954 and 1.98px. The tier's geometry was never the problem. What made it certain rather than plausible is that the IoU mean sat at 0.6755–0.6756 at *every* point of *every* sweep run for the child, moving in the fourth decimal while the tint alpha moved by a third and the border alpha went all the way to zero. A metric that refuses to move under the variables you are sweeping is telling you what it is actually measuring.
- K5: **tier coherence and per-tier fidelity are different objectives, and they can point opposite ways.** Fitting the CSS tier's mapping against the native fixtures and fitting it against the GPU tier disagree monotonically across the whole swept range — cross-tier ΔE rises 0.0078 → 0.0115 exactly as the CSS tier's own ΔE against Apple falls 0.0118 → 0.0094. The reason is simple once seen and was not obvious before: the GPU tier is not itself exactly on the reference, so "closest to the other tier" and "closest to Apple" are different points. A child asked to close a coherence gap by measurement has to decide *which* measurement first, and the answer is not automatically the one the harness was built for.
- K5: **a tuned scalar can be a legitimate fit and still not mean what its name says.** The mapping's `referenceBackdropLuminance` is derived from the two pipelines' transfer functions, so the name describes the mechanism exactly — but the fitted value also absorbs every *other* difference between them (the CSS tier's `saturate()`, the renderer's body-blur LOD chain), and it lands at 0.02, far below any canonical scene's mean backdrop. The form is principled and the value is empirical, and a reader who assumed the second followed from the first would misread it.
- K5: **C9a's tint tune silently neutralised an accessibility floor, in both tiers.** Reduced transparency raises occlusion by `Math.max(nominal, increasedOcclusionAlpha)` with the floor at 0.62 — a real lift while nominal was the advisory 0.28, and inert the moment C9a measured nominal at 0.62. It had been dead on the GPU tier since C9a with nothing noticing, and only surfaced here because deriving the CSS tier from the same profile made a unit test that asserted the lift fail. A tuning pass can move a constant past a threshold expressed in the same units and turn a policy into a no-op without touching the policy.
- C9a: **a hand-driven frame loop silently owns every async path the real frame loop would have serviced.** The GPU tier's adaptive tint is driven by an analysis reduction read back off the device, fired once per frame and never awaited, so the observation lands on a later task. The calibration page stepped every frame in one synchronous loop — so no readback could resolve before the final draw, and **every capture rendered the un-adapted material, a state no application ever shows.** What hid it is that the frame *count* made no difference at all: captures at 8 and at 240 frames are byte-identical. Found by injecting marker colours into the two adaptation ends and observing a neutral interior. Yielding to the macrotask queue between frames is not enough (the readback needs the GPU to finish); pacing on `requestAnimationFrame` is. A capture that is stable is not thereby correct.
- C9a: **Apple's Regular material does not invert its tint against the backdrop; it keys on the colour scheme.** vitrea's adaptive tint crossfaded white-over-dark to near-black-over-light — always contrasting with what is behind it — and the reference's interior instead rises monotonically with the backdrop across the whole canonical range (0.680 at a backdrop of 0.003 to 0.932 at 0.891, light scheme). What changes it is the scheme: over the same bright checkerboard the reference sits at 0.809 in light and 0.055 in dark. The inversion was costing more than the entire tint tune was worth.
- C9a: **the dom tier had never been diffable, and nothing said so.** The scene page reported its colour space as an explanatory sentence where X9 types a cell key's `colorSpace` as the closed `"srgb"`, so the diff refused every CSS-tier cell. Invisible because every run until C9a was GPU-tier, where the sentence happened to reduce to exactly `"srgb"`. Prose in a key field, and a whole tier unmeasured behind it.
- C9a: **the reference does not union its group members at the one spacing the canonical matrix declares**, which makes the union caps unmeasurable by this scene set. Counting connected components settles it: three separate bodies on *both* sides, 1572px each for the reference against 1769px for vitrea, where an unmerged 44px capsule is 1520px. A full fill of the two 12px gaps would add 23% to the group's area; the reference adds 3.3%. Sweeping the declared merge distance across 24, 12 and 4 gives a byte-identical silhouette, because the blend never engages at this separation whatever the threshold is. C7's flow-back had assumed the native container reaches "one merged body" at spacing 12 and built the web side's merge distance around matching that; refuted. The same count exposed what the union hypothesis was hiding — vitrea's material extends ~1.4px further past its contour than the reference's, which is what holds the group scene's IoU down.
- C9a: **a metric can report its own invalidity in a field nobody reads.** The material axis was reporting a blur σ of 50.6px at a fit residual of 63× the step height, because the default measurement region was a strip across the interior that straddled many checkerboard edges. A single error function fitted to a multi-edge profile is not ill-conditioned — it answers a different question, confidently. Also: three of the four canonical solid-backdrop cells cannot measure the material at all, because over a flat backdrop of its own tone the reference is within 0.02 linear luminance of it. A real property of the material, not a capture fault, and it leaves no interior to sample.
- The design-research run **refuted** the caniuse-derived "85.56% global WebGPU support" figure and the blanket "Firefox disabled by default" claim — caniuse's Firefox row is stale. No verified market-share number exists; the spec cites per-engine facts only.
- The research run's competitive and squircle pillars produced **zero surviving claims**; two independent design reviews filled the gap, and their competitive citations were then verified directly (liquid-glass-studio: WebGL2/WebGPU SDF+springs parameter studio over self-owned backgrounds; liquidGL: shared-canvas snapshot rasterizer whose all-targets-one-z-index constraint is field evidence for the plane model).
- Both independent reviews, working separately, found the **same two contract contradictions** (GPU analysis without a texture; GPU verdicts steering DOM color without readback) — a reminder that internally convergent designs still carry externally visible faults.
- Apple's concentric shapes govern **radius derivation only**, not the corner curve profile — an earlier claim conflating the two was refuted; continuous curvature applies throughout.
- Chromium alone renders SVG displacement inside `backdrop-filter`; WebKit refuses it (bug 245510) and Mozilla lists it as unsupported — confirming the fork's premise and the W3C `svgwg#1142` standards gap.
- The adversarial review caught that "analytic SDF of the contour" was mathematically vacuous — exact cubic-Bézier distance requires quintic root-finding — and that exact inward offsets of continuous-corner cubics leave the cubic family; both forced the pseudo-SDF / level-set reformulation.
- `importExternalTexture` handles expire at task end and imported video arrives unpremultiplied — per-frame reacquisition is a WebGPU semantic, not an optimization choice.
- Filter Effects 2's Backdrop Root definition explicitly lacks working-group consensus, so cross-engine backdrop sampling equivalence cannot be assumed — only probed (hence spike S1).
- S1: Gecko and WebKit render backdrop-filter as a complete no-op in every automatable capture path (Playwright headless and headed, retail --screenshot, WebDriver BiDi, WKWebView takeSnapshot) while rendering it live — screenshot-based CI can never see the dom tier on those engines, which reshapes C5's verification strategy and makes the manual self-scoring page a release gate.
- K2: a WebGPU canvas is only readable while its frame texture is current — after present, drawImage and createImageBitmap return fully transparent images over a canvas the page still shows; a readback suite can pass on a confident, wrong zero unless draw and read share one evaluate. And unconfigure() does not clear the canvas — the dead tier's last frame sits above the live CSS tier until the backing store is resized.
- C8: a React portal delegates synthetic events to its container — moving a promoted host out from under it leaves the element rendered but deaf; unit promotion must move a portal-owned `display: contents` mount node, never the host itself.
- C8: menus that activate on pointer-up plus a platter still growing means the opening press picks whichever item slides under the cursor first — morphing content must not be an activation target until settled.
- C6: GPU benchmark ordering is a real confounder — the same config measured 0.995ms in the first slot and 1.901ms in the last purely from GPU clock state; interleaved configs agree within 6%. C7's calibration methodology must interleave.
- C6: seven suite-caught shader bugs included `target` being a WGSL reserved keyword (three uniform structs silently failing every pipeline) and a gradient interpolating in encoded rather than linear space — X5's color-pipeline lock is what made the latter catchable.
- C5: WebGPU adapter behavior on this machine inverts the naive expectation — on a localhost secure context, Playwright's WebKit returns a real adapter and device while Chromium and Firefox expose `navigator.gpu` but return no adapter; probe tests must assert agreement with the engine's own answer, never a hardcoded environment.
- C5: cross-plane DOM promotion silently drops keyboard focus (reparenting is removal + insertion) — focus must be captured and restored around the move; caught by the promotion-under-focus test the acceptance demanded.
- S2: Apple's own `.continuous` corner is not G1 — a measured 2.4532° tangent break exists at both shoulder/arc joins of Apple's actual path, and its edge-reach 1.528665 matches the published `cornerCurveExpansionFactor`. Every "smooth corner" article assuming G2 continuity is describing a curve Apple does not draw.
- S2: the Figma-squircle family at its best smoothing sits further from Apple's real curve (1.96e-3·r) than the pseudo-SDF's own error against that curve (1.38e-3·r) — and the best Apple-matching Figma smoothing is 0.66, not the widely cited 0.6.
- S1: the padded-box/exact-mask construction is normative, not an optimization — Filter Effects 2 clips a filter's input to the filtered element's own border box, so padding is the mechanism that recovers native-like sampling at edges.
- C7: **Liquid Glass is composited by the window server, so an app cannot capture its own material.** Every in-process path fails, and differently: `ImageRenderer` renders a `glassEffect` subtree as *nothing at all* — mad 0.0000 against a bare rect of the same size, the material absent *and* the wrapped content dropped — while `NSView.cacheDisplay` renders the wrapped content but no material (measured: 30 of 30 fixtures pixel-identical to their own background). `CGWindowListCreateImage` is not merely deprecated on macOS 26; it is a hard compile error ("Please use ScreenCaptureKit instead"). That leaves ScreenCaptureKit as the only oracle, and it is TCC-gated: `SCStreamErrorDomain -3801` for the app's *own* window, from an ad-hoc-signed bundle with its own identifier, launched via LaunchServices, **with no permission dialog presented** — a recorded denial, not an unrequested grant. Native fidelity ground truth therefore has a human in its critical path on this machine, which is a scheduling fact the roadmap did not price.
- C7: macOS exposes the accessibility display modes as **get-only** `EnvironmentValues` (`accessibilityReduceTransparency`, `accessibilityDifferentiateWithoutColor`, `colorSchemeContrast`). Only `\.colorScheme` is writable, so light/dark capture in one run but each a11y profile needs its own run with a System Settings toggle. The harness reads `NSWorkspace.accessibilityDisplayShould…` and skips a profile whose key claims a mode the machine is not in, rather than emitting a mislabelled fixture — X9's keys are only meaningful if the key describes the conditions that actually held.
- C7: the Xcode **license gate lives only in the wrapper tools**. `xcodebuild -showsdks`, `xcrun`, `swift` and therefore SwiftPM all refuse when the accepted-license version trails the installed one (16.4 on record vs 26.6 installed), while `swiftc` invoked directly with an explicit `-sdk` compiles and links fine — and `xcodebuild -version` answers too. An "Xcode is unusable" gate can be a wrapper problem rather than a toolchain problem, worth probing before treating it as blocking.
- C7: **the group-union constants are not wired to anything the scene declares** — a real gap, found only by building the comparison. `@vitrea/geometry`'s `DEFAULT_GROUP_UNION` (`neckWidth: 8`, `maxBulge: 2`, `separationThreshold: 16`) governs what the union actually *draws*, and the renderer bridge never forwards core's `mergeDistance` into it. So the toolbar-group scene measures an untuned default rather than the spacing it declares. Those three constants are explicitly calibration-delegated, which makes this simultaneously a C7 tuning target and a C6 wiring gap; the wiring has to land before the tuning means anything.
- C7: **material thickness has no shared value between the two sides.** `scenes.json` declares no thickness channel, so the web side uses its own 8 CSS px default and SwiftUI uses whatever its material does — a quantity Apple does not expose. Any metric that moves with thickness is comparing two unrelated defaults, not measuring a fidelity gap. X8 carries `thickness` as a morph channel, but the calibration matrix has no way to pin it; a scene-matrix field is the candidate fix.
- C7: **the glass-over-glass scenes are necessarily a mixed-backend cell.** Natively the upper `glassEffect` samples the lower one's rendered output; on the web that relationship *is* the plane sandwich, so the overlay group must be declared `dom` — a texture-backed overlay would sample the raw raster and miss the entire point of the scene. Those cells therefore resolve to `gpu-texture+css-backdrop` with `refraction: "approximate"` even on the GPU tier. It is a fidelity difference the cell names rather than a fault, but it means S1's mandated holdout scenes can never be a clean texture-tier claim.
- C7: **the pressed scenes compare two independently-derived poses**, not a measurement against an observed one. The web page poses the settled pressed state from `DEFAULT_MOTION_PROFILE`, while native `Glass.interactive()` is Apple's and unparameterised — so those four scenes are a C9 tuning target rather than a fidelity comparison.
- C7: accessibility is the one axis where **the web side can do more than native**, inverting the usual direction. The web runtime sets reduce-transparency and increased-contrast through `accessibilityOverrides`, so those profiles are reachable per-render; macOS exposes them as read-only system state. The native side is the constraint on that axis, not vitrea.
- C7: the spec's canonical `-2x-` profile keys are **unreachable on this machine** — a Mac14,12 driving a 1920×1080 panel reports `backingScaleFactor` 1.0, and configuring ScreenCaptureKit to twice the window size upsamples rather than rendering at 2x. Fixture keys read `-1x-`, and the scale axis needs a Retina display or a HiDPI mode to close.

## Outcomes & Retrospective

Pending — written at finish.

## Revision Notes

- 2026-08-25 (eighteenth revision): **C9a landed.** Full 30-cell matrix measured and committed; a material tunables seam built and the first two constants tuned through it (`tintAlpha` 0.28 → 0.62, adaptive crossover made inert), with a committed profile per colour scheme, a pin test against the runtime default, and a committed sweep so the search is reproducible. Per-tier claims written (`c9a-fidelity-claims.md`) with holdout measured once against the frozen configuration and thresholds proposed for the human gate. Every holdout axis improved; a mild overfitting signal is reported with its mechanism named. Six rulings (Decision Log #27), six parent-impact items, five findings in Surprises — one of which corrects this document's own C9 tuning target, which had the direction backwards. The dom tier got its first measurement ever, after two bugs that had made it unmeasurable were fixed. Remaining children: C9b (in-flight), C9c (unblocked by these claims), C9d.
- 2026-08-24: Initial spec from the chartering grill, design-research run (wf_ce8e34b5-10e), two independent design reviews, and the approved revised design pass.
- 2026-08-24 (same day, second revision): adversarial spec review round — six findings, all adopted (Decision Log #14): checked overlap constraint and unit promotion, per-group masked proxies with conformance probe, resolved-state capability model, BackdropFrame protocol, pseudo-SDF geometry reformulation, per-cell calibration matrix, gating spikes S1/S2.
- 2026-08-24 (third revision): decomposing run extends this document into the composite spec — parent citation, Parent-Level Acceptance, eleven children (S1, S2, C1–C9), nine cross-child contracts (X1–X9), ordering map, risks, tracking map (Decision Log #15–16). Board materialization pending the human gate.
- 2026-08-25 (twenty-second revision): **K5 landed** — the CSS tier stopped holding a material of its own and now converts the one the root carries, through a mapping whose single fitted constant was tuned against the *cross-tier* delta rather than against the fixtures (Decision Log #32, claims amended in `c9a-fidelity-claims.md` §3/§3.1/§3.2/§5/§7). The tier-switch gap C9a opened is closed and measured: over the 17 light calibration and validation cells the interior level GPU-to-CSS goes from 3.19× (per-cell range 1.67–9.18×) to 0.99× (0.90–1.07), cross-tier OKLab ΔE from 0.0370 to 0.0080, and the CSS tier's own fidelity followed rather than traded — calibration ΔE 0.0275 → 0.0113, dark 0.0505 → 0.0162, the dark figure being the seam existing at all rather than the tune. Holdout measured once at the freeze; all 30 dom-tier cells committed (matrix 30 → 60); dom-tier perceptual and coherence thresholds now proposable. The foreground rule had to move with the material — K4 mapped a hinted backdrop tone straight to the ink token, which inverts once the material stops being transparent, and the demo measured WCAG contrast 1.24 against a 4.5 floor before it was corrected. Two further items handed back, both measured and neither forced: the dom tier's `box-shadow` owns its entire shape axis (off, IoU 0.676 → 0.942), and the reduced-transparency occlusion floor has been inert since C9a. Exact coherence is unreachable and the residual is stated as arithmetic, not as a hope.
- 2026-08-25 (twenty-first revision): the user directed C9d's gate procedure (Decision Log #31) — isolation-proof golden regeneration, token-level demo fixes with no assertion loosened, all sequenced after K5.
- 2026-08-25 (twentieth revision): C9c landed — rename executed and verified (117 replacements, 82 files; X7 tests re-asserted on the built artifacts), READMEs with typechecked quickstarts and claims quoted verbatim from the claims doc, NOTICE trademark clause, per-package license files, 0.1.0 changeset with the fixed pair verified, clean dry-runs. Live spec claims updated to the new names (acceptance #8, publish surface, finish line); Decision Log history left historical. Rulings in #30.
- 2026-08-25 (nineteenth revision): C9a landed — the dispatch brief's luminance-slope target was refuted as blur-confounded (the real gap: Apple passes 0.26 of the backdrop vs our untuned 0.70; tintAlpha 0.28→0.62); every holdout axis improved with the overfitting signal and its size-term mechanism reported (Decision Log #29). The two tiers now differ >2× in opacity — K5 opened (blocked-by K4, same file) per C9a's recommendation that tier coherence be closed before any public claim implies it. The tuned material is a visible breaking change the showpiece inherits.
- 2026-08-25 (eighteenth revision): C9b landed — the survey-plate showpiece with live-vs-native reference pairs bound to scenes.json geometry and build-time matrix figures; zero dev diagnostics; static deploy verified under a path prefix; renderer scissor bug fixed in-branch (Decision Log #28); K4 opened.
- 2026-08-25 (seventeenth revision): the grant landed; real fixtures captured (30/30 material, deterministic); acceptance #7's blocker cleared to "tuning pending"; first real cell recorded; C9 decomposed into C9a–C9d and C9a/C9b dispatched.
- 2026-08-25 (sixteenth revision): C7 landed **with one axis externally blocked**. Delivered: `packages/calibration` (four metric axes, 91 tests against analytically known answers, X9's per-cell result matrix, a `diff` CLI and a `compare` orchestrator), `apps/reference-apple` (SwiftUI harness on the real glass API, six byte-stable shared raster backgrounds, 30 fixtures, manifest), `scenes.json` as the shared geometry contract, and a seed-constants profile. One automated comparison run closes the loop on a real GPU-tier web capture (Chromium 151, WebGPU, `gpu-texture` sampling, Apple Metal-3 adapter, byte-deterministic across two independent page loads) and writes its cell into the matrix. **Blocked:** the native material cannot be captured — window-server composition plus a denied Screen Recording grant — so the fixtures are provisional, every entry marked `materialRendered: false`, and parent acceptance #7 stays open pending a human. Six rulings recorded (Decision Log #26); five findings in Surprises, one of which corrects this document's earlier "Xcode 26.6 licensed" note.
- 2026-08-25 (fifteenth revision): the user's manual check closed S1's Gecko/WebKit verdicts (evidence archived; note for C5's conformance table: the D1 audit tiles show a per-engine `will-change: opacity`-breaks / `will-change: transform`-doesn't asymmetry — the labeled per-engine re-run belongs to C9's release gate). Xcode license accepted; C7 dispatched.
- 2026-08-25 (fourteenth revision): K2 landed — renderer bridge in platform-web; GPU glass end to end with acceptance #2's mechanism behaviorally visible; channel vocabulary deduplicated (react re-exports platform-web's); no second rAF loop. Three rulings accepted (Decision Log #25). Remaining children: C7 (waiting-external: Xcode license), C9 (blocked-by C7).
- 2026-08-25 (thirteenth revision): C8 landed — bindings, five controls, the morph pair, the playground; acceptances #1/#3/#4/#6 pass with evidence on all three engines (pixel-free suites). Six flow-backs ruled (Decision Log #24); core dts fixed on main; K2 (renderer bridge) dispatched — until it lands the demo is CSS-tier and acceptance #2 waits.
- 2026-08-25 (twelfth revision): C6 landed — the optical engine is real: all five BackdropFrame providers, pyramids under the dirty-epoch invariant, field/optics/highlight passes in linear premultiplied light, family-C gate open (f32 check passed), goldens on real headless Chromium (software-adapter gate fails rather than skips), benchmark measured against the 2ms hypothesis. Five flow-backs ruled (Decision Log #23); bundle-shape test fixed on main; C8 dispatched.
- 2026-08-25 (eleventh revision): K1 landed — X2's webgpu probe axis is three-valued, CSS-by-choice resolves healthy (6 new ok-states, pin 43→49); C6 notified to merge main.
- 2026-08-25 (tenth revision): C3 landed — two-reference corner model and the level-set rider ratified (Decision Log #22); C6 dispatched with the f32 cross-check obligation, the level-set concentric mandate, and the one-pass analytic gradient.
- 2026-08-25 (ninth revision): C5 landed — four suite-caught bugs fixed in-branch (device-lost collapse, first-frame CSS transition, unrecoverable probe demotion, focus-dropping promotion); three flow-backs ruled (Decision Log #21); K1 corrective dispatched on core for the X2 css-by-choice amendment.
- 2026-08-24 (eighth revision): S2 landed — rsupn adopted, Apple-direct smoothing seed replaces the Figma routing, uniform-radii v1 restriction, family-C governor step conditional on C6's f32 check (Decision Log #20). Spike harness becomes C3's regression base.
- 2026-08-24 (seventh revision): C4 landed — eight flowed-back interpretations ratified (Decision Log #19), dual-cap rule added to the honesty core as binding on C5/C6, clear-dimming refusal and 15Hz/24px advisories recorded, enforcement split for the proxy constraints written into X1's rendering-contract paragraph.
- 2026-08-24 (sixth revision): C2 landed — four advisory overturns recorded by the child, accepted: per-state target vectors instead of per-transition tables (path independence made structural; the transition view is derived), the capped-step rule applied at the frame boundary rather than inside drivers (preserves exact frame-rate invariance), six driver classes implementing the eight-row channel table (two pairs collapse to parameterizations), and no settle-snapping (settled is a pure predicate; snapping would break step-count independence). C5's acceptance narrowed per Decision Log #18.
- 2026-08-24 (fifth revision): S1 landed and folded in — X1 corrections (box/mask split, 3σ floor, mergeDistance constraint), probe design, capture-blindness discovery, glass-over-glass calibration scene (Decision Log #17). C5's acceptance narrowing is proposed and awaits the human; Gecko/WebKit manual verdicts pending. Xcode installed by the user (license acceptance pending), opening C7's external gate.
- 2026-08-24 (fourth revision): human gate passed — roadmap approved, spec-only tracking chosen (no board tickets), Xcode install accepted by the user as C7's external gate. Wave 0 dispatched: S1, S2, C1 in-flight as isolated-worktree workers.
