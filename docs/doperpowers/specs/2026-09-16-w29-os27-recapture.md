# W29 — the OS 27 recapture: Apple's material measured again on macOS 27, what moved, and vitrea refit to it

**Status: CLOSED 2026-09-20 — 0.19.0 PREPARED, UNPUBLISHED.** All seven Parent-Level Acceptance
clauses MET, with seven declared rows recorded UNMET inside clause 4 and one consequence recorded
inside clause 6; the verification is in Outcomes & Retrospective below. Apple's material was
captured again on macOS 27.0 build 26A428 under new keys, measured native-against-native on 619
cells before a line of vitrea moved, refit on both tiers under bounds declared before the read, and
**selected as what a page draws by default** — by selection over an unmoved `DEFAULT_MATERIAL_PROFILE`,
with the macOS 26.5 freeze intact at 1,818 entries and both macOS 26.5 documents still shipped and
pinned. `pnpm release` is the user's hand and the tag follows it.

**0.19.0 PUBLISHED 2026-09-20, by the user's `pnpm release` on `26250e97` (the parent's final
chain record, the head at publish); tag `v0.19.0` annotated on that commit and pushed by the
parent the same day.** Registry: core 04:40:24.213Z, react 04:40:27.518Z, web 04:40:46.317Z — the
record beside the "PREPARED, UNPUBLISHED" status above, as the 0.18.0 record sits in W28; the
verification is in §Outcomes & Retrospective.

*The wave's opening status, kept as written:*

**OPEN 2026-09-18 — chartered 2026-09-16 on the user's ruling (W28 Decision Log 4);
adversarially reviewed the same day (three blocking findings and ten others, all folded, Decision
Log 1 amended). The machine is on macOS 27.0 build 26A428 since 2026-09-18; the 26.5 freeze
verified intact on it; Decision Log 2 records the user's advance ruling on clause 5 (the runtime
default is 27); G0 dispatched 2026-09-18 under claims §5.149 and **closed the same day** — the
granted bundle still captures, the material is not SDK-gated as far as the declared material can
say, the slider moves every cell, and the centre is 0.5 while the machine sits at 0.5459057. **G1
waits on Decision Log 3, drafted below**, and on two things only the user can give: the slider
position the bed is captured at, and how Show Borders is attested when macOS 27 exposes no key for
it. Decision Log 3 ruled both on 2026-09-18 and **G1 closed 2026-09-19** under claims
§5.150: Part A built the bed's infrastructure (the keys, `scenes.json` version 6, the sitting's
script and its refusals, the verified bar table, the rehearsals, the SDK-gating residual closed on
pixels) and Part B published it — the parent ran the sitting, and **624 cells over six
`apple-macos-27.0-…-glass0.5` profiles are filed with no shortfall and no refusal**, the 26.5 freeze
intact. **G2 closed 2026-09-19** under claims §5.151: the noise bar declared and committed before
the first pair, then **619 pairs read — Apple's material moved on every one of them and Apple's
geometry did not**. The level, the tone response, the diffusion, the rim band, the highlight, the
tint's lightness and the recede all moved; the silhouette, the corner and the tint's hue did not.
**Decision Log 4 is drafted below for the user** and G3 is chartered from it; still no vitrea
change of any kind.**

Parent: `2026-08-28-post-v1-wave.md` (post-close addendum 2026-09-16 names this wave next);
`2026-09-14-w28-footprint-response.md` Decision Log 4 (the OS ruling); `2026-09-10-w27-coverage-wave.md`
§Deferred ("the OS 27 recapture … as new `apple-macos-27.0-…` profile keys beside the frozen 26.5
ones, never replacing them"). Ledger: `c9a-fidelity-claims.md` (sections assigned at each dispatch).
Research: `docs/research/2026-09-10-macos-27-release-status.md`.

## Purpose

Every native pixel vitrea has ever been measured against was captured on macOS 26.5.2. Apple
shipped macOS 27 on 2026-09-14 and says the material changed underneath every app without a
recompile: stronger diffusion of complex content, a darkened edge, brighter specular highlights,
more uniform refraction, a common tighter window corner, edge-to-edge sidebars, uniform toolbars,
and a user-facing transparency slider whose default is the centre of a range from ultraclear to
fully tinted. The project's goal is the least possible gap between vitrea and Apple's material, and
from 2026-09-14 "Apple's material" is what a Mac on 27 draws. This wave moves the reference.

It does so in the order the fidelity discipline requires. First the 26.5 evidence is frozen and
proven untouched. Then Apple's 27 material is captured under new keys with every axis that can move
a pixel attested per run, including the two 27 introduces (the slider; Show Borders decoupled from
Increase Contrast). Then what 27 changed is **measured native against native**, cell by cell,
against a noise bar taken from the 27 bed's own run-to-run behaviour, and written into the ledger
before any line of vitrea moves — so the refit that follows is a response to a measurement, not to
a press release. Only then is the material refit, on the WebGPU tier with the CSS tier derived in
the same wave, under 27 profile documents, judged by bounds declared before the read, with the
holdout read once. The 26.5 documents, fixtures and rows stay as committed evidence; the 26.5
bounds keep running against the frozen 26.5 rows and the 26.5 material fingerprint stays pinned,
which together prove that nothing there moved.

What this wave does **not** do: it does not identify the abscissa (W28 §Deferred; re-runnable on
the 27 bed afterwards), it does not build the motion-metrics harness (next), and it does not
capture the slider's two ends as evidence classes unless G0 shows the slider moves the cells at all
and the user rules them in.

## Parent-Level Acceptance

1. **The 26.5 evidence is untouched.** A manifest of SHA-256 over every file under
   `apps/reference-apple/fixtures/apple-macos-26.5-*/`, `fixtures/backgrounds/`,
   `packages/calibration/profiles/apple-macos-26.5*`, and every 26.5-keyed row of
   `results/matrix.json` is written and committed **before G0 opens**, and re-verified at every
   child's merge and at close. The 26.5 adopted bounds, regression floors and `PREDICATE_EXCLUDES`
   keep running against those rows unchanged.
2. **The 27 bed exists under new keys with every pixel-moving axis attested per run.** Profile keys
   `apple-macos-27.0-{1x,2x}-{light,dark}-standard` and the two 1x light accessibility keys, declared
   in `scenes.json` beside the six 26.5 keys (the scene list unchanged). Every run manifest records
   the exact OS build, the slider position, Reduce Transparency 0, Increase Contrast 0, Show Borders
   off, scale, scheme and pose, and a run whose record disagrees with its key is refused before a
   fixture is written. Every pass is captured at **one declared bar, seven runs**, and published
   through the same plurality publication; G1 commits a table of the 26.5 bar per cell where the
   manifest's `bedProvenance` can attribute it (it names cells for 91 and carries 73 at a
   seventeen-run bar) and names the cells it cannot attribute as such. The per-profile count equals
   the 26.5 count or the shortfall is named cell by cell.
3. **What 27 changed is measured before anything is changed.** A native-against-native read of
   every 27 cell against its 26.5 counterpart — silhouette, contour, SSIM, OKLab ΔE, interior level,
   rim band, highlight — with the verdict *moved / not moved* per cell declared against a noise bar
   that is **defined per metric before any 26.5 pair is read**: the 27-against-27 run-to-run
   distribution of that same metric on that same cell, computed from the sitting's own seven raw
   runs (which stay on the machine; the derived per-cell distribution is committed). The 26.5
   plurality record is corroboration only — it is byte-level, transient in `plurality.ts`, and
   carries no SSIM, ΔE or contour distribution, so it cannot be the bar. Sheets (26.5 | 27 |
   difference) per profile at both scales, in a ledger section, with **no vitrea change in the same
   child**.
4. **vitrea is refit to 27 on the fidelity target.** Profile documents keyed 27 (the light patch
   with every fitted constant; the dark difference document; the receded endpoints), the material
   moved on the WebGPU tier where clause 3 says Apple's law moved and the CSS tier derived in the
   same wave; bounds for the 27 profiles declared before the fit is read; **no 27 regression
   floor adopted in this wave** (floors need the seventeen-run bar, which the bed is not captured
   at); holdout read once per frozen configuration; every residual named. The 26.5 bounds keep
   running against the committed 26.5 rows, which proves those rows were not edited; the proof that
   the 26.5 *material* still resolves as measured is `tuned-profiles.test.ts`'s pinned
   `resolvedMaterialSha256` over the runtime default plus the 26.5 patch, which is why Decision
   Log 1 rules that the runtime default does not move (below).
5. **Which material a page gets is a stated decision.** The runtime's default profile after this
   wave, and whether the 26.5 documents remain shipped and selectable, are ruled in this Decision Log
   by the user, and the README says what a page draws by default.
6. **The landing.** 27 rows in the canonical matrix per tier; `adopted-thresholds` blocks for the 27
   profiles beside the 26.5 blocks; the demo's fixture pair and the Pages workflow's assertion on 27;
   the coverage matrix re-scored; `CLAUDE.md`, READMEs and the ledger's pointers updated; the fixed
   group versioned and prepared, `pnpm release` the user's hand.
7. **By eye, and the ledger.** Native | WebGPU | difference sheets for every 27 profile at the fit's
   read; the demo beside the 27 harness capture; every gap to macOS 27 a named line in a claims
   section, this wave's Deferred, or the tracker.

## Grounding Baseline (main at `6ac184c1`, 0.18.0 published)

- **The 26.5 bed.** Six profile directories, 619 fixture PNGs (164 / 164 light standard 1x / 2x,
  115 / 114 dark standard, 32 increased-contrast, 30 reduced-transparency; 277 of them inactive),
  32 background PNGs, one `manifest.json` whose `hardware` block reads macOS 26.5.2 build 25F84,
  MacOSX26.5.sdk, Xcode 26.6 17F113, Mac14,12 M2 Pro. `results/matrix.json` schema 5, 1,107 rows
  (563 light standard, 437 dark standard, 55 IC, 52 RT; probe 720, calibration 264, validation 64,
  holdout 59). `scenes.json` version 5: 168 scenes, split calibration 37 / validation 12 / holdout 20
  / recorded 8 / probe 91.
- **The appearance setting the 26.5 bed was captured under.** macOS 26 offers Liquid Glass as
  *Clear* or *Tinted* in System Settings › Appearance. No preference key for it exists in any plist
  under `~/Library/Preferences` on the capture machine (searched 2026-09-16, before the update), which
  is what a never-touched default leaves behind; the bed therefore stands at Apple's 26 default. The
  setting itself was never recorded in a manifest. **Recorded here as an inference from the absence
  of a key, not as an attestation**; the user can confirm the pane's state before updating.
- **What is coupled to 26.5.** One hard gate: `results/2026-09-11-w27-26.5-run/run-sitting.sh:50`
  refuses any `sw_vers` not `26.5*`. The Swift harness has no `#available`, reads no OS version
  except to *record* it (`Manifest.swift`), and validates the a11y, scale and scheme tokens of a
  profile key against the machine but **never the OS token** — nothing in the harness would stop a
  27 machine writing into a 26.5 directory. Profile keys are literals in `scenes.json` (six of
  them), copied verbatim into fixture directory names and manifests; the material profile documents
  are named by convention and passed by path (`--material-profile`), with three consumers that
  hard-code the 26.5 filename (`platform-web/src/dark-profile.ts`, two calibration tests). Tests
  pinning 26.5 names: `adopted-thresholds` (74 hits: floors, per-profile bounds, cell counts,
  36 `PREDICATE_EXCLUDES` entries), `scene-matrix` (16), `tuned-profiles`, `report`, `profile-key`,
  `vibrancy` (asserts the build set is exactly `Version 26.5.2 (Build 25F84)`), five more with one
  or two each; the demo's site, calibration and vite config name the two fixture directories it
  ships and `pages.yml:73` asserts the light one exists.
- **The key grammar.** `PROFILE_KEY_PATTERN` accepts `\d+\.\d+` for the OS token, so
  `apple-macos-27.0-…` parses today and a bare `27` would not. The grammar ends at the a11y token:
  it has **no slot for the slider** and its `a11yMode` enum cannot express Show Borders decoupled
  from Increase Contrast (c9a §5.134 already names both).
- **The granted bundle.** `apps/reference-apple/build/VitreaReference.app` is not in git; built by
  `build.sh` with plain `swiftc`, `-target arm64-apple-macos26.0`, MacOSX26.5.sdk, ad-hoc signed,
  bundle id `dev.vitrea.reference-apple`. Screen Recording is granted to that signature and a rebuild
  invalidates the grant until the user toggles it (README, tracker); a recorded denial suppresses
  the prompt, so a broken grant fails silently. Its ScreenCaptureKit calls are all ≤ macOS 14 APIs,
  none deprecated; deployment target 26.0 launches on 27. **It can be reused byte-for-byte**, and
  `VITREA_BUILD_OUT` builds a side bundle beside it for any experiment.
- **The scientific risk of reuse.** The bundle links the 26.5 SDK, and macOS routinely gates
  appearance changes on the linked SDK version. Apple's statement is that existing apps get the 27
  material without recompiling; if any part of it is SDK-gated, a bed captured with the granted
  bundle would be labelled 27 and draw 26-era glass. Settled by one comparison (G0).
- **What the metrics can and cannot do.** `measureCell` is native-against-web by construction;
  `ResultCellKey` requires a `WebCell`, so the result matrix cannot hold a native-against-native
  row. The primitives are image-pair generic (`ssim`, `oklabDeltaE`, `silhouetteIoU`,
  `contourDistance`); `cli/tier-delta.ts` is the precedent for a pair diff with no fixture in it;
  `src/plurality.ts` classifies byte-level run-to-run differences as noise or structural. A
  native-delta driver has to be written (G2) and its rows live beside the matrix, not in it.
- **Capture cost, from the 26.5 record.** The harness runs 9.5 s per cell. W27c's 188-cell round
  was 29.8 min per run, 3.5 h at the seven-run bar before attempt loss, 4.4 h wall in `sitting.md`.
  A full 27 bed of 624 declared cells is about 99 min per run; at seven runs ≈ 11.5 h before the
  record's 1.3–2.5× attempt-loss, so **15–29 h of unattended machine time** across passes, each
  pass one scale × one accessibility mode × one pose (about eight passes); the two accessibility
  passes need the user to flip System Settings. The 624 is the declared count; 619 were ever
  published on 26.5. Those hours are idle wall-clock during which the machine can run no browser
  suite (X7) and must stay untouched (the 45 s idle gate), so the bed costs **days of machine
  unavailability, not hours of work**; the 73 cells that had a seventeen-run bar on 26.5 are not
  re-captured at seventeen (clause 4 adopts no floor).
- **The bar the 26.5 cells were published at.** `bedProvenance` has 13 entries with run counts
  and a single `cellsSha256` each; it names cells for 91 (the frequency-settled ones), 73 cells sat
  at a seventeen-run bar (entries 0–2), 121 fixtures carry `recoveredProvenance` at one run per
  cell, and the publication events sum to 444 against 619 fixtures. A per-cell 26.5 bar is not
  derivable for most of the bed; clause 2 therefore declares one bar per pass.
- **The display.** Every profile's `display.displayName` is `가상 16:9`, a BetterDisplay virtual
  screen, and the two scales are reached by switching it between displayplacer modes 68 and 69.
  Without that third-party driver there is no 1x bed at all (four of six keys). The manifest's
  `sdk` field is read from the `VITREA_SDK` environment, which `capture.sh` derives from the
  installed SDK, not from the binary — after Xcode 27 is installed a run with the 26.5-linked
  bundle would attest `MacOSX27.sdk`.
- **The machine.** Mac14,12 (M2 Pro, Apple silicon, so 27-eligible), macOS 26.5.2, Xcode 26.6.
  Software Update offers macOS 27 build **26A428** (the same build as the 2026-09-09 RC) and macOS
  26.7. Reduce Transparency and Increase Contrast read 0 / 0 on 2026-09-16.
- **Public record of the change** (all secondary except the WWDC26 State of the Union and Apple's
  macOS page; see the research note): darkened edge, brighter speculars, stronger diffusion, more
  uniform refraction; window corners common and tighter (secondary: 20 pt from 26); sidebars
  edge-to-edge with colour; uniform toolbars on scroll; window shadows separating active from
  inactive more strongly; the slider at System Settings › Appearance, centred by default, asked for
  during installation, reported by reviewers to move menus, toolbars and sidebars and **nothing
  reported about `.glassEffect()` surfaces**; no public API or defaults key for its position;
  `NSGlassEffectView.effectIsInteractive` the one new glass symbol; `appearsActive` the environment
  value for the inactive appearance; no ScreenCaptureKit acquisition change in the RC notes.

## Design (advisory unless marked)

- **Keys.** [binding] `apple-macos-27.0-<scale>x-<scheme>-<a11y>`, the OS token equal to
  `sw_vers -productVersion` major.minor read at the run's opening. The shell gate that refused
  non-26.5 becomes a gate that refuses non-27.0 for this bed, and `materialize` refuses a manifest
  whose parsed major.minor (from `hardware.osVersion`, which reads `Version 26.5.2 (Build 25F84)`
  today, so the check parses rather than prefix-matches) and `osBuild` do not match the key and
  the declared build — the check the harness never had, added in TypeScript so the granted bundle
  is not rebuilt for it.
- **The slider.** [binding] G0 measures whether it moves the harness's cells at all, before the
  key grammar is touched. If it does not move `.glassEffect()` surfaces, the position is attested in
  the manifest as a required field read from the machine (a defaults key found by diffing before
  and after moving it, or whatever store G0 finds), and the key stays as above. If it does, the
  grammar gains a trailing token and the bed is captured at the default (centre) position only;
  the ends are evidence classes for the user to rule in or out. **If G0 finds no machine-readable
  position, the wave stops at Decision Log 2**: an attestation in this project is a read that can
  refuse (as Reduce Transparency and Increase Contrast are read in `main.swift`), and a screenshot
  of the pane cannot refuse anything; if the user rules a screenshot in, the manifest field is
  labelled an operator assertion and the ledger says so. Either way the 27 bed is captured at the
  centre, which is what a Mac shows by default.
- **Show Borders.** Off and attested in every run; not an evidence class in this wave (Deferred).
- **The native delta driver** (G2). A CLI shaped like `tier-delta.ts` over two fixture
  directories, writing `results/<dir>/native-delta.json` rows keyed `{ profileKey27, profileKey26,
  sceneId }`, with the same metric primitives the fidelity read uses and the rim-band and highlight
  reads from the 26.5 waves (W23, W24). The noise bar is taken from the 26.5 plurality record
  (run-to-run maxDelta / changedPx on the same cell) so "moved" means "moved more than 26.5 moved
  against itself".
- **The runtime default does not move** [binding — Decision Log 1]. `DEFAULT_MATERIAL_PROFILE`
  stays the 26.5 light material, because the 26.5 light document is pinned as the identity with the
  runtime default (`tuned-profiles.test.ts`, `dark-profile.ts`) and moving the default would turn
  that frozen document red with no remedy X1 allows. The 27 material ships as **patch documents**
  — the 27 light patch over the runtime default, the 27 dark difference document, the 27 receded
  endpoints — resolved through the same `withMaterialOverrides` path, each carrying its own
  `resolvedMaterialSha256`. Which document a page gets by default is then a **selection** in
  `platform-web`, which is clause 5's ruling, not a change to the default constant. A later wave
  may convert the 26.5 light document to an explicit patch and move the default, under a named
  one-time exemption recorded before it opens; this wave does not.
- **The refit** (G3) is chartered from G2's reading, not from this document: which laws moved
  (rim/edge darkening, highlight amplitude, scatter/diffusion, tone response, corner geometry) is a
  finding, and the child's acceptance names them. The mechanism precommit rule from W28 applies: no
  new operator is added to the material because a review article names an effect; an operator is
  added when the native delta shows a structure the existing profile cannot fit.
- **The default target** (clause 5) is the user's; the parent's recommendation is 27 by default
  with the 26.5 documents kept in `profiles/` as evidence and selectable through the existing
  `applyMaterialProfile` path, since every Mac that installs the update draws 27 and a page has no
  OS to follow.

## Children

### G0: Pre-flight on 27 — nothing filed, everything attested — opens on the updated machine

- **Owner:** an opus `general-purpose` agent on the machine, with the user's hand for the update,
  the System Settings pane and Xcode 27 (the 27 SDK is needed for one comparison).
- **Does:** (a) records the machine: `sw_vers`, build, Xcode / SDK versions, the **linked SDK of
  the granted bundle read from its own `LC_BUILD_VERSION`** (`vtool -show`), RT / IC / Show Borders
  / slider state, and **the virtual display**: that the BetterDisplay screen `가상 16:9` comes back
  after the update with the same name and colour profile and both displayplacer modes (68 and 69),
  in a G0 record beside the 26.5 hash manifest of clause 1. (b) Proves the granted bundle still
  captures on 27: TCC intact (`dump-layers` on one scene, no prompt, no silent denial),
  `deactivate-probe` still shows the `.accessory` recede as non-key and non-active. (c) **The
  SDK-gating comparison:** a cell set that spans the laws G2 will read — a structured backdrop
  (checkerboard capsule), the light `hc-text__rrect-sm`, a corner-bearing large radius, a photo
  `rrect-md` in each scheme, and **one inactive cell** — captured with the granted bundle and with
  a `VITREA_BUILD_OUT` side build against the 27 SDK, into scratch, and diffed by bytes. The side
  build has no Screen Recording grant of its own and a recorded denial suppresses the prompt, so
  its grant is **positively checked** (a capture that renders the material, `materialRendered`
  true) before any of its bytes are compared; a structural difference stops the wave for a user
  decision (rebuild and re-grant, or capture with the side build under its own grant). (d) **The
  slider probe:** six cells — a photo `rrect-md` rest in each scheme, the light `hc-text__rrect-sm`,
  the checkerboard capsule, **one inactive cell and one 2x cell** — captured into scratch at the
  slider's left end, centre and right end, with `dump-layers` beside each; the read says per cell
  whether any pixel moved beyond that cell's own centre-position run-to-run variation (three
  centre runs, the same construction as clause 3's bar), and finds the position's backing key by
  diffing `defaults` before and after. (e) Checks the window geometry: whether the tighter window
  corner or the changed shadow enters the harness's capture region on any scene (the canvas is
  inside the window; the corner is at the window's edge). (f) Declares the pass plan (scale ×
  a11y × pose) with hours at the seven-run bar, and the 26.5-bar table clause 2 asks G1 to commit.
- **Acceptance:** the record; the two comparisons with verdicts; the slider verdict with the key
  decision it implies written into Decision Log 2 for the user; nothing under `fixtures/`,
  `profiles/` or `results/matrix.json` touched (clause 1 manifest re-verified).
- **Stop conditions:** SDK-gated material; TCC broken; the recede no longer reachable by the
  `.accessory` path; a scene whose capture region the window geometry now cuts into; the virtual
  display or either of its modes gone; no machine-readable slider position.

### G1: The 27 bed — the user's machine, the child's runbook — opens after Decision Log 2

- **Does:** `scenes.json` version 6 adding the 27 profile keys (scenes unchanged); a
  `run-sitting-27.sh` derived from the W27 script with the version gate on `27.0*`, the slider,
  Show Borders and the bundle's linked SDK (from the binary, not the environment) in its per-run
  attestation and the `materialize` OS-and-build refusal; the passes run
  at the declared bar; plurality publication into `fixtures/apple-macos-27.0-*/` with a manifest
  whose `hardware` block carries the 27 record; per-run logs committed as the W27 sitting's were.
  The accessibility passes wait on the user flipping the settings and are attested from the
  machine's state, never assumed.
- **Acceptance:** clause 2 in full; a `sitting.md` record with wall-clock per pass; the 26.5 hash
  manifest re-verified; **no read of any 27 fixture against vitrea** in this child.
- **Stop conditions:** a run that fails to attest (quarantined, as the W27 script does); the
  per-profile count short of 26.5's without a named reason per cell.

### G2: The native delta — what 27 changed — controlled, no vitrea change

- **Does:** the native delta driver; the per-metric 27-against-27 noise bar computed from the
  sitting's raw runs and declared, per cell, before the first 26.5 pair is read; every 27 cell
  against its 26.5 counterpart; sheets per profile at both scales; a ledger
  section that says, per law the material names (silhouette, rim band, highlight, interior level,
  tone response by backdrop, scatter, tint shade, corner geometry, the recede), whether Apple moved
  it, by how much, and on which cells — and where it did not.
- **Acceptance:** clause 3; the reading's independent review (read-only opus) closed; a Decision
  Log 3 draft naming the laws G3 must refit and the bounds it must declare.
- **Stop conditions:** a delta the noise bar cannot separate from run-to-run variation (then the
  bar is the finding and G1 runs more rounds for those cells before G3 is chartered).

### G3: The refit — the WebGPU tier first, the CSS tier derived — chartered from G2

- **Does:** 27 profile documents; the material moved where G2 says the law moved; the CSS
  derivation, as patch documents over the unmoved runtime default; bounds declared for the 27
  profiles before the read and no floor adopted; calibration and validation sets fit and read; holdout read once; `PREDICATE_EXCLUDES` and the count tests moved
  to what the machine says; goldens regenerated only behind the isolation proof with the named
  constant per change.
- **Acceptance:** clause 4; independent review closed; the eye's sheets.
- **Stop conditions:** a declared bound missed (recorded, refereed; re-pinning is the user's).

### G4: The landing — the default ruled, the demo on 27, the cut prepared

- **Does:** clause 5's ruling executed as a selection in `platform-web`; clause 6 in full, which
  is larger than a fixture pair: the 26.5 directory names in `apps/demo/vite.config.ts`,
  `src/site/scenes.ts`, `src/site/calibration.ts`, `e2e/site.spec.ts`, `e2e/color-scheme.spec.ts`,
  the committed `e2e/fixtures/*.cell.json`, `.github/workflows/pages.yml`'s assertion, and
  `scripts/generate-dark-profile.mjs` with `dark-profile-export.test.ts` gating the file it
  regenerates inside a published package; the coverage matrix re-scored; the changeset(s); the c9d
  chain at the version head; the report to the user.
- **Acceptance:** clauses 5, 6, 7; `pnpm release` the user's hand; the tag after.

## Cross-Child Contracts

- **X1 — 26.5 is frozen.** No child writes under a 26.5-keyed path or changes a 26.5 row, bound,
  floor or document, and `DEFAULT_MATERIAL_PROFILE` does not move; the clause 1 manifest and the
  26.5 `resolvedMaterialSha256` pin are the checks, run at every merge.
- **X2 — attestation before pixels.** No 27 fixture is filed from a run whose manifest does not
  carry OS build, slider, RT, IC, Show Borders, scale, scheme, pose and the capturing bundle's
  linked SDK, all read from the machine or the binary, never from the environment; a value that
  cannot be read by something that can refuse is not an attestation.
- **X3 — measure before moving.** G2 changes no material; G3 changes nothing G2 did not name.
- **X4 — the granted bundle is not rebuilt** except by a Decision Log entry that also plans the
  re-grant; experiments use `VITREA_BUILD_OUT`.
- **X5 — holdout once per frozen configuration; bounds declared before reads** (as every wave).
- **X6 — the key names every axis that moved a pixel**; if G0 finds the slider moves the cells,
  the key carries it, and no 27 fixture is filed under a key that omits it.
- **X7 — GPU exclusivity and accessibility reads** as before: one capture process at a time; RT and
  IC read 0 before every browser run.
- **X8 — no GPT-rung agents**; children and reviews are opus `general-purpose` (fable for
  judgment-heavy readings if the parent chooses); ledger section numbers assigned at dispatch.

## Ordering & Dependency Map

G0 → Decision Log **3** (slider / key / SDK verdicts; the user) → G1 → G2 → Decision Log **4** (the
laws to refit) → G3 → G4. The clause 1 hash manifest precedes G0, and within G2 the noise bar is
declared and committed before the first 26.5 pair is read. The numbering is Decision Log 2's
renumbering, which this line predated; the default target was ruled in advance at Decision Log 2 and
is no longer waiting on G2. The abscissa's identifying experiment (W28 §Deferred) and the
motion-metrics harness follow this wave.

## Risks & Mitigations

- **SDK-gated material** (G0 (c)); mitigated by measuring before the bed, not after.
- **The slider as an unmeasured axis**; G0 (d) measures it on the cells that matter before any
  key is chosen.
- **TCC after the OS update**; G0 (b) checks with a scene that would prompt, and the README's
  toggle recipe is the recovery.
- **Machine hours**; G0 (f) prices the passes from the actual bar per cell, and the bed can be
  captured standard-first (four profiles) with the accessibility passes as a second sitting.
- **Confounding the version change with the recede** (c9a §5.134's warning); G2 reads active and
  inactive cells separately and reports the recede's delta as its own row.
- **The virtual display driver after a major OS update**; G0 (a), a stop condition.
- **Re-fitting to a press release**; X3.

## Deferred / Out of Scope

*As chartered:* the slider's two ends as evidence classes (unless ruled in at Decision Log 2); Show
Borders as an a11y profile; dark and 2x accessibility, the `clear` variant and the stack regime
(W27 §Deferred, now on the 27 bed); the identifying sitting on 27; the motion-metrics harness;
`prominent`; `NSGlassEffectView.effectIsInteractive` and the OS 27 interaction response; a
26.5-vs-26.6.2 control read (foreclosed on this machine by the update; the research note's
suggestion stands only for a machine that still has 26.x).

### At close, 2026-09-20 — what this wave measured and did not do

Each of these is a named piece of work with its evidence already on disk, in the order the parent
would charter them. None is a loose end: every one is a thing this wave established the existence
and the size of.

1. **The operator wave — the span-graded shadow and the scale-selective scatter, under one
   exemption.** Decision Log 7 (a). macOS 27's shadow blur is linear in the casting span
   (σ_css ≈ 0.133 · (span − 30) over spans 96–160) and `sigmaPx` is one constant, so 0.19.0 ships
   the thin cells' shadow **5.97× too wide at 1x and 2.80× at 2x**, carrying the right energy in
   the wrong shape; the diffusion residual is conditioned on the backdrop's spatial scale
   (§5.153 §6) and, this wave's landing adds, on the colour scheme as well. Both add leaves to the
   renderer default and so move every document's fingerprint, the frozen macOS 26.5 pair included,
   which is why they are **one wave under a single one-time X1 exemption** rather than two.
   Chartering them apart would spend two exemptions.
2. **The chromatic-transmission child.** Decision Log 6 (c). Four missed rows, one dark
   `photo__rrect-lg__rest` cell whose `oklabDeltaEP95` did not move to the fourth decimal across
   the shadow refit, and the eye's reading of the light photo cells beside it: vitrea's body is grey
   where Apple's is coloured. It is also what closes the recede's worst cell on every row of its
   missed bound (§5.154 §8).
3. **The identifying sitting, on the 27 bed.** W28 §Deferred, re-runnable now. W28 G0's verdict was
   *not identifiable from this bed*, and the bed it could not identify from is the macOS 26.5 one;
   the abscissa's kind and scale are still unknown and the silhouette mean is the candidate that
   passed a bound rather than Apple's law.
4. **The highlight's directionality reader.** Decision Log 6 (e), and the last of Decision Log 4
   (a)'s six laws with no instrument behind it. `angularRead` takes a raster and a declared box and
   has nothing native about it; what is missing is a call in `cli/measure.ts` that runs it on both
   captures. Until then the law is measured native-against-native and unfittable.
5. **The decoupled increased-contrast profile's read**, once `compare`'s accessibility flag keys on
   the profile key rather than on the manifest. Decision Log 5 kept that profile without a table
   because it is confounded by the toggle; the coupled one was captured and fitted in its place.
   The blocker is a tracker entry and is a small change.
6. **The motion-metrics harness.** W28 §Deferred, unchanged: the activation transit's 240 ms /
   120 ms are inherited and unmeasured against any native sequence, and every sheet this wave
   produced is a settled endpoint.
7. **The CSS tier over a pure black backdrop.** Found at this landing: the tier renders about 58
   codes above the curve's target there, and the rendered level moves the wrong way across the
   ramp's first step. macOS 26.5's collapse owned that region and hid it; macOS 27's inert band
   exposes it. No committed row covers a backdrop that dark, and the GPU tier's behaviour there is
   unmeasured. Tracker.
8. **The site's tone stage, which was designed around a convergence macOS 27 does not have.** The
   prose and the cases moved with the material at this landing; whether the stage should be
   re-ranged, re-subjected or retired is a decision about what the site is for. Tracker, for the
   user.
   > **CLOSED 2026-09-21** (W31 Decision Log 1 (c), ruled by the user; recorded at W31 G4, claims
   > §5.165 §3). The answer is the first of the three: **re-ranged, and kept.** W30 G4 executed it
   > — the control's value is a POSITION on a geometric ladder between the same two ends, 81 stops
   > of equal ratio from 0.0020 to 0.1600, so the band where the plates separate holds 40 stops
   > against 16 of the old 80 and both ends survive (claims §5.160). The ruling turned on a reading
   > that reversed the premise this item was written on: the three bodies separate MOST over the
   > dark half (0.026, about 11 % of their own level, over a ground of 0.006) and least at the
   > bright end (0.0148, 2.4 %), so what closes at the bottom is their ORDER and not their
   > separation. The tracker entry "the site's tone stage was designed around a convergence macOS
   > 27 does not have" is closed and removed with this item; the 40 px plate's alpha band is the
   > one thing the ruling keeps recorded, and it stays open as its own entry.
9. **The matrix's generation question.** `results/matrix.json` is 72.1 MB and holds two macOS 27
   generations; "which generation is the shipped one" is answerable only by timestamp, and the demo
   now answers it that way. G3b put three shapes to the user and G4 seconds the generation split.
   Tracker.
10. **The receded pose's edge, and the largest spans.** The recede is fitted on the body and its
    edge is a subtraction rather than a fitted term — measured at this landing as twice the active
    pose's ring error in both schemes — and the bed declares no inactive calibration cell above
    span 96 at all, which is a scenes decision before it is a fit. Tracker.

## Tracking Map

| Child | Status | Claims section | Evidence |
| --- | --- | --- | --- |
| G0 | CLOSED 2026-09-18 — (a), (b), (d), (e), (f) complete; (c) settled on the declared material and **stopped before pixels** for want of a TCC grant the user must give. Decision Log 3 drafted below | §5.149 | `results/2026-09-18-w29-g0-preflight/` |
| G1 | **CLOSED 2026-09-19.** Part A: the keys, `scenes.json` v6, `run-sitting-27.sh` and its 27-row refusal test, the `materialize` refusals, the bar table verified, the SDK pixel arm closed on pixels, the rehearsals. Part B: the sitting run by the parent (56 runs, none quarantined) and **published — 624 cells over six profiles, no shortfall, no refusal**, the freeze re-verified intact after amendment. **G2 is unblocked** | §5.150 | `results/2026-09-18-w29-g1-bed/`, `fixtures/apple-macos-27.0-*/` |
| G2 | **CLOSED 2026-09-19**, and its independent review closed the same day (ten findings, none flipping a verdict; claims §5.151 §12, Revision Note 2026-09-19). The bar declared and committed before the first pair; 619 pairs read, 231 recede rows, 5 cells reported not diffed, 0 **pair** cells unbarred — the recede rows were unbarred on 158 of 231 until the review, and now carry a bar on all 2,570 measurable readings. **The material moved on every cell and the geometry did not**; the level, tone response, scatter, rim, highlight, tint lightness and the recede all moved, the corner and the silhouette did not, and the tint's hue did not. Decision Log 4 drafted below for the user | §5.151 | `results/2026-09-19-w29-g2-native-delta/` |
| G1c | **CLOSED 2026-09-19.** Part A built Decision Log 4 (b)'s coupled bed — the key's fourth a11y token, `scenes.json` version 7, the pass's variant and its two-toggle refusals, `materialize`'s refusal, 36 stubbed rows — and captured nothing. Part B published **32 cells at seven runs per pose, 28 unanimous and 4 voted, 0 refused**, declared the bar before any 26.5 pair was read (`848713cf`), and read the bed: **the material moved on every cell and the geometry did not**, and the decoupling's share of §5.151 §9 is measured — the rim and the scatter are the material (1.00×), the level, tone offset and tint lightness are mostly the toggle (2–5×). **A bound is drafted for the user** (Decision Log 5 below): every material row of the native-to-native distribution is inside the 26.5 increased-contrast allowance at its worst cell, which is true of one other profile in Decision Log 4 (c) — `1x-light-reduced-transparency`, once that table's transcribed allowances were corrected against the test file (review closure 2026-09-19; claims §5.151 §12, §5.152 §B §12). Its independent review closed the same day: six findings, none flipping a verdict, Decision Log 5's ruling unchanged. The freeze intact at 1,818 | §5.152 | `results/2026-09-19-w29-g1c-coupled/`, instruments in `results/2026-09-18-w29-g1-bed/` |
| G3 | **CLOSED 2026-09-19** (dispatched after Decision Log 4 on main `101d153f`; Decision Log 5's sixth profile folded mid-flight). Bounds declared in two commits before any fit was read; **`DEFAULT_MATERIAL_PROFILE` did not move** and the 34 goldens are byte-identical; two 27 patch documents carry the refit and the CSS tier derives from them in the same commit; 332 rows appended beside the 1,107 26.5 rows, which are bit-identical across the append **(corrected beside 2026-09-19 by the review closure: 455 rows were appended — 332 from the calibration+validation read and 123 from the holdout read; the bit-identity is unaffected)**; holdout read once. **The whole-cell ΔE mean roughly halves on every standard profile on both tiers and the ΔE p95 tail falls 0.46 → 0.14 light and 0.27 → 0.17 dark; fifteen declared rows are missed** and recorded as UNMET with no floor — seven of them caused by the outer shadow, which moved on 27 and which G2 never read, so X3 forbade following it. Decision Log 6 is drafted below | §5.153 | `profiles/apple-macos-27.0-*`, `results/2026-09-19-w29-g3-refit/` |
| G3b | **CLOSED 2026-09-19** under Decision Log 6 (a), (d) and (e). The shadow axis was added to the native delta at G2's own bar, declared and committed before the first 26.5 pair (and reproducing G2's thirty-two metrics over 190,683 readings with zero disagreements); **the shadow moved on every measurable cell of every profile** — 342/342 on the departure, 246/246 on the fitted σ and amplitude — and is dimmer, tighter, shorter and less displaced. Eight leaves move in the 27 light document and seven in the dark, the shadow block and nothing else, proved by field diff; the departure residual goes 0.0068 → 0.0007. **The 27 receded endpoints are fitted** through this child's own `--receded-profile` driver — the tracker's proposed shape, built in the calibration package — with the response re-anchored to the active document's knots and the outer shadow RESTORED, because on 27 the recede no longer removes it; the inactive level residual goes 0.1174 → 0.01017. Four documents re-sealed, all six profiles re-read on both tiers, holdout once. `DEFAULT_MATERIAL_PROFILE` unmoved, 34 goldens byte-identical, the 26.5 freeze intact at 1,818. **`MISSED_27_ROWS` goes 15 → 7**, all eight cleared rows the shadow's. The recede's bound, declared before the fit at W27c G1b's three clauses and its 26.5 values, **holds on 1 of 12 profile-tiers**, worst on `photo__rrect-lg__inactive` on every row — which is the chromatic cell Decision Log 6 (c) already chartered a child over. **Decision Log 7 is drafted below**, with two items for the user: the shadow's σ is no longer span-invariant on 27 and one constant cannot hold 1.8 and 17.4, and the recede's missed bound. **Its independent review closed the same day** (ten items, no measurement, bound, verdict or missed row wrong; claims §5.154 §11, Revision Note 2026-09-19) — the span law restated as σ_css ≈ 0.133 · (span − 30) over spans 96–160 with the thin spans open, two mis-attributed tier-worsts corrected, and Decision Log 7 (a)'s cost split: the goldens would not move, the four documents' fingerprints would, so a one-time X1 exemption is part of that option's price | §5.154 | `results/2026-09-19-w29-g3b-shadow-recede/`, `profiles/apple-macos-27.0-*` |
| G4 | **CLOSED 2026-09-20.** Clause 5 executed as a selection (Decision Logs 1 (i), 2, 7 (b)): one material document per measured material, `macos27MaterialProfileDocument` the default and `macos26MaterialProfileDocument` shipped beside it, the fitted 27 receded endpoints applied by the runtime at last, `blurSigmaScale` 2.2 the default crossing, and `root.material` / `GlassGroupState.materialDocument` naming the endpoint that drew. The four shipped patches are **generated** from the four documents and pinned to them; the join is published: 455 of the 456 newest macOS 27 cells were read at exactly the shipped documents' file hashes, and the one that was not is §5.154's refused reading, named. Clause 6 in full — the demo's pair and figures, `pages.yml`, the capabilities panel, the playground's law readouts, `CLAUDE.md`, both READMEs, the coverage matrix re-scored — and clause 7's sheets with a real focus change moving the selection on screen. **Eight browser cases and four unit cases moved, each with its reason in the file**, of which two are findings in their own right (the adaptation's collapse is gone; the CSS tier overshoots over a pure black backdrop, which macOS 26.5's collapse used to hide). Three defects the eye caught and the suites did not are fixed and pinned. `DEFAULT_MATERIAL_PROFILE` unmoved, 34 goldens byte-identical, the freeze intact at 1,818. **0.19.0 prepared and unpublished** | §5.155 | `results/2026-09-20-w29-g4-landing/` |

## Decision Log

### Decision Log 1 — the charter (2026-09-16; the parent, on the user's W28 ruling)

**Context.** W28 Decision Log 4: the identifying sitting forgone on 26.5, the machine updates to
macOS 27, the OS 27 recapture is the next wave. The user's words: "updating OS is indeed the right
call, we'll get macOS 27 and do the recapture."

**Ruled by the parent (mechanical, within the standing rules):** the order measure-then-refit
(X3); the 26.5 freeze proven by hash manifest before G0 (clause 1, X1); keys `apple-macos-27.0-…`
with the OS token gated in the run script and in `materialize` rather than by a harness rebuild
(X4); the 27 bed captured at the slider's default position; G0's checks before any key is
committed. **After the adversarial review, three more:** (i) `DEFAULT_MATERIAL_PROFILE` does not
move and the 27 material ships as patch documents, so the frozen 26.5 light document's identity
pin stays green (the reviewer's blocking finding 1; the alternative, a one-time X1 exemption to
convert the 26.5 document to an explicit patch, is rejected for this wave as an edit to committed
evidence the wave does not need); (ii) clause 3's noise bar is the 27-against-27 per-metric
run-to-run distribution from the sitting's own raw runs, declared per cell before any pair is
read, because the 26.5 plurality record is byte-level and transient and cannot bar SSIM, ΔE or
contour (finding 2); (iii) one bar per pass, seven runs, no 27 floor adopted, because
`bedProvenance` cannot attribute a bar to most 26.5 cells (finding 3). Folded as should-fix: the
virtual display and the linked SDK into G0 (a) and X2; the SDK comparison widened to a cell set
with a positive grant check; the slider stop condition; the slider probe widened to an inactive
and a 2x cell; clause 4's freeze wording; the `materialize` check parses; G4's file list; the
web-page sentence moved out of Design.

**Open for the user (Decision Log 2, after G0; Decision Log 3, after G2):** whether the slider
enters the key and whether its ends become evidence classes; what to do if the granted bundle's
material is SDK-gated; the runtime's default material after the refit and the fate of the 26.5
documents at runtime; whether the accessibility passes run in the first sitting or a second.

**Recommendations carried into those entries:** capture at the default only, because it is what a
Mac shows by default and the parent's view is that a web page should match that; 27 as the
runtime default (by selection) with 26.5 selectable; standard-first sitting if the hours run long.

### Decision Log 2 — the runtime default is 27, ruled in advance; the machine's state on arrival (2026-09-18; the user, then the parent)

**The user's ruling.** With the machine updated: *"After G2's native read: the runtime default 27."*
Clause 5 is therefore ruled before G2 rather than after it, and G3/G4 execute it as Decision Log 1
(i) requires — by **selection** in `platform-web`, the 27 documents chosen by default and the 26.5
documents kept shipped and selectable through `applyMaterialProfile`; `DEFAULT_MATERIAL_PROFILE`
does not move. The ruling takes effect at the landing (G4), after G2's native read has said what
moved; nothing about the order of children changes. The entry the charter planned "after G2" now
carries only the laws G3 must refit.

**The machine on arrival (the parent, before G0).** `sw_vers` 27.0, build **26A428** (the same
build Apple posted as the RC on 2026-09-09); Reduce Transparency 0, Increase Contrast 0; Xcode
still 26.6 with SDKs 26.0 / 26.5, **and the Command Line Tools carry `MacOSX27.sdk`** (27.0), so
G0's side build can target 27 without Xcode 27; the granted bundle's binary reads `minos 26.0 sdk
26.0` from `vtool`; the BetterDisplay screen is back with both modes (68 current, 69) at the same
persistent id; the freeze (`results/2026-09-16-w29-freeze/`) verifies **intact on 27**, 1,762
entries; `capture.sh tint-doctor` (no GUI, no permission) runs. **The slider's backing key is
found without a probe:** the global domain now holds `NSGlassTintAmount = 0.5459057` (float),
written 2026-09-18 11:59 local, a key that did not exist on 26.5. Its value is **not 0.5**: G0 must
establish what the pane's centre maps to (delete the key and read what the system writes back at
the default; move the slider by the key and confirm the pane follows) before the bed's "default"
is declared, because clause 2 attests the position and X6 says a key names every axis that moved
a pixel. The `.GlobalPreferences.plist` timestamp is also the time the 26.5-era inference in
Grounding stops being checkable: the pane was not read before the update.

**Renumbering.** The slider / key / SDK verdicts after G0 are Decision Log 3; the laws to refit
after G2 are Decision Log 4.

### Decision Log 3 — RULED 2026-09-18 (the user, on G0's draft below): the bed at 0.5; Show Borders' key found by the user's toggle; the side bundle granted

**Ruled.** (a) **The 27 bed is captured at the system default, `NSGlassTintAmount` 0.5**, set by
writing the key before the sitting and attested per run; the position enters the profile key as
the trailing token the charter's Design provides (X6), and the machine's as-found 0.5459057 is a
recorded reading, not a bed. (b) **Show Borders is attested, not asserted**: the user toggles it on
and off once in System Settings while the parent diffs a snapshot of every preference domain
(635 domain exports taken 2026-09-18 04:00Z, before the toggle) to find its key; G1 then reads that
key per run like Reduce Transparency and Increase Contrast. If the diff finds nothing, the fallback
is the operator assertion the draft names, and the ledger says so. (c) **The 27-SDK side bundle is
granted Screen Recording by the user's hand** (System Settings › Privacy & Security › Screen &
System Audio Recording, `~/vitrea-w29-g0-scratch/c/side-sdk27/VitreaReference.app`), so the
pixel half of the SDK-gating comparison closes; G1 re-checks that the granted harness bundle still
captures **before** any other step, since the two share a bundle identifier and the effect on the
existing grant is unknown; if the granted bundle's grant is lost, the recovery is the README's
toggle and that is recorded as a Surprise. The parent's recommendation on (c) was the other way;
the user's ruling stands and is the decision.

*G0's draft, as it was put to the user:*

### Decision Log 3 draft — the slider, the key, and the SDK verdict (2026-09-18; G0)

Drafted by G0 from `results/2026-09-18-w29-g0-preflight/` and claims §5.149. Nothing here is ruled;
the three questions Decision Log 1 left open after G0 are put with what was measured against each.

**(a) The slider moves the harness's cells, on every axis the bed has.** `NSGlassTintAmount` is a
float in `NSGlobalDomain` and it **drives rendering with no GUI**: the harness's declared material
carries the key's value unrounded as `inputBlurFillNormalOpacity`, and 10 of 10 probe cells moved
beyond their own three-run centre spread at 0.0, at 1.0 **and** at the machine's as-found
0.5459057 — in the active pose and in the recede, at 1x and at 2x, in both schemes
(`slider-probe.json`, `sheets/slider.png`, §5.149 §4). X6 therefore says the key must carry it, and
the grammar gains a trailing slider token as the charter's Design provides.

**(b) What the centre maps to: 0.5, and the machine is not at it.** With the key **deleted** the
material renders at exactly the 0.5 arm — byte-identical captures on every cell of every arm — and
0.5 is the knee of both declared ramps. So the system's own default is 0.5 and this machine sits
0.0459057 to the right of it, which is above the noise bar on all ten cells. **The decision this
implies is what the 27 bed is captured at**, and it is the user's:

- *the system default, 0.5* — what a Mac with an untouched pane draws, and what the charter means
  by "the default (centre) position". Reached by `defaults write -g NSGlassTintAmount -float 0.5`
  or by deleting the key, with no GUI.
- *the machine's as-found 0.5459057* — what this machine has actually been drawing since the update
  and what the user may have set at the installer's prompt. Continuous with nothing: no 26.5
  capture was taken at any slider position, because the key did not exist.

The parent's recommendation is **0.5**, because the bed's claim is about what Apple draws by
default rather than about what one machine was left at, and because a bed captured at an arbitrary
float is a bed nobody can reproduce. Either way the position enters the key and the manifest.

**(c) The slider is not yet attestable, and that is a small harness change.** X2's bar is a read
that can refuse. Nothing in the harness reads the key today, and a shell `defaults read` beside the
run is not an attestation. The change is one field in `Manifest.swift`'s hardware block plus a
refusal; putting the refusal in `cli/materialize.ts` instead keeps X4 intact and needs no rebuild
of the granted bundle. **Show Borders has no such path**: it has no key in
`com.apple.universalaccess`, none in the global domain, and no property in the 27 SDK's
`NSAccessibility.h` (§5.149 §1), so "off and attested in every run" is not reachable from the
machine. The user's call is between reading the pane by hand and labelling the field an operator
assertion, as Design already rules for the slider's fallback, and one hand on System Settings to
find the key by diffing the domains across a toggle.

**(d) SDK gating: not observed, with one residual the user can close.** Two bundles from one
toolchain and one source revision, recording `sdk 26.5` and `sdk 27.0`, declare a **byte-identical
material on 9 of 9 cells** — same classes, all 81 filter inputs equal — and the granted bundle,
which records the oldest value of the three, agrees on 8 of 9 (the ninth is a pose difference, not a
material one). The granted bundle is plainly drawing 27's material: its dumps carry the new SDF
effects and input keys that do not exist in any committed 26.5 dump. **The bed can be captured with
the granted bundle**, and X4 stands.

The residual is that the window server composites from the tree this arm reads and could in
principle read the binary's linked SDK itself. Closing it needs the 27-SDK side bundle's own Screen
Recording grant, which TCC will not give without a hand: the side bundle is a new ad-hoc identity
and a recorded denial suppresses the prompt. `tccutil reset` was deliberately not run — it resets by
bundle identifier and the granted bundle shares that identifier. **The user's call is whether to
grant the side bundle at all**, knowing the shared identifier makes the effect on the granted entry
unknown; the parent's recommendation is **not to**, because the declared-material reading is
consistent across three linked SDK values and the grant risks the wave's one irreplaceable
permission to close a residual nothing else depends on.

Evidence for each: `results/2026-09-18-w29-g0-preflight/slider-probe.json` and `sheets/slider.png`
(a, b); `machine.json` and `showborders-search.txt` (c); `sdk-gating.json`,
`dump-compare-sides.json`, `dump-compare-three.json` and `sheets/sdk-gating.png` (d).


### Decision Log 4 — RULED 2026-09-19 (the user, on G2's draft below as amended by the review closure): the 27 bounds at the 26.5 values on the five unconfounded profiles, none on increased contrast; a coupled increased-contrast pass is captured

**Ruled.** (a) **G3 declares the 27 tables at the 26.5 tables' values** on `1x-light-standard`,
`2x-light-standard`, `1x-dark-standard`, `2x-dark-standard` and `1x-light-reduced-transparency`,
per tier, before its read, and **no table for increased contrast** this wave. The user rules this
knowing the native-to-native tail on both dark profiles lies outside the dark allowance (ΔE max
0.155 against 0.09; edge-weighted 0.119 against 0.04, Decision Log 4 (c) table): a dark miss at
G3's read is therefore **a floor decision for the user, recorded**, not a bound loosened after the
fact and not a fit failure. The alternative — dark tables declared in advance at the measured p90 —
is rejected as spending fidelity before the fit has been tried. (b) **A second increased-contrast
pass is captured with Reduce transparency forced on**, the state 26.5's profile of that name was
captured in, under a profile key of its own (the decoupled capture keeps
`…-increased-contrast-glass0.5`), 32 cells at 1x in both poses at the seven-run bar, the toggles
driven through the codex companion's GUI runtime per the user's 2026-09-19 instruction; its
native-against-native read against the 26.5 increased-contrast bed is the like-for-like
comparison the decoupled capture cannot give, and its reading joins §5.151 beside the confounded
one. Whether that profile then gets a bound is decided on its reading, not now. (c) The laws G3
refits are the six the draft ranks below, in that order, and the two it says to leave alone are
left alone; no mechanism is precommitted. (d) The child for (b) is G1c under claims §5.152; G3
opens after the machine is free of it, because the two cannot share the GPU.

*G2's draft, as amended by the review closure and put to the user:*

### Decision Log 4 draft draft — the laws G3 must refit, ranked by evidence, and the bounds it should declare (2026-09-19; G2)

Drafted by G2 from `results/2026-09-19-w29-g2-native-delta/` and claims §5.151. Nothing here is
ruled and **no mechanism is proposed for any law**: the charter's Design puts the refit's mechanism
in G3, and W28's precommit says an operator is added when the delta shows a structure the existing
profile cannot fit — so this entry names the structures and leaves the fit alone. Every count is
against each cell's own 27-against-27 bar; every "did not move" is the weaker of the two statements,
because the bar contains no 26.5 run-to-run spread (§5.149 §6).

**The one-line result.** Apple's material moved on **all 619 cells**; Apple's **geometry did not**.

#### (a) The laws G3 must refit, in the order the evidence ranks them

1. **Interior level, conditioned on more than the backdrop's mean.** Every one of 589 cells moved,
   median |Δ| 0.074 linear active and 0.111 receded, and the sign is not constant: bed-wide median
   signed +0.0026 active and −0.0614 receded, per-cell p05/p95 −0.188/+0.200 and −0.273/+0.179.
   Within a single backdrop the change spans ±0.1–0.2 and changes sign, so a constant, and a
   function of backdrop mean alone, are both refuted before G3 starts. **This is first because every
   other material law is read against the body's own level.**
2. **The rim band.** All 330 active cells with a declared box; median |Δ| 0.112 luminance per CSS px;
   signed median −0.0099 with 192 of the moved cells down against 138 up; the radial peak −0.039
   with 253 of 340 down; and the rim **wider** by a signed median +0.42 px (156 up, 63 down). Two
   quantities moved, amplitude and width, and W23's fit carries constants for both.
3. **The highlight's directionality.** The angular ratio of brightest to dimmest bin moved on 132 of
   132 cells that resolve one, signed median **+6.31**, 97 up against 35 down; the brightest bin's
   direction shifts by more than one 22.5° bin on 169 of 330 active cells. W24 chartered a wave on
   exactly this number reading near 1 for a drawn rim; on 27 it is several times larger.
   **What moved inside the ratio** (review closure): the median ratio is 4.62 on 26.5 against 20.17
   on 27, and the rise is mostly the **dimmest bin falling**, 0.0784 → 0.0242, while the brightest
   bin barely moves, 0.3770 → 0.3632 — so G3 fits the bins and not the ratio. The angular shift's
   median is **45° over all 330 active cells and 112.5° over the 169 that moved**.
4. **Scatter, as a function of the backdrop's spatial scale.** 589 of 589 moved. Over structured
   backdrops the signed median is −0.0046 (182 up, 255 down) and over solid ones +0.0003 (78/74,
   the null control). Ordered by backdrop: `checkerboard` −0.033, `hc-text` −0.013, the fine grids
   −0.005 to −0.006, against `checkerboard-64` **+0.016** and `photo` **+0.010**. Fine structure
   survives the 27 material less and coarse structure survives it more. The transfer slope gives the
   same ordering independently (`photo` 0.091 → 0.283 with r² 0.13 → 0.43; `checkerboard-64`
   0.138 → 0.215 with r² 0.57 → 0.75; `checkerboard` 0.069 → 0.039 at flat r²).
5. **The recede.** 231 pairs, as a difference of differences. On 27 the recede **darkens the body
   more and takes away less rim**, and the size depends on which population is read — the figures
   first entered here were over the 18 to 70 rows that carried a bar of their own, and the review
   closure adds the full measurable population beside them (claims §5.151 §8;
   `review-closure.txt` §1). Body: −0.0049 → −0.0254 on the 55 barred rows, **+0.00068 → −0.01725 on
   all 219 measurable pairs** — the 26.5 term changes sign, so **"five times as much" is withdrawn**;
   what holds on both is the per-cell median difference, −0.0290 barred and **−0.0275 full**. Rim:
   −0.1096 → −0.0628 barred and −0.1110 → −0.0657 full, at a per-cell median difference of +0.0473
   barred against **+0.0178 full**, so the rim loss is reduced on 27 but by less than the barred
   subset said. Angular ratio: −0.068 → +0.332 barred, +0.0046 → +0.332 full. **G3 fits the full
   population**; the barred subset is the cells whose 27 captures happened to disagree across runs,
   which is a property of the capture. The inactive endpoint is a separate fit in this project and
   needs its own refit, not a rescaling of the active one.
6. **Tint lightness, and only lightness.** On the 88 tinted cells ΔL moved by a median 0.026 and
   chroma by 0.0022, while the **hue did not move**: on the 113 cells whose interior chroma is at
   least 0.01 the median hue shift is 1.0°. The 82° median on the 456 near-neutral cells is the
   metric read where it is undefined and is not evidence. (456 + 113 is not the bed: 20 further cells
   cross chroma 0.01 between the two beds and the measurable population is 589 — review closure.)

#### (b) The laws the evidence says to leave alone

- **Silhouette and geometry.** Median IoU complement 3.6 × 10⁻⁴ (IoU 0.99964), mean contour distance
  0.0145 px, area difference 8 px. Beyond the bar on two thirds of cells and invisible to the eye.
- **Corner geometry.** Median |Δκ| 7.8 × 10⁻⁴ /px with a median **signed** change of exactly 0.000
  and 126 cells up against 104 down (the moved active cells, artefact cells included; over the 537
  healthy cells of both poses it is 207 up, 170 down and 160 exactly tied — review closure); every
  single-box component reproduces its declared radius on both beds (`capsule-button`
  17.92 → 17.92 px, `rrect-md` 18.89 → 18.89, `rrect-80` 16.04 → 16.04).
  The public record's tighter window corner is not in this bed and G0 measured why it cannot be
  (§5.149 §5): the capture region is the window's own rectangle and the window corner never enters
  it. **No corner constant should move in G3.**
- **Tint hue**, per (a) 6.

#### (c) The bounds G3 should declare, per profile, before its read

Mirroring the 26.5 bound shape in `adopted-thresholds.test.ts` — one table per profile per tier,
the same metric names, declared before the fit is read. The parent's recommendation is to **declare
the 27 tables at the 26.5 tables' values on the four standard profiles and on reduced transparency**,
and to declare **no table at all for increased contrast until the decoupling is separated**:

- The 26.5 tables are the bar this project has already proven reachable against Apple's material on
  this bed, and G2 measures the two OS versions at a whole-cell ΔE of 0.014 at the median — an order
  under the 0.07 the light texture table already allows. A refit that lands the 27 material at the
  26.5 numbers is therefore a defensible target rather than an optimistic one, and declaring looser
  numbers in advance would spend fidelity the evidence does not say has to be spent.
- **The median is not what those tables bound** (review closure, 2026-09-19). Every row in
  `adopted-thresholds.test.ts` is a **per-cell** bound: one cell over it fails the suite. So the
  native-to-native distribution is given below at its p90 and its max as well, per profile, with the
  profile's own 26.5 **texture-tier** allowance in the same row, restated as the complement where the
  adopted row is a `≥` so that every line reads *lower is closer*. Full table, dom tier included and
  with the all-cell shape tails, in `review-closure.txt` §4.

  | profile (27 against its 26.5 twin) | metric | median | p90 | max | 26.5 allowance |
  | --- | --- | ---: | ---: | ---: | ---: |
  | `1x-light-standard` | IoU complement † | 0.0001 | 0.0070 | 0.400 | ≤ 0.18 |
  | | contour mean px † | 0.004 | 0.142 | 3.02 | ≤ 2.5 |
  | | contour p95 px † | 0.000 | 1.00 | 15.0 | ≤ 5.0 |
  | | SSIM complement | 0.0219 | 0.0791 | 0.206 | ≤ 0.12 |
  | | **ΔE mean** | 0.0153 | **0.0468** | **0.0967** | ≤ 0.07 |
  | | ΔE p95 | 0.0772 | 0.1286 | 0.441 | ≤ 0.17 |
  | | edge-weighted | 0.0212 | 0.0773 | 0.143 | ≤ 0.11 |
  | | SSIM-outside complement | 0.0260 | 0.1240 | 0.719 | ≤ 0.16 |
  | `2x-light-standard` | IoU complement † | 0.0005 | 0.0050 | 0.373 | ≤ 0.15 |
  | | contour mean px † | 0.029 | 0.234 | 5.75 | ≤ 5.0 |
  | | contour p95 px † | 0.000 | 1.00 | 29.0 | ≤ 10.0 |
  | | SSIM complement | 0.0141 | 0.0646 | **0.117** | ≤ 0.11 → **≤ 0.07** ‡ |
  | | **ΔE mean** | 0.0140 | **0.0480** | **0.0963** | ≤ 0.07 |
  | | ΔE p95 | 0.0655 | 0.1286 | 0.441 | ≤ 0.17 |
  | | edge-weighted | 0.0199 | 0.0769 | 0.144 | ≤ 0.12 |
  | | SSIM-outside complement | 0.0196 | 0.1867 | 0.679 | ≤ 0.14 → **≤ 0.13** ‡ |
  | `1x-dark-standard` | IoU complement † | 0.0006 | 0.0551 | 0.623 | ≤ 0.07 |
  | | contour mean px † | 0.019 | 1.37 | 6.23 | ≤ 0.5 |
  | | contour p95 px † | 0.000 | 10.8 | 37.0 | ≤ 1.5 |
  | | SSIM complement | 0.0491 | 0.1628 | 0.236 | ≤ 0.13 |
  | | **ΔE mean** | 0.0231 | **0.1106** | **0.1547** | ≤ 0.09 |
  | | ΔE p95 | 0.1183 | 0.2161 | 0.310 | ≤ 0.17 |
  | | **edge-weighted** | 0.0121 | **0.0725** | **0.1177** | ≤ 0.04 |
  | | SSIM-outside complement | 0.0689 | 0.1488 | 0.210 | ≤ 0.17 |
  | `2x-dark-standard` | IoU complement † | 0.0005 | 0.0242 | 0.625 | ≤ 0.07 |
  | | contour mean px † | 0.033 | 1.28 | 7.85 | ≤ 1.0 |
  | | contour p95 px † | 0.000 | 9.00 | 54.0 | ≤ 1.5 |
  | | SSIM complement | 0.0387 | 0.1258 | 0.188 | ≤ 0.12 |
  | | **ΔE mean** | 0.0233 | **0.1105** | **0.1542** | ≤ 0.09 |
  | | ΔE p95 | 0.1267 | 0.2148 | 0.313 | ≤ 0.17 |
  | | **edge-weighted** | 0.0120 | **0.0710** | **0.1188** | ≤ 0.04 |
  | | SSIM-outside complement | 0.0435 | 0.1080 | 0.248 | ≤ 0.14 |
  | `1x-light-reduced-transparency` | IoU complement † | 0.0002 | 0.234 | 0.310 | ≤ 0.13 |
  | | contour mean px † | 0.004 | 3.30 | 3.62 | ≤ 1.5 |
  | | contour p95 px † | 0.000 | 14.0 | 15.0 | ≤ 3.5 |
  | | SSIM complement | 0.0081 | 0.0339 | 0.0463 | ≤ 0.05 |
  | | ΔE mean | 0.0042 | 0.0094 | 0.0211 | ≤ 0.04 |
  | | ΔE p95 | 0.0119 | 0.0329 | 0.0359 | ≤ 0.08 |
  | | edge-weighted | 0.0091 | 0.0215 | 0.0536 | ≤ 0.10 |
  | | SSIM-outside complement | 0.0197 | 0.0605 | 0.0926 | ≤ 0.07 → **≤ 0.16** ‡ |
  | `1x-light-increased-contrast` (confounded) | IoU complement † | 0.0000 | 0.106 | 0.234 | ≤ 0.15 |
  | | contour mean px † | 0.000 | 1.24 | 3.52 | ≤ 1.8 |
  | | contour p95 px † | 0.000 | 7.85 | 15.0 | ≤ 11.5 |
  | | SSIM complement | 0.0420 | 0.0815 | 0.122 | ≤ 0.14 |
  | | ΔE mean | 0.0143 | 0.0541 | 0.101 | ≤ 0.06 |
  | | **ΔE p95** | 0.1099 | **0.1716** | **0.1919** | ≤ 0.10 |
  | | edge-weighted | 0.0379 | 0.1208 | 0.225 | ≤ 0.17 |
  | | SSIM-outside complement | 0.0713 | 0.1493 | 0.228 | ≤ 0.20 → **≤ 0.31** ‡ |

  ‡ **Corrected beside, 2026-09-19 (the G1c Part B review closure; claims §5.151 §12).** The
  allowance column of this table was **transcribed** into `review-closure.py` by hand and four of its
  forty-eight texture rows were wrong. The distribution — every median, p90 and max above — is
  unaffected; only the allowance each is held against moves, and with it some statements of the form
  *this profile is over*. The four are marked in the rows above; the allowances are now **parsed**
  from `adopted-thresholds.test.ts` (`adopted_allowances.py`, imported by both review closures) and
  `review-closure.py`'s item 11 prints the audit against the original transcription, which is kept.
  The first output stands as `review-closure.v1.txt`.

  **The one consequence for this entry's ruling.** `2x-light-standard`'s SSIM complement is bounded
  at **≤ 0.07**, not the ≤ 0.11 written here when (a) was ruled, and the native-to-native
  distribution **exceeds it at the max (0.117)** while its **p90, 0.0646, is inside** at 0.92 of the
  allowance. So the light bed carries a tail on the wrong side too, of exactly the shape (a) already
  ruled on for dark: **a 2x-light SSIM miss at G3's read is a floor decision for the user, recorded**,
  not a bound loosened after the fact and not a fit failure. (a) is unchanged — it declared the 27
  tables at the 26.5 tables' *values*, whatever those values are, and the corrected value is the one
  the test file always carried. The other three corrections do not touch a ruling: `2x-light-standard`
  SSIM-outside was over at p90 and max already and stays over at ≤ 0.13; `1x-light-reduced-transparency`
  moves **inside** at ≤ 0.16 (worst cell 0.0926), which makes it the second profile inside every
  material row at its worst cell; and `1x-light-increased-contrast` is inside ≤ 0.31, which is
  Decision Log 5's subject and is noted there.

  † The three shape rows are given over the **healthy** cells throughout, median included: the §4
  cells whose two silhouettes differ by more than 2× are the extractor reading the level rather than
  a shape, and their
  tails run to an IoU complement of 0.997 and 116 px of contour p95 on every profile. The other five
  rows are over every pair the profile carries.

- **What the worst case implies for the recommendation, in both directions.** It is not this entry's
  to rule and the recommendation above is unchanged; what the numbers say is this. **For declaring
  the 26.5 values unchanged:** on the two light standard profiles and on reduced transparency the
  whole distribution of the version difference sits inside the allowance — ΔE mean p90 0.047 and
  0.048 against 0.07, and 0.009 against 0.04 — so on three of the five profiles the OS change is
  smaller, cell for cell, than the gap vitrea is already permitted, and a refit landing on the 26.5
  numbers is not being asked for something the delta itself exceeds. **Against it:** on both **dark**
  profiles the tail is on the wrong side — ΔE mean p90 0.111 and max 0.155 against an allowance of
  0.09, and edge-weighted p90 0.072 against 0.04 — which means that on the dark bed the two operating
  systems differ, on the worst cells, by more than vitrea is allowed to differ from either. A 27
  table declared at the 26.5 dark values therefore asks G3 to close a per-cell gap wider than the
  one the 26.5 table was fitted to, and if G3 misses it the miss is a floor decision for the user
  rather than a fit failure. The same reading argues that the dark profiles, not the light ones, are
  where G3's fit should be judged, and it is one more reason to declare **no** table for increased
  contrast: its ΔE p95 tail already exceeds its allowance and the profile is confounded.
  Neither direction is a reason to widen a bound before the read: a bound declared loose in advance
  spends fidelity the evidence has not said must be spent, and the wave's discipline records a miss
  rather than pre-empting it.
- **Increased contrast is confounded** (§5.150 Part B §3, §5.151 §9): the 27 bed of that name is a
  different accessibility state from the 26.5 bed of that name. A bound declared across that pair
  would be a bound on the decoupling. The recommendation is that G3 fits and reads it, records the
  numbers, and that the *adopted* bound waits for a pass that captures 27 increased-contrast with
  Reduce Transparency forced on — which is a capture decision for the user, not a fit.
- **No 27 regression floor in this wave**, unchanged: clause 4 already rules it out because the bed
  is at the seven-run probe bar and not the seventeen-run freeze bar (§5.150 Part B §4).
- `PREDICATE_EXCLUDES` and the per-profile cell counts will move; they are the machine's output and
  are transcribed, not chosen.

#### (d) Two things this entry does not settle, and one is the user's

- **What the level law is conditioned on.** (a) 1 refutes a constant and refutes backdrop mean
  alone; it does not identify the abscissa. W28 §Deferred already carries the identifying experiment
  and the charter puts it after this wave, on the 27 bed. G3 fits what it can and records the
  residual; whether the identifying sitting is brought forward is the user's.
- **Whether a second increased-contrast pass is captured** with the two toggles coupled as 26.5 had
  them, which is the only way to separate the material from the decoupling on that profile. It costs
  one short pass (the 26.5 profile is 32 cells) and one hand on System Settings.
### Decision Log 5 — RULED 2026-09-19 (the user, on G1c's draft below): the coupled increased-contrast profile gets its 27 tables at the 26.5 increased-contrast values, and G3 fits and reads it in this wave

**Ruled.** G3 declares, before its read, the 27 tables for
`apple-macos-27.0-1x-light-increased-contrast-coupled-glass0.5`, per tier, at the values of the
26.5 increased-contrast tables in `adopted-thresholds.test.ts` — the same form and argument as
Decision Log 4 (a) — and fits and reads that profile as its sixth. SSIM-outside complement's worst
native-to-native cell (0.237 against the 0.20 allowance) is a **named risk, recorded here and not
loosened**: a miss on that row at the read is a floor decision for the user, as a dark ΔE miss is
under Decision Log 4 (a). The decoupled profile of nearly the same name stays without a table (it
is confounded by the toggle) and is not read against vitrea until `compare`'s accessibility flag
keys on the profile key rather than the manifest (tracker entry from G1c Part B). G3 is told now,
mid-flight, and records the addition in §5.153.

**Beside the ruling, 2026-09-19 (the parent, on the G1c Part B review closure):** the risk this
ruling names was a transcription; the row has headroom at 0.237 against 0.31; the ruling is
unchanged. The adopted row is `ssimOutside ≥ 0.69` in `TEXTURE_TIER_INCREASED_CONTRAST`, so the
complement is **≤ 0.31** and not the ≤ 0.20 the draft was read against — the profile's worst
native-to-native cell sits at 0.76 of its allowance, inside like the other four material rows. There
is therefore **no SSIM-outside tail to carry as a named risk**, and the sentence above that does so
is superseded by this note rather than deleted, because it is what the user ruled on. What the
ruling itself says — the 27 tables declared at the 26.5 increased-contrast values, per tier, before
G3's read, with this profile fitted and read as the sixth — stands exactly as written, and the
evidence for it is stronger without the risk than with it. If G3 misses that row at its read it is,
as every other row is, a floor decision for the user. Claims §5.152 §B §11 carries the corrected row
and §5.151 §12 the audit that found it.

*G1c's draft, as put to the user:*

### Decision Log 5 draft — a table for the coupled increased-contrast profile (2026-09-19; G1c)

Drafted by G1c from `results/2026-09-19-w29-g1c-coupled/` and claims §5.152 §B. Nothing here is
ruled. Decision Log 4 (b) leaves this to the reading — "whether that profile then gets a bound is
decided on its reading, not now" — and the reading is in.

**What changed since Decision Log 4 (a).** That entry declared no table for increased contrast
because the only 27 profile of that name was captured with the transparency reduction off and could
not be compared like for like with the 26.5 bed. That profile is unchanged and still confounded.
`apple-macos-27.0-1x-light-increased-contrast-coupled-glass0.5` is not: it was captured in the state
macOS 26.5 forced, 32 cells at the seven-run bar, and its native-to-native read against
`apple-macos-26.5-1x-light-increased-contrast` differs in the operating system and nothing else.

**The evidence.** Decision Log 4 (c)'s table, for this profile, against the 26.5 increased-contrast
**texture-tier** allowance (§5.152 §B §11; shape rows over the healthy cells, as that table's are):

| metric | median | p90 | max | allowance |
| --- | ---: | ---: | ---: | ---: |
| IoU complement † | 0.0001 | 0.0358 | 0.275 | ≤ 0.15 |
| contour mean px † | 0.003 | 0.623 | 3.95 | ≤ 1.8 |
| contour p95 px † | 0.000 | 1.00 | 31.0 | ≤ 11.5 |
| SSIM complement | 0.0357 | 0.0717 | 0.115 | ≤ 0.14 |
| ΔE mean | 0.0086 | 0.0226 | 0.0392 | ≤ 0.06 |
| ΔE p95 | 0.0352 | 0.0506 | 0.0687 | ≤ 0.10 |
| edge-weighted | 0.0252 | 0.0632 | 0.110 | ≤ 0.17 |
| **SSIM-outside complement** | 0.0778 | 0.1553 | **0.237** | ≤ 0.20 → **≤ 0.31** ‡ |

† the three shape rows are the extractor reading a level rather than a shape and are over the
allowance on every profile in Decision Log 4 (c).

‡ corrected beside 2026-09-19 (review closure): the allowance was transcribed. `ssimOutside ≥ 0.69`
gives ≤ 0.31, so this row is **inside at 0.76 of its allowance** and is not the one row outside that
the draft below weighs. See the note beside the ruling above.

**Every material row is inside the allowance at its worst cell**, which is true of **only one other
profile, `1x-light-reduced-transparency`** (amended 2026-09-19: against its own corrected
SSIM-outside allowance of ≤ 0.16 its worst cell is 0.0926, so it is inside all five rows too; the
draft's "no profile that did get a table" was read off the transcribed column). Of the rest: the two
light standard profiles exceed ΔE mean at their max — and `2x-light-standard` its SSIM complement at
the max as well, against the corrected ≤ 0.07 — both dark profiles exceed ΔE mean and edge-weighted
at p90 and max, and the **decoupled** increased-contrast profile exceeds ΔE p95 at p90 (0.1716) and
max (0.1919) where this one reads 0.0506 and 0.0687. The confound is what put that row over.

**The question for the user.** Does G3 declare the 27 tables for this profile, per tier, at the 26.5
increased-contrast tables' values, before it reads its fit — the same form and the same argument
Decision Log 4 (a) ruled for the five unconfounded profiles?

The parent's recommendation is **yes**, with two things weighed openly:

- It **adds a sixth profile to G3**, which was chartered on five and is already running. The
  alternative is to hold the table for a later child and let G3 fit the five it has. This is a
  scheduling call, not an evidential one; the evidence does not expire.
- ~~`SSIM-outside complement` has a max of **0.237** against ≤ 0.20 — the one material-adjacent row
  whose worst cell is outside, and the one line of the draft that is a prediction rather than a
  measurement, because the outside region is where the rim's disappearance lands. The
  recommendation is to declare the table anyway and record that tail as a named risk in the ruling,
  which is the shape Decision Log 4 (a) already chose for the dark profiles' ΔE tail: a miss there
  is then **a floor decision for the user, recorded**, and not a bound loosened after the fact.~~
  **Withdrawn 2026-09-19 (review closure):** the ≤ 0.20 was a transcription of an adopted row that
  reads `ssimOutside ≥ 0.69`, so the allowance is ≤ 0.31 and 0.237 is inside it. There was no row
  outside and nothing to weigh here; the first point — that this adds a sixth profile to a running
  G3 — was the whole of what the user had to weigh, and the ruling stands.

Not proposed, and named so that silence is not read as an omission: **no regression floor** (the bed
is not captured at the seventeen-run bar, Decision Log 1 (iii)), **no table for the decoupled
profile** (still confounded, and its `as-captured` web mode is wrong for it until the tracker's
`compare` item lands), and **no mechanism** for any law — §5.152 §B measures, it does not fit.

### Decision Log 6 — RULED 2026-09-19 (the user, on G3's draft below as reviewed): the shadow and the recede are fitted before the landing; the large spans and the chromatic transmission are residuals with their own later charters

**Ruled.** (a) **The outer shadow** — seven of the fifteen missed rows — is read native-against-native
on the 27 bed with the shadow metrics `cli/measure.ts` already computes, the bar declared before the
pair is read as G2's was, and the `outerShadow` constants refit in the 27 documents; the documents
are re-sealed and **every 27 profile is re-read once under the new hashes** (calibration,
validation, holdout once), because a document change re-keys every row. (b) **The six largest-span
rows are accepted as residual**: they stay in `MISSED_27_ROWS`, no light SSIM row is re-pinned, and
the scale-selective scatter is a later wave's mechanism question, chartered from §5.153 §6. (c)
**The chromatic transmission** — four rows, one dark photo cell, and the eye's reading of the light
photo cells beside it — is **a child of its own after this wave**, with its own charter and bound;
its rows stay recorded as missed for 0.19.0. (d) **The 27 receded endpoints are fitted before G4**:
the browser driver flag the tracker proposes (`--receded-profile` against `web/scene.ts`'s existing
candidate seam) is built in the calibration package, the two 27 receded documents (per scheme) are
fitted on the inactive calibration rows, checked on validation, and the holdout read once, with the
bound declared before the read in the form and at the values W28's §5.134 §6 `bound.json` used for
the 26.5 recede — a miss is recorded for the user, as Decision Log 4 (a) shaped it. (e) (a) and (d)
are one child, **G3b, under claims §5.154**, dispatched now on the GPU; G4 opens after it. The
highlight's directionality stays unfitted and named (Decision Log 4 (a)'s unclosed law), its
web-side reader a tracker entry.

*G3's draft, as reviewed and put to the user:*

### Decision Log 6 draft — the fifteen missed rows, and the law nobody measured (2026-09-19; G3)

Drafted by G3 from `results/2026-09-19-w29-g3-refit/` and claims §5.153. Nothing here is ruled.
Decision Log 4 (a) already ruled the SHAPE of this entry — "a dark miss at G3's read is a floor
decision for the user, recorded, not a bound loosened after the fact and not a fit failure" — and
the review of §5.152 §B extended the same shape to a 2x-light SSIM miss. This puts the fifteen rows
that came out of the read to the user in that form, with the three causes separated, because they do
not want the same answer.

**Nothing was re-fitted after the holdout read**, no bound was widened, and no 27 regression floor
was adopted. `MISSED_27_ROWS` in `adopted-thresholds.test.ts` records each row with its reading and
the bound it misses, enforces no value, and fails CI if the set changes in either direction.

#### (a) Seven rows caused by a law that moved and that nobody has measured

Every `ssimOutside` miss, and the `ssimMean` misses that carry the same exterior. **Apple's outer
shadow changed on macOS 27**: on the 2x light bed `checkerboard__rrect-md__rest`'s native exterior
departure falls **0.01279 → 0.00284** and its falloff σ **35.8 → 18.8 device px**, while vitrea
draws the 26.5 shadow it was fitted to and the cell's `ssimOutside` falls 0.9943 → 0.8495. On
`checkerboard__rrect-lg__rest` it reads 0.7724.

G2 did not read the shadow axis — §5.151 §2 lists what it read — so **X3 forbade G3 from following
it**, and G3 did not. The options, and they are not exclusive:

- *Charter a short child that reads the shadow axis native-against-native on the 27 bed and refits
  the fifteen `outerShadow` constants.* The instrument exists: `cli/native-delta.ts` is the read and
  `cli/measure.ts` already computes the shadow metrics. This is the parent's recommendation — it is
  the one cause of the fifteen that is a genuine unmeasured law rather than a known residual, and it
  is cheap because no new capture is needed.
- *Accept the seven as residual for this wave*, with the tracker entry and the claim standing UNMET,
  and fold the shadow into the next recapture.

#### (b) Six rows at the largest spans, which are the residual W25 and W26 already named

`rrect-lg`, `rrect-ml` and `glass-over-glass` on the two light profiles. Their 26.5 twins read
0.97–0.99 and the 27 rows read 0.86–0.91. **Three of them were predicted**: §5.152 §B §12's review
closure, correcting this charter's own transcribed allowance column, found `2x-light-standard`
bounded at an SSIM complement of ≤ 0.07 with a native-to-native max of 0.117 outside it and a p90
inside, and said in advance that a miss there would be "a max-only miss, and a floor decision for the
user if G3's read lands there". It did land there, on exactly those three cells. §5.153 §6 measures
why: the 27 diffusion is conditioned on
the backdrop's **spatial scale** in a way one sharp-plus-heavy Gaussian mix cannot express — it
passes less at a 16 px pitch and more at both a finer and a coarser one — and `sizeToneLevelFar`,
the far-span level term, is still the constant W25 declined for want of a reading that could choose
its sign. Accepting these as residual and chartering the scale-selective scatter is the parent's
recommendation; re-pinning the two light SSIM rows is the alternative and is the user's.

#### (c) Two rows where vitrea's body is grey and Apple's is coloured

`photo__rrect-lg__rest :: oklabDeltaEP95` on both dark profiles and both tiers (four rows, one
cell). The level agrees — native 0.1814 against 0.1636 — and the interior's spread does not: 0.0417
against 0.0142. The sheets say it more plainly than the metric does: over a photograph the 27 dark
material carries the backdrop's **hues** and vitrea's renders a flat neutral panel.

**No constant in either document can close this**, and that is the finding rather than a complaint:
the tone response's solve is achromatic by construction — it shifts the neutral's luma and leaves
chroma alone, which the shader's own comment states — so a chromatic backdrop transmission is a
mechanism this material does not have. The question for the user is whether that becomes a child of
its own (the parent's recommendation) or waits behind the abscissa's identifying sitting, which it
is related to: §5.151 §5's "conditioned on something beyond the backdrop's mean" and this are
plausibly the same finding read two ways.

#### (d) What is NOT proposed

No bound is widened and no 27 floor is proposed — the bed is at the seven-run probe bar and clause 4
rules a floor out. The **coupled increased-contrast** profile needs no ruling at all: Decision Log 5
named its SSIM-outside tail as a risk and the profile clears every row of both tiers with room
(worst texture `ssimOutside` 0.946 against ≥ 0.69). That risk as drafted rested on a 0.20 allowance
transcribed from this charter; the table in `adopted-thresholds.test.ts` — which is what G3 declared
against, and the only copy the rule allows — permits a complement of 0.31.


### Decision Log 7 — RULED 2026-09-19 (the user, on G3b's draft below as reviewed): the span-graded shadow deferred to one operator wave after 0.19.0; the recede's miss recorded, and the fitted 27 endpoints are what G4 selects with 27

**Ruled.** (a) **The span-graded shadow σ is deferred**: 0.19.0 ships the 27 material with the
thin cells' shadow several times too wide (5.97× at 1x, 2.80× at 2x, energy correct; §5.154 §4 as
corrected by the review closure), recorded in the tracker and §5.154, and **one operator wave after
this one** carries both the span-graded shadow and the scale-selective scatter (§5.153 §6) under a
**single one-time X1 exemption** — the class Decision Log 1 (i) reserved — since each adds leaves to
the default and so moves every document's fingerprint, the frozen 26.5 pair included. Chartering
them apart would spend two exemptions. (b) **The recede's missed bound is recorded** as Decision Log
4 (a) shaped it — no re-fit, no widening, no floor — and **the fitted 27 receded documents are the 27
recede a page draws when G4 selects 27 by default** (Decision Log 2), with the chromatic-transmission
child (Decision Log 6 (c)) named as what closes the worst cell on every row; the alternative of
keeping 26.5's recede at runtime (level residual 0.1174 against 0.0102) is rejected. (c) G4 opens now
under claims §5.155.

*G3b's draft, as reviewed and put to the user:*

### Decision Log 7 draft — the shadow's blur stopped being span-invariant, and the recede's bound is missed (2026-09-19; G3b)

Drafted by G3b from `results/2026-09-19-w29-g3b-shadow-recede/` and claims §5.154. Nothing here is
ruled. Two items: **(a)** an operator question Decision Log 6 (a) deliberately left outside the
ruling, and **(b)** the bound Decision Log 6 (d) told this child to declare before the read and put
to the user if it missed. It did.

#### (a) The shadow's blur stopped being span-invariant, and one constant cannot hold it

Decision Log 6 (a) ruled the shadow's **constants** refit and contract X3 admits a new operator only
where a named structure needs one; this is the structure, measured, with the operator it would need
and the reason the child did not reach for it.

**What was measured.** `MaterialOuterShadow`'s own header records, as a positive finding of W8 and
W14, that the reference's three lengths are span-invariant on macOS 26.5: across spans 32, 44, 96
and 160 the fitted σ stays within 15.4–15.9 CSS px and "the size law reaches the amplitude
(`sizeGain`) and nothing else". On macOS 27 the same reading gives

| casting span | 32 | 44 | 96 | 128 | 160 |
| --- | ---: | ---: | ---: | ---: | ---: |
| native falloff σ, CSS px, 1x | — | 1.84 | 8.80 | 13.10 | 17.30 |
| native falloff σ, CSS px, 2x | 2.08 | 3.92 | 9.40 | 13.28 | 17.59 |

on the light standard bed, and the dark bed (1.52 / 8.87 / 13.42 / 17.43) and reduced transparency
(1.51 / 8.50 / 16.97) read the same law. It is linear in the casting span to within the reading's
own noise, σ ≈ 0.131 · (span − 26), and it is scale-invariant in CSS px at every span the amplitude
identifies. The downward offset grades with it — 4.0 CSS px at span 44 against 8.0 at 128 — where on
26.5 it was 7.95 everywhere. †

† **Restated beside, 2026-09-19 (review closure); the full recomputation and the cells every figure
comes from are claims §5.154 §4.** Each figure in the table is one cell — the plain
`checkerboard__<component>__rest` cell of its profile, except in the dark row, where 13.42 is the
span-**130** `glass-over-glass` stack and 17.43 is `light-solid__rrect-lg__rest`. **The law holds
above span 96 and not below it.** Least squares over the 1x light cells gives σ_css = 0.1334 · span −
4.013, zero at span 30.1, so the published intercept is wrong: at span 44 that law gives 2.36 against
1.84 read (−28 %), and on the 2x bed 0.79 against 2.08 at span 32 and 2.36 against 3.92 at span 44
(1.66×). **Restated: σ_css ≈ 0.133 · (span − 30) over spans 96–160**, where it holds to 0.1–0.5 % at
1x and 1.7–6.6 % at 2x and where the two scales agree to 1.3–6.8 %. Below span 96 scale invariance
fails outright — 1.84 at 1x against 3.92 at 2x, a ratio of 2.13, the device pixel ratio — so at the
thin spans the σ is nearly constant in device px, and at σ ≈ 1.8 CSS px a 1x capture leaves two or
three rings above the body's edge to fit it on, so whether that is Apple's material or the instrument
is not settled by this bed. **What the decision rests on is unchanged**: the thick regime is linear
and scale-invariant, one `sigmaPx` cannot serve both ends, and the thin-span behaviour is now stated
as open structure rather than as a law. The "about six times too wide" below is a **1x** figure —
11.0 against 1.84 is 5.97×, against the 2x span-44 reading of 3.92 it is 2.80×, and against the 1x
bed-wide median of 1.52 it is 7.2×.

**What that costs, and what it did not cost.** `sigmaPx` is one constant the shader reads from a
uniform, so no value of it is both 1.8 and 17.4. The 27 documents carry 11.0, which is the best
single value the thick regime admits and which is where every row Decision Log 6 (a) exists to clear
sits; the thin cells keep a shadow about six times too wide. The amplitudes fitted beside it absorb
the energy error — the bed-wide departure residual is 0.0007 against 0.0068 before — so the fit is
not visibly wrong on any metric this bed gates. It is wrong in the SHAPE of the thin cells' exterior
and that is what this entry is about.

**The two options, and they are not exclusive.**

- *Charter a span grading on the shadow's length, profile-gated.* It is smaller than the last two
  operator questions this wave has raised: the casting surface's span is already in the shader as
  `shadowAux.z` and `outer_shadow_thick` already grades the amplitude by it, so the change is that
  interpolation applied to `ou.shadow.y`, new leaves under `outerShadow`, the CSS tier's mirror, and
  `outerShadowReachPx` taking a maximum over spans for the pad. Gated at a default that reproduces
  today's behaviour, `DEFAULT_MATERIAL_PROFILE` and both 26.5 documents resolve bit-identically and
  the 34 goldens do not move. ‡ **This is the parent's recommendation** if the 27 material is to be
  the shipped default: the thin cells are the common case in a real interface, and a capsule button
  is where a user sees the shadow most.

‡ **Split beside, 2026-09-19 (review closure): the pixels would not move, the fingerprints would, and
that is a cost this option has to carry.** The claim conflated two things. **What holds:** at a
default that reproduces today's behaviour the rendered material is unchanged, so the **34 renderer
goldens stay byte-identical** and nothing any page draws moves — that half is a pixel statement and
it is right. **What does not:** `resolvedMaterialSha256` is not a digest of the patch, it is
`seal.ts`'s `fingerprint` — a canonicalising SHA-256 over the **whole resolved material**, keys
sorted — and `test/tuned-profiles.test.ts` pins it for all four documents by re-resolving each patch
over `DEFAULT_MATERIAL_PROFILE`. New leaves under `outerShadow` on the default therefore appear in
every document's resolved object and **change every one of the four digests, at any default value**.
Two of those four documents are the 26.5 pair, and they are **byte-frozen** in
`results/2026-09-16-w29-freeze/sha256.txt` (entries 652 and 653 of 1,818), so re-recording their
digests edits frozen 26.5 evidence. **The cost, stated plainly: a one-time X1 exemption — the class
Decision Log 1 (i) named and reserved for a later wave — is part of this option's price, and it is
the user's to grant.** The same price applies to the scale-selective scatter operator claims §5.153
§6 names, for the same reason and in the same amount: it too adds leaves to the default. **So one
operator wave carrying both structures spends one exemption where two waves would spend two**, which
is a reason to charter them together beyond the one the second option below already gives.
- *Accept it as residual for this wave*, with the tracker entry standing and the ledger's §5.154
  naming it, and fold it into the operator wave that already owes the scale-selective scatter
  (§5.153 §6). Both are "one operator cannot express a structure the bed measures"; chartering them
  together is cheaper than chartering them apart, and the shadow's is the better-identified of the
  two.

**What is NOT proposed.** No bound is widened and no 27 floor is proposed — clause 4 rules a floor
out on every 27 profile. No 26.5 **measurement** moves under either option and neither option changes
what the runtime draws by default, which is still the 26.5 material until G4 — but under the first
option the two frozen 26.5 documents' recorded digests do move, for the reason ‡ gives, and that is
the exemption the user is being asked for rather than a detail.

#### (b) The 27 recede's bound holds on one of twelve, and what the miss actually is

Decision Log 6 (d) ruled the bound declared before the read "in the form and at the values W28's
§5.134 §6 `bound.json` used for the 26.5 recede — a miss is recorded for the user, as Decision Log
4 (a) shaped it". It was declared (`bound.json`, committed `3e9975b0`, before either endpoint was
fitted), the read happened once at the sealed documents, **nothing was re-fitted afterwards, no
threshold was widened and no floor was adopted**, and the full table is claims §5.154 §8. This is
the record of the miss, in the shape that entry gave it.

**The verdict.** Clauses 1, 2 and 3 hold jointly on **one of twelve** profile-tiers —
`1x-light-reduced-transparency` on the texture tier. Clause 1 (the per-cell ceiling at the active
material's own adopted bound) holds on seven of twelve and misses by 3–13 % where it misses. Clause
2 (the per-profile mean interior ΔE) misses on eleven. Clause 3 (no cell past twice clause 2's
threshold) misses on ten.

**What it is not.** It is not the fit failing on the regime it was fitted for: the inactive body's
level residual went **0.1174 → 0.01017** and on the seventeen inactive calibration rows the worst
cell is +0.033. Nor is it a CI gate: the adopted gate drops the inactive pose on every set, so
`pnpm -r test` is green with this bound missed, exactly as it was green with the 26.5 recede at
0.1174.

**What it is, on the evidence.** Clause 3's worst cell is `photo__rrect-lg__inactive` or its tinted
twin on **every one of the twelve rows**, and that is two things at once.

- *A span the fit never saw.* The inactive calibration set's largest span is 96 and `rrect-lg` is
  160. W27c G1b built the checking set that way on purpose ("deliberately weighted toward the large
  components the fit never saw"), and the 26.5 endpoint was asked in the same way and passed.
- *The mechanism neither document has.* It is the same cell that carries four of the seven
  remaining ACTIVE misses, for the reason Decision Log 6 (c) already chartered a child over: over a
  photograph the 27 material carries the backdrop's hues and vitrea's renders a flat neutral panel,
  and the tone response's solve is achromatic by construction. The recede inherits it because the
  recede is a difference over that material. **A 27 receded endpoint cannot pass a clause whose
  worst cell is a chromatic one until the chromatic child lands**, and that is the single most
  useful thing this read says.

There is also an honest asymmetry of effort to weigh: the 27 endpoints are three fitting rounds by
one child on one bed, where the 26.5 endpoint is the accumulated work of W27c G1, G1c, G1d and W28
G1 — four gates, four sittings and two model-form questions settled. Two residuals this child
measured and did not fit are named in §5.154 §6 and §9 and are candidates for the next round: the
receded tint shade, whose least squares wants a negative dark end the law cannot represent, and the
receded body's diffusion, which passes far more of the backdrop's structure than the reference does
and has no inactive calibration cell above span 96 to be fitted on.

**The options.**

- *Record the miss and ship the endpoints as calibration documents anyway.* Nothing regresses: the
  runtime still uses the 26.5 recede (G4 owns the selection, Decision Log 2), the documents are
  evidence and a starting point, and the ledger carries the verdict. **This is the parent's
  recommendation** — the endpoints are an order of magnitude closer than what ships and the bound's
  worst cell is blocked on a child that is already chartered.
- *Hold the 27 recede out of G4's selection until the bound holds*, which means at minimum the
  chromatic child plus one more receded fitting round, and means shipping the 27 active material
  with a 26.5 recede for at least one more release.
- *Re-pin the bound for the 27 bed* — for instance clause 2 at this read's own validation mean. It
  is the user's and nobody else's, and the parent does not recommend it: the whole value of carrying
  W27c's numbers across is that they were produced by an experiment that predated the bound, and a
  threshold computed from this child's own read is a bound this child could not have failed.

**What is NOT proposed here either.** No 27 floor, on the active or the inactive pose — clause 4
rules them out on every profile, and a floor over a bed at the seven-run probe bar is a promise the
bed cannot keep.



## Surprises & Discoveries

- **`backdropToneMax` is not the adaptation's amplitude — it is the response law's gate as well**
  (2026-09-19, G3; §5.153 §2). The obvious way to stop the material converging on a dark backdrop is
  to turn the adaptation's cap down, and it is wrong: `renderer.ts` passes
  `backdropToneUnderPolicy(policy, material) * material.backdropToneMax` as the ONE uniform the
  shader reads for both the adaptation and the response solve's strength, so a document naming
  `backdropToneMax: 0` disables the very law being fitted. Measured rather than reasoned — at max 0
  the dark cells render the bare material at 0.4766 against a reference of 0.2899, and the fitted
  response rows have no effect at all. The band (`backdropToneLow` / `High`) is the lever, and the
  general lesson is the one this project keeps relearning: a constant that gates two laws cannot be
  read as belonging to either.

- **macOS 27's outer shadow shrank by half and weakened by four times, and nothing in this wave was
  looking at it** (2026-09-19, G3; §5.153 §5). The 2x light `checkerboard__rrect-md__rest` fixture's
  native exterior departure goes 0.01279 → 0.00284 across the two beds at a falloff σ of 35.8 → 18.8
  device px. G2's native delta read nine laws and the shadow axis is not one of them, so by X3 the
  refit could not follow it, and it is now the named cause of seven of G3's fifteen missed rows. The
  procedural lesson for the next recapture: **the laws a native-against-native read enumerates
  become the laws the refit is allowed to move**, so an axis left out of the read is an axis the
  wave cannot fix however plainly the fidelity read then shows it.

- **Over a photograph Apple's 27 material carries the backdrop's colour and vitrea's body is grey**
  (2026-09-19, G3; §5.153 §9). Strongest on the dark bed at the largest span, where the amplified
  difference is the whole body rather than its edge, and it is the same cell as the four dark ΔE p95
  misses. No constant in either document can close it: the tone response's solve is **achromatic by
  construction** — it shifts the neutral's luma and leaves chroma untouched, as the shader's own
  comment says — so a chromatic backdrop transmission is a mechanism this material does not have.
  The metrics saw a tail; the eye saw what it was.

- **Every 27 profile's near-tone cells came back** (2026-09-19, G3; §5.153 §8). `NO_SHAPE_AXIS_SCENES`
  is empty for all six 27 profiles on both tiers, where the 26.5 profiles list the cells whose
  material sat inside the extractor's 0.02 threshold of its own backdrop and yielded no contour at
  all. The refitted material follows the reference out of that threshold, so the shape axis exists
  on every cell of the 27 bed. `PREDICATE_EXCLUDES` tells the same story in reverse: the `dark-solid`
  cells that failed the conditioning predicate on 26.5 pass on 27, and `light-solid` and `photo`
  cells fail that did not.

- **Apple changed the increased-contrast material MORE than the ordinary one** (2026-09-19, G1c;
  §5.152 §B §8). Read like for like — both toggles on, as macOS 26.5 forced — the coupled bed's
  median `interiorStdDevDelta` is **0.152** against 0.0098–0.014 on the five unconfounded profiles,
  its `rimContourDeltaMax` **0.898** against 0.011–0.111, and its `transferSlopeDelta` **0.151**
  against 0.0053–0.041. Its whole-cell rows are unremarkable and sit inside the range those five
  span, so this is not a bed reading differently; it is three named laws moving an order of
  magnitude further on this accessibility mode than on the standard material. What the eye sees is
  one thing: on 26.5 the increased-contrast material is an opaque white slab with a crisp dark
  outline, and on 27 the outline is simply gone. Nobody planned to measure the accessibility
  material's own change — the second pass was captured to remove a confound — and it turned out to
  be the largest per-law movement anywhere in the wave.
- **The decoupling's share of a confounded reading can be measured, and it is not uniform**
  (2026-09-19, G1c; §5.152 §B §7). With both 27 contrast profiles declaring the same 32 cells over
  one 26.5 counterpart, the two readings differ in exactly one thing, and the laws split cleanly:
  the **rim and the scatter moved by the same amount in both states to within 1 %** — they are the
  material — while the **level, the tone offset and the tint's lightness read two to five times
  larger on the decoupled profile** than the material alone accounts for. §5.151 §9's blanket
  refusal to attribute anything on that profile was right, and it was coarser than the evidence
  now allows: a confound is not a switch that voids a whole profile, it is a term whose size can be
  estimated once the controlled capture exists.
- **A bar taken over one pass is not the bar the rule means** (2026-09-19, G1c; §5.152 §B §1 and
  `bar-declaration.md`). G2's zero-spread fallback is "the bed's smallest non-zero pairwise max",
  and on 624 cells that is a well-supported constant. On this sitting's 32 it is **undefined on
  twelve metrics**, including every geometry metric — so the rule read literally off the subset
  would give those twelve a bar of exactly zero, the sharpest instrument there is, on the thinnest
  possible evidence for it. Taking the minimum over the whole 27 bed instead (read from G2's
  committed figures, never re-derived) changes **15 of 929 verdicts**, every one of them from
  not-moved to moved, and every one on floating-point dust of order 10⁻¹⁵, a single 22.5° highlight
  bin, or exactly 1 px of silhouette. The general lesson: a bed-wide constant inside a rule is a
  statement about the bed, and a later sitting that re-derives it from its own share quietly
  changes the rule while appearing to follow it.
- **`compare` cannot tell macOS 27's two contrast states apart** (2026-09-19, G1c; §5.152 §B §10).
  `webAccessibilityFlags` maps the **manifest's** `a11yMode` to the web side's flags, and that field
  is `SystemAccessibility.current`, which answers only "is contrast on" — both 27 contrast profiles
  record `increased-contrast`. Its default `as-captured` mode therefore renders both flags for both
  of them: correct, and now *measured*, for the coupled profile, and **wrong** for the decoupled one,
  whose like-for-like mode is `contrast-only`. Nothing committed is affected, because no 27 contrast
  profile has been read against vitrea. Tracked with its fix shape (key on the profile KEY's
  `a11yMode`, which the grammar now distinguishes), to land with or before the first such read.

- **On macOS 27 Apple's glass over near-black is no longer invisible, and the shape axis is how we
  found out** (2026-09-19, G2; §5.151 §4). 66 of 619 cells have one bed's silhouette under half the
  other's, 55 of them with the small one on 26.5 and over `dark-solid` (32), `impulse` (11),
  `light-solid` (6), `mid-dark-solid` (4), `hc-text` (1) and `hc-text-7` (1) — the last two were
  dropped from the first writing of this list, which summed to 53 (review closure 2026-09-19).
  The extractor calls a pixel "inside" when it differs
  from the backdrop by 0.02 linear, and on 26.5 Apple's material over those backdrops did not:
  `dark-solid__rrect-64__rest` is **52 px** of silhouette on 26.5 and **6,996 px** on 27. The
  tracker already carries this as a fault of the shape axis — and on this read it is not noise, it
  is the finding. These are the very cells the user's eye named on the W22 sheets ("glass on black
  invisible where Apple's shows an outline", §5.99), and two waves of vitrea work were fitted to a
  reference that has moved on exactly them. The procedural consequence: every silhouette-masked
  statistic on those cells is taken over a few dozen pixels, so the level and tone laws are read
  from the mask-free `bodyLevel` instead.
- **The noise bar reproduced the sitting's publication record without being asked to** (2026-09-19,
  G2; §5.151). The number of cells whose whole-cell metrics have exactly zero spread across all 21
  within-bed pairs is **505 of 624** — precisely the count of cells the plurality published
  unanimously across all seven runs (§5.150 Part B §4). One construction works on bytes and the
  other on SSIM, ΔE and edge-weighted difference, and they agree cell for cell. Nothing planned
  this; it is the strongest single check the gate has that the bar is reading the bed it thinks.
- **Three metrics have no resolution in this bed at all** (2026-09-19, G2; §5.151). Across 13,104
  within-bed pairs, `contourDistanceP95Px`, `oklabDeltaEP95` and `rimPeakDepthDeltaPx` never once
  took a non-zero value: a 95th percentile over pixels and a rounded ring index are both blind to
  the handful of pixels a re-run moves. The declared zero-spread fallback — the bed-wide minimum
  non-zero spread — is undefined there, so the rule was extended **before the first pair was read**
  to make the bar exactly zero, which is the strongest of the three levels rather than the weakest.
  The general lesson: a bar taken from a bed's own variation has a floor, and a metric can be so
  coarse that the bed's variation never reaches it.
- **A hue is not a reading until its chroma is stated** (2026-09-19, G2; §5.151 §7). `tintResponse`
  documents that its hue is meaningless at near-zero chroma, and the bed splits exactly there: 456
  of 569 cells have an interior chroma under 0.01 on both beds and a median hue "shift" of **82°**,
  while the 113 cells at chroma ≥ 0.01 read **1.0°**. Read bed-wide the row says the tint's hue
  swung 67°; read on the cells where a hue exists it says the hue did not move. The bar cannot catch
  this — the readings are real numbers and they clear it — so the condition has to come from the
  metric's own documentation, which is an argument for keeping such conditions next to the code.
- **macOS 27 does NOT couple Reduce Transparency to Increase Contrast** (2026-09-19, G1 Part B;
  §5.150 Part B §3). On 26.5 Increase Contrast force-enabled Reduce Transparency and the
  transparency checkbox could not be uncleared while contrast was on (user-verified 2026-08-29), so
  the coupled state was the **only** reachable increased-contrast state and the 26.5 profile carries
  a caveat saying its fixtures were captured in it. On 27 all seven increased-contrast runs attested
  `increaseContrast=1` with `reduceTransparency=0`, and the harness — which records the coupling as
  a profile caveat whenever it observes it — wrote **no coupling note in any of the 56 run
  manifests**. So the 27 increased-contrast bed is a *different state* from the 26.5 bed of the same
  name, and a difference between the two on that profile is confounded with the decoupling until
  they are separated. Recorded as a caveat on the two 27 accessibility profile entries, **beside the
  26.5 caveat and never over it**; the evidence under it is an attestation, not a note. Whether the
  coupling's disappearance is itself a change G2 should read is a question for G2's charter.
- **The backdrops did not move across the OS change** (2026-09-19, G1 Part B). All 32 background ids
  the 27 runs composited over are byte-identical to the rasters the 26.5 bundle indexes — 896
  comparisons, zero disagreements. The harness draws them with its own CoreGraphics generators and
  nothing guaranteed 27 would rasterise them identically; had they moved, `materialize` would have
  refused the publication rather than filing cells under a backdrop they were not drawn on. G2
  therefore reads 27 against 26.5 over the same backdrop bytes on both sides.
- **The 27 bed is five cells LARGER than the 26.5 bed** (2026-09-19, G1 Part B). Clause 2 asks for a
  shortfall named cell by cell and there is none: every profile filed exactly what it declared, and
  the five cells 26.5 declared but never published were all captured on 27 — including
  `light-solid__capsule-button__inactive` on both dark profiles, which claims §5.148 §1 found the
  26.5 publishing run passing over. A 27 cell with no 26.5 counterpart is one G2 reports rather than
  diffs.
- **A whole-file hash of the shared manifest could not survive a second bed** (2026-09-19, G1 Part B;
  §5.150 Part B §8). `freeze.py` hashed `fixtures/manifest.json` as one file, which was right while
  the bundle held one bed and became unable to say anything useful the moment the 27 profiles'
  entries went into it — it can only report "the 26.5 bed moved" for a change that added a bed beside
  it. It now hashes the manifest per unit, exactly as it already hashed the matrix per 26.5 row. The
  superseded whole-file reading is recorded, and the amendment is proved against the pre-publication
  snapshot three ways before it was adopted. **The general lesson for X1: a freeze over a file two
  beds share has to be a freeze over the units of that file, not over its bytes.**
- **`materialize` could not publish a profile the bed had never held** (2026-09-19, G1 Part B). On
  the new-key path it copied every PNG in and skipped the manifest silently — fixtures on disk the
  record does not describe. Unreachable until this wave, because every prior publication appended to
  a profile the bundle already declared. Fixed with the entry built from the runs' own headers and a
  refusal when they disagree.
- **The 27 material is not SDK-gated, on pixels: the two bundles produce identical bytes**
  (2026-09-18, G1 (4); §5.150 §6). Sixteen of sixteen attested cell pairs carry the same SHA-256
  from a binary recording `LC_BUILD_VERSION` `sdk 26.0` and one recording `sdk 27.0`, on one
  machine at slider 0.5, and each arm's own run-to-run spread is **zero** on every cell and every
  metric — so the arms are not inside a bar, there is no bar. G0's residual is closed; X4 stands.
  The **inactive** half is not taken and is tracked: the side arm's inactive run failed after one
  cell while it held the grant and re-granting costs the harness's.
- **The side arm's `active-r3` is not evidence, and its own record says why** (2026-09-18, G1's
  audit of the parent's banked runs). It attested 3 of 8 cells: five recorded
  `presentedActive: false` at 1.0–4.7 s of HID idle. Six of its eight cells differ from the harness
  arm; the five pose-lost ones and one that attested the pose at **0.1 s** of idle. Its only two
  undisturbed cells (255 s and 246 s of idle) are byte-identical to the harness arm. Every
  disagreement in the comparison is a run's own record of being disturbed, and none is the bundle —
  which is the concrete case for the per-run audit and for reading it on a probe, not only a bed.
- **The harness cannot be narrowed to one profile, and with two beds declared that costs the
  sitting twice over** (2026-09-18, G1 (1); §5.150 §2, tracker). `main.swift` selects profiles by
  accessibility mode and display scale and by nothing else; `--scenes` narrows cells. So a 2x
  standard pass against the canonical version-6 declaration selects **four** profiles and would
  write 27 pixels into `apple-macos-26.5-…` directories inside the run snapshot. The fix that needs
  no rebuild is a 27-only specification derived from the canonical file at every pass's opening
  (`pass-spec.py`), refusing unless the canonical file still declares exactly what clause 2 names.
- **The only thing the granted bundle reads out of a profile key is `-<scale>x-`** (2026-09-18,
  G1 (1)). The colour scheme and the accessibility mode come from the profile's own declared JSON
  fields, and the key is otherwise used verbatim as a directory name and a manifest value. So the
  slider token's placement is free as far as X4 is concerned, and the charter's trailing form is
  adopted because it leaves every earlier axis at the offset a 26.5 key has it at — which keeps
  `key.includes("-2x-")`- and `endsWith("-standard")`-shaped readers meaning what they meant.
- **Show Borders is `ButtonShapesEnabled` in `com.apple.Accessibility`** (2026-09-18, Decision Log
  3 (b) executed): the user's toggle changed exactly that key, 0 → 1 and back, against a 635-domain
  snapshot; nothing in `com.apple.universalaccess` or the global domain moved. G1 attests it per run
  from that key. The name is iOS's "Button Shapes"; macOS 27 surfaces it as Show Borders.
- **Two bundles with one identifier cannot both hold Screen Recording; the grant moves** (Decision
  Log 3 (c) executed, and the risk it named materialised). The first attempt toggled the existing
  "VitreaReference" row and the side bundle stayed denied. Adding the side bundle's path explicitly
  with **+** granted it — and the granted harness bundle, which had captured minutes before, was
  refused on the next capture: TCC keeps one row per identifier and the last code hash added owns
  it (grant checks under `~/vitrea-w29-g0-scratch/grant-check/`). The parent used the window: the
  pixel arm's side-sdk27 half was captured at slider 0.5 while the side bundle held the grant
  (`~/vitrea-w29-g0-scratch/sdk-pixel/side-sdk27/`, three runs per pose), and the grant is moved
  back to the harness bundle by the user re-adding its path before G1 opens. G1 captures the
  harness half of the same cells and diffs the two. **X4's rule now has a sharper reason**: any
  bundle added under the harness's identifier evicts the harness's grant.
- **The slider is a material parameter, not a policy.** `NSGlassTintAmount`'s value appears
  unrounded as the glass filter's `inputBlurFillNormalOpacity`, and the face fill's alpha follows a
  two-segment ramp whose knee is at 0.5 — which is also what the material draws when the key is
  absent. So 0.5 is the designed centre, readable from the material rather than from the pane
  (G0 (d); §5.149 §4).

- **`LC_BUILD_VERSION`'s `sdk` field does not record the SDK a `build.sh` bundle compiled against.**
  `swiftc` links through `clang` with `--sysroot`, which loses the SDK version, so `ld` writes
  `sdk == minos`. The granted bundle's `sdk 26.0` is therefore not evidence that it links 26.5, and
  a side build meant to carry a different linked SDK has to say so with an explicit
  `-platform_version` (G0 (a) and (c); §5.149 §1).

- **macOS 27's Show Borders has no preference key and no SDK property on this machine**, so the
  axis the charter plans to attest "off in every run" cannot be read by anything that can refuse
  (G0 (a); §5.149 §1).

- macOS 27 backs the appearance slider with a global float, `NSGlassTintAmount`, and the installer
  (or the user's hand at the prompt) left it at 0.5459057, not 0.5 (Decision Log 2; G0 read it and
  restored it to that value).

- The 26.5 bed's Liquid Glass Clear/Tinted state was never recorded and no key for it exists in any
  preference plist on the machine; the bed stands at Apple's 26 default by inference (Grounding).

## Outcomes & Retrospective

**CLOSED 2026-09-20 by G4's landing**, against the seven Parent-Level Acceptance clauses, on this
wave's branch at the 0.19.0 version head. The cut is prepared and unpublished: `pnpm release` is
the user's hand and the tag follows it.

**0.19.0 PUBLISHED 2026-09-20, by the user's `pnpm release` on `26250e97` (the head after the
parent's final chain record; the version bump itself landed on G4's branch at `b203b997`); tag
`v0.19.0` (annotated, on that commit) pushed by the parent on 2026-09-20.** Registry: core
04:40:24.213Z, react 04:40:27.518Z, web 04:40:46.317Z — **not dependency order this time**: the
React binding, which requires `@vitreajs/vitrea-web@^0.19.0`, was listed 19 s before the web
package was, so the release-chain window that 0.17.0 and 0.18.0 avoided recurred for 19 s; no
install was attempted inside it. First-to-last span 22 s against 36 s at 0.18.0. `changeset
publish` was run once, as before, and the order was not chosen. The publish is the user's
acceptance of the React driver-timing red the parent's final chain disclosed (`press.spec.ts` on
chromium, recorded in the tracker and in the Revision Note of 2026-09-20 below, not rerun).
Verified by a cold install outside the workspace after all three were listed (`--prefer-online`):
all three at 0.19.0, ranges rewritten to `^0.19.0` (web → core; react → core and web), no private
`@vitrea/*` package installed, all three entry points import — core **44** exports (unchanged),
web **256** against 247 at 0.18.0, react **37** (unchanged) — with `macos27MaterialProfileDocument`
and `macos26MaterialProfileDocument` both exported from the web package, the shipped 27 document
named `apple-macos-27.0-glass0.5` / `macOS 27.0` with `active` and `receded` endpoints per scheme
and its `cssTierMapping`, and the shipped 26.5 document's light endpoint carrying
`resolvedMaterialSha256` `b2b570e4adcea8fb` — the frozen digest, unchanged. Installed 1 848 /
1 880 / 644 kB (core / web / react) against 1 848 / 1 816 / 628 at 0.18.0.

The one-line result of the wave: **Apple's material moved on every one of 619 cells and Apple's
geometry did not; vitrea is refit to it on both tiers, seven declared rows are recorded missed
rather than met and the recede's declared bound is recorded holding on one of twelve
profile-tiers, and a page draws the macOS 27 material by default from 0.19.0 — by selection, with
`DEFAULT_MATERIAL_PROFILE` and both macOS 26.5 documents untouched.**

### The acceptance, as verified

1. **The 26.5 evidence is untouched — MET.** The hash manifest was written and committed before G0
   opened, and `results/2026-09-16-w29-freeze/freeze.py verify` reports **intact at 1,818 entries**
   at this close as it did at every child's merge. The 26.5 adopted bounds, regression floors and
   `PREDICATE_EXCLUDES` keep running against the 26.5 rows unchanged, which is what proves those
   rows were not edited; `tuned-profiles.test.ts`'s `resolvedMaterialSha256` pin over the runtime
   default plus the 26.5 light patch stays green; `DEFAULT_MATERIAL_PROFILE` did not move and the 34
   renderer goldens are byte-identical at the version head. **Two things G4 changed are named here
   rather than left to be discovered**, because both are about the macOS 26.5 material without being
   26.5-keyed paths, rows, bounds, floors or documents: `MATERIAL_OPTICS` stopped being what a root
   draws and is now the renderer's own identity with that stated in its doc comment, and
   `samplingPaddingFor` — public API a layout calls — now derives from the default document rather
   than from those constants. X1 is about committed evidence and is intact; these are about what the
   runtime resolves, which is exactly what clause 5 moved.
2. **The 27 bed exists under new keys with every pixel-moving axis attested per run — MET.** 624
   cells over six `apple-macos-27.0-…-glass0.5` profiles at seven runs per cell, **no shortfall and
   no refusal**, with the per-run manifest carrying OS build, slider position, Reduce Transparency,
   Increase Contrast, Show Borders, scale, scheme, pose and the capturing bundle's own linked SDK,
   and `materialize` refusing a manifest whose parsed OS and build disagree with its key (§5.150).
   The axis macOS 27 introduced was found rather than assumed: `NSGlassTintAmount` has no public
   API and no documented key, G0 found it by diffing 635 preference domains across the update,
   measured that it moves 10 of 10 probe cells with no GUI, and established that the system's own
   default is 0.5 by deleting the key and reading what the material rendered at (§5.149). It is the
   trailing token of every key in this bed (X6). A seventh profile joined mid-wave under Decision
   Log 4 (b) — the coupled increased-contrast pass, 32 cells, whose own two-toggle refusals are
   §5.152's.
3. **What 27 changed was measured before anything was changed — MET.** The noise bar was defined
   per metric from the 27 bed's own run-to-run behaviour and **committed before the first macOS 26.5
   pair was opened**, twice: once for G2's thirty-two metrics and again for G3b's shadow axis, whose
   instrument reproduced G2's over 190,683 readings with zero disagreements. 619 pairs read, sheets
   per profile at both scales, and **no vitrea change in either child** (X3). The reading is that
   Apple moved the level, the tone response, the diffusion, the rim band, the highlight, the tint's
   lightness, the recede and the outer shadow, and did not move the silhouette, the corner or the
   tint's hue.
4. **vitrea is refit to 27 on the fidelity target — MET, with seven rows recorded UNMET.** Bounds
   were declared before any fit was read, as aliases of the 26.5 constants rather than as
   transcriptions (X5); **no 27 regression floor was adopted on any profile**, and a case enforces
   that rather than remembering it; holdout was read once per frozen configuration; the CSS tier was
   derived from the same documents in the same commits. The whole-cell ΔE mean roughly halves on
   every standard profile on both tiers and the p95 tail falls 0.46 → 0.14 light and 0.27 → 0.17
   dark. Fifteen declared rows missed at G3's read and **eight cleared at G3b's**, all eight the
   outer shadow's; the seven that remain are all holdout cells and are the two causes Decision Log 6
   accepted as residual. The recede's declared bound holds on **one of twelve profile-tiers** and
   the miss is recorded rather than re-fitted, widened or floored (Decision Log 7 (b)).
5. **Which material a page gets is a stated decision — MET.** Decision Log 2 ruled it in advance,
   and G4 executed it exactly as Decision Log 1 (i) required: as a **selection** between patch
   documents over an unmoved default, not as a moved constant.
   `createGlassRoot({ materialProfileDocument })` takes one whole measured material — the active
   patch and the receded difference per colour scheme plus the CSS crossing —
   `macos27MaterialProfileDocument` is the default, `macos26MaterialProfileDocument` is shipped
   beside it, and `<GlassRoot materialProfileDocument>` surfaces it in React. Both package READMEs
   say what a page draws by default and how to pin macOS 26.5. The honesty half is in the runtime:
   `root.material` and every resolved `GlassGroupState` name the endpoint that drew, its digest and
   whether the app tuned it, and the browser suite pins the renderer's own resolved digest to each
   document's recorded `resolvedMaterialSha256` (§5.155 §2).
6. **The landing — MET.** 27 rows in the canonical matrix per tier, `adopted-thresholds` blocks for
   the 27 profiles beside the 26.5 blocks, the demo's fixture pair and `pages.yml`'s assertion on
   the 27 light standard directory, the coverage matrix re-scored 2026-09-20 with two rows moving
   upward, `CLAUDE.md`, both package READMEs and the ledger's pointers, and the fixed group
   versioned to **0.19.0** and prepared with the publish dry runs green. One consequence is recorded
   rather than smoothed over: the site's reference pair shows the **macOS 27 bed only**, because a
   material document is selected at construction and the site has one root, so it cannot present
   both beds at once (`apps/demo/src/site/scenes.ts`, `packages/platform-web/README.md`).
7. **By eye, and the ledger — MET.** Native | WebGPU | difference sheets at G3's and G3b's reads,
   and at this landing the demo beside the macOS 27 harness capture at 2× in both schemes and both
   poses, plus a real window-manager focus change read over bare CDP (§5.155 §7). Every gap to macOS
   is a named line: in a claims section, in this wave's Deferred below, or in the tracker.

### What the wave cost, and what it found

The bed cost days of machine unavailability rather than hours of work, which the Grounding Baseline
priced and `sitting.md` records. What it bought is a reference that is current rather than
superseded, and five findings that no amount of reading the announcement would have produced: that
Apple's adaptation band is **inert** on macOS 27 — the material no longer vanishes into a dark
backdrop anywhere on this bed — that the diffusion change is **conditioned on the backdrop's spatial
scale** and on the colour scheme rather than uniform, that the outer shadow's blur **stopped being
span-invariant** and one constant cannot hold 1.8 and 17.4 CSS px, that the highlight's
directionality moved by the **dimmest bin falling** rather than the brightest rising, and that a
receded window on macOS 27 **keeps** its outer shadow where macOS 26.5 removed it entirely.

Three defects were found by the landing that no suite had caught, and all three were found by
looking rather than by measuring: the React capability store never notified a change in any field
the platform folds on, so the playground's material readout named a document the root had stopped
drawing; `GlassToolbar` opened a split's gap exactly level with the padding the overlap check tests
with a strict inequality, which on the macOS 27 material under Reduce Transparency agreed to
fourteen decimal places; and the demo's law readouts and three browser fixtures resolved against the
renderer's constants rather than against the material a root draws. Each is fixed and pinned.

### What is written down that was not before

The wave's own instruments are committed and re-runnable: the native-against-native delta driver and
its per-metric noise bar, the receded-endpoint fitting driver, the OS-and-build refusal in
`materialize`, the slider's attestation, and the document-identity join that proves the shipped
material is the material the bed was read at. The ledger's §§5.149–5.155 are the longest run of
consecutive sections any one subject has in that file, and the coverage matrix's §6 entry about the
reference being a moving target that nobody had priced is now half answered: the price is paid and
recorded, and what stays open is what happens at macOS 28.

## Revision Notes

- 2026-09-20 (the parent): **0.19.0 PUBLISHED**, by the user's `pnpm release` on `26250e97`
  (registry core 04:40:24Z, react 04:40:27Z, web 04:40:46Z — react listed 19 s before its web
  dependency, so a 19 s release-chain window recurred; span 22 s); tag `v0.19.0` annotated on
  `26250e97` and pushed; cold install verified (core 44 / web 256 / react 37 exports; both material
  documents exported; the 26.5 light digest unchanged in the shipped bundle; 1 848 / 1 880 / 644 kB).
  The publish accepts the React driver-timing red as disclosed. Status and §Outcomes carry the
  record beside the "PREPARED, UNPUBLISHED" text; the operator wave (Deferred item 1, Decision
  Log 7) is chartered next on the user's word.
- 2026-09-20 (the parent): G4 merged (`cb7c8ae5`) with the full chain green on the merged tree
  (browsers included: platform-web 404, React 174, demo 57); independently reviewed (publishable
  after amendments), the closure merged (`eac168bb`). The parent's final chain on that tree: build,
  lint, root eslint, 2,556 unit tests, goldens 34, GPU 28, platform-web **410**, React **173 passed,
  1 failed** — `press.spec.ts`'s re-press trajectory on chromium, the tracker's driver-timing class,
  red once in three runs today and not rerun (tracker entry amended; log committed under the G4
  results). The 26.5 freeze intact at 1,818. Main pushed; `pnpm release` and the tag are the user's.
- 2026-09-20 (G4, review closure): **the landing's independent read-only review closed**, nine
  items, on a cut it found publishable after amendments. Under claims §5.155 §10. **Nothing
  material moved**: no constant, profile document, fixture, golden, matrix row, bound or floor, the
  version stays 0.19.0 and unpublished, and the consumed changesets are not re-created. The two
  changes with teeth are pins rather than fixes: **the macOS 26.5 selection had nothing checking
  it** — `macos27-profile-export.test.ts` pins the default document and `tuned-profiles.test.ts`
  pins the macOS 26.5 profile documents, and neither stood on the seam clause 5 moved — so
  `calibration/test/macos26-document-selection.test.ts` now resolves what that document *selects*
  to `b2b570e4adcea8fb` and `874be66ea501621b`, and `window-activation.spec.ts` loops over both
  shipped documents with the four macOS 26.5 digests restored from `fbf59ca6`, which is the only
  pin the two macOS 26.5 **receded** endpoints have anywhere (they have no profile document on
  disk). And **`tuned` over-reported after a React withdrawal**: `<GlassRoot>` withdraws by sending
  the empty patch, `tuned` asked whether the root held anything at all, and a page that had taken
  its tuning back went on being reported as tuned — fixed by a recursive emptiness test and pinned
  in both packages, with the endpoint's digest unmoved throughout, which is the point of the field.
  Two stale README paragraphs about which patch a scheme selects were rewritten; `samplingPaddingFor`'s
  **exactly 2.2×** move (10.3125 → 22.6875 CSS px at the floor, 33.00 → 72.60 on `clear`) is named
  in the 0.19.0 CHANGELOG entry and both READMEs; the one-line result above gained the recede's
  bound so that it matches clause 4; `eye.md` gained the **chromatic** half of its residual, measured
  — Apple's pane carries the backdrop's hue at 0.087–0.103 OKLab chroma against vitrea's
  0.013–0.031, the hue right to four degrees and the amount a third in light and an eighth in dark,
  which is why the 8× difference columns are coloured — with the chromatic-transmission child
  (Decision Log 6 (c)) named as its home. `GlassToolbar`'s inability to pass its root's document to
  `samplingPaddingFor` is a tracker entry rather than a fix: the error is over-padding by exactly
  2.2× and never under, and closing it means adding public API to a prepared cut. Two figures stand
  **beside** what they correct rather than over it — the ring maxima's cell attribution, and the
  heavy-mask case's headroom at one code — and one reading disagrees with the review and says so:
  the receded `Publish` chip reads lighter than the active orange over the light ground and
  **darker** over the dark ground, so what moves is the composite and not the tint. Re-run green:
  calibration 530, platform-web 618, React 163, the browser suite 410 across four projects, lint
  and root ESLint clean, the macOS 26.5 freeze intact at 1,818.

- 2026-09-20 (G4, the landing): **the wave is CLOSED and 0.19.0 is prepared and unpublished.**
  Clause 5's ruling executed as a selection in `platform-web` (Decision Logs 1 (i), 2, 7 (b)):
  one material profile document per measured material, `macos27MaterialProfileDocument` the default
  and `macos26MaterialProfileDocument` shipped beside it, the fitted macOS 27 receded endpoints
  applied by the runtime for the first time, and the four shipped patches generated from the four
  calibration documents and pinned to them. `DEFAULT_MATERIAL_PROFILE` did not move, the 34 goldens
  are byte-identical, and the macOS 26.5 freeze verifies intact at 1,818 entries at the close.
  Clause 6 in full and clause 7's sheets; the coverage matrix re-scored with two §1.7 rows moving
  upward; Outcomes & Retrospective added above with all seven clauses verified. **Eight browser
  cases and four unit cases moved with the material, each with its reason in its own file**, and
  two of the moves are findings rather than adjustments: the macOS 26.5 collapse that made a small
  surface become its backdrop is gone, and with it exposed, the CSS tier overshoots the curve's
  target over a pure black backdrop by about 58 codes — a region macOS 26.5's collapse used to own
  and no committed row covers. Three defects were found by looking at the sheets rather than by any
  suite — the React capability store's snapshot comparison, `GlassToolbar`'s gap sitting exactly
  level with the padding, and three fixtures resolving against the renderer's constants rather than
  the drawn material — and all three are fixed and pinned. §Deferred rewritten at close with ten
  named pieces of work, each with its evidence already on disk. Claims §5.155.

- 2026-09-19 (G3b, review closure): **the gate's independent read-only review closed**, ten items, of
  which two needed no change. **No measurement, bound, verdict or missed row was wrong**, and nothing
  material, no profile document value, bound, floor, fixture or matrix row was touched; every figure
  written was recomputed from the committed evidence and each correction stands **beside** the number
  it corrects. Under claims §5.154 §11. (1) **§7's prose carried the after-column's number onto the
  before-column's cell twice**: `2x-light-standard` texture `checkerboard__rrect-lg__rest` reads
  **0.96931** on `ssimOutside` and **0.96396** on `ssimMean`, where 0.95599 and 0.95441 are that
  tier's new worsts on `light-solid__rrect-md__rest` and `hc-text__rrect-md__rest`. The table itself
  is right and every cleared row cleared by more than it showed. (2) **Decision Log 7 (a)'s cost claim
  is split** (‡ above): a span-graded operator gated at a behaviour-preserving default leaves the 34
  goldens byte-identical but **changes all four documents' `resolvedMaterialSha256`**, because
  `seal.ts`'s fingerprint is a canonicalising digest over the whole resolved material and
  `tuned-profiles.test.ts` pins it — and the two 26.5 documents are byte-frozen in the freeze list, so
  **a one-time X1 exemption is part of that option's price**. The scale-selective scatter of §5.153 §6
  carries the same price, so one operator wave carrying both spends one exemption instead of two.
  (3) **The span law is restated** as σ_css ≈ **0.133 · (span − 30) over spans 96–160** († above and
  claims §5.154 §4): the published 0.131 · (span − 26) misses the span-44 cell by −28 %, and below
  span 96 scale invariance fails outright — 1.84 at 1x against 3.92 at 2x, the device pixel ratio —
  which is now recorded as open structure rather than as law. "Six times too wide" is a 1x figure;
  2.80× at 2x. (4) **§4's span table named no cells and its dark row mixed beds**: every figure is now
  attributed and a bed-wide median row added, which differs from the quoted cell at span 44 (1.52
  against 1.84 at 1x light) and agrees to 0.05 CSS px at every span above it. (5) **The holdout drop
  is the shadow half's only**: the recede half was read through `fit.py table`, which has no filter,
  so its X5 guarantee is **procedural** — per-invocation `--set` / `--scene` — and is recorded that
  way in the ledger and in the gate's README, with a reader-side drop for both halves tracked.
  (6) **§8's verdict table is footnoted for the scope breach §6 records**: the two `impulse` cells are
  eight of the 95 checking rows, they hold clause 1 and clause 3 at ΔE 0.0009–0.0066, dropping them
  *raises* clause 2's mean on all four light rows (which miss either way), and the single joint hold
  contains no `impulse` cell. (9) **Four by-eye residuals added to §9**, each measured off the sheet
  it names: the receded pose's exterior halo at span 160 (far-exterior ×8 mean 17.42 WebGPU / 15.89
  CSS against ≤ 5.92 on any active strip), the **missing** contour hairline on the receded pose where
  the native draws a 56-level one-pixel stroke, the body-structure error changing sign with the bed
  (dark: WebGPU 0.55× and CSS 0.32× of native; light: WebGPU 1.77×), and Reduced Transparency's
  under-opaque GPU body (sd 3.49 against the native's 0.43, CSS 1.03) with the `impulse` specular
  point measured to be WebGPU's alone. (10) **The changeset's "ship as two more documents" is
  reworded** — the receded documents live in the repository, not the tarball — and
  `bar-reproduction.txt`'s generated "thirty-one metrics" is corrected by a note beside rather than by
  editing the generated line; its own header and the ledger both say **thirty-two**. Four tracker
  entries opened. Items 7 and 8 needed no change.
- 2026-09-19 (G3, review closure): **the gate's independent read-only review closed**, four
  should-fix items and two notes, all six recorded under claims §5.153 and every number recomputed
  from the committed evidence. **No verdict, bound, floor or missed row moves, and no material
  constant, profile document, fixture or matrix row was touched.** (1) **The row count was wrong in
  three places** — this charter's Tracking Map row, the claims header and the claims §10 record all
  say 332 appended. The matrix went 1,107 → 1,562, so **455** were appended: 332 is the
  calibration+validation read and the holdout read that every one of those records also describes
  appended a further **123**. Corrected beside in all three, with the per-profile split, and the
  bit-identity of the 1,107 26.5 rows re-verified in order. (2) **The web shadow σ was the native
  reading standing in for the web one**: claims §5.153 §5 says vitrea draws at "σ 35" where the
  instrument reads `falloffSigmaWeb` **36.8** on `checkerboard__rrect-md__rest` at
  `2x-light-standard`; 35.8 is the 26.5 NATIVE σ. The finding is strengthened, not weakened, and the
  web σ being identical on both beds (36.8162) is itself the evidence that no shadow constant moved.
  (3) **`platform-web`'s README claimed no profile field is new for the 27 documents**, which is
  false: `cssTierMapping.blurSigmaScale` is absent from both 26.5 documents and set to 2.2 in both 27
  ones. Named and explained in the README, and a case in `tuned-profiles.test.ts` now pins it.
  (4) **The README's selection instruction was wrong and would have shipped a mixed material** — a
  document is TWO options, `materialProfile: doc.patch` and `cssTierMapping: doc.cssTierMapping`, and
  passing only the first gives a page the 27 GPU material with a CSS tier still blurring at the 26.5
  scale. The README now says it with a snippet; **no API was added** (that is G4's landing) and the
  shape that would close it — one option that takes a document, which the React binding could then
  surface as one prop where it surfaces neither today — is in the tracker. (5) **"The adaptation band
  measured inert" is stronger than this bed supports** and is reworded beside: what was measured is
  that Apple's material converges on its backdrop nowhere on the bed, so the band moves to the bottom
  of its range; at `backdropToneHigh` 0.0001 against a darkest backdrop of ~3e-4 linear **no cell
  exercises it**. (6) **By eye the unfollowed 26.5 outer shadow is the most visible defect on the
  sheets** — the checkerboard around vitrea's `rrect-md` and `rrect-lg` is washed grey over about
  twice the distance the native's is — and it is now in §5.153 §9's list beside the metric that found
  it. The reviewer read it off the ×8 difference column as a bright halo; measured off the sheet's
  own pixels, vitrea's column is a broad DARKENING and the bright ring is the amplified difference,
  which is what §5 now says.
- 2026-09-19 (G3): **the refit landed and G3 closed**, under claims §5.153. Bounds declared in two
  commits before any 27 fit was read — the five of Decision Log 4 (a), then Decision Log 5's coupled
  increased-contrast profile mid-flight — every table an ALIAS of the 26.5 constant it is ruled equal
  to, taken from `adopted-thresholds.test.ts` and never from this document's tables, which is what
  made the gate immune to the three allowance rows the §5.152 §B review found mis-transcribed here.
  **`DEFAULT_MATERIAL_PROFILE` did not move**: the 34 renderer goldens are byte-identical, both 26.5
  documents resolve to the digests they recorded, and the 27 material ships as two patch documents,
  which is what makes those three things true at once. Six laws answered inside the EXISTING
  operators and no operator added — the level law refit with a fourth knot and its adaptation band
  measured inert (Apple's glass stopped vanishing into a dark backdrop, and vitrea now follows it;
  reworded beside by the note above — the band is NOT EXERCISED by this bed rather than measured
  inert on it),
  the rim at a fifth of its amplitude and four times its width, the diffusion on a wider heavy tap,
  the tint's shade measured not to need to move, and the CSS tier derived from the same documents in
  the same commits. **332 rows appended beside the 1,107 26.5 rows, bit-identical across the append;
  holdout read once; the whole-cell ΔE mean roughly halves on every standard profile on both tiers
  and the ΔE p95 tail falls 0.46 → 0.14 light and 0.27 → 0.17 dark.** (Corrected beside in the note
  above this one: **455** rows were appended — 332 from the calibration+validation read and 123 from
  the holdout read this same sentence records; the bit-identity is unaffected.) Fifteen declared rows are
  missed, recorded as UNMET with no floor and put to the user as Decision Log 6 above; the coupled
  increased-contrast profile, whose SSIM-outside row Decision Log 5 named as a risk, clears every row
  of both tiers with room. Four things the charter did not foresee are in Surprises, and the largest
  is that **macOS 27's outer shadow moved and G2 never read that axis**, so X3 forbade following it —
  the cause of seven of the fifteen. The recede is NOT refit and is named as such; the freeze verifies
  intact at 1,818 entries.
- 2026-09-19 (G1c Part B, review closure): **the gate's independent read-only review closed**, six
  findings, under claims §5.152 §B §12. **No verdict moves and Decision Log 5 is unchanged.** The
  finding under all of them is that the **26.5 allowance column was transcribed by hand**, in this
  charter's Decision Log 4 (c) and in both children's closures: four of the forty-eight texture rows
  were wrong, three of them named by the review and the fourth found by auditing the rest
  (`2x-light-standard` ssimMean ≤ 0.07 not ≤ 0.11 and ssimOutside ≤ 0.13 not ≤ 0.14;
  `1x-light-reduced-transparency` ssimOutside ≤ 0.16 not ≤ 0.07; `1x-light-increased-contrast`
  ssimOutside ≤ 0.31 not ≤ 0.20). Allowances are now **parsed** from `adopted-thresholds.test.ts` by
  `adopted_allowances.read_allowances()`, which both closures import; the original transcription is
  kept and printed against the file as item 11, and the first output stands as
  `review-closure.v1.txt`. What follows: **(1)** the SSIM-outside tail Decision Log 5 names as a risk
  is inside its allowance at 0.237 against 0.31, so that risk is withdrawn beside the ruling and the
  ruling stands; **(2)** "inside every material row at its worst cell" is shared with
  `1x-light-reduced-transparency` and is not unique to the coupled profile; **(3)**
  `2x-light-standard` is bounded at SSIM complement ≤ 0.07 and its native-to-native **max** 0.117 is
  outside it while its **p90 0.0646 is inside** — a max-only miss, and a floor decision for the user
  if G3's read lands there, the same shape as the dark ΔE tail (the review said p90 as well;
  recomputed, it does not, and the narrower statement is recorded); **(4)** the tone response row is
  conditioned on `transferR2` (median 0.0075 → 0.0160, 22 of 28 cells under 0.1 on 27) and the
  transfer offset tracks the body level at r = 0.9985, so §B §7's triple is two independent readings;
  **(5)** the two median conventions — the instrument's upper middle and `read-checks.py`'s average —
  are named where each is read, in §B §3 and in `law-tables.txt`'s own header, and no figure moves;
  **(6)** the attested field is `showBorders`, not the `defaults` key `ButtonShapesEnabled`. Every
  figure was recomputed from the committed rows by
  `results/2026-09-19-w29-g1c-coupled/review-closure.py`. **No material, profile, bound, floor,
  fixture, golden or matrix row was touched**, `adopted-thresholds.test.ts` was read and never
  edited, `law-tables.txt` regenerates byte-identical but for the header line, and the 26.5 freeze
  verifies intact at 1,818 entries.
- 2026-09-19 (G2, review closure): **the gate's independent read-only review closed**, ten findings,
  under claims §5.151 §12. **No law's verdict moves**: the material moved on every cell, the geometry
  did not, and every count and magnitude above survives its correction. Every corrected figure was
  recomputed from the committed rows by `results/2026-09-19-w29-g2-native-delta/review-closure.py`
  into `review-closure.txt` rather than copied from the review, and one recomputation disagrees with
  it (23 cells with a 27 highlight ratio over 100, not 21). The ten: **(1)** the recede was barred
  only where both 27 cells had a spread of their own, leaving 158 of 231 rows unbarred and §8's
  medians on 18–70 rows of 219 measurable — the instrument now gives the recede rows the pair rows'
  own bed-minimum fallback, `recede-delta.json` is regenerated with `recede-delta.v1.json` beside it
  and the tables as `law-tables.v2.txt`, and **"the recede darkens the body five times as much" is
  withdrawn** because the 26.5 term changes sign on the full population (+0.00068 → −0.01725, a
  difference of −0.0275 against the barred subset's −0.0290); **(2)** the 32 increased-contrast rows
  sit inside every bed-wide median, so the ex-IC medians are now beside them and a
  non-increased-contrast exemplar beside the rim's; **(3)** no 26.5 profile carries an `attestation`
  block, so the 26.5 reduced-transparency bed is unconfounded **by inference** from its `a11yMode`
  and the harness's mode gate, not by attestation; **(4)** Decision Log 4 (c) quoted a median against
  tables that bound the per-cell worst case, so the p90 and max per profile per bounded metric are
  now in it with each profile's allowance, and what they imply for the recommendation is stated in
  both directions and left for the user to rule; **(5)** the corner's 126 up / 104 down is the moved
  active cells, 207 / 170 / 160 over the healthy ones; **(6)** §5's backdrop table showed 8 of 15;
  **(7)** the 55-cell breakdown summed to 53; **(8)** the highlight ratio's +6.31 is mostly the
  dimmest bin falling, 0.0784 → 0.0242; **(9)** the 45° angular shift is over all 330 cells and
  112.5° over the 169 that moved; **(10)** the hue's 456-of-569 split omits 20 cells that cross
  chroma 0.01 between beds, so the population is 589. **No material, profile, bound, floor, fixture,
  golden or matrix row was touched**, the pair rows are bit-identical across the re-run, and the 26.5
  freeze verifies intact.
- 2026-09-19 (G2): **the native delta read and G2 closed**, under claims §5.151. The instrument is
  `cli/native-delta.ts` — sited there rather than beside the numbers because a bed-against-bed read
  recurs at every OS recapture and because clause 3 asks for the metric primitives the fidelity read
  uses — with W23's contour rim reader and W24's angular highlight reader ported from those waves'
  Python and checked against their own committed 26.5 output. The bar was declared in
  `bar-declaration.md` and committed **in a commit that precedes the one reading a 26.5 pair**, as
  clause 3 requires. **619 pairs, 231 recede rows, 5 cells reported rather than diffed, 0 unbarred**
  — of the *pair* rows; the recede rows' barring was corrected by the review closure recorded above.
  The result is one sentence: **the material moved on every cell and the geometry did not.** Four
  things the charter did not anticipate are in Surprises — the bar reproduces the sitting's 505
  unanimous cells exactly, three metrics have no resolution in this bed at all, the extractor's known
  near-tone fault turns out to *be* the largest finding rather than to obscure it, and the hue row
  had to be conditioned on chroma before it said anything. Decision Log 4 is drafted above; it names
  six laws to refit and two to leave alone, proposes no mechanism for any of them, and puts two
  questions to the user: the bounds to declare for the confounded increased-contrast profile, and
  whether a second increased-contrast pass is captured with the two toggles coupled as 26.5 had them.
- 2026-09-19 (G1, Part B): **the 27 bed is published and G1 is closed**, under claims §5.150 Part B.
  The parent ran the sitting — eight passes, seven runs each, 56 runs, none quarantined, every run
  attesting in full, 12 h 49 m of wall clock against the plan's 12.25 h at the bar. `materialize`
  published all eight passes at seven runs: **624 cells over six profiles — 505 unanimous, 110
  voted, 9 frequency-settled, 0 refused, 0 state-ambiguous**, so no `--omit` ruling and no hole. Per
  profile the count equals the declaration and **exceeds 26.5 by five cells**, so clause 2's
  shortfall list is empty. The 26.5 half of the manifest is proved byte-identical two independent
  ways and the freeze verifies intact at 1,818 entries after the amendment §5.150 Part B §8 records.
  Three findings the charter did not anticipate are in Surprises: macOS 27 decouples the two
  accessibility toggles, the backdrops did not move, and a whole-file hash of a shared manifest
  cannot survive a second bed. Two instruments were changed and both changes are recorded with
  tests: `materialize` could not publish a new profile key at all, and one top-level `hardware`
  block cannot describe two beds — the 27 record now sits per profile, beside the 26.5 one.
  **G2 is unblocked**; nothing in this child read a 27 fixture against vitrea.
- 2026-09-18 (G1, Part A): the 27 bed's infrastructure, built and proved against the real machine
  and the granted bundle, under claims §5.150. **Nothing filed.** `scenes.json` is version 6 with
  the six 27 profile keys `apple-macos-27.0-…-glass0.5` beside the six 26.5 ones, declaring the same
  scenes in the same split (six, not eight — eight is the number of passes, and a pose is not a
  profile); `PROFILE_KEY_PATTERN` and `NativeProfile` carry an optional trailing slider axis and a
  26.5 key parses with **no** `glass` property, because the axis is absent before 27 rather than
  unspecified. `results/2026-09-18-w29-g1-bed/` holds `run-sitting-27.sh` — the version-and-build
  gate, the slider, Show Borders, the accessibility mode, the display's mode and the binary's linked
  SDK all read and refused before pixels, a closing re-read diffed against the opening one, the W27
  per-cell audit and quarantine unchanged, and run-level checks read out of the manifest the harness
  wrote — with a 27-row refusal test against a stubbed machine, the runbook, the re-priced plan
  (624 cells, 12.25 h at the bar) and the verified bar table. `cli/materialize.ts` refuses before it
  opens a PNG when a run's OS, build or attested slider position disagrees with the keys it filed
  under, or when the runs were taken on different builds (`src/run-provenance.ts`, 14 rows). The
  SDK-gating pixel arm is **closed**: identical bytes from both bundles at zero spread. The freeze
  verifies intact (1,762 entries). Three things the charter did not foresee are in Surprises: the
  harness has no profile filter, the side arm's third run is not evidence, and the granted bundle
  reads nothing out of a key but the scale token.
- 2026-09-18 (G0): pre-flight run on the updated machine and closed under claims §5.149. (a), (b),
  (d), (e) and (f) complete; (c) settled on the declared material and **stopped before pixels** —
  the 27-SDK side bundle has no Screen Recording grant and TCC presents no prompt, and clearing the
  denial would risk the granted bundle's own grant, so the pixel arm is a user decision. Verdicts:
  the granted bundle still captures and the `.accessory` recede is reachable and capturable; the
  material is not SDK-gated as far as three linked-SDK values can declare; the slider moves 10 of 10
  cells beyond their own spread at every position including the machine's as-found one, and the
  centre is 0.5 while the machine sits at 0.5459057; the window corner and shadow do not enter any
  capture region. Decision Log 3 drafted for the user. Nothing filed; the freeze re-verified intact
  (1,762 entries). Two axes the charter assumed readable are not: Show Borders has no key at all,
  and the manifest's `sdk` field is environment-derived and reads `unknown` through a pass's own
  launch.
- 2026-09-19 (G1c, Part B): **the coupled bed published and read**, under claims §5.152 §B. 32 cells
  over `apple-macos-27.0-1x-light-increased-contrast-coupled-glass0.5` from seven runs per pose — 28
  unanimous, 4 voted, 0 refused — with the attestation (IC=1, RT=1, ButtonShapesEnabled=0, slider
  0.5) in the profile's manifest entry and a caveat naming the state beside the harness's own
  coupling note, never over it. The bar was declared and committed in `848713cf`, which precedes the
  commit that read a 26.5 pair, and it carries one declaration of its own: the zero-spread fallback
  is the minimum over the 27 **bed** rather than over this 32-cell pass, read from G2's committed
  figures through a new `--fallback-bar`, because a 32-cell minimum is undefined on twelve metrics
  and reading the rule off the subset would hand them a bar of exactly zero on the thinnest evidence
  for it. The reading: **the material moved on every one of the 32 cells and the geometry did not**
  — level, tone response, scatter, rim, highlight and tint lightness all moved; silhouette, corner
  and the tint's hue did not. **The decoupling's share is now a number**: the rim and the scatter
  moved by the same amount in both states to within 1 % and are the material, while the level, the
  tone offset and the tint's lightness read two to five times larger on the decoupled profile than
  the material alone accounts for. Two findings the charter did not anticipate are in Surprises —
  Apple changed the increased-contrast material *more* than the ordinary one, and `compare` picks its
  web-side accessibility flags off a field that cannot tell the two 27 contrast states apart. **A
  bound is drafted for the user as Decision Log 5**; nothing in vitrea moved, no capture was taken,
  and the 26.5 freeze verifies intact at 1,818 entries.
- 2026-09-19 (G1c, Part A): **the coupled increased-contrast bed's infrastructure, and nothing
  captured**, under claims §5.152. Decision Log 4 (b)'s second pass gets a key of its own —
  `apple-macos-27.0-1x-light-increased-contrast-coupled-glass0.5` — and the grammar a fourth
  accessibility token that names a machine STATE, both toggles on, rather than a macOS setting.
  `scenes.json` is version 7 with one new profile entry declaring the 26.5 increased-contrast list
  verbatim; its `a11y` field is `increased-contrast` and not the key's token, deliberately, because
  `SystemAccessibility.current` answers "is contrast on" and the granted bundle is never rebuilt
  (X4). What keeps one pass from capturing both contrast profiles is structural: `pass-spec.py`
  fills ONE contrast slot per pass, `run-sitting-27.sh` reads both toggles and each contrast pass now
  refuses the other's state, and `src/run-provenance.ts` judges the key against the two attested
  booleans before a PNG is opened. Proved by rehearsal on the machine rather than by reasoning — the
  refusals fire from the real state, both derivations carry one contrast profile and the same 10 / 22
  cells, and the granted bundle reads the version-7 declaration unrebuilt — with the one thing the
  toggles gate named as such: `cells presented: 10` and `22` is the parent's pre-pass rehearsal
  (RUNBOOK §3b). The stubbed test is 36 rows, one of them a REVERSAL: it asserted that a coupled
  machine ran the plain contrast pass, which was true while the coupled state was the only one macOS
  allowed. The 26.5 freeze verifies intact at 1,818 entries.
- 2026-09-19 (G3b): **the shadow read and the recede fitted, under claims §5.154.** The shadow axis
  joined `cli/native-delta.ts` at G2's own bar, declared and committed before the first 26.5 pair
  (`3e9975b0`) and reproducing G2's thirty-two metrics over 190,683 readings with zero
  disagreements. **The shadow moved on every measurable cell of every profile** — 342/342 on the
  departure, 246/246 on the fitted σ and amplitude, median Δ/bar 10⁵–10⁷ — and it is dimmer,
  tighter, shorter and less displaced; §5.153 §5's two figures reproduce exactly. The refit moves
  eight leaves in the light document and seven in the dark, the `outerShadow` block and nothing
  else, proved leaf by leaf against each document's own state at G3's seal; the departure residual
  goes 0.0068 → 0.0007. The two 27 receded endpoints are fitted through the `--receded-profile`
  driver this child built (the tracker's proposed shape, in the calibration package), with the
  response re-anchored to the active document's knots and the outer shadow **restored** — on 27 the
  recede keeps the elevation that on 26.5 it removes — and the inactive level residual goes
  0.1174 → 0.01017. Four documents re-sealed, all six profiles re-read on both tiers at them,
  holdout once, 455 rows appended with 0 of the 1,562 existing rows missing or changed.
  **`MISSED_27_ROWS` goes 15 → 7**, all eight cleared rows the shadow's, the seven that remain all
  holdout and all in the two classes Decision Log 6 accepted as residual. The recede's bound, at
  W27c G1b's three clauses and its 26.5 values, **holds on 1 of 12 profile-tiers**, worst on
  `photo__rrect-lg__inactive` on every row — a floor decision for the user, recorded, changing no CI
  gate. `DEFAULT_MATERIAL_PROFILE` unmoved, 34 goldens byte-identical, the 26.5 freeze intact at
  1,818 at this gate's opening and close. **Decision Log 7 is drafted above**: on 27 the shadow's σ
  is no longer span-invariant and one constant cannot hold 1.8 and 17.4.
- 2026-09-20 (the parent): G3b merged (`ba1c5593`), the chain green on the merged tree, independently reviewed (stands with amendments), the closure merged (`95216172`); Decision Log 7 ruled by the user; G4 dispatched under §5.155.
- 2026-09-19 (the parent): G3 merged (`b2a42bde`), the full chain green on the merged tree, independently reviewed (stands with amendments; fix wave dispatched); Decision Log 6 ruled by the user; G3b dispatched under §5.154. GitHub warns `results/matrix.json` is 52.9 MB, past its recommended 50 MB; carried to G4 as a tracker item.
- 2026-09-19 (the parent): G1c part B merged (`ee3998b4`); Decision Log 5 ruled by the user — the coupled profile gets its tables at the 26.5 values and joins G3 as the sixth profile; G3 told mid-flight.
- 2026-09-19 (the parent): G1c part A merged (`101d153f`); the coupled increased-contrast pair captured 07:27Z–08:07Z by the parent's hand with the toggles driven through the codex companion; G1c handed Part B; G3 dispatched under §5.153.
- 2026-09-19 (the parent): G2 merged (`48d273d1`), independently reviewed (stands with amendments), the fix wave merged (`c88d62e2`); Decision Log 4 ruled by the user — bounds at the 26.5 values on five profiles, none on increased contrast; the coupled increased-contrast pass captured as G1c under §5.152.
- 2026-09-19 (the parent): G1 merged in two parts (`c0fb52a8`, `3d476331`); the eight passes run by the parent's hand on the machine 2026-09-18 07:03Z to 2026-09-19 01:19Z, the two accessibility toggles driven through the codex companion's GUI runtime after the user's instruction, one needing a confirmed resume; G2 dispatched under §5.151.
- 2026-09-18 (the parent): the machine on macOS 27.0 26A428; freeze verified intact; Decision Log
  2 records the user's advance clause-5 ruling (runtime default 27, by selection) and the arrival
  record, including the `NSGlassTintAmount` key at 0.5459057; G0 dispatched under §5.149.
- 2026-09-16 (the parent, after review): an independent adversarial read (read-only opus)
  returned three blocking findings — the freeze and the refit incompatible through the 26.5 light
  document's identity pin; the noise bar not derivable from the plurality record; no per-cell 26.5
  bar in `bedProvenance` — and ten should-fix / notes. All folded; Decision Log 1 amended with the
  three rulings; status CHARTERED. Verdict was "dispatchable after amendments"; G0 still waits on
  the machine being on macOS 27.
- 2026-09-16 (the parent): drafted from a read-only grounding of the repository's 26.5 coupling,
  the fixture and matrix inventory, the granted bundle's build record, the metrics pipeline's
  native-against-web shape, and the public record of the 27 change. Not yet reviewed; no child
  dispatched; the machine still on 26.5.2.
