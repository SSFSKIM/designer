# W33 — the rim wave: the exterior's black floor and Apple's contour stroke (2026-09-22)

**Status: LANDING PREPARED 2026-09-22, v5 — 0.23.0 generated and rehearsed; independent review pending, unpublished.** — chartered by the parent on the user's "W33 Rim wave" after the 0.22.0
publish (main `55b1b28d`), under the standing "rest on your judgement" and the routing the user set
the same day (X9). Grounded on a read-only memo taken off the committed captures and fixtures on
this machine (`/Users/new/.claude/jobs/17c7ce02/tmp/w33-grounding.md`, the parent's; its numbers are
reproduced by G0 into this wave's own evidence before anything is fitted). Sent to adversarial
review before any child opens; five blocking findings folded (Revision Notes).

**CLOSED 2026-09-22 by G2's merge and 0.23.0 PUBLISHED 2026-09-23.** G2 was independently
reviewed on its branch (every artifact reproduced, merged as-is, two record nits closed there as
`d26a2fa9` and `75435daf`) and merged to main as `e6c80c7f` with `--no-ff` on 2026-09-22;
`freeze.py verify` 1,818 at the merge and the chain re-run on main (build and lint 9 / 9, unit
2,719 over 189 files). **0.23.0 published by the user's `pnpm release` on `e6c80c7f`** (no commit
followed the merge before the publish); tag `v0.23.0` (annotated, on that commit) pushed by the
parent the same day. Registry: core 2026-09-23T00:05:49.034Z, web 00:05:51.043Z, react
00:05:52.031Z — the whole group listed in 3.0 s, and every tarball served on the first cold install
two minutes later. Cold install verified in a fresh `mktemp -d` directory
(`/tmp/vitrea-cold-0.23.0-GAl9`, so the 0.22.0 record's leftover-directory pitfall could not recur):
all three at 0.23.0, ranges `^0.23.0` in both dependents; exports core 44 / web 258 / react 38
(`useGlassRootHandle` a function); the shipped macOS 27 documents report `dcbccbd9feac9881` /
`e59f9106bcd7c966` / `f34dcc03e2774db3` / `6b6237b7ae241638` and the frozen 26.5 pair
`b2b570e4adcea8fb` / `874be66ea501621b`, all six reading their own recorded fields; the web bundle
`dist/index.js` is 303,207 B (0.22.0 shipped 303,215) and carries `liftAmplitude: 0` on the macOS 27
endpoints with the frozen 26.5 actives' `0.01` / `51e-4` beside, `spreadPx: 0.5` / `1.8` and
`sigmaThinOffsetPx: -6.318`. The three tarballs fetched back from the registry are **583,802 /
568,292 / 188,179 B — byte-for-byte the rehearsal's sizes in `dry-run.txt`**, so what npm serves is
the tree the chain ran on.

## Purpose

After W32 the shadow's falloff matches Apple's per span (C1, twelve of twelve) and what the eye
still sees at a surface's edge is two things the shadow wave was forbidden to touch (W32 X3):

1. **The exterior's black floor.** Over a backdrop pixel Apple renders as exactly (0, 0, 0), the
   WebGPU tier renders (1, 1, 1) — never more — on 35.3 % of the native-black exterior pixels at
   span 160 on 1x light (3,334 of 9,440), 12.5 % at 128, 0 % at 44 (W32 Deferred 2; claims §5.169
   §5). The grounding memo settled its cause off disk: **Apple's macOS 27 material has no lift over
   black at all** — 0 of 67 light cells and 0 of 50 dark cells nonzero over 2,646,074 backdrop-black
   exterior pixels ≥ 2 CSS px out — while Apple's macOS 26.5 material does (21 of 69 and 18 of 56
   cells, up to a native mean of 19.9 bytes on `hc-text-28`). The fitted `liftAmplitude` (0.001
   light / 0.0005 dark) is the lit pixels' degeneracy (§5.62 §4: the shadow's and the lift's shapes
   correlate 0.9998) leaking into a leaf the black pixels determine at 0, and sRGB's slope of 12.92
   at the origin (`wgsl/optics.ts:771`, stored to `rgba8unorm`) is what turns a 5 × 10⁻⁴ linear
   addition into one whole code. The receded pose, whose documents hold `liftAmplitude: 0`, reads
   0.0 % on all four macOS 27 beds. **Gap (A) is a leaf deletion on the four macOS 27 documents with
   a compensating move on the three thick anchors (an ESTIMATE of 5–7 % of the full-falloff shadow on lit pixels at span 160 — 1.65 lift bytes against a 31.6-byte shadow over white, the encoding `1 − (1 − occlusion)^(1/2.4)` applied; the memo's 2.4 % treated linear occlusion as encoded alpha), not a fit.**
2. **Apple's contour stroke.** Apple draws one DEVICE pixel of dark stroke at the contour, on both
   poses, and vitrea draws none (W32 Deferred 3, re-chartered at G2's review closure; §5.169 §10
   B2). The memo's model-free notch statistic (`native[d=0] − min(native[d=±1])` on an exact
   rounded-rect SDF) reads a median of −19.78 bytes with 46 of 51 active cells below −5 on 1x light,
   −21.30 / 31 of 37 inactive, −13.74 / 31 of 40 on 1x dark; **on the frozen macOS 26.5 bed +2.68
   with 1 of 68**, and vitrea's own notch tracks 26.5's to a byte (+1.81), which rules out the
   estimator. It is one device pixel at 1x AND 2x (notch −37.04 vs −35.28 on `light-solid__rrect-md`,
   with `d=+1` at 2x already back at the shadow). §5.166 §7 pins its form: darker than both backdrop
   and body over a light square and **lighter than the backdrop over a dark one, which a
   transmission cannot be** (156 over 255; 0–6 over 0). **Gap (B) is a macOS 27 addition and a NEW
   leaf** — a signed, opacity-blended dark stroke of declared device-pixel width at the contour, in
   the family `strongBorderRim`'s signed `rimAlpha: −3.5` already exercises — absent from the frozen
   26.5 material, so both frozen documents want it at its identity.

The purpose is the two exterior terms closed on the macOS 27 material with every stop W32 adopted
or re-stated still met, the frozen bed untouched, and every gap left written down.

## Parent-Level Acceptance

1. **The cut precedes the fit, on the widened population, with the statistics declared.** G0
   reproduces the memo's readings into this wave's evidence: (A) the black-floor statistic — the
   fraction of native-black exterior pixels (≥ 2 CSS px out, backdrop byte (0,0,0)) where the web
   reads > 0, per bed × span, WebGPU tier, active pose, with a separate `web > 1` row — which is NOT
   zero on the widened bed: `hc-text-28__rrect-lg__rest` 1x light reads 66 pixels at 2 (of 1,848
   eligible, 1,827 nonzero) and `hc-text-7` 39 (of 1,437) under the integer-offset convention, 44
   and 39 under an analytic ≥ 2 CSS px mask, so it is recorded per cell as a BASELINE for
   non-regression and the post-stand-down target of 0 is a separate, stronger row — over the NINE backdrops that
   carry pure black (the five black/white checkerboards, `impulse`, the three `hc-text` rasters; 264
   of 448 macOS 27 native cells, 14,325,464 black exterior pixels; 232 texture-tier non-holdout
   rows), read across the pitch ladder and `impulse` so a lift Apple does not have (pitch-independent)
   separates from a compositing defect at the alpha floor (pitch-tracking); `halo.py`'s max (137.10
   today) recorded as corroboration, not bounded. **The exterior convention is stated once and used
   everywhere**: the memo's pixel totals use integer offset 2, whose pixel centres sit 1.5 CSS px
   outside the box at 1x, which is not an analytic ≥ 2 CSS px distance; G0 names which it uses and
   prints both where they differ. (B) the stroke residual — `contour-stroke.py`
   turned from a detector into a referee — and NOT by taking `|n − w|` of channel-and-sample means,
   which cancels spatially and across colour (the review fitted a uniform black stroke at alpha
   0.2199 that zeroes that statistic on `photo__rrect-lg__rest` while leaving 10.23 bytes mean
   absolute RGB error and a worst channel error of 36.06): the bound is on a PER-PIXEL, PER-CHANNEL
   absolute residual at one device pixel out, stratified by side, corner and backdrop class, with
   the signed mean kept beside for diagnosis; offsets 2–6 as a per-cell BASELINE that must not
   worsen (on the widened population they are not +0.00 to +0.21: `light-solid__rrect-ml__rest`
   reads −1.42 at offset 2 and 122 of 440 1x-light readings exceed 0.215 in magnitude), the CORNER ARCS read by a
   corner-normal sampler or the decline recorded (X12), widened past `photo` at 44 and 160 to every
   `rest` and `inactive` cell with a committed capture (51 / 37 on 1x light, 40 / 30 on 1x dark —
   OF WHICH five per pose on light and two per pose on dark are HOLDOUT scenes, e.g.
   `photo__rrect-lg__{rest,inactive}`: the referee and the round runner EXCLUDE `scenes.json`'s
   holdout membership by construction during every candidate round, carry a deliberate post-seal
   inclusion flag for the one read, and print both populations — reading committed evidence is not
   a holdout read, fitting a constant against a holdout row is), the stroke's COLOUR and
   compositing space identified before an amplitude-and-width model is committed to (the evidence
   establishes a notch, not a neutral signed rim: the transect's three channel ratios 0.63 / 0.37 /
   0.68 are not one scalar alpha), and the stroke's
   CONDITIONALITY explained before it is fitted: the two dark-standard active capsules read −0.27
   against −13 to −32 everywhere else, and the dark active beds are systematically shallower
   (median −13.74 vs −19.78) but not absent. Neither statistic is stated on ΔE, SSIM or the
   departure; `0-3` is not re-opened into `T` or C1; `b3-stop.py` stays as ruled (W32 DL 3 (a));
   `departure-stat.py`'s whole-exterior form runs every round as a WARNING, recorded, not bounded.
2. **The compositing form is settled against the bed's own gates before a constant moves.**
   `declaredContourMaxWeb ≤ 1` (`adopted-thresholds.test.ts:3169`) reads the FINISHED capture's
   alpha thresholded at `DRAWN_ALPHA_THRESHOLD = 0.5` — the existing coverage and shadow already
   contribute (the ring outside `light-solid__rrect-md__rest` spans 0.0078–0.4275 today), so a
   source-over stroke at 0.49 lands at 0.494–0.708, thresholds 320 more pixels and moves the
   contour max 1 → √2; "outside at alpha < 0.5 survives" is FALSE. The row is at exactly 1 on 254
   of the 442 macOS 27 texture rows that carry it, and on 48 of the 115 rows the test's own
   non-probe active selection reads. G0 tables each form by its FINAL RGBA and thresholded mask on
   real captures, composition included, not by the stroke's own alpha, and states what each does to `declaredContourMaxWeb`,
   `silhouetteAreaWeb`, `PREDICATE_EXCLUDES` (both arms are region-bounded: a term one device px
   OUTSIDE moves neither; INSIDE the ring it moves the 67-line list and twelve derived counts) and
   the interior metrics M2 reads. A fourth form is tabled beside the three: a sampled-colour modification
   at HELD coverage (premultiplied feasibility tested, not assumed equivalent to source-over). If
   every form the bytes admit thresholds outside the rect, re-adopting `DECLARED_CONTOUR_MAX_PX` is a **user ruling taken before the fit** (Decision
   Log 2), not a landing gate turned red.
3. **Bounds before reads; every bound is a number by a stated rule; stops stated with their
   expected post-fit values.** (A)'s stop: the black-floor fraction ≤ the bound G0 derives from the
   receded pose's reading (0.0 % on all four beds — the same material with the leaf at 0) rounded
   the way C1's was; the `web > 1` floor at 0. (B)'s bound by clause 1's residual at the read,
   rounded up to two significant figures (W30's rule for B3, used for C1 and B3 in W32). The stops
   carried: C1 ≤ 0.0042 on all twelve rows; the thin per-cell stop against the 0.002044 bar; B1's
   six windows (the σ law does not move here); **B3 at 0.000056 with its expected post-fit value
   stated by G0 before the fit** — its inactive half reads exactly 0 over the admitted bands and
   0.00079672 over the whole exterior, which IS the stroke, so the admitted-band stop is blind to
   this wave by construction and the whole-exterior warning is where the change will show; M2 under
   W32 Decision Log 4 (this is an adopting gate: the reference re-pointed at the generation G1
   reads, every gated cell's move recorded, and the stroke on the antialiased ring — the same ring
   §5.168 §7 names as M2's candidate mechanism — is a reason to move that is not the body).
4. **The lift stands down on macOS 27 and the anchors take the trade under C1.** G1 sets
   `liftAmplitude` to 0 on the four macOS 27 documents as a DECLARED reading (Decision Log 1 (a),
   the way W32 Decision Log 2 declared the recede's amplitude), re-solves the three thick anchors
   on the window-restricted departure so the lit pixels lose nothing (the estimate above, replaced by the rendered round), and shows
   (a) the `3-6` band held, (b) whether the dark document's 0.0005 was separately identifiable at
   all (the dark bed's pooled nonzero share is 0.44 %), (c) the black-floor fraction at 0.0 % on
   every bed and span with `web > 1` at 0, and (d) `halo.py`'s max below 137.10. The two frozen
   26.5 documents keep their lifts (0.01 / 0.0051 — Apple's 26.5 material has one) and their digests.
5. **The stroke is a term of the material on both tiers' terms, routed through the identity table.**
   G1 adds the leaf as FLAT leaves on the profile (`contourStrokeAlpha` the GATE,
   `contourStrokeWidthDevicePx` the GATED leaf; names G1's to the convention, the FLATNESS the
   parent's) — a nested `contourStroke { … }` container is refused: `materialDigestInput` removes
   named paths and does not prune the empty parent, so `contourStroke: {}` stays in the digest
   input and the review measured the frozen light digest moving `b2b570e4adcea8fb → da59d059f9526c73`
   at alpha 0, where a flat pair under the same gate-group preserved it — with the width in a named
   DEVICE-PIXEL unit (Decision Log 1 (c)), an `inertLawCase`, the sweep-the-gated-leaf proof in
   `w31-gate-groups.test.ts`, and **both frozen resolved digests proven unmoved by a committed script
   BEFORE any shader code is written**; appends its `MATERIAL_IDENTITY_TABLE` entry (the prefix pin
   permits the append; `w31-identity-table.test.ts`'s LITERAL per-gate identity assertion gains the
   new gate too, and rule 2 itself does not change), fits it on the WebGPU tier on
   both poses of the four standard macOS 27 documents against clause 1's referee with clause 3's
   stops, with the conditionality rule G0 found written into the leaf or its doc, and settles the
   CSS tier by EXPERIMENT (an inset `box-shadow` ring following `border-radius` exists at
   `css-tier.ts:1824`; a `1px` inset is two device px at dpr 2 and `0.5px` is rasteriser-dependent;
   the dom tier's `ssimOutside` at 1x light has +0.00026 of headroom) — carried if it holds, or
   declined on the measurement with the decline pinned against the SHIPPED documents the way
   `BODY_CHROMA_RETENTION` is (`tier-coherence.test.ts:3325–3358`). The goldens stay byte-identical
   because the leaf's identity is the drawn state.
6. **One seal, one read, one merge.** G1's re-seal of the four macOS 27 documents lands in the same
   merge as its canonical read (W30 DL 4 (b): `SHIPPED_DOCUMENT_HASHES` is derived, so a seal without
   its read empties the 27 half of the gated bed), the split (`--read-claims "c9a §5.168"`, the
   gate whose read produced the 786 rows; never this wave's section), the holdout read once by
   artifact (`configuration.py` refuses a read at today's configuration; G1 is the ONLY gate that
   moves a constant and spends the one read — Decision Log 1 (d)), and the capture tree copied to
   canonical with the checker run there; the replaced generation moved to
   `web-captures-superseded/<sha>/`.
7. **The landing and every gap written down.** G2 adopts (A)'s stop and (B)'s bound as the parent
   rules them, re-baselines M2, lands the records (`CLAUDE.md`'s lift and rim paragraphs;
   `apps/demo/src/site/calibration.ts:187`, which describes the lift's width relative to the
   shadow's and is falsified by a zero lift on the default document; both READMEs; CHANGELOG; the
   coverage matrix; the four stale records the memo found — "14 to 30 bytes" is −13.19 to −31.95 at
   four sites, `exterior-cut.py:170–175`'s stale over-fill reason for excluding `0-3`, the tracker's
   dead halo half at ~3562 and its superseded over-fill paragraph at ~5147), runs the chain with X6,
   versions the fixed group to **0.23.0** and prepares the cut; the Outcomes & Retrospective against
   these seven clauses; the Deferred-at-close list with span 160 first (no calibration or validation
   scene exists there; every black-floor cell at 160 is `probe`, so the claim there is probe-strength
   the way `thickOcclusionAt160` is — Decision Log 1 (b)) and the second black-floor-shaped residual
   the memo found on the dark RECEDED document (§5.154 §6: +0.0475 above the reference over a
   near-black backdrop where the lift is already 0 — a different phenomenon, one line).

## Grounding Baseline (main at `55b1b28d`, 0.22.0 published)

- Matrix 1,893 rows (786 macOS 27 at the shipped documents, 230 gated; 1,107 frozen 26.5), freeze
  1,818, `PREDICATE_EXCLUDES` 67, capture tree 1,900 / 1,893 match. Holdout reads recorded: three
  (§5.164, §5.164 §13, §5.168); `configuration.py show` at this head is byte-identical to read #3.
- Documents: light `40a6dec2dc34c748` (file `d5bdd6eac432`), dark `bd1814fac34f9b30` (`431cabd391c4`),
  receded light `f34dcc03e2774db3` (`45acb6d916b9`), receded dark `6b6237b7ae241638` (`4e68f81869f6`);
  `liftAmplitude` 0.001 / 0.0005 active, 0 receded; `liftSpanMin` 64. `MATERIAL_IDENTITY_TABLE` has
  four entries (W30's three gate-groups and W31's plain drop); the declaration pin is a prefix
  assertion over the declaration file's SHA (`w31-identity-table.test.ts:20, :71, :84`).
- Stops at the read: C1 0.00088–0.00391 / 0.0042; thin worst 0.02168 vs the 0.002044 bar (an
  accessibility cell; W32 Deferred 10); B3 0.00005596 / 0.000056 (0.07 % unused), inactive half
  0.00000000 admitted / 0.00079672 whole; whole-exterior departure 0.00072 (warning); M2 26 cells,
  worst per-wave −1.477 %, worst cumulative −2.775 % re-baselined at §5.169.
- Gates with no headroom this wave can touch: `declaredContourMaxWeb ≤ 1` (max exactly 1 on 442
  rows; `declaredIoUWeb` min 0.99872 / 0.99); dom-tier `ssimOutside` 1x light 0.82026 / 0.82.
- The bed: 168 scenes, canvas 320 × 200, `rrect-lg` 19.5 px clearance (W32's span-160 truncation
  stands); fittable black-floor cells per span × standard profile: 32 → 8/8/6/6, 44 → 21/21/14/14,
  96 → 15/15/13/13, 128 → 7/7/6/6, 160 → 11/11/11/11 (all probe at 160). `dark-solid` is (28,28,30)
  and `photo` has no black pixel; "native-black exterior" is a property of Apple's draw as well as
  the backdrop (four `dark-solid` capsule inactive cells carry 680 black exterior pixels).
- Instruments on disk: `b4-black-floor.py`, `halo.py`, `contour-stroke.py`
  (`results/2026-09-21-w32-g2-landing/`), `exterior-cut.py`, `departure-stat.py`, `b3-stop.py`,
  `anchor-solve.py`, `chroma-cut.py`, `m2-rebaseline.py`. Both capture trees are on this machine;
  `python3.12` has PIL and numpy where the repo's `python3` does not.
- Goldens: 13 PNGs under `packages/renderer-webgpu/e2e/goldens/`, the suite's cases pinned by the
  isolation proof (`e2e/golden/isolation.spec.ts`), which records that eight of nine hashes once went
  stale on main — G0 verifies it green at open.

## Design (advisory unless marked)

**(A) is a declaration.** The four macOS 27 documents write `liftAmplitude: 0` as a declared reading
with the measurement beside it; `liftSpanMin` and the lift's shape leaves stay (inert at zero
amplitude, recorded as unread). The anchors' compensation is the same closed-form window solve W32
G1 used (`anchor-solve.py`), with C1 and the thin stop as stops and the `3-6` band watched. The
lift's doc comment at `material.ts:332` ("exactly zero over black") is corrected: it holds for a
uniformly black backdrop and fails on a structured one, because the term's source is the σ-40
blurred chain at the pixel's own position (`optics.ts:585–589`); §5.65's S6 note said so in 2026-09
and was never followed.

**(B) is a new leaf pair, FLAT on the profile: `contourStrokeAlpha` (gate) and
`contourStrokeWidthDevicePx` (gated)** — no nested container (the empty parent survives the digest
drop and moves every frozen digest); the names are G1's to settle to the surrounding convention; the
UNIT is the parent's (Decision Log 1 (c)): a device-px
width named as such, not a CSS-px pair (1.0 / 0.5) that encodes the same fact twice and drifts at
dpr 3. Signed, opacity-blended, applied at the contour on both poses with no policy asserted
(`strongBorderRim` is policy-substituted and replaces the variant rim wholesale — the wrong shape);
the WebGPU tier has the signed distance at `wgsl/optics.ts:362` and `rimLight` already composites a
signed amplitude. The receded documents carry their own value (on the inactive pose the stroke is
Apple's entire exterior: body 188, first exterior row 156 over 255). The accessibility fold's effect
on the stroke is declared by G0 (does the fold replace it, as it replaces the rim? — decide, do not
inherit) and the two accessibility documents' values are read, not fitted (W32 Deferred 10 owns their
shadow amplitudes; the stroke on those beds is recorded at whatever the fold gives).

**The compositing form is the wave's first real decision** (clause 2). Four candidates G0 tables, each by its
FINAL composited RGBA and thresholded mask on real captures: (i) the stroke one device px OUTSIDE
the declared rect, source-over at the measured depth — the finished alpha crosses 0.5 wherever the
existing ring plus the stroke does, and the contour max goes 1 → √2 on those rows; (ii) the stroke
on the OUTERMOST declared device pixel — moves `silhouetteAreaWeb`, the interior ring M2 reads and
`PREDICATE_EXCLUDES` with it; (iii) outside at a stroke alpha below 0.5 — does NOT survive by itself
(0.49 over a 0.43 ring thresholds), so it is tabled with the composed alpha, not the stroke's; (iv) a
sampled-COLOUR modification at HELD coverage (the pixel's colour darkened, its alpha untouched), whose
premultiplied feasibility G0 tests. Which one Apple's bytes demand is a measurement (the SDF
transect places the notch at d ∈ [0, 1), outside), and which the bed can carry is a ruling.

**The CSS tier** gets an experiment, not an assumption: one inset ring at a dpr-aware length,
rendered on the bed, read by clause 1's referee and by the dom tier's own bounds; carried or
declined with the decline pinned against the shipped documents.

**Rounds** run on scratch (`VITREA_WEB_CAPTURES`, `--out-matrix`) with W32 G1's `round.sh` shape,
RT/IC 0 and the slider 0.5 before each, a ten-round budget, the convergence test as repeatability
at one candidate (W32 §5.168 §10 N-6: say "best of the sampled points", never "located minimum"
unless bracketed and refined).

## Children

### G0: The cut and the declarations — no material change, no capture

Ledger **§5.170**; evidence `packages/calibration/results/2026-09-22-w33-g0-rim-cut/`. Reproduces the
memo's readings (§1.5, §2.5) from the canonical tree and the fixtures with committed scripts;
builds the two referees of clause 1 (black-floor statistic on the nine-backdrop bed with the
`web > 1` floor; the stroke residual with corner sampling and the conditionality sweep); tables the
three compositing forms of clause 2 against `declaredContourMaxWeb`, `silhouetteAreaWeb`,
`PREDICATE_EXCLUDES` and M2's ring; declares the accessibility fold's treatment; derives every bound
by rule; states B3's expected post-fit value and the whole-exterior warning's; verifies the isolation
proof green; fixes the four stale records (clause 7) beside; writes `bounds-declaration.md`. Owns its
dir, `test/w33-*.test.ts`, and nothing under `src/` or `profiles/`. Reviewed before G1 opens.

### G1a: The contour term identified — no seal, no read; opens after G0's merge (v3)

Ledger **§5.171**; evidence `packages/calibration/results/2026-09-22-w33-g1a-contour-model/`. The
pre-fit identification step the review of G0 recommended and Decision Log 2 rules: from G0's
referee and the pixels on disk, identify the contour term's ANGULAR response (the normal-angle law
the readings demand: dark active `rrect-md` vertical sides −54.5 / −50.5 against top/bottom −0.35 /
−3.05 on photo and −17.44 against −0.35 / −1.23 on `dark-solid`; light active `light-solid` md
−63.4 / −63.4 against −21.0 / −24.9; the inactive pose restoring the horizontal sides at −29.6 /
−29.4; at 2x pooled horizontal −0.74 vs vertical −16.73), its RADIAL COVERAGE (the one-device-pixel
placement separated from continuous-corner and raster-coverage effects — `Capsule()` arcs as the
geometry-clean control, `.continuous` rounded rectangles as the confound), and its COLOUR response
by backdrop (which of encoded / linear space, multiply / add / signed affine, neutral / tinted — the
eight families G0 refuted are the floor, not the menu). The shipped directional rim
(`wgsl/optics.ts:1589–1624`, `(√2·|dot(normal, rimLitAxis)|)^exponent`, normalised to the SAME value
on horizontal and vertical sides with a zero straight-side along-side mean) is shown NOT to have the
shape; its normal and coordinate machinery may be reused, its law may not. The output is ONE
physically composable candidate (or the finding that none is identifiable on this bed) expressed
as leaves — each new leaf joining the same flat gate-group and inert/gated proof G0 gave the pair —
with, for that candidate, the two prices tabled side by side: (i) drawn exactly where Apple's bytes
sit, the finished-alpha consequences on `declaredContourMaxWeb`, `declaredIoUWeb`,
`PREDICATE_EXCLUDES` and M2's mask, cell by cell; (ii) the shape-preserving construction the review
demonstrated (coverage allowed to grow, previously sub-threshold pixels capped at 127/255, the
premultiplied colour solved inside the feasible interval: 0 / 0 shape failures, ring MAE 34.2 →
3.5 bytes offline, exact on `light-solid` md, 290 cells still carrying an infeasible pixel) and its
measured fidelity loss. Offline algebra on the committed captures first; a scratch render of the
candidate's SHAPE on a handful of cells is allowed under X6 to confirm the composited result matches
the algebra, with no seal, no read, no document change. Reviewed; then Decision Log 3 (the bounds
the candidate needs) goes to the user WITH these numbers.

**Review correction beside, 2026-09-22 (§5.171):** the circular-control interpretation above
is withdrawn. `SceneViews.swift:60` supplies `Capsule()`, whose default is `.continuous`, not
`.circular`. `capsule-geometry.swift` and its `.txt` output attest the 120×44 paths without
rendering: default equals explicit continuous and differs from explicit circular; the straight
segment starts at 33.6306 (1.528665 × 22), not 22. The independent review's curve sampling found
−0.303570…+0.043123 CSS px against the circular SDF (down to −0.607 device px at 2x), whereas
explicit circular's cubic approximation stays within +0.006 CSS px. These are path diagnostics,
not a recapture or proof of the fixture's raster alignment. The recorded shell/grid numbers stay;
pixel-area spread is possible but is NOT isolated from geometry, and capsule-vs-md is NOT a
circle-vs-continuous experiment. G0's fixed-mask ceilings remain valid non-regression measurements
because candidate and baseline use the same declared pixels; corner ceilings do not isolate
colour-law accuracy. Both supplied shapes require geometry qualification.

### G1b: The lift's stand-down, the contour leaves, the seal and the read — one merge; opens after G1a is merged and Decision Log 3 is ruled

Ledger **§5.172**; evidence `packages/calibration/results/2026-09-22-w33-g1b-rim-fit/`. In order: the
pre-fit bed on scratch at the shipped bytes; (A)'s declaration and the anchor compensation, one
round, both stops read; the candidate's leaves added through the identity table with their tests; the WGSL term
and the CSS experiment; the stroke's rounds on both poses; the CSS carry-or-decline pinned; the seal
of the four documents (rule 2, every digest site, `macos27-profile.ts` regenerated, the
`window-activation.spec.ts` hashes moved with reason and kept beside); the canonical read, the
ladder, the holdout read once (`configuration.py record` in the seal commit, before the passes); the
split; `PREDICATE_EXCLUDES` as the machine says; the gated count; the sheets and the eye with the
LSB check beside every ×8 panel (W32 §5.168 §10 B-4); M2's per-wave move; the changeset. Reviewed on
the branch; merged with the capture tree.

### G2: The landing — 0.23.0 prepared

Ledger **§5.173**; evidence `packages/calibration/results/2026-09-22-w33-g2-landing/`. The stops
adopted as ruled (Decision Log 3), the cut re-run into its own dir with the `atDocuments` /
`withHoldout` guards and every figure re-derived from the matrix; M2 re-baselined per W32 DL 4;
`CLAUDE.md`, `calibration.ts:187`, both READMEs, CHANGELOG, coverage matrix; the `/laws/` stage
re-checked (its readout is keyed on the digest and the reseal moves what it resolves; the pin must
stay green); the chain (`chain.sh`, `dry-run.sh`, `record-machine.sh` widened to record the
foreign-process count and the idle interval X6 names — W32 §5.169 §10 N7); `pnpm changeset version`
to 0.23.0; Outcomes & Retrospective; Deferred-at-close; the Tracking Map row.

## Cross-Child Contracts

- **X1 — the freeze, intact.** No macOS 26.5-keyed path, row, bound, floor or document changes;
  `freeze.py verify` 1,818 at every merge; the goldens byte-identical (the new leaf's identity is the
  drawn state; the default does not move); the 1,107 gated-row pin holds; the 26.5 digests stay the
  live pin with no exemption spent — which is only true if the leaf is routed through the identity
  table (X13). `digest-supersessions.json` gains no record.
- **X2 — the cut precedes the fit.** G0's referees, forms and bounds are committed and reviewed
  before G1 opens; G1 fits against them and no other.
- **X3 — the fit moves the lift's amplitude (to 0), the three thick anchors (compensation), and the
  new stroke leaf on the macOS 27 documents; nothing else.** Not the σ law, not the lengths, not the
  thin anchors, not the accessibility fold's amplitude, not any structure, chroma, tone, tint or
  scatter constant. A fit that moves a row outside the exterior axis is a warning, not a result.
- **X4 — bounds before reads; a miss is recorded, not widened; holdout once by artifact.** C1, the
  thin stop, B1 and B3 are not re-stated in this wave; a stop that breaks is recorded with its
  decomposition (W32's `b3-window.py` precedent) and put to the user.
- **X5 — no native capture; the canvas does not change; `apps/reference-apple/build` is never
  rebuilt; nothing under `dev.vitrea.reference-apple`.**
- **X6 — RT and IC 0, the slider 0.5, one capture process, ≥ 60 s idle** before every browser run,
  rounds included, every run in `browser-runs.txt` with the foreign-process count.
- **X7 — the evidence layout changes by the one rule**: the split after the read that supersedes,
  `--read-claims "c9a §5.168"`; no schema bump; the replaced pixels to `web-captures-superseded/`.
- **X8 — padding untouched**; the reach's numbers recorded if the stroke widens the drawn extent.
- **X9 — routing.** Children, reviews and fix waves run on `astra-medium` / `astra-high` or the
  default `sol` worker at effort xhigh (the user's ruling, 2026-09-22); the charter's own review on
  `doperpowers:adversarial-reviewer`; reviews read-only; ledger sections as assigned; merges
  `--no-ff -F <file>` with the freeze verified; no attribution trailers, no session URLs; committed
  evidence never rewritten, corrections beside.
- **X10 — the 27 bed never empties between merges.** The four documents' bytes change only in G1's
  merge, which carries rows read at the new bytes; `MATRIX_CELLS` / `PREDICATE_EXCLUDES` move only
  in the commit that carries the read; the gated count pinned at every merge.
- **X11 — file ownership.** G0 owns its dir and `test/w33-*.test.ts`; G1 owns `material.ts`'s new
  leaf and table entry, the WGSL term, `optics.ts`/`css-tier.ts`'s mirror or decline, the four
  documents, the generated profile, `adopted-thresholds.test.ts`'s `PREDICATE_EXCLUDES` and
  `MISSED_27_ROWS`; G2 owns the adopted rows and the docs. Nobody touches `receded-profile.ts`'s
  26.5 block or `DEFAULT_MATERIAL_PROFILE`'s existing values.
- **X12 — the corners are read or the decline is recorded.** No rim term is fitted on straight
  edges alone without the corner arcs' reading beside it (W25 G0's gap, tracker ~1379).
- **X13 — the leaf enters through the identity table in the commit that adds it**, with an
  `inertLawCase` and the gate-group sweep test, so the default IS its identity forever (W29 DL 1 (i)
  + rule 2) and no frozen digest moves.

## Ordering & Dependency Map

G0 (cut, forms, bounds; reviewed, merged `17e8aef5`) → Decision Log 2 RULED (the route: identify
before fitting) → G1a (the model; reviewed) → Decision Log 3 (the bounds the candidate needs —
`DECLARED_CONTOUR_MAX_PX` / IoU re-adopted for an exact term, or the shape-preserving construction
with its loss: the user's, with G1a's numbers) → G1b (one branch, one seal, one read, one merge;
reviewed) → Decision Log 4 (the adopted forms of (A)'s stop and (B)'s bound, the parent's on G1b's
tables) → G2 (landing; reviewed) → the user's `pnpm release` → tag `v0.23.0`.

## Risks & Mitigations

- **`declaredContourMaxWeb` reds on 442 rows** — clause 2: the form settled and, if needed, ruled
  before the fit.
- **`PREDICATE_EXCLUDES` moves as a bed that "grew"** — the same decision; a form outside the rect
  moves neither arm.
- **B3 is green by cancellation and this wave removes the cancelling term** — expected value stated
  by G0; the admitted-band stop is blind to the first pixel by ruling; the whole-exterior warning
  is where it shows.
- **The CSS tier cannot be device-pixel exact** — an experiment with the decline pinned.
- **A device-px unit in a CSS-px material** — named as a new kind in the leaf's doc (DL 1 (c)).
- **The dark active capsule has no stroke** — conditionality explained at G0 before the fit (X12's
  sibling); a term fitted without its off-switch is fitted wrong.
- **The holdout is spent by the wrong gate** — G1 only (DL 1 (d)).
- **The isolation proof is stale on main** — G0 verifies it at open.
- **The stroke population leaks holdout rows into the fit** — excluded by construction (clause 1).
- **A neutral signed rim is assumed where only a notch is measured** — colour space identified at
  G0; the referee is per pixel and per channel.
- **The 26.5 lift is real** — the frozen documents keep it; (A) is 27-only.

## Deferred / Out of Scope

Span 160's canvas (native re-capture, `scenes.json`); promoting a span-160 scene into calibration
or validation (a split change, the user's — DL 1 (b) takes probe strength); the accessibility
documents' shadow amplitudes (W32 Deferred 10); the surface-conditioned retention; the receded
body's opacity (W32 Deferred 12); candidate (i); the per-backdrop residual; the WebGPU pose
crossfade; the split script's per-generation reader; M2's mechanism measurement; B3's instrument
bar and C1's re-pin (W32 Deferred 16, 17); the dark receded document's +0.0475 floor (§5.154 §6);
the second-order rim terms (the lit rim's along-side gap, tracker ~1379) beyond the stroke.

## Tracking Map

| child | status |
| --- | --- |
| G0 | **CUT COMPLETE, REVIEW PENDING 2026-09-22** — §5.170;232 non-holdout black-floor cells,304 single-shape stroke cells with corner arcs,380 finished-alpha counterfactual pairs; native-only census448/264 but14,329,648 pixels (memo14,325,464 unreproduced). Flat identity route proven on all six documents. Four forms tabled: none both reproduces bytes and preserves every gate. DL2 and a direction-aware colour model remain open BEFORE G1. Calibration638/638, freeze1,818, parent isolation17/17. |
| G1a | identification complete as a negative finding, §5.171; 304 non-holdout cells, no full angular stratum closed, diagnostic candidate misses52/304 ceilings; DL3 draft recommends stop, unruled; review pending |
| G1b | **FIT/SEAL/READ COMPLETE, REVIEW PENDING 2026-09-22** — §5.172; **initial record: two commits**, seal`0d0594b2` and read/split`6f3435de` (one existed before that row, so it landed as the second). Declared386-cell pre-fit exact; A removes the lift, B compensates only the three thick anchors, R repeats B exactly. C1 twelve/twelve, B1 six/six, thin206/206 under0.002044, B3=0.0000512877 under0.000056. Black floor232/232 non-holdout cells zero; **four composite holdout cells retain five unchanged base-edge pixels admitted by G0's overlay-only box**, recorded, not widened; G2 corrects that domain before adoption. Two active documents sealed, both receded and both frozen unchanged. Read786 appended, split786 with reader§5.168, working1893; both append-checks6/6, tree786/786, gated230/786, predicate67. M2 all26 pass, worst per-wave−0.039391%;17+9=26 sheets viewed, no new eye-only stop (2x CSS probe gaps labelled, no new rows). Chain2,715/189 files, goldens34, activation6, freeze1,818. No contour leaf, rim move, native capture, merge or publish.  **Final hand-off: three commits** — a records-only verification-scope correction is carried by this third commit. Full-range diff-check reports1,511 whitespace diagnostics only in23 retained raw text logs; excluding those logs, authored source/docs/JSON pass. |
| G2 | **LANDING PREPARED, REVIEW PENDING 2026-09-22**, §5.173. X1:218 standard single-shape cells,12,681,980 integer pixels /12,593,524 analytic, all0 >0/>1; G0's232 preserved with10 accessibility+4 composites excluded. Union-box correction:12 composites zero, five legacy holdout pixels retained. M2 confirms26/26, worst per-wave−0.039391%; missed set8, predicate67.24 canonical+4 demo sheets viewed; temporary stripe stop closed by raw two-axis samples, no fix or W32 eye correction. Chain:2,719 units/189 files, goldens34, GPU48, platform410, React174 pass/3 skip, demo61 on isolated5197 after normal5177 setup collision (zero tests, kept). X6 all four facts before every launch; final freeze1,818, gated230/786. Version0.23.0 generated, dry-run/packs green; no material/document/read change, merge, push, publish or tag. |

**G1a status clarification, 2026-09-22:** G0's row above preserves its gate-close wording.
G0 was subsequently reviewed and merged at`17e8aef5`; DL2 is RULED as recorded below, not open.
G1a's finding now blocks the contour model, not the already-completed G0 review.

**Rows above kept as written at each gate's close; the parent's record, 2026-09-23:** every child
was reviewed and merged — G0 `17e8aef5`, G1a `5fdba5f4`, G1b `979c63e3`, G2 `e6c80c7f` — and
0.23.0 is PUBLISHED (Status, Outcomes, Revision Notes). No "REVIEW PENDING" above is still open.

## Decision Log

### Decision Log 1 — 2026-09-22: (a), (b), (c), (d) RULED by the parent under the standing "rest on your judgement"

**(a) The lift stands down on macOS 27 — RULED: a declared reading, not a fit.** Apple's macOS 27
exterior over a backdrop-black pixel is exactly 0 on 2,646,074 pixels across 117 cells, both
schemes; Apple's macOS 26.5 exterior is nonzero on 39 of 125 cells. `liftAmplitude` is written 0 on
the four macOS 27 documents with the measurement beside it, the frozen documents keep theirs, and
the anchors take the trade (estimated 5–7 % at span 160) under C1. Reason: a leaf the black pixels determine at 0 was
being fitted on pixels that cannot see it (§5.62 §4's 0.9998), and W32 Decision Log 2 is the
precedent for declaring what the bed measures at zero.

**(b) Span 160 — RULED: probe strength.** No calibration or validation scene exists at span 160 and
every black-floor cell there is `probe`; the claim at 160 is stated at probe strength the way
`thickOcclusionAt160` is, and promoting a scene is a `scenes.json` split change the user may make
for a later wave (Deferred).

**(c) The stroke's width is a DEVICE-PIXEL unit, named as such — RULED.** The stroke is one device
pixel at 1x and 2x; a CSS-px pair (1.0 / 0.5) would encode that fact twice and drift at dpr 3. The
material gains its first device-px leaf, declared as a new kind in the leaf's doc comment, with the
2x bed as the proof.

**(d) G1 spends the one holdout read — RULED.** `configuration.py` refuses a read at today's
configuration; G1 is the only gate that moves a constant; the read follows its seal and precedes
nothing else. Two seals in one wave would be two reads and two generations; this wave has one.

### Decision Log 2 — RULED by the parent 2026-09-22 at G0's merge, under the standing "rest on your judgement": the ROUTE is identification before fitting; the bound question is deferred to Decision Log 3 with numbers

**Ruled.** G0's tables (below, unruled as G0 drafted them) show that none of the four tested
constructions both reproduces Apple's bytes and keeps the contour and IoU promises, that the term
depends on the contour's orientation (a scheme × pose × orientation dependence at least), and that
no one- or two-parameter colour family closes. G0's independent review added that the committed
oracle is a source-over oracle and not a bound over every replacement layer (a replacement-colour
oracle reduces the failures from 73 / 59 to 40 / 45 broad, 19 / 22 selected), and demonstrated a
shape-preserving construction (coverage grows, sub-threshold pixels capped at 127/255) at 0 / 0
shape failures and a ring MAE of 3.5 bytes offline. Asking the user to weaken two adopted promises
for a two-leaf scalar model G0 has shown insufficient would be the wrong question. So: **G1 is
split. G1a identifies the term's angular, radial-coverage and colour response and produces one
composable candidate with both prices tabled; Decision Log 3 then puts the bound question to the
user with those numbers; G1b fits, seals and reads.** The lift's stand-down (A) waits for G1b so
the wave keeps one seal and one read (Decision Log 1 (d)). What this costs: one more gate before
any document moves, and a wave that may end with the finding that the term is not identifiable on
this bed — recorded, with the shape of the capture that would identify it.

**G0 draft, 2026-09-22 — UNRULED (§5.170).** Over380 non-holdout captures:

| construction | broad contour/IoU failures | actual selected failures | consequence |
| --- | ---: | ---: | --- |
| outside measured scalar source-over |17/17|1/1|does not reproduce the full colour law either|
| inside measured scalar source-over |2/1|0/0|wrong side of the measured contour; five broad conditioning changes, one selected; all26 M2 cells touched|
| outside source-over0.49 |113/123|37/41|finished alpha crosses0.5 despite stroke alpha below it|
| colour at held coverage |0/0|0/0|native RGB infeasible on380/380 cells,378 with one-byte tolerance|

Even an independently coloured per-pixel minimum-alpha oracle forces73/59 broad and23/24
selected contour/IoU failures. **Proposed ruling question, not an adopted change:** permit
re-adoption of `DECLARED_CONTOUR_MAX_PX` from1 to√2 for an exterior term, AND decide what to
do about the measured IoU conflict, or explicitly accept a held-coverage approximation with
its residual. Re-pinning contour alone does not settle the choice; no new IoU bound is invented
here. An inside stroke avoids the selected conformance failures but does not draw the term
where Apple's bytes place it. No option has been silently chosen.

**The model decision is also open:** dark active capsule straight edges read−0.27 while its
arcs read−19.75. Span or a capsule-wide disable is insufficient; orientation×scheme×pose is
necessary. Tested neutral/tinted multiply/add/affine families in encoded/linear sRGB leave
multi-byte per-channel residuals even with side stratification. A two-leaf amplitude/width
implementation must not claim an identified colour law G0 did not find. The flat identity
route is proved independently of which eventual colour law wins.

### Decision Log 3 — the bounds the identified candidate needs (after G1a; the user's)

Put to the user with G1a's numbers: re-adopt `DECLARED_CONTOUR_MAX_PX` (1 → √2) and the IoU floor
for a term drawn where Apple's bytes sit, or take the shape-preserving construction with its
measured fidelity loss, or stop the term at the finding. A bound's re-pin is the user's.

**G1a draft, 2026-09-22, §5.171 — UNRULED. Recommend stop at the finding.** None of the
sampled angular families closes a full16-bin stratum under the one-byte diagnostic; an
angular affine colour candidate still misses52 of G0's304 ceilings. This is not a reason to
weaken two shape promises. Its exact placement prices36 contour/14 IoU failures on298
conformance-bearing non-holdout cells (42 distinct rows; selected2/1 failures on2 rows),
max√2/minIoU.971292, with ringMAE18.1383 bytes. Its shape-preserving cap prices0/0 but leaves
18.4324 bytes, losing.811/.909/.661 RGB against that already-unclosed target. A native-colour
oracle reaches0 error but costs40/45 failures on380 conformance cells, minIoU.945652; its cap
keeps shape at3.4464 bytes on386 cells (six additional cells have no conformance axis). That
oracle is NOT a material law. Cell-by-cell tables, all six offsets and stop forecasts are in
G1a's evidence; no bounds have moved.

The capsule raster control places the nominal one-device-pixel band at centre.5 but shows
pixel-area residual in adjacent shells. An antialiased implementation can touch M2 where
this price experiment's support[0,1) does not; it needs its own price. Next identifying work
is a repeated uniform-grey/RGB, subpixel-phase capsule capture, then matched continuous
rectangles, both scales/poses/schemes, with a declared split and noise bar. That needs a new
native-capture authorisation beyond X5. The lift's stand-down is still independently supported
and waits under the one-seal route; splitting it is not a decision G1a has taken.

**Review correction beside, 2026-09-22 (§5.171):** the circular-control interpretation above
is withdrawn. `SceneViews.swift:60` supplies `Capsule()`, whose default is `.continuous`, not
`.circular`. `capsule-geometry.swift` and its `.txt` output attest the 120×44 paths without
rendering: default equals explicit continuous and differs from explicit circular; the straight
segment starts at 33.6306 (1.528665 × 22), not 22. The independent review's curve sampling found
−0.303570…+0.043123 CSS px against the circular SDF (down to −0.607 device px at 2x), whereas
explicit circular's cubic approximation stays within +0.006 CSS px. These are path diagnostics,
not a recapture or proof of the fixture's raster alignment. The recorded shell/grid numbers stay;
pixel-area spread is possible but is NOT isolated from geometry, and capsule-vs-md is NOT a
circle-vs-continuous experiment. G0's fixed-mask ceilings remain valid non-regression measurements
because candidate and baseline use the same declared pixels; corner ceilings do not isolate
colour-law accuracy. Both supplied shapes require geometry qualification.

**Third option beside the DL3 draft, 2026-09-22 (§5.171), UNRULED:** the independent reviewer
also demonstrated a gate-preserving EMPIRICAL partial correction, rather than waiting for an
identified law or relaxing conformance. It uses isotropic affine on light-active and both
inactive endpoints, `|nx|²` on dark-active, accessibility unchanged, and coefficients shared
across 1x/2x. Attenuation is sampled under every G0 ceiling with the finished-alpha cap.
Pooled ring MAE is **34.979077 → 20.599081 bytes**, with **0/0** contour/IoU failures,
zero predicate/area/body/native-mask changes, min IoU **0.998722**, and whole-exterior warning
**0.000446**. Isotropic-only leaves **23.768**; `|nx|²`-only **29.561** (dark-active
**25.649 → 12.304**). The baseline is **34.979077**, not “~20”; 20.599 is the partial result.
These are in-sample, offline, unrendered review readings: no CSS result and no identified law.
`review-partial-combined.py` and `review-partial-prices.json` preserve the review artifacts.
**Recommendation remains stop at the finding:** geometry and colour remain unseparated, and
in-sample non-regression is not identification or rendered validation. This empirical route
is the user's third alternative, not a ruling or authority to implement it; no gate moves.

**Amended next-capture plan beside, 2026-09-22 (§5.171):** capture an explicitly circular,
path-ATTESTED capsule beside the default continuous one, then matched continuous rectangles;
export the supplied geometry and alignment and include an opaque geometry/raster control.
Use matched local-colour scenes with varied backdrop-gradient direction and magnitude and a
small spatial-frequency ladder, alongside uniform-grey/RGB and subpixel-phase sweeps at both
scales, schemes and poses. No-glass references must have identical colour management. Take
enough repeats to estimate per-bin, per-channel variability BEFORE choosing a closure threshold;
declare calibration/validation/holdout membership before fitting. This is a plan requiring new
native-capture authorisation, not a capture performed here; X5 remains intact.

**RULED by the user 2026-09-22 at G1a's merge: stop; G1b is the lift's stand-down alone; the
identifying capture is a later wave's.** Put with G1a's numbers and its review's: no angular,
radial or colour law closes on this bed (0 of 152 strata at one byte, 8 at three); the harness's
capsules are `.continuous` by default so corner geometry and colour are not separable here; the
falsified candidate drawn exactly would red 42 rows for an 18-byte ring error; the gate-preserving
empirical partial correction reaches 20.60 from 34.98 bytes with every bound kept but is an
in-sample fit with no identified law, no render and no CSS result. The three options put were: stop
(recommended), ship the empirical correction as declared leaves, or re-pin the contour and IoU
bounds for the exact candidate. Ruled: stop. What follows: X3 narrows to the lift's amplitude and
the three thick anchors; no contour leaf enters the material this wave; the identity route G0 and
G1a proved is recorded for the wave that adds one; the contour term goes to Deferred-at-close
FIRST with §5.171's capture plan as the shape of the work (path-attested circular capsules beside
the default continuous ones, matched continuous rectangles, uniform grey/RGB levels, independent
x/y subpixel-phase sweeps at 1x/2x, matched local-colour scenes with varied backdrop gradients and
a spatial-frequency ladder, colour-managed no-glass references, repeats for a per-bin variability
estimate before any closure threshold, and the calibration/validation/holdout split declared before
fitting — a native capture, so a wave with the harness and X5 lifted by the user).

### Decision Log 4 — the adopted forms of (A)'s stop and (B)'s bound (after G1b's read)

**RULED by the parent at G1b's merge and confirmed at G2, 2026-09-22 (§5.173).**
(A) is adopted as **X1**: fraction of native-black exterior pixels where WebGPU reads>0,
and separately count>1, both exactly0, on non-holdout standard-profile single-shape cells
in both poses across the nine black-bearing backdrops. Integer offset>=2 from the declared
box is G0's convention; analytic distance>=2 CSS px is recorded beside. The cut is regenerated
at G2 by G0's byte-identical reader with shipped/no-holdout guards; the test derives the
population from scenes+matrix and every count from matrix-named capture pixels. Missing pixels
are UNMEASURED, not a vacuous pass; cut/pixel/excuse mutations must fail.

**Population correction, not a changed selection:** G0's232 includes10 accessibility and4
composite cells; this ruled domain is **218**. The accessibility cells read0 too, but the
fold stands down the material W33 changed, so the row would otherwise promise something about
a material the wave did not change, as M1/M2/C1's exclusions already recognize. Composites
are declined: G0's overlay-only box admitted five base-surface pixels on four holdout cells.
G2 records the union-box correction beside those counts, without replacing the legacy evidence.
(B) has no adopted row under Decision Log3; G0's304 stratified ceilings remain declared
script-read stops, not a fit or an adopted contour fidelity claim.

## Surprises & Discoveries

- G1b (§5.172): the holdout's five one-byte pixels all sit INSIDE the base box of
  glass-over-glass and outside the overlay box G0 used as the whole component.
  Their bytes are unchanged from the prior generation, including the receded
  poses. The material's non-holdout lift floor closes; the composite referee's
  domain must be corrected by G2 before adoption, without deleting this reading.
- G1b: thick anchors reach span44 through the existing thin/thick blend (weight
  0.0239777479), so thin is bounded, not identical, despite unchanged thin anchors.
  The first scratch selection also overreached into undeclared probes;19 measured
  extras and two unmeasurable cells are retained as diagnostic-only and excluded
  from the386-cell fit and786-row canonical read.


- The black floor's population in W32's record was wrong on two of three backdrops: `photo` has no
  black pixel and `dark-solid` is (28,28,30); the bed is nine backdrops and the worst cells are the
  `hc-text` rasters at up to 98.9 % nonzero, not the checkerboards.
- "14 to 30 bytes darker" at four committed sites is outside the artifact at both ends (−13.19 to
  −31.95). G0 corrects beside.
- Apple's macOS 26.5 material has a lift and no stroke; macOS 27 has a stroke and no lift. Both
  gaps are 27-only, which is what lets the frozen bed stay untouched.

## Revision Notes

- 2026-09-23 (the parent): **0.23.0 PUBLISHED**, by the user's `pnpm release` on `e6c80c7f`; tag
  `v0.23.0` pushed; cold install verified in a fresh directory (exports 44 / 258 / 38, the four
  macOS 27 digests and the 26.5 pair reading their own fields, `liftAmplitude: 0` and the fitted
  lengths in the bundle, the three published tarballs byte-for-byte the rehearsal's sizes); the
  Status line, the Outcomes, the Tracking Map clarification and the c9d row carry the record. The
  wave is closed and published.
- 2026-09-22 (the parent, G2's merge): G2 merged to main as `e6c80c7f` with `--no-ff` after an
  independent review that reproduced every artifact and merged as-is, its two record nits closed
  on the branch (`d26a2fa9` the 0.23.0 row joining its table and the publish-history sentence
  corrected beside; `75435daf` the rehearsal note); `freeze.py verify` 1,818; no capture tree moved
  (G2 read G1b's canonical tree and captured nothing); the chain re-run on main after the merge —
  build and lint 9 / 9, `pnpm -r test` 2,719 over 189 files; an orphaned Vite server (PID 53715, port
  5177) left by the removed W32 G2 worktree was stopped by the parent, which is why G2's demo run
  is the single isolated-port one. Versions 0.23.0 across the fixed group; `pnpm release` handed
  to the user.
- 2026-09-22 (the parent, G1b's merge): G1b merged to main as `979c63e3` with `--no-ff` after an
  independent review with no blocking findings; `freeze.py verify` 1,818; the read's capture tree
  copied to the canonical `web-captures/` (checker 1,900 / 1,893 match / 0 mismatch / 7 no-row), the
  replaced generation aside at `web-captures-superseded/d5bdd6eac432/` and `431cabd391c4/`; gated
  230 / 786 pinned on main. Decision Log 4 (the adopted forms) ruled by the parent in G2's brief:
  (A) adopted as a row over the non-holdout single-shape cells, composites declined pending the
  union-box correction; (B) has no adopted row.

- 2026-09-22 (G1b verification-scope closure, §5.172 §11): the full branch's
  diff-check is qualified for retained CLI whitespace in raw text evidence.
  Source/docs/JSON are clean; no material, matrix, capture or test result changes.
  The branch has three commits including this records-only closure.


- 2026-09-22 (G1b, §5.172): the lift-only route is executed under DL1(a)/(d) and
  DL3. B's compensated material meets all declared fitting stops and repeats
  exactly; the canonical read is sealed once and includes one holdout artifact.
  G0's overlay-only composite exterior mask admits five unchanged base-edge pixels
  on four2x holdout rows. That zero-target miss is kept, not fitted or widened;
  G2 owns the union-box/decline correction before adopting the referee. The thin
  order-statistic +0.000011 is diagnostic; the per-cell bar is met. Near3–6 bands
  trade while C1 passes, and CSS's no-floor statement is mask-qualified.


- 2026-09-22 (the parent, G1a's merge): v4. Decision Log 3 ruled by the user — stop the contour
  term; G1b is the lift's stand-down, the seal and the read alone; X3 read as narrowed to the lift's
  amplitude and the three thick anchors; clause 5 and the contour halves of clauses 1–3 are
  recorded as executed by G0/G1a to the finding, not to a fit; the capture that would identify the
  term is Deferred-at-close's first item. G1a's merge is `5fdba5f4` (two commits, `b943bab4` and
  the review closure `a77f79de`).

- 2026-09-22 (the parent, G0's merge): v3. Decision Log 2 ruled as a route on G0's tables and its
  review; G1 split into G1a (identification, §5.171) and G1b (fit, seal, read, §5.172); G2 moves
  to §5.173; Decision Log 3 becomes the user's bound question with G1a's numbers and Decision Log 4
  the parent's adopted forms. The clauses' text still says "G1" where it means G1b; read it so.

- 2026-09-22 (the parent): v2 after the adversarial review, five blocking findings folded, all
  reproduced by the reviewer against the artifacts: (1) a nested `contourStroke {}` container
  survives rule 2's drop and moves the frozen light digest (`b2b570e4adcea8fb → da59d059f9526c73`)
  — flat leaves, proof before shader code, the literal identity assertion widened; (2)
  `DRAWN_ALPHA_THRESHOLD` reads the finished capture's alpha, so "outside at < 0.5 survives" was
  false (a 0.49 stroke over the 0.43 ring thresholds 320 pixels), the "442 rows at zero headroom"
  is 254 of 442 (48 of the test's 115), and a fourth form (colour at held coverage) is tabled; (3)
  the widened bed is not at the floors v1 called green — `web > 1` reads 66 / 39 pixels on the two
  `hc-text` cells and offsets 2–6 read to −2.07 — so those are per-cell baselines, the zero is a
  separate target, and the exterior convention (integer offset 2 ≠ analytic ≥ 2 CSS px) is named;
  (4) the stroke population carried five (light) / two (dark) holdout scenes per pose — excluded
  from every round by construction; (5) `|n − w|` of channel-and-sample means can be zeroed by a
  wrong uniform stroke — the referee is per pixel and per channel, stratified, and the stroke's
  colour space is identified first. Non-blocking: the 2.4 % trade estimate used linear occlusion
  as encoded alpha; corrected to 5–7 % and labelled an estimate.

- 2026-09-22 (the parent): v1 chartered on main after the 0.22.0 publish record, from W32 Deferred
  items 2 and 3 and the grounding memo; sent to adversarial review before any child opens.


- 2026-09-22 (G0, claims §5.170): the widened cut is committed without moving a material.
  Its full-exterior native inventory reproduces448/264 and30 held-out cells but not the memo's
  pixel total:14,329,648 under a declared far-exclusive bbox convention,14,325,464 retained as
  an unreproduced helper reading after four convention checks. The separate backdrop-black
  lift inventory reproduces exactly, including39/125 nonzero frozen-control cells. Corners
  falsify the whole-capsule stroke off-switch; simple colour families do not identify the
  intended compositing law. Finished-alpha tests find an IoU conflict beside the expected
  contour conflict, so DL2 is drafted rather than ruled. A's0 target and304 stratified B
  ceilings are declared, per-cell offsets2–6 preserved, accessibility independence declared,
  flat versus nested identity routes proved. Four stale-record categories corrected beside,
  plus repeated copies. Parent's opening proof17/17 has X6 accessibility readings but no
  recorded process count or idle interval. Calibration638/638; freeze1,818. G1 stays closed
  until review and model/form resolution; no source, profile, matrix or26.5 path changed.

- 2026-09-22 (G1a, claims §5.171): identification complete with no law closed. Angular/radial/
  colour tables and a17-flat-leaf diagnostic candidate committed; all six digests held in
  memory; candidate exact/capped and native-colour oracle prices kept separate. DL3 now has
  numbers and a stop-at-the-finding recommendation, UNRULED. G1b remains blocked on the model
  decision; no source, document, seal, capture, holdout read or bound moved. Calibration644,
  freeze1,818. The0 M2-touch forecast is for support[0,1), not an antialiased corner kernel.


## Outcomes & Retrospective — W33 G2, 2026-09-22 (§5.173)

1. **The widened cut preceded the fit.** G0 reproduced the black-floor and absolute-RGB
   referees, including corners and both exterior masks. Its232-cell floor population remains
   recorded; X1 narrows it to218 by the ruled standard/single-shape selection. The contour
   half executed to G1a's finding, not to a fit: angular response, continuous-corner geometry
   and colour cannot be separated on this bed. The unexplained4,184-pixel native-census
   difference remains beside the reproduced14,329,648, not rewritten away.
2. **Compositing prices were read before any constant moved.** G0/G1a exposed both contour
   and IoU costs and the held-coverage infeasibility; the capped empirical construction keeps
   gates but is no identified law. The user chose the finding, not either re-pin. No contour,
   area, conditioning or identity-table change was smuggled into G1b.
3. **Bounds preceded reads.** C1, B1, B3 and thin stops held at B/R; A's failures remain
   recorded. M2 is confirmed against W32 G1's generation under W32 DL4, all26 passing with
   cumulative drift still visible. The contour half executed to304 declared ceilings and
   forecasts, not to an adopted bound. G2 adopts only X1 under DL4; predicates stay67.
4. **The lift stands down and the anchors take the trade.** Both active amplitudes are0,
   both receded endpoints already0. B/R closes232/232 referee cells; the separate holdout
   composite-mask miss is corrected as geometry, not fitted away. The dark leaf-only
   experiment removes23,015 nonzero pixels, so its0.0005 is separately visible. The actual
   span160 peak encoded-alpha reductions are7.9983% light/1.9815% dark. C1 passes12/12,
   but the3–6 band is a near/far trade, not literally held unchanged; §5.172 records every
   increase. Halo max6.119589 on the non-holdout probe analogue, with post-seal held-out
   readings6.11/6.12. Frozen26.5 keeps its real lifts and both digests.
5. **Executed to the finding, not to a fitted stroke.** DL3 supersedes this clause's planned
   implementation: no contour leaf, shader term, identity-table append or CSS experiment
   landed. Flat identity routing was proved for later work; it does not identify a material.
   The user's declined empirical partial correction and the identifying capture remain first
   in the deferred work, rather than an implemented approximation called closure.
6. **One seal/read/merge, already landed by G1b.** Two active files changed, both receded and
   both frozen files stayed byte-identical. The786-row read, its one holdout artifact, the
   split with reader§5.168 and the canonical capture-tree transfer landed together. G2 only
   reads those pixels and matrix rows; no native or calibration capture and no second seal.
7. **Landing and every gap recorded.** X1's cut/pixel/excuse discrimination is red on each
   deliberate scratch mutation; missing capture trees explicitly skip the pixel case as
   UNMEASURED. Docs now describe zero active lift and the unidentified contour. The sheets
   carry the LSB check; raw samples resolved a temporary eye stop without a fix or correction
   to W32's eye record. The version is generated0.23.0; the per-suite chain and rehearsal
   record are §5.173's close, with review and publication still the parent's/user's actions.

**CLOSED 2026-09-22 by G2's merge and 0.23.0 PUBLISHED 2026-09-23.** G2 was independently
reviewed on its branch (every artifact reproduced, merged as-is, two record nits closed there as
`d26a2fa9` and `75435daf`) and merged to main as `e6c80c7f` with `--no-ff` on 2026-09-22;
`freeze.py verify` 1,818 at the merge and the chain re-run on main (build and lint 9 / 9, unit
2,719 over 189 files). **0.23.0 published by the user's `pnpm release` on `e6c80c7f`** (no commit
followed the merge before the publish); tag `v0.23.0` (annotated, on that commit) pushed by the
parent the same day. Registry: core 2026-09-23T00:05:49.034Z, web 00:05:51.043Z, react
00:05:52.031Z — the whole group listed in 3.0 s, and every tarball served on the first cold install
two minutes later. Cold install verified in a fresh `mktemp -d` directory
(`/tmp/vitrea-cold-0.23.0-GAl9`, so the 0.22.0 record's leftover-directory pitfall could not recur):
all three at 0.23.0, ranges `^0.23.0` in both dependents; exports core 44 / web 258 / react 38
(`useGlassRootHandle` a function); the shipped macOS 27 documents report `dcbccbd9feac9881` /
`e59f9106bcd7c966` / `f34dcc03e2774db3` / `6b6237b7ae241638` and the frozen 26.5 pair
`b2b570e4adcea8fb` / `874be66ea501621b`, all six reading their own recorded fields; the web bundle
`dist/index.js` is 303,207 B (0.22.0 shipped 303,215) and carries `liftAmplitude: 0` on the macOS 27
endpoints with the frozen 26.5 actives' `0.01` / `51e-4` beside, `spreadPx: 0.5` / `1.8` and
`sigmaThinOffsetPx: -6.318`. The three tarballs fetched back from the registry are **583,802 /
568,292 / 188,179 B — byte-for-byte the rehearsal's sizes in `dry-run.txt`**, so what npm serves is
the tree the chain ran on.

## Deferred at close — in priority order

1. **Identify the contour term on a qualified native bed, first.** §5.171's amended plan:
   path-attested explicitly circular capsules beside default continuous ones, matched
   continuous rectangles and exported geometry/alignment plus an opaque raster control;
   uniform-grey/RGB levels; independent x/y subpixel-phase sweeps at1x/2x, both schemes and
   poses; matched local-colour scenes with varied gradient direction/magnitude and a spatial-
   frequency ladder; identically colour-managed no-glass references; repeats for per-bin,
   per-channel variability BEFORE any closure threshold; calibration/validation/holdout
   membership declared before fitting. This is native capture with X5 lifted by the user.
2. **The empirical partial correction is declined, not forgotten.**20.599081 from34.979077
   bytes, gate-preserving and in-sample, offline/unrendered, with no CSS result or identified
   law. DL3 chose not to ship it; reopening it is the user's option, not implied next work.
3. **Composite referee geometry.** G2's union box closes the five legacy pixels and the
   urgent correction; X1 still declines composites. An adopted composite row needs a
   geometry-qualified domain, not the overlay surrogate or an unexamined box contour.
4. **Span160's canvas and split.**19.5px clearance, no calibration/validation scene there;
   claims remain probe-strength. A larger native canvas and promotion of a scene require
   their own capture/split decision. Span128's truncated outer band stays excluded.
5. **Accessibility shadow amplitudes.** The inherited thin mismatch up to0.02168 is not
   fixed by zeroing the standard lift; these documents were not fitted. Isolated IC and
   dark/2x accessibility remain unmeasured; no policy generalization is inferred.
6. **B3's instrument bar.** The adopted0.000056 functional still needs its own native-pair
   variability measurement; the admitted inactive half is blind to the stopped contour.
   Whole-exterior departure is a warning, not a new bound.
7. **C1's re-pin is the user's.** The unchanged0.0042 leaves headroom at96; a fit that widens
   that span can pass. Do not claim the current bound forbids every near-band trade.
8. **Dark receded +0.0475 floor**, §5.154 §6, over near-black with lift already0: a different
   body phenomenon. The receded body's excess structure/opacity gap remains beside it.
9. **CSS near-contour residual.** Integer-box centres1.5CSSpx out carry170/134/165 pixels
   on the cited cells, while analytic box>=2 is0; curved-contour reach differs again.
   Geometry-qualified contour work owns it, not another lift fit. Eight2x CSS probe panels
   remain unmeasured and labelled; no row was invented to fill a sheet.
10. **M2's mechanism and cumulative drift.** Test the contour-ring hypothesis by eroding
    the native-derived mask on existing captures. Rebaselining bounds per-wave movement,
    not accumulated change; −2.774796% on the small inactive light cell stays recorded.
11. **Body chroma/structure and per-backdrop residuals**, surface-conditioned retention,
    the unfitted directional/along-side rim and fitted-σ candidate(i) remain separate gaps.
    A floor closure is not a full material match, as the photo and checker sheets show.
12. **WebGPU pose crossfade and pre-mount drawn identity.** The former remains one-frame
    endpoint swapping; the latter cannot be inferred solely from a selected document or its
    untuned endpoint digest. The page reads the tuned flag beside identity.
13. **Evidence tooling limits.** Capture-tree checking names a generation, not pixel hashes
    of a re-capture at unchanged documents; the split's per-generation reader and the
    unverified-helper4,184-pixel census discrepancy remain recorded, not normalized away.

### G2 revision note — 2026-09-22

DL4's domain is made explicit as218 rather than the broader232 referee cells, with the
excluded14 named and read. A temporary eye stop on apparent stripes was falsified by raw
checker samples and two-axis interior transects; the shadow'sσ was not the body's blur.
No W32 eye correction or demo fix was warranted. Six green non-browser chain steps were
retained and only the unrun browser tail resumed behind fresh X6. No material, document,
identity leaf, matrix row, holdout artifact or frozen-keyed path changed at this gate.


**G2 close, 2026-09-22 (§5.173):** the single isolated demo invocation passed61/61;
its normal-port predecessor ran no tests because an orphan from a removed worktree held5177.
The parent stopped that orphan after the isolated run had begun; no second demo run followed.
All required chain suites now pass, with the setup failure retained rather than overwritten.
The final freeze after the last browser is 1,818. Version 0.23.0 is prepared and rehearsed, not published;
independent review remains next. No branch merge or outward release action was taken here.
