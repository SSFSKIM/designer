# W37 — the edge wave: Apple's active inner line as a colour-conditioned angular law (2026-09-25)

**CLOSED 2026-09-25 at the finding, by the ruled hard stop, at G0b's merge (`2b61747e`).** Two
identification gates on W34's archive, five declared law families identified on Apple's pixels
alone, none nominated: the best reaches 8.13 codes on the calibration greys' straight bins against
one, every family trades bins against the old treatment, and the archive carries a
coefficient-independent obstruction — at matched inward depth Apple's TOP edge reads 250 where its
BOTTOM reads 253 on light grey-255, at both scales, repeatable, persisting 7–14 CSS px inward — so
no law even in the normal's vertical component can close. Nothing shipped moved; no capture, no
browser run, no holdout read; freeze 1,818 at both merges. The edge's next wave needs a directional
term and a native experiment (Deferred at close 1). Status history: chartered v1–v3 2026-09-25; G0
merged `e143360d`; G0b merged `2b61747e`.** v2 had two adversarial rounds (one P1 and three P2 folded, then approved with no material finding; Revision Notes). Chartered by the parent on the user's
"W37 edge wave (Recommended)" after 0.24.0 published (main `4a74311d`), under the standing "rest on
your judgement" and the routing the user set on 2026-09-22 (X9, with the Codex-limit fallback).
Grounded on a read-only memo taken off W34's archive through W35's guarded readers, W36's preserved
black captures and the runtime's sources (`/Users/new/.claude/jobs/17c7ce02/tmp/w37-grounding.md`,
an `astra-high` product whose 208-cell replay matched W35's stored readings with zero difference;
its evidence under `~/vitrea-w37/grounding/`; every number it carries is reproduced by G0 into this
wave's committed evidence before anything is fitted).

## Purpose

Apple's macOS 27 material draws, in the ACTIVE pose only, a bright line about one CSS px wide just
inside the body's edge, over a shallow shoulder that rises through the six CSS px before it. W35
(§5.177) read it and found the existing rim cannot draw it; W36 (§5.179–§5.180) then landed the deep
body's level at black, so the line now sits on a correct body at black and the edge is the largest
gap left on every active window. The memo establishes three facts an operator has to express:

1. **It is vertically two-lobed, not diagonal.** On the 2x grey-128 circular capsule the line's
   excess over the deep body by outward-normal angle reads (G channel, light / dark) −2.5 / −2.0 at
   0°, 6.7 / 10.1 at 45°, 20.5 / 27.0 at 90°, −2.5 / −2.0 at 180° and 25.0 / 33.3 at 270°; the matched
   rectangles read top/bottom +24 / +23 light and +30 / +29 dark at 1x against left/right −3 / −3
   and −2 / −2. vitrea's rim is `(sqrt(2)·|n·axis|)^0.85` on the axis (−0.7071, −0.7071): a diagonal
   band that lights the sides Apple leaves dark. The lobe's exact form (|sin θ|, sin²θ, a sharper
   even power) is discriminable on the archive but not yet selected; the raw profile is more
   concentrated near vertical than a sine, the upper and lower extrema sample different mean inward
   distances (0.431 versus 0.334 CSS px), and a bidirectional light and an even two-lobe function
   are the same function on this bed.
2. **It is colour-conditioned in a way neutral light cannot be.** On the greys the excess falls with
   level (light 1x: 30 at grey-0 → 24 at grey-128 → 2 at grey-255; dark 1x: 41 → 30 → 25); on the
   saturated solids the strong channels rise while the weak channel is flat or FALLS (dark cyan
   18/186/186 → +1/+47/+47 on the last 2x row; light yellow 230/230/81 → +24/+24/−7 at 1x; dark
   yellow +39/+39/−5; light blue +27/+27/0 with blue censored at 255). No scalar multiplication,
   white addition or white-screen coefficient in either linear or encoded light fits two channels of
   one cell (dark cyan: encoded multipliers 1.056 versus 1.253, linear 1.077 versus 1.659). The
   existing boundary has the form `k(d)·bodyRGB + scalarWhite(normal, d)`, so neutral light cancels
   from channel differences and the isotropic multiplier is all that is left; native dark cyan loses
   chromatic contrast at horizontal normals (0 / −2.25 / −2.25) and gains it at vertical ones
   (+1 / +32.5 / +32) at matched depths. Rotating `rimLitAxis` and narrowing `rimWidth` fix the
   morphology; they cannot supply that.
3. **It depends on the surface.** The 160×96 continuous validation capsule reads a top excess of
   only 6 codes at 1x dark (7 and 22.85 at 2x) against 30 on the 120×44 circular capsule, so the
   line's amplitude is conditioned on thickness or on the corner's path. The calibration set holds
   the pair that separates the PATH at one thickness (the continuous 120×44 beside the circular
   120×44) and no second thickness at all — every admitted uniform calibration geometry casts span
   44 — so a thickness response is not identifiable on calibration in this wave (clause 2).

And two facts that bound the wave: after W36 the deep body at black is exact (132 / 32) while the
top line still reads 149 / 64 at 1x against Apple's 162 / 73 — the edge is the remaining miss on
black, not the level; and the inactive pose is flat to the code on every admitted circular cell, so
whatever ships is zero in both receded documents by construction.

The purpose is that line on the WebGPU tier on the four macOS 27 endpoints, as a declared law fitted
on W34's archive and transferred to the canonical bed under every stop the ledger carries, with an
edge row of its own adopted, the CSS tier deriving what box-shadow can carry, the frozen bed
untouched, and every gap written down — including "unidentified", which is a permitted outcome.

## Parent-Level Acceptance

1. **The cut precedes the fit, with the memo reproduced and the closure declared.** G0 reproduces
   the memo into committed scripts through W35's guarded readers (calibration and validation
   admitted; W34's holdout spent and refused; the canonical native reader's probe extension as
   W35 left it): the grey ladder per scheme × scale × row, the twelve solids per channel, the
   angular profile at 2x by whole-pixel arc bin and the straight bins at both scales, the matched
   rectangles, the continuous-versus-circular pair at 120×44 and the 160×96 continuous cells, the
   shoulder shells −6…−2, the inactive identity, W36's preserved black captures, and the bar
   (≤ 0.5 code, `bar.json`). It then DECLARES, before any candidate is scored, the wave's closure:
   the estimand is the line's excess over the deep body (the channel median at d ≤ −6 CSS px) per
   cell and per channel on W35's frozen whole-pixel domain (`domain.json`, −6 ≤ d/scale < 0, all
   four corners inside the aligned body; straight and arc bins never pooled; population floor 4);
   the identification tolerance is max(1 code, the bin's bar) per channel; the transfer cells are
   grey-96, cyan and the 160×96 continuous cells (never fitted) and the canonical bed's stops; and
   the edge row **E1** is frozen completely in G0 — its population (which canonical rows, WebGPU,
   macOS 27 standard endpoints, active pose), its estimator (re-derivable from matrix fields and
   the matrix-named captures at the adopting bytes, as X1 is), its bound with the reason, and its
   MATERIAL-axis amendment (an isolated uniform-boundary estimand on a fixed native reference). A
   partial closure (greys close, chroma partly) is a real outcome to be recorded per bin, not a
   tolerance to be widened.
2. **The law is an effective boundary law on the composite, discriminated before it is fitted.**
   Apple's internal layers are not recoverable from these composites and the wave does not try:
   the target is a law that reproduces the composite's excess on the admitted domain and transfers
   to the canonical bed. G0 takes the memo's two families — a linear-light luminance/chroma
   boundary transform on the local post-retention body (a compact radial profile × a vertical even
   angular factor, with luminance and chroma responses controlled separately), and its encoded-light
   counterpart — plus any third the cut suggests, and DISCRIMINATES them on the archive through
   W35's opaque-full-coverage forward model (`edge.forward`, extended only beside and only with
   its own tests): whole-pixel arc forward sampling at both scales, the matched rectangles, the
   continuous-versus-circular pair, the grey ladder and the uncensored weak channels. Existing
   controls (`rimLitAxis`, `rimLitExponent`, `rimWidth`, the inner shadow's depth and reach) are
   examined for one coherent meaning inside the new law and reused where they have it, never
   stacked under it. A family is nominated only with its leaves, its identity, its placement (after
   the black solve, the body mix and the chroma retention; before or after the author tint as the
   canonical active tinted cells — `*__rest-tint-orange`, `photo__capsule-button__rest-tint-blue`,
   `-orange-half` — discriminate, W23 G1's orange-on-orange reading in §5.102 favouring after), its
   thickness conditioning — DECLARED, not fitted: the circular 120×44 and 200×44, the continuous
   120×44 and the radius-12 rectangle all cast span 44 (`split.json` ~142–149), so an amplitude and a
   thickness slope are inseparable on calibration and the 160×96 cells are transfer, never fit; G0
   enumerates the identifiable parameters, and a thickness response enters only as a fixed form
   with no free parameter (the runtime's existing `sizeThickness` weight, or none), read against the
   160×96 cells as transfer with the miss recorded and the thick reach labelled extrapolation
   (Decision Log 2) — and its forward residual per bin on calibration AND validation; "no family
   closes" goes to the parent as Decision Log 1 with what a larger budget would need.
   **The sampling-backend contract is declared with the law.** The law is specified on the
   `gpu-texture` composite, which is what every bed cell is; the WebGPU tier's `css-backdrop` and
   `none` paths do not hold the page composite (`wgsl/optics.ts` ~1323–1341 distinguishes the
   composite from the browser-composited layers; ~1367–1369 substitutes `adapted` for `colour` and
   keeps a non-unit body alpha), and W35's forward helper refuses that case (`edge.py` ~38–49), so
   a nonlinear composite transform applied to layer RGB is a different effect over the page. G0
   declares, per backend, either a layer projection with its proof or the old path preserved (the
   operator declined on that backend with a named residual); G1a implements the declaration with
   platform-web e2e coverage of unsampled groups over differing backdrops; opaque full-coverage
   identification certifies nothing about those paths implicitly (X17).
3. **The deep body is the baseline and is never compensated — and subtracting it is not enough.**
   The excess over each cell's own deep median cancels only an ADDITIVE body miss. A
   body-conditioned law — any response with a slope on the body's luminance or chroma, which is
   what the greys' falling excess and the solids' channel behaviour demand — evaluated on vitrea's
   body carries `g·(L_web − L_native)` after the subtraction, and W36 left non-black body misses of
   +3…+8 codes on the greys and over 100 on the saturated solids (§5.178 §1, §5.180) that a
   coefficient fitted on rendered pixels would absorb without any body leaf moving — the failure
   W35 ruled out (its clauses 3 and 5; §5.177 §5). So the discipline is W35's, restored in full:
   coefficients are IDENTIFIED ON NATIVE PIXELS with the native deep as the law's conditioning
   input; the transfer to the runtime's actual conditioning input is MEASURED separately and
   tabled per cell (the law at the web body minus the law at the native body, at the identified
   coefficients); a synthetic pre-composition test shows a baseline change at fixed coefficients
   is detected and a coefficient change at fixed baseline is recovered (§5.177 §5's shape); and
   G1a's rendered-pixel comparison is a CHECK of the shader against the forward model at the
   native-identified coefficients — a refinement on rendered pixels is admitted only where its
   difference from those coefficients is attributed to the forward-versus-shader discrepancy
   (0.87 code on the web branch, §5.177 §5) and not to the baseline, with both coefficient sets
   recorded. Where the body miss is large (the saturated solids), the cell's residual after the
   edge is a BODY miss recorded under W36 Deferred 2, and the edge's closure there is judged on
   the native-conditioned prediction. No tone, chroma-retention or black-branch leaf moves (X3);
   a candidate whose closure improves when one does is a finding for the ledger, not a fit.
4. **Bounds before reads; a miss is recorded, not widened; holdout once by artifact.** The canonical
   holdout is read once at the sealed configuration by `configuration.py record` (its sixth read,
   in G1b; W36's fifth is spent, the source hash will differ); W34's holdout stays spent. Every
   ruling that can change a shipped source or document — the CSS carry-or-decline (Decision Log 5),
   M2's mask or bound (Decision Log 3), the operator's final form — is taken on G1a's non-holdout
   evidence BEFORE the seal, because CSS sources are part of the configuration
   (`holdout-configuration/configuration.py` ~25–46) and a change after exposure would make the
   read describe a configuration that no longer ships; after the holdout read the only outcomes
   are accept or stop. Every existing stop
   is read with its expected post-fit value declared in G0: the per-cell tables and
   `MISSED_27_ROWS`; M1 in [0.8, 1.2] (a coloured edge moves both its numerator and denominator —
   G0 prices it); M2 ±2 % against the generation this wave supersedes, with the ring's known
   sensitivity (−60.0 % / −48.4 % of the dark photo capsule's spread under one device px of
   erosion) put to the user as Decision Log 3 WITH the candidate's measured delta before any mask or
   bound moves; L1 ≤ 0.055 and growth ≤ 0.005 against its NAMED W33 baseline (never re-baselined
   with M2; a line that Apple draws and vitrea did not should reduce the silhouette-mean miss, and
   G0 shows on which rows it does not); C1 ≤ 0.0042, X1 = 0 and B1 ±5 % unchanged and SHOWN on the
   pixels, because an operator with interior support can still reach an upper layer's sampled
   backdrop in the stacked and grouped cells; tier coherence 0.8–1.25 and ΔE ≤ 0.05 with its
   UNMEASURED states; the predicate as the machine says, churn counted; the 34 goldens byte-identical
   at identity and attributed at G1. W29 G3's "2.2 CSS px" rim-width prose is reconciled beside,
   not inherited (it is 2.416 device px at 1x and unresolved at 2x on the artifact it came from).
5. **The fit's reach.** The new gate-group and its leaves on the four macOS 27 documents; the
   existing rim, inner-shadow and angular leaves ONLY as the nominated law gives them one meaning
   (Decision Log 2 rules whether the old diagonal rim is replaced behind the gate on the 27
   documents or refitted inside the law); explicit zeros for the new gate in both receded
   differences. Nothing else: not the tone curve, the black branch, the chroma retention, the lens,
   the outer shadow (its exterior rows are the proof), the scatter, the size law or the tint shade.
   The frozen 26.5 documents' values do not move (W29 Decision Log 1 (i)) and at identity they draw
   today's rim exactly.
6. **The CSS tier derives what box-shadow can carry and declines the rest by measurement.** The
   tier draws one constant-colour inset rim today (`css-tier.ts` ~2089–2130); a vertical two-lobe
   line is representable as top/bottom insets derived from the same leaves, a colour-conditioned
   response is not (the tier never sees the filtered body's RGB), and two straight rows at DPR 2
   cannot both be met by one constant output (minimax 6.5 codes light grey-128, 8.5 dark). G1a
   derives the projection on the 27 documents, renders it on the bed and the canonical NON-holdout
   CSS cells through the tune path, and records the residual per row against those analytic
   bounds; the parent rules carry or decline as Decision Log 5 on that measurement BEFORE G1b
   seals; no new CSS facility (no gradient or mask overlay for an optical operator) is added, and
   no CSS-only wave is chartered (W36 Deferred 4).
7. **Seal, read, tree.** G1b seals the four documents under rule 2 with the new gate-group in the
   identity table (identity 0, the gated leaves dropped together, a literal identity assertion and
   an executable inert-law test; both 26.5 digests and the four 27 digests unmoved at identity by a
   script in W33 G0's `identity-proof.ts` shape, and the rendered identity shown on the goldens and
   the CSS cells), registers configuration 6, reads the canonical bed once including the holdout,
   splits the superseded generation, and moves the capture tree to canonical at its merge with the
   replaced generation aside and `check-capture-tree` run. G1b changes no source, leaf value or
   document beyond what G1a delivered and the rulings on it fixed; its read's outcome is accept or
   stop.
8. **Landing.** G2 adopts E1 re-derived at the adopting bytes; re-baselines M2 per W32 Decision
   Log 4 with the cumulative drift tabled; sweeps `CLAUDE.md`, both READMEs, the CHANGELOG, the
   coverage matrix and the `/laws/` stage (the W29 rim paragraph retired beside for macOS 27);
   runs the chain with X6's four facts; `pnpm changeset version` to 0.25.0; writes Outcomes,
   Deferred-at-close and the Tracking Map; and puts the eye on the captures beside the native
   fixtures — the sides must stay dark where Apple's are.

## Grounding Baseline (main at `4a74311d`, 0.24.0 published)

- **The line** (memo §1): greys, light 1x excess 30 / 29 / 27 / 26 / 24 / 21 / 2 at grey
  0 / 32 / 64 / 96 / 128 / 160 / 255 over deep 132 / 148 / 164 / 179 / 195 / 210 / 253; dark 1x
  41 / 38 / 35 / 32 / 30 / 29 / 25 over 32 / 62 / 89 / 113 / 134 / 151 / 184; the 2x pairs (inner,
  outer row) 23,40 → 2,2 light and 30,53 → 19,33 dark; channel differences of a code or two retained
  per cell. Solids per the memo's table (light red +0/+22/+21, green +1/+9/+1, blue +27/+27/0,
  yellow +24/+24/−7, magenta +0/+20/+0, cyan −2/+15/+15 at 1x; dark red +13/+26/+26, green
  +4/+39/+4, blue +34/+34/0, yellow +29/+29/−4, magenta +20/+23/+20, cyan +1/+36/+36). Angular at
  2x grey-128 as in Purpose; rectangles top/bottom versus left/right as in Purpose; the inactive
  pose exact to its own deep RGB on every admitted circular cell (grey-128 188 / 127), with W35's
  2-code dark 1x arc floor kept as the qualification.
- **The runtime** (memo §2): `rw = clamp(1 − |d|/width, 0, 1)²`, `lit = (√2·|n·axis|)^exponent`,
  `along = max(1 + slope·sizeThickness·x̂·ŷ, 0)`, the rim adding `rw·lit·along·(a + g·L_shadowed)`
  in linear light (`wgsl/optics.ts` ~367–370, ~935–941, ~1509–1655); light width 6.5 / 5.85,
  a 0.115, g −0.122; dark width 2.2 / 1.35, a 0.055, g 0.44; exponent 0.85, along-slope 0.1, shadow
  depth 0.35 / alpha 0.05, axis (−0.7071, −0.7071); both receded patches zero the rim amplitudes,
  gain, collapsed rims and shadow alpha. W35's best tested existing-leaf settings miss by 25.50 /
  33.75 / 36.00 / 46.57 codes (1x light / dark, 2x light / dark). After W36 at black: deep 132 / 32
  exact, top line 149 / 64 at 1x against 162 / 73 native, 147.11,150.18 / 45,66 at 2x against
  155,172 / 62,85.
- **The archive** (memo §3): 416 calibration + 80 validation probe cells (280 + 56 glass);
  circular capsules 120×44 and 200×44 at radius 22, the continuous 120×44, the radius-12 rectangle;
  validation 160×96 continuous on grey-128, grey-255 and red; the larger circular radius is in the
  spent holdout column and is not available; the phase axis is unreachable (`phase-reading.json`).
- **The gates** (memo §4): as clause 4; the identity-table and gate-group tests
  (`w31-identity-table.test.ts`, `w31-gate-groups.test.ts`); the holdout configuration log at five
  records, W36's spent.
- **The CSS tier** (memo §5): one constant-colour inset plus the outer shadow on the overlay; the
  minimax 6.5 / 8.5 two-row bound at DPR 2.
- **The readers**: W35 G0a's (`results/2026-09-24-w35-g0-edge-cut/`, `w35_readers.py`, `edge.py`
  opaque full coverage only, `domain.json` frozen at SHA b03ea804…); W34's `wave.py` and
  `split.json`; the memo's `replay.py` / `diagnostics.py` under `~/vitrea-w37/grounding/`.
- **The machine**: the original harness bundle holds its grant; no native capture is planned;
  the GPT rungs answered on 2026-09-25 (the memo) — a worker on those rungs may be unable to write
  `.md` files and writes `.txt` beside (W35's precedent), which X9 admits.

## Design (advisory unless marked)

**The operator.** One gate-group through the identity table: `bodyEdgeStrength` (identity 0) gating
the line's leaves — a line width and a shoulder width in CSS px with the shoulder's share, the
angular factor (a vertical even lobe with an exponent, the axis fixed vertical unless G0 shows the
existing `rimLitAxis` can carry it with one meaning), the luminance response (intercept and slope
on the local body's linear luminance, which is what makes the excess fall with level) and the
chroma response (what lifts a strong channel and lets a weak one fall — a chroma gain or a
saturation term on the post-retention body, in the colour space G0 discriminates), and the
thickness conditioning if the 120×44 / 160×96 pair identifies one. At strength 0 the old path runs
exactly (the 26.5 digests and the goldens unmoved); at strength 1 the new boundary treatment
replaces the old rim on the four macOS 27 documents (Decision Log 2 — the memo's "blend complete
old/new treatments at the gate rather than stacking another line over the wrong inherited rim").
Placement: after the black solve, the body mix and the chroma retention; before or after the author
tint as the canonical tinted cells rule.

**The identification.** G0 scores each family on the archive through the forward model, per bin and
channel, calibration to fit and validation untouched; the discriminating cells are named in
advance (dark cyan / green / yellow, the uncensored weak channels, the grey ladder, the
shoulder-extrapolated rows, the rectangles, the continuous pair), the coefficients identified on
NATIVE pixels with the native deep as the conditioning input and the transfer to the web body tabled
per cell (clause 3). G1a then carries those coefficients into the operator and compares its RENDERED
pixels through the tune path (W35 G0a's scratch-document mechanism) against the forward model's
prediction at the same coefficients — the check of the shader, not a refit; W35 bounded the
forward-versus-shader discrepancy at 0.87 code on the web branch, and a rendered-pixel refinement is
admitted only under clause 3's attribution rule. G1a also renders the CSS projection and measures
M2's delta on non-holdout cells, so Decision Logs 3 and 5 are taken before anything is sealed.

**The stops** (declared in G0 with expected values): E1; the per-cell tables and `MISSED_27_ROWS`;
M1; M2 with the measured delta for Decision Log 3; L1 against W33; C1 / X1 / B1 unchanged and
shown; tier coherence; the goldens at identity; the receded documents flat; the accessibility
stand-down (the new operator stands down exactly where the rim does today — G0 names the switch).

## Children

### G0: The cut, the closure, the families discriminated, E1 frozen — no material change, no capture

Ledger **§5.181**; evidence `packages/calibration/results/2026-09-25-w37-g0-edge-identification/`.
Owns its dir, `test/w37-*.test.ts`, and any read-only extension of W35's readers and forward model
(beside, with tests). Does: clause 1 in full; clause 2's discrimination with the forward residual
per bin for every family on calibration and validation; the placement read on the canonical
tinted cells at the current generation; the thickness/path decomposition; the accessibility switch
named; every stop's expected post-fit value in `bounds-declaration.md` (or `.txt`); the identity
route proved for the proposed gate-group (a script in `identity-proof.ts` shape: both 26.5 digests
and the four 27 digests unmoved at identity); the CSS projection plan with its analytic bound per
row; the Decision Log 1 and 2 drafts. Acceptance: every memo number reproduced or corrected beside;
the declaration reviewed; nothing under `src/`, `profiles/`, `fixtures/`, `results/matrix.json` or
the canonical `scenes.json` touched; freeze 1,818. Stop conditions: no family closes calibration
within the tolerance on the straight bins (the wave goes to Decision Log 1 for a ruling); the
canonical tree at a refused generation.

### G0b: The second identification — a predeclared larger budget on the archive, one gate, then a ruling (no material change, no capture)

Ledger **§5.182**; evidence `packages/calibration/results/2026-09-25-w37-g0b-edge-identification/`.
Owns its dir, `test/w37-g0b-*.test.ts`, and read-only extensions beside G0's `law.py`, `identify.py`,
`canonical.py` and W35's readers. Authorised by Decision Log 1 on G0's finding and the review's
form-obstruction witness. Does: (a) DECLARES, before scoring and in a committed, hashed
declaration, the radial basis exactly — a signed line term and a signed, independently shaped
shoulder/tail able to express the near-cutoff depression (light grey-255 at 2x reads 250 at
5.3–5.8 CSS px against deep 253; W35's light grey-128 trough 192 at 8–14 CSS px against 195), with
its knots and parameter ranges; the kernel SHAPE family (squared, linear ramp, exponential, or a
declared spline) and the PIXEL-INTEGRATION model (area-integrated versus centre-sampled), because
the 1x single row (24) and the 2x row pair (18, 31) on grey-128 are consistent with a ramp within
the last CSS px averaged over pixel areas (ratio 0.58) and not with the squared kernel sampled at
centres (0.39); separate line and shoulder colour responses, each with its own luminance and
chroma response, the level-dependence form declared per scheme (affine in the encoded native deep
first — the dark ladder's excess is affine at about −0.105 code per code — a second form only if the
first fails); the objective, the weighting, the rank checks and the ranges; at most three
families; native-only conditioning; thickness factor 1 fixed; the existing tolerance; validation
excluded. (b) Scores the families through the forward model as G0 did, every bin tabled, the
transfer tabled separately, the old rim's per-bin contribution compared per bin (better / same /
worse at 0.5 code) for every family. (c) Repairs G0's E1 reader so an absent side or shell is an
explicit UNMEASURED record, never omitted (the review's P3), and re-derives G0's 14 rows with it.
(d) For any family that closes the calibration greys' straight bins at both scales: the
prospective canonical price — E1 predicted on the 14 rows through the forward model at the
native reference, M2's ring effect estimated on the 26 M2 cells from the eroded-mask attribution,
the C1 / X1 / B1 zero-change argument — and the Decision Log 1 material. Acceptance: the
declaration committed and hashed before the first score; every G0 number reproduced or corrected
beside; nothing under `src/`, `profiles/`, `fixtures/`, `results/matrix.json`, the canonical
`scenes.json` or the canonical `web-captures/` touched; freeze 1,818; no browser run, no capture, no
holdout read. **Hard stop (ruled):** if the best G0b family does not close the calibration greys'
STRAIGHT bins at both scales within the tolerance, the wave CLOSES at the finding — no G0c, no
prototype; if it closes the greys and the solids only partly, Decision Log 1 reopens for a
partial-adoption ruling with E1's bound unwidened and its population restricted only with the
excluded rows named as misses.

### G1a: The operator, the rendered check, the CSS projection, M2's delta — pre-seal, no configuration change

Ledger **§5.183**; evidence `packages/calibration/results/2026-09-25-w37-g1a-edge-operator/`. Owns
the gate-group and its identity-table entry and tests, the WGSL operator and its
sampling-backend declaration (X17) with platform-web e2e coverage, the CSS projection's code, the
scratch documents. Does: the operator behind the gate with the inert-law test and the rendered
identity (goldens byte-identical, CSS identity); the native-identified coefficients carried in and
the rendered pixels checked against the forward prediction through the tune path on scratch
documents (calibration and validation, never holdout); any refinement attributed under clause 3;
the CSS projection rendered on the bed and the canonical non-holdout CSS cells with its residual
per row; M2's delta on the 26 M2 cells with the eroded-mask attribution beside; the unsampled
backends rendered over differing backdrops; every stop read on non-holdout cells with its expected
value beside; the Decision Log 3 and 5 material. Acceptance: clauses 2, 3, 5 and 6; a miss recorded,
never widened. Stop conditions: any 26.5 digest moves; the receded renders move; the goldens move
at identity; the rendered check exceeds the forward-versus-shader bound without attribution.
Nothing is sealed, registered or read canonically in G1a.

### G1b: The seal, the read, the tree — one merge

Ledger **§5.184**; evidence `packages/calibration/results/2026-09-25-w37-g1b-edge-seal/`. Owns the
leaf values on the four documents, the four documents, the generated profile,
`adopted-thresholds.test.ts`'s `PREDICATE_EXCLUDES` and `MISSED_27_ROWS`, the goldens' attributed
change, `window-activation.spec.ts`'s hashes if they move. Does, with the rulings on G1a fixed: the
seal; `configuration.py record` before the passes; the canonical read, the ladder, the holdout
once; the split; the gated count; the sheets and the eye; every stop read with its expected value
beside. Acceptance: clauses 4 and 7; the outcome after the holdout read is accept or stop. Stop
conditions: a stop breaks; any 26.5 digest moves.

### G2: The landing — E1 adopted, 0.25.0 prepared

Ledger **§5.185**; evidence `packages/calibration/results/2026-09-25-w37-g2-landing/`. Clause 8.

## Cross-Child Contracts

- **X1 — the freeze, intact.** No macOS 26.5-keyed path, row, bound, floor or document changes;
  `freeze.py verify` 1,818 at every merge; the goldens byte-identical until G1's attributed change;
  the 26.5 digests the live pin with no exemption spent (the gate-group through the identity table,
  X13).
- **X2 — the cut precedes the fit.** G0's reproduction, closure, discrimination, E1 and Decision Log
  drafts are committed and reviewed before G1 opens; G1 fits against them and no other.
- **X3 — the fit's reach** as clause 5; the deep body's leaves never move; the photo / impulse /
  checkerboard / hc-text / structured cells are never fit targets; validation cells never fitted.
- **X4 — bounds before reads; a miss is recorded, not widened; holdout once by artifact.**
- **X5 — no native capture; no bundle built, launched or granted; W34's bed read-only through the
  guarded readers; W34's holdout never read.** The claims are bounded to the admitted archive and
  the canonical bed; larger-radius, subpixel-phase, blind-confirmation and accessibility-boundary
  claims are not made and are named in Deferred.
- **X6 — RT and IC 0, the slider 0.5, Show Borders 0, one capture process, ≥ 60 s idle and the
  foreign-process count** before every browser run, every run in `browser-runs.txt`; a foreign
  browser present is recorded and the run's own determinism checks are the detector.
- **X7 — the evidence layout by the one rule**: the split after the read, the capture tree to the
  canonical path at G1's merge with the replaced generation aside, `check-capture-tree` run.
- **X8 — padding untouched.**
- **X9 — routing** as W36's: `astra-high` (or `astra-medium` / `sol-xhigh`) for children; the
  default Claude worker when the Codex limit refuses them, each such dispatch recorded; a worker
  that cannot write `.md` writes `.txt` beside and says so; the charter's review on
  `doperpowers:adversarial-reviewer`; reviews read-only; merges `--no-ff -F <file>` with the freeze
  verified; no attribution trailers; corrections beside.
- **X10 — the 27 bed never empties between merges.**
- **X11 — file ownership** as the children state; nobody touches `receded-profile.ts`'s 26.5 block
  or `DEFAULT_MATERIAL_PROFILE`'s existing values; W35's readers, forward model and `domain.json`
  are extended beside, not edited; W34's spent receipt untouched.
- **X12 — censored channels excluded by rule** (a channel at 255 on either side), named per cell,
  never fitted; a censored channel's excess is reported, not inverted.
- **X13 — new leaves through the identity table**, a gate-group at identity 0 dropping its gated
  leaves together, the rule-2 version recorded in every sealed document, explicit zeros in both
  receded differences. The table is append-only: once G1a's merge lands the entry at identity, it
  stays even if the wave stops before G1b — an inert gate on main is the recorded outcome of a
  stopped wave, not something to remove.
- **X14 — M2 re-baselined per W32 Decision Log 4 at G2** against the superseded generation, the
  cumulative drift tabled; within the wave M2 is read against the generation this wave supersedes.
- **X15 — the excess is the estimand, and the coefficients are native-identified.** Every closure
  number is line minus the cell's own deep median, per channel; coefficients are identified on
  native pixels with the native deep as the conditioning input, the transfer to the web body is
  tabled per cell, and a deep-body miss never enters a coefficient (clause 3).
- **X16 — the inactive pose is identity.** Both receded documents zero the gate; the inactive
  renders are byte-identical to today's and a test says so.
- **X17 — the sampling-backend contract.** The law is specified on the `gpu-texture` composite; for
  `css-backdrop` and `none` G0 declares a layer projection with its proof or the old path preserved
  with a named residual, G1a implements and tests it over differing backdrops, and nothing is
  inferred for those paths from the opaque full-coverage identification.

## Ordering & Dependency Map

G0 → independent review → merge → Decision Logs 1, 2 and 4 (the parent) → **G0b (ruled 2026-09-25) →
review → merge → Decision Log 1 reopened on its finding: G1a, or the wave closes** → G1a →
review → merge (operator at identity on main, scratch evidence) → Decision Logs 3 (the user, on the
measured delta) and 5 (the parent), any final form fixed → G1b → review → merge with the capture
tree → G2 → review → merge → `pnpm release` (the user's hand) → tag. No ruling that changes a source
or a document is taken after G1b's holdout read.

## Risks & Mitigations

- **No family closes the chroma.** The saturated solids' weak channel FALLS while the strong rise;
  a luminance/chroma transform may still not express yellow and cyan together. G0's per-bin
  residual says so before any shader is written; a grey-only closure with the chroma residual
  recorded per cell is a candidate for a qualified adoption under Decision Log 1, not a widened
  tolerance.
- **The forward model does not transfer.** The composite differs from the shader by up to 0.87 code
  on the web branch; G1 fits on rendered pixels, and G0's declared tolerance carries the bar.
- **M2 breaks on the ring.** Known and priced; the candidate's measured delta goes to the user
  before any mask moves (Decision Log 3); the 2 % stop is not waived by the wave.
- **L1 grows on some rows.** A line Apple draws reduces the silhouette-mean miss where the row's
  miss is short of native and grows it where it overshoots; G0 prices per row and the growth stop
  is read as adopted.
- **The stacked and grouped cells move exterior rows.** An interior operator reaches an upper layer's
  sampled backdrop; C1 / X1 / B1 are shown on pixels, not assumed.
- **1x arcs are under the floor.** The lobe is certified at 2x; 1x closure is on the straights and
  the admitted arc bins only, recorded as such.
- **Thickness is not identifiable on calibration.** Every admitted uniform calibration geometry
  casts span 44; a fitted thickness slope would consume the 160×96 transfer cells. The response
  is declared with no free parameter or left out, and the thick reach is labelled extrapolation.
- **A ruling after the holdout read would spend the configuration.** Every source-changing ruling
  is taken on G1a's evidence before G1b seals; after the read, accept or stop.
- **The unsampled backends draw something else.** X17 declares them; G1a tests them.
- **The GPT rung cannot write Markdown.** `.txt` beside, admitted by X9.

## Deferred / Out of Scope

- A native experiment (larger circular radius, a reachable phase mechanism, blind confirmation,
  accessibility boundaries) — after the wave, under the user's X5 authority, if the transfer
  leaves a question the archive cannot answer.
- The forward helper's fractional-coverage branch (W35 Deferred 3) — only if G0 needs the
  partial-coverage bins, and then beside with its own tests.
- The grey middle, the chroma of the deep body, the receded formula (W36 Deferred 1–3).
- The CSS tier's colour conditioning — declined by representation, recorded.

## Tracking Map

| child | state |
| --- | --- |
| G0 | DELIVERED FOR REVIEW, §5.181 — native-only linear/encoded families both fail; no nomination; declaration `6f2e89ed`, E1 frozen, no material/capture change. **MERGED 2026-09-25 as `e143360d`** (§5.181; review reproduced every table and the identity proof, one P3 folded into G0b; freeze 1,818; no capture tree moved). |
| G0b | DELIVERED FOR REVIEW, §5.182 — declaration `89f45616`; three native-only families fail grey straights, even-normal symmetry gives a 1.5-code floor; hard stop fires, recommend close W37. E1 missing-side repair; no material/capture change. **MERGED 2026-09-25 as `2b61747e`** (§5.182; review reproduced all 66,765 bins, 624 transfer tables and the obstruction, merged as-is; freeze 1,818; no capture tree moved). |
| G1a | not opened — the wave closed at the finding |
| G1b | not opened — the wave closed at the finding |
| G2 | not opened — no release from this wave |

## Decision Log

### Decision Log 1 — the law and adoption at partial closure (after G0; the parent's, under the standing "rest on your judgement")

**RULED (second) by the parent 2026-09-25 at G0b's merge (`2b61747e`): CLOSE W37 at the finding.**
The hard stop fired as ruled: G0b's best family (F3, ramp line, signed independent shoulder,
quadratic level response) misses the calibration greys' straight bins by 8.13 codes at the
tolerance of one, the misses concentrated in the line row across the grey ladder (dark grey-64's
outer 2x row 45 native against 36.87; light grey-160's 1x row 23 against 17.07), and every family
trades bins against the old treatment (F3 20,650 better / 8,927 same / 2,847 worse at 0.5 code,
worst calibration worsening 4.36). No G0c, no prototype, no partial adoption: the excluded bins
would be the line itself. The archive's coefficient-independent obstruction is the reason a third
gate would not help: every declared family, and any law even in the normal's vertical component,
must predict the same excess for matched top and bottom straight pixels, and Apple's read 250 and
253 — repeatable across seven repeats with zero variation, not registration, persisting 7–14
CSS px inward on the top only. The edge has a directional component this bed cannot separate from
registration, reconstruction and thickness without matched controls and a reachable subpixel-phase
mechanism, which is a native experiment under the user's X5 authority and a fresh charter
(Deferred at close 1).

**RULED by the parent 2026-09-25 at G0's merge (`e143360d`): no family is nominated and G1a does
not open on G0's coefficients; ONE G0b is authorised with a predeclared larger budget and a hard
stop.** The grounds. The encoded family misses 11.84 / 7.96 codes (calibration / circular
validation) against one and fails about six bins in ten; on native-conditioned evaluation against
the old rim's own forward contribution it cuts the mean per-channel-bin miss from about 3 to 1.2
codes and improves roughly half the channel-bins, but worsens one to eight percent of them by up to
5.4 codes on the near-vertical 2x arcs — it trades bins, which this Decision Log's own rule says
does not ship. The independent review located a family-FORM obstruction rather than a procedural
one: at light grey-255, 2x, shells −12 / −11 (5.3–5.8 CSS px inward, 152 uncensored pixels each)
Apple reads 250 against a deep of 253 — a depression near the shoulder's cutoff that every declared
kernel puts at zero or forces into a 10.8 : 1 ratio, a 2.49-code minimax floor before any colour
coefficient is chosen. That is a reason to declare a different basis, not to widen a tolerance.
G0b's budget is fixed in its child section: a signed, independently shaped shoulder/tail; the
kernel shape and the pixel-integration model declared; separate line and shoulder colour
responses with the level form declared per scheme; objective, weighting, rank checks and ranges
declared; native-only conditioning; thickness fixed; validation excluded; at most three families.
The hard stop: no closure of the calibration greys' straight bins at both scales within the
tolerance closes the wave at the finding — no G0c and no prototype — and a grey closure with the
solids partly open reopens this Decision Log for a partial-adoption ruling with E1 unwidened. The
alternative put aside: closing now, as W34 and W35 closed, would leave the edge at 25–47 codes with
the next wave starting exactly where G0b starts; the cost of G0b is one archive-only gate.

Open. G0 drafts: which family (or none) closes calibration within the tolerance on the straight
bins and the admitted arcs, per channel; its validation residual; what a partial closure leaves
(greys closed and chroma partly, or the reverse); whether the old rim's contribution has one
coherent meaning inside it. The parent rules whether G1 opens, on which law, and whether a partial
closure ships with its residual recorded per bin — the W36 precedent (black alone shipped, the
middle refused) is the shape: a change that moves the composite toward Apple on every admitted bin
without moving any stop ships with the misses named; a change that trades bins does not.

**G0 draft, not ruled (§5.181).** Nominate **none**. Linear misses calibration20.882831 /
circular validation19.666667 codes (continuous160 validation26.776469); encoded11.843356 /
7.955766 (continuous16022.053616), against1. Neither is a grey closure with only chroma
deferred. `fits.json` and `transfer.json.gz` retain native-only coefficients and per-cell
body-conditioning transfer (linear −15.769234…+48.630010, encoded −7.882067…+35.822687).
Do not open G1a on these coefficients. A larger predeclared budget could separate line/shoulder
colour responses and test nonlinear native-level/chroma dependence with a finer radial/angular
search; no promised closure, no validation fit or inferred thickness slope. The spent column
stays refused; new radius/phase/blind confirmation needs new native authority.

**G0b draft, not ruled (§5.182).** **Close W37 at the finding; nominate none.** The pre-score
expanded budget (`89f45616`) gives F1/F2/F3 calibration-grey-straight maxima **9.074443 /
8.706337 / 8.126100** codes against one; all-calibration **10.404696 / 10.985204 / 8.506613**;
full validation including span/path diagnostics **19.911310 / 20.135255 / 20.637789**. No grey-only
closure exists to support partial adoption. More strongly, light grey-255 circular-200 matched
straight top/bottom bins at both scales have identical conditioning and depth but native 250/253:
every declared even-normal basis must predict the same value, giving a **1.5-code minimax floor**
independent of coefficients. The signed tail removes the old cutoff restriction but not this
symmetry obstruction. Better/same/worse active channel-bin counts against the old full boundary
are **20,489/8,902/3,033**, **20,519/9,004/2,901**, **20,650/8,927/2,847** (threshold 0.5;
absolute signed-mean error on both sides), so each also trades bins. Native-to-web conditioning
transfer is F1 **−7.447281…+33.741197**, F2 **−7.938211…+35.222517**, F3
**−8.163969…+29.217063** codes. The ruled hard stop fires: no G0c, no prototype, no G1a.
No prospective E1/M2/CSS price is triggered without a grey-closing family. E1's reader now emits
504 bins, 108 explicitly UNMEASURED; six of 14 rows have missing capsule sides, while every old
numeric bin and maximum reproduces. Keep E1's bound and population, no partial-adoption exclusions.
A future wave needs signed-normal versus path/alignment/phase discrimination on matched controls
and a second calibration span for thickness, with new native authority and a predeclared split;
that is not an implicit continuation of W37. Parent review and closing ruling are next.

### Decision Log 2 — the old rim's fate and the operator's placement (after G0; the parent's)

**RULED by the parent 2026-09-25 at G0's merge: as G0 drafted.** If a family is admitted, the new
treatment REPLACES the old rim and inner-shadow treatment behind the gate on the four macOS 27
documents — never stacked — because the old rim alone contributes +6.5 / +17.5 codes at the
horizontal arcs where Apple reads −2.5 / −2 (§5.181); `rimLitAxis`, `rimLitExponent` and `rimWidth`
may be reused only with one coherent meaning inside the new law (the old √2 normalisation
accounted for), `rimAlongSideSlope` stays on the old identity path, and the old path is exact at 0.
The operator conditions on the POST-author-tint body (an effective composition choice from the 20
measured tinted pairs, eight UNMEASURED; not a claim about Apple's layer order). The thickness
factor is 1 with no free slope; the span-96 continuous cells are read as transfer and their miss
(dark 22.05 codes under G0's encoded family) is recorded, not fitted. The WebGPU tier's
`css-backdrop` and `none` paths keep today's pixels (X17). The stand-down follows the rim's actual
gates — Reduce Transparency keeps the ordinary rim, Increase Contrast substitutes the strong
border, forced-colors draws no glass — not the chroma retention's occlusion predicate.

Open. Default: at strength 1 the new treatment REPLACES the old diagonal rim on the four macOS 27
documents (Apple's sides read −2 to −3 codes, darker than the deep body, where the old rim lights
them), the operator sits after the chroma retention and — pending the canonical tinted cells'
reading — after the author tint, and the thickness response is a fixed form with no free parameter
or absent, with the 160×96 read recorded as transfer. G0 supplies the tinted-cell reading, the old
rim's per-bin contribution and the identifiable-parameter enumeration; the parent rules.

**G0 draft, not ruled (§5.181).** If a later family is admitted, replace the old treatment,
never stack another line under its wrong horizontal light. Reuse axis/exponent/width only with
one coherent gated meaning. The20 native tinted pairs support effective conditioning after
paint, not a claim about Apple's internal layer order. Thickness factor1/no free slope was
declared and fails dark span96 transfer; same-span straight path controls agree, continuous
arcs remain unresolved. No fitted path or thickness leaf is nominated. Preserve old pixels on
WebGPU css-backdrop/none. Follow the actual rim policy: strong-border/no-glass stands down;
Reduce Transparency alone does not suppress today's ordinary rim (unlike chroma retention).

### Decision Log 3 — M2's mask or bound on the ring (after G1a's measured delta, before the seal; the user's)

Open, carried from W35 Decision Log 3. Not ruled in advance: the 2 % stop stands until a candidate
supplies its own delta on the 26 M2 cells with the eroded-mask attribution beside; the user rules
on a mask (k device px of erosion) or a re-pin, with the reading recorded.

### Decision Log 4 — E1's frozen form (after G0; the parent's)

**RULED by the parent 2026-09-25 at G0's merge: E1's frozen form is CONFIRMED as the proposal
and not adopted.** Fourteen canonical untinted uniform active standard-27 WebGPU rows; per-side,
per-shell, per-channel mean absolute EXCESS over the d ≤ −6 deep against fixed native PNG hashes;
physical whole straight pixels with the 1.6-radius corner exclusion; shells −6…0; floor 4; bound
one code; no-reach bins retained; an absent side or shell an explicit UNMEASURED record (G0b
repairs the reader that omitted them — the review's P3); no predicate drop; re-derived at the
adopting matrix and document bytes with the source receipt, UNMEASURED when the tree is absent.
Today 14 / 14 miss, maximum 29.36 codes. The bound is not widened for either failed family.

Open. G0 freezes population, estimator, bound, amendment and the treatment of rows the line does
not reach; the parent confirms before G1.

**G0 draft, not ruled (§5.181).** Confirm or reject `e1-declaration.json`:14 canonical active
uniform untinted WebGPU/gpu-texture rows, fixed native references, per-side/shell/channel mean
absolute own-deep excess error≤1 code, floor4, no predicate drop, no-reach bins retained,
missing UNMEASURED, rederived at adopting matrix/document bytes. Current14/14 misses (maximum
29.359375) mean this gate cannot adopt E1 or call it passed. Its bound is not widened to admit
either failed family; all existing stops retain their owners.

### Decision Log 5 — the CSS projection: carry or decline (after G1a's measurement, before the seal; the parent's)

Open. G1a renders the top/bottom-inset projection and records its residual per row against the
analytic 6.5 / 8.5 bound and the tier's coherence contract; the parent rules carry (the projection
ships on the 27 documents with the residual recorded) or decline (byte-pinned, as the chroma
retention's was).

## Surprises & Discoveries

- **Apple's edge is not even in the normal's vertical component** (the parent's reading of G0b's
  witness and its review): at matched inward depth on light grey-255 circular-200, the top straight
  reads 250 and the bottom 253 at both scales, on seven repeats with zero witness variation and a
  recorded translation of [0, 0]; the top's depression persists roughly 7–14 CSS px inward while
  the bottom stays at the deep body. Every law W35–W37 declared, and vitrea's own rim, is even in
  that component. A directional (signed-normal) term — a top-lit shading, in plain words — is the
  first thing the next edge wave has to declare, and the archive cannot separate it from
  registration, reconstruction and thickness effects on its own.
- **The line's shape is a ramp averaged over pixel areas, not a squared kernel sampled at
  centres**: G0b's area-integrated ramp reproduces grey-128's 1x row (24 → 24.17) and 2x pair
  (18, 31 → 17.46, 30.89) before any coefficient is fitted; the 1x integral equals the mean of the
  2x pair exactly. A consistency check, not identification of Apple's reconstruction filter — but
  the forward model every future thin-feature fit should start from.
- G0b's independently signed tail can express G0's cutoff depression, yet the archive contains a
  stronger obstruction: matched light grey-255 circular-200 top/bottom depths read250/253, so an
  even-normal shoulder cannot close at one code, whatever its coefficients (§5.182).
- The E1 omission repair adds108 explicit UNMEASURED bins; six capsule rows cannot be called fully
  measured. All396 old numeric bins and their maxima reproduce; no population or bound changed.
- G0's replay verification included absolute source/guard worktree paths in its equality. A sibling
  wrapper now preserves that provenance and compares every numerical field exactly; no old script
  or number was edited (§5.182).

- G0's encoded family is closer but still fails both greys and chroma; the declared compact
  shared radial response plus affine luma/chroma response is not a closing law. This is a finite
  search finding, not a global impossibility proof (§5.181).
- Same-span circular/continuous/rectangle grey128 straights agree exactly; dark span96's line
  falls from30 to6 codes at1x. The chosen factor1 is a declared failed transfer, not evidence for
  a fitted sizeThickness slope; curved-path/span attribution stays unresolved.
- The rim and chroma-retention policy predicates differ: RT alone retains the ordinary rim,
  while IC substitutes a strong border. G0's backend/policy contract names actual sources rather
  than copying retention's occlusion stand-down to an operator with different ownership.
- Current canonical orange values differ from W23's26.5 historical readings; the new pairs
  support post-tint effective conditioning without proving hidden native layer order.

## Outcomes & Retrospective

**CLOSED at G0b's finding**, against the eight Parent-Level Acceptance clauses:

| clause | outcome and evidence |
| --- | --- |
| 1 — cut before fit, closure declared | Met. G0 reproduced the memo (208 cells, zero difference), froze the closure, the tolerance, the transfer cells and E1 before scoring (`6f2e89ed`, SHA-256 270ba479…); G0b froze its larger budget before scoring (`89f45616`, SHA-256 0ddc7666…). Both declarations hashed and pinned by tests (§5.181, §5.182). |
| 2 — effective law discriminated | Executed to a qualified negative. Five families identified on 28 native circular calibration cells per scheme: G0's linear 20.88 / 19.67 and encoded 11.84 / 7.96 (calibration / circular validation); G0b's F1 9.07, F2 8.71, F3 8.13 on the calibration greys' straights, 6.45 / 7.25 / 7.99 circular validation. Existing controls examined (axis, exponent and width reusable with one meaning; the old rim contributes +6.5 / +17.5 codes at the horizontal arcs where Apple reads −2.5 / −2); placement read post-tint on 20 pairs; thickness declared, not fitted; the backend contract declared. None nominated. |
| 3 — no compensation | Met. Every coefficient native-identified; the transfer tabled per cell (F3 −8.16…+29.22 codes); synthetic pre-composition tests pass for every family; no body leaf moved; nothing rendered. |
| 4 — bounds, holdout | Met. No bound widened; E1 frozen at one code, 14 / 14 miss (max 29.36) and not adopted; no holdout read; W34's spent receipt untouched; M2, L1, C1, X1, B1 unchanged with G0's 280-row stop snapshot exact at the no-change outcome. |
| 5 — reach | Nothing moved: no source, profile, fixture, matrix, scene or capture. |
| 6 — CSS | Not exercised beyond the analytic bound (6.5 / 8.5 codes at DPR 2); nothing to render. |
| 7 — seal, read, tree | Not reached. |
| 8 — landing | Not reached; no release. |

**Retrospective.** Declaring the budget before scoring, twice, is what let the wave stop
honestly: G0's families were shown to fail on their form (the cutoff depression), G0b's on a
symmetry no coefficient can break, and neither finding could have been absorbed by a wider
tolerance or a fourth family. The parent's reading that the excess estimand makes a
body-conditioned fit immune to the body's misses was wrong and the adversarial review caught it
before dispatch; the native-only identification it forced is why the transfer table is a
measurement rather than a leak. Two gates on the archive cost no capture and moved nothing; the
next wave starts with a directional term, a pixel-integrated ramp and a named native experiment.

## Deferred at close

1. **A directional term and a native experiment.** The next edge charter declares a signed-normal
   (top-lit) response beside the even lobe, and captures what this archive cannot supply: matched
   top/bottom controls at several depths, independent subpixel phases through a mechanism proved
   reachable first (W34's offsets collapsed to two states), repeated 1x / 2x captures, a second
   calibration span under a fresh split, and a larger circular radius — under the user's X5 / TCC
   authority. The effective composite is the target; Apple's layer order is not identifiable from
   composites (§5.182, the G0b review).
2. **An existing-leaf refit with the axis rotated to vertical was never scored.** W35 G0a's search
   held `rimLitAxis` diagonal; G0 showed the old rim's side light is the largest single wrong
   contribution and that axis, exponent and width can carry the morphology. A bounded wave could
   price rotating the axis and narrowing the width on the four macOS 27 documents as a colour-blind
   improvement, with the goldens attributed and every stop read — a smaller change than a new
   operator, and possibly most of the eye-visible gain.
3. **The line's amplitude across the grey ladder.** F3 underpredicts the mid-greys' line row
   (dark grey-64 2x outer row 45 against 36.87; light grey-160 1x 23 against 17.07) because the
   joint fit over greys and saturated solids compromises; a future level response may need
   neutral and chromatic conditioning separated, declared, not fitted past its stops.
4. **The pixel-integration model.** Area-integrated kernels for every thin feature in the forward
   model, and a check that the runtime's own rasterisation of a one-CSS-px feature at 2x (the
   rim's `rw` sampled per pixel) agrees with the integral it is fitted to.
5. **Thickness and path.** Dark span-96's line reads 6 codes against 30 at span 44 while light
   barely changes; factor 1 fails that transfer by 22 codes; no second calibration span exists
   and the curved continuous path's arcs remain unresolved (G0 §5.181).
6. **E1's population on capsules.** Six of the 14 rows have no measurable left/right straight
   pixels under the corner exclusion; E1 as re-declared should say top/bottom-only for capsules
   rather than carry UNMEASURED sides forever (G0b's repaired reader keeps them explicit).
7. **The transfer to vitrea's body.** Any edge law conditioned on the body differs by up to 29
   codes on dark solids between Apple's body and vitrea's (F3's table); W36 Deferred 1–2 (the grey
   middle, the chroma) stand between an identified edge law and a rendered one.
8. **Carried unchanged:** the CSS two-row bound (W35 Deferred 5, W36 Deferred 4); M2's ring
   (W35 Deferred 4); the forward helper's fractional-coverage branch (W35 Deferred 3); W34's
   Deferred 2–7.

## Revision Notes

- 2026-09-25 (the parent, G0b's merge and the close): G0b merged to main as `2b61747e` with
  `--no-ff` after an independent review that reproduced the declaration hash, all 66,765 residual
  bins, the 624 transfer tables, the obstruction on guarded pixels (seven repeats, zero
  variation, translation [0, 0]) and the E1 repair, and merged as-is; `freeze.py verify` 1,818;
  no capture tree moved. Decision Log 1 ruled a second time: close at the finding by the hard
  stop. Status, Surprises (the directional component; the integrated ramp), Outcomes against the
  eight clauses and Deferred at close written. G1a, G1b and G2 were not opened; no release.
- 2026-09-25 (G0b, delivered for review): §5.182 records declaration89f45616 before scoring, three
  expanded native-only families, all failed grey straights, the coefficient-independent1.5-code
  even-normal obstruction, full old-rim trades/transfer, and E1's missing-side correction. The
  ruled hard stop fires; DL1's G0b draft recommends close W37, not another family or prototype.
  No material, capture, holdout or release change. Calibration50 files/700 tests, ESLint and four
  TypeScript checks pass; freeze1,818 before/after. Parent review and closing ruling pending.

- 2026-09-25 (v3, the parent, G0's merge): G0 merged to main as `e143360d` with `--no-ff` after an
  independent review that reproduced all 44,510 residual bins, the 416 transfer records, the
  coefficients to 1.8e−13, the 208-cell replay, E1's 14 / 14 misses, the placement pairs and the
  in-memory identity proof, and found one P3 (the E1 reader omits UNMEASURED sides) folded into
  G0b; `freeze.py verify` 1,818; no capture tree moved. Decision Logs 1, 2 and 4 ruled (above):
  no family nominated, G0b authorised with a predeclared larger budget and a hard stop, the old rim
  replaced behind the gate if a family is admitted, post-tint conditioning, thickness fixed, E1
  confirmed as the proposal. Ledger numbering: G0b §5.182, G1a §5.183, G1b §5.184, G2 §5.185.
- 2026-09-25 (G0, delivered for review): §5.181 records declaration `6f2e89ed` before scoring,
  the exact208-cell memo replay, both failed native-conditioned families, separate transfer,
  tint placement, six-digest identity route, backend/policy/CSS declarations and the fully frozen
  E1 proposal. DL1/2/4 above are drafts only; no G1a dispatch, source/material change, browser,
  native capture or holdout exposure. Evidence README is `.txt` under the worker restriction.

- 2026-09-25 (the parent, second adversarial round on v2): approved with no material finding —
  the reviewer confirmed clause 3's separation of native identification, baseline transfer and the
  shader check (the refinement allowance targets the forward-versus-shader discrepancy only, and an
  improvement obtained by absorbing the baseline violates clause 3 and X15 rather than using an
  exception; W35's 0.87-code reading does not stand in for the new operator's own rendered check),
  the thickness restriction, the backend contract, and the G1a/G1b ordering (E1's adoption and M2's
  re-baseline at G2 are evidence adoption on a fixed population and reference, not another material
  choice). One implication folded into X13: the identity-table entry G1a lands is append-only even
  if the wave stops. G0 dispatched.
- 2026-09-25 (v2, the parent, after the adversarial review's four findings): **P1 folded** — the
  claim that subtracting the deep median makes the fit immune to W36's remaining body misses was
  false for a body-conditioned law (`g·(L_web − L_native)` survives the subtraction); clause 3 now
  restores W35's discipline in full (native-identified coefficients, the transfer tabled per cell,
  the synthetic pre-composition test, the rendered check attributed to the shader and not the
  baseline), with Design and X15 rewritten to match. **P2 folded** — thickness is not identifiable
  on calibration (every uniform calibration geometry casts span 44), so the response is declared
  with no free parameter and the 160×96 cells stay transfer (clause 2, Purpose, Decision Log 2,
  Risks). **P2 folded** — the CSS carry-or-decline and M2 rulings came after the holdout read and
  would have changed a configuration after exposure; G1 is split into G1a (pre-seal, scratch
  evidence, the rulings' material) and G1b (seal, read, tree), the ledger numbering moves to
  §5.182 / §5.183 / §5.184, and post-holdout outcomes are accept or stop (clauses 4, 6, 7,
  Ordering, Decision Logs 3 and 5). **P2 folded** — the WebGPU tier's `css-backdrop` and `none`
  paths do not hold the composite; X17 declares the sampling-backend contract per backend and G1a
  tests it (clause 2, Children, Risks).
- 2026-09-25 (v1, the parent): chartered on the grounding memo; nothing dispatched; the charter
  goes to `doperpowers:adversarial-reviewer` before G0 opens.
