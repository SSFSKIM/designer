210:**Imagery ladder, per image slot.**
211-
212:| Slot | Rung | Why the rungs above it did not answer |
213-|---|---|---|
214:| Hero artifact — a stocked shop aisle | **2, Unsplash** (`tE6zayejDNY`, KC Shum) | Rung 1 returned nothing usable: the project's own inventory is 200 calibration fixtures at 320x200 and one favicon. Six queries were run for this slot. A pegboard wall (`ciY4tc7539I`) was picked first, rendered, looked at, and rejected on sight: every label in it is in Japanese, which is the wrong cultural context for a Vermont shop and the embedded-text disqualifier at once. |
215:| Guide plate — a gutter and downspout | **2, Unsplash** (`kq3l2WLNp8Y`, Miles Smith) | Same rung-1 result. Two queries: an autumn phrasing returned only leaf piles, so the query dropped the season and kept the object. |
216-| Six tool tiles | **4, drawn as content** | The subject is an artifact the page explains, not a world, and a shelf tag's own picture has always been a catalogue line drawing. Two searches were run first and recorded (`search-7`, `search-8`): the first returned nothing, and the second returned six photographs of four unrelated subjects at three production standards, which is the mismatched-quality disqualifier. No matched set exists for these six items. All six share one vocabulary: monoweight `currentColor` stroke at 1.6 on a 120 x 84 box, square joins, no fill, no colour of their own. |
217-| Story artifact — the shop's own enamel sign | **4, drawn as content** | The subject is the specific object the paragraph beside it is about, so no photograph of any other shop could stand in for it. Four searches were run for a storefront (`search-5`) and a Western hardware interior (`search-9` to `search-13`); the usable results all carry another business's name on the fascia, and casting a stranger's shop as this one is a lie the page cannot tell. The sign is the one drawn artifact that carries lettering; it is drawn in the page's own display face and the page's own paint, and it is the only place the signature band becomes an object. |
218-
