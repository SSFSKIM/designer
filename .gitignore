# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log

# Dependency & build output (language-agnostic; prune as the stack settles)
node_modules/
dist/
build/
out/
target/
__pycache__/
.venv/
venv/

# Obsidian workspace
.obsidian/

# Skill-creator eval workspace (scratch, not part of the skill)
figma-design-workspace/

# playwright-cli scratch
.playwright-cli/

# Playwright run output (packages/platform-web/e2e)
test-results/
playwright-report/
.playwright/

# Build metadata
*.tsbuildinfo
coverage/
.vite/

# Written and removed again by packages/core/test/purity-law.test.ts
*.purity-seed.ts

# Scratch directory for one-off probes and harnesses, already ignored by
# eslint.config.mjs. Nothing here is part of any package's build or test graph.
.vitrea-tmp/

# Web-side calibration captures. Derived and cheap to regenerate
# (`pnpm --filter @vitrea/calibration run capture:web -- --all`, about a minute),
# unlike apps/reference-apple/fixtures/, which is machine-specific ground truth
# and IS committed. The measurements taken from these live in results/.
packages/calibration/web-captures/
