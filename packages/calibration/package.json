{
  "name": "@vitrea/calibration",
  "version": "0.0.0",
  "private": true,
  "description": "Internal tooling: fixture schema, diff metrics, profile-fitting CLI. Never published.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint . && tsc --noEmit && tsc --noEmit -p tsconfig.cli.json",
    "test": "vitest run",
    "diff": "tsx cli/diff.ts",
    "compare": "tsx cli/compare.ts"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "@types/pngjs": "catalog:",
    "@vitrea/core": "workspace:*",
    "@vitrea/motion": "workspace:*",
    "@vitrea/platform-web": "workspace:*",
    "eslint": "catalog:",
    "pngjs": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
