W38 G0 — recommend close at the finding (§5.183)

C1 fails the one-code per-channel-bin veto: dark-yellow2x circular120 arc6
shell−1 blue worsens25.868909→72.645686, +46.776776 codes, inside an improving
aggregate stratum. C2's3201-point-per-scheme native-only constrained grid has0
feasible points in either scheme. Selected=null; tabulated C2 points are FAILURE
WITNESSES only. No material change, render, native capture or holdout payload read.
The parent still rules DL1/2/4; G1a does not follow from this evidence.

Reproduction and order
- memo-replay.py / memo-limits.py reproduce the grounding's84cells/9637bins exactly,
  the864-point grids, sampling numbers and16.812762-code chroma floor.
- reproduction-summary.json adds the reviewer's18/31 counterexample and literal
  C1 scale .7448326599749504 versus ideal .7448387315613512. The parent ruled literal
  ratio before scoring because passes.ts does not normalise [-.7071,-.7071].
- bounds-declaration.txt and declaration-pins.json were committed in9f1fca9c.
  declaration-receipt.json names that ancestor. score.py refuses changed pins.
- model.py admits only W34 calibration before opening crops/images; search uses
  native deep only, then separately opens historical web bodies for transfer.
- search-results.json.gz and fits.json preserve every point and null selections.
  verify-scores.py independently reconstructs all6402 trials, discrepancy0.
- candidate-bins.json.gz records every native/old/new bin and veto; c1-angular-bins
  adds actual per-pixel lobe means. Structured constant-deep readings are diagnostic
  counterfactuals, not an identification of the body's spatial field.
- transfer.json.gz records280 candidate/cell conditioning transfers; the named
  nonblack W34 and W36 retained-black origins are not new canonical captures.

Canonical baseline and collapsed price
- e2.py --verify regenerates212 active nonholdout standard27 rows (including140
  canonical probes under W25 DL3(e)) from guarded native and matrix-named main-tree
  captures. Both document hashes, native PNG hash, renderer/backend, deterministic
  metadata and fixed scene geometry bind the read. No holdout payload is opened.
- E2:136 covered rows/76 UNMEASURED capsules;7728 measured/1488 absent bins.
  Compare measured top/bottom bins even on incomplete rows. Four grouped rows use
  per-member circular whole-pixel/normal-angle cuts with minimum coverage.
- The initial72-row cal/validation-only declaration, baseline and summary are kept
  as e2-calval-provisional-*; the212-row files are the actual pre-score freeze.
- All14 repaired E1 rows reproduce numeric bins exactly. Estimator3 is DECLARED
  ONLY for six profile×scene stacks across two scene IDs; the charter's “two rows”
  shorthand is corrected, no holdout pixel read.
- collapse-census.json finds zero T>0 cells among212. All40 tinted rows are T0;
  tinted-c1-top.json.gz retains720 measured native-comparison bins with analytically
  zero held-top delta. This is NOT a new render or a tinted-arc pass.
- The collapsed branch's factor1.34258 has no canonical27 witness and no named
  existing pixel fixture that reaches it under27. Span44+ cannot collapse under
  tone-high .0001/size-bias .05; near-black smaller spans can if policy permits.
  Current collapsed-tone goldens are26.5/span44/.01. No new fixture was built.

CSS and stops
CSS must multiply the blended amplitude by actual top-lobe factor before conversion,
not divide a scaled intercept. Full collapse bare.02432→.03265136326761601,
painted.3328→.44680812892527166. The T0/.5/1 × bare/painted tables, clear and
strong-border cases are in css-and-stop-notes.json. The literal GPU correction
versus old CSS ideal normalisation is8.15ppm, not an untested byte-identity claim.
The6.5/8.5 two-row bound stays. No CSS render or carry/decline was performed.

stop-baseline.json.gz retains610 nonholdout current rows. A constant-deep boundary
model cannot predict actual M1/M2/L1/predicate/coherence values; stop-price.json.gz
says UNIDENTIFIED, adoption-blocking, rather than inventing passes. Since neither
candidate survives, no-change closure keeps every current row/list, churn0.
Correction beside sealed declaration: M2 erosion -60.02832691048684/-48.43312779842039
is dark photo capsule1x/2x, NOT light/dark. No number or bound rewritten.
The13 golden PNG hashes, default-material harness, activation digest pins and E1
artifact are named in pre-w38-baselines.json. No isolation/golden/browser run.

Read-boundary surprise and parent's ruling
The brief simultaneously requested full calibration tests and said W34 validation
pixels “never opened.” The full inherited suite runs:
  w37-edge-identification.test.ts -> W37 verify-scores.py
  w37-edge-identification.test.ts -> G0b reproduce-g0.py --verify-native
  w37-g0b-identification.test.ts -> G0b verify-scores.py --verify
Those scripts replay their previously declared validation cells through W35 guards.
The helper completed that historical suite705pass/1skip; no W38 fit or selection
consumed those pixels. Upon auditing this conflict, a second full invocation was
stopped and the parent ruled the historical-gate replay acceptable, correcting the
brief's wording beside. No further unfiltered replay was run. Final W38-only8tests
and calibration ESLint/four TS checks pass. The skipped historical X1 pixel test
had no worktree capture tree. It was then run alone with the read-only main tree
and passed on218 canonical nonholdout cells (other100 tests filtered). Both the
generation check and pixel check now have separate green receipts. Freeze1818
passes before/after and git diff --check is clean. Logs retained separately.

A Python name collision initially imported W35 model.py after e2 inserted its path;
that script evaluated historical numerical JSON, not pixels. Its exclusive-write
guard refused to replace recorded line-ramp-level.json. Import order corrected;
no prior evidence bytes changed. These process details do not enter a coefficient.

Re-running
Use python3.12 (PIL/numpy). All new score/output writers refuse existing files.
For an exact fresh reproduction, redirect outputs to a scratch copy of this evidence
directory while keeping repository-relative dependencies resolvable; never delete
recorded evidence to make a script run. e2.py --verify and test-*.py are read-only.
No .md refusal occurred; new evidence prose uses .txt, while the existing ledger
and charter are updated directly. Independent parent review/closing ruling is next.
