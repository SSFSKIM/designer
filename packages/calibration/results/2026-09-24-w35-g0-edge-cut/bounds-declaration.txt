W35 G0a declaration — c9a §5.177, clauses 1–5 and E1's form

STATUS: a qualified negative, not a post-fit material declaration. The frozen domain
was committed in ae7492a8 before all eight candidate launches. It is not narrowed
following their residuals. No candidate closes it. The pre-rim field is a nominated
counterfactual; native coefficients have only an effective-response interpretation.
No coefficient transfer, G1 fit or E1 adoption is authorised by this evidence.

Why this file is .txt: the worker's higher-priority instructions forbid Markdown
reports. The machine declaration is domain.json, whose SHA is pinned by the test;
this file inventories the unchanged stops and the limits on expected values.

INSTRUMENT AND MODEL

The full cut is [-H,4) device pixels, H half the component's shorter extent: 22/44
for the 44 CSS px high geometries at 1x/2x; 48/96 for continuous-160's 96 CSS px.
The deep body is the RGB median at d<=-6 CSS px, so the 8–14 CSS px trough is INSIDE
that estimator's population, not discarded. Its min/max and max deviation+0.5 are
reported without sample-count shrinkage. The memo's d<=-6 DEVICE px reading stays
beside it. Repeat bars cover every populated shell/part/normal/channel separately,
normal and long protocols separately, all admitted runs before plurality. There are
2,352 normal observations, 48 long observations, 238,292 bin rows, maximum bar0.5.
Population floor4, tau=max(1 code,bar), absolute values before spatial reduction.

Closure: uniform circular solids, calibration/validation only, -6<=d/scale<0,
all four pixel corners inside the aligned convex body. Its exact-body integral is
one under the nominated path. Arcs/straights and angular bins never pool. Pixels
failing the whole-pixel proof (including some straight line pixels), the outside
stroke, deeper trough, structured and continuous geometries are diagnostic-only.
The measured isoresponse alignment is NOT a hidden native raster-origin attestation.
The W34 half-pixel alignment and radius-scaled path uncertainty stay separate from
tau; a nominal whole pixel is not proof against every possible hidden native path.
This qualification limits physical identification, not the declared point test.

Forward model: decode native deep RGB to linear sRGB C, nominate sampled-texture
A=1, add a compact ramp before inner shadow, then k=1-shadowProfile*depth*alpha,
A'=1-k(1-A), C'=kAC/A', L=A'Y(C')+(1-A')*tone. Rim light is
rw*lit*along*(a+gL), added before encoding; E(d)=B_line(d)+B_ramp(d) is the
observable diagnostic split, not two observed Apple layers. The runtime's actual
uniform span44 inputs have depth .35, alpha .05 and reach9.18125 CSS px. Size bias
keeps presence1 even over black. Native final RGB does not expose this intermediate.
The line diagnostic is the last1 CSS px minus the affine continuation of the last
two ramp shells; ramp is excess above the own deep median over the preceding5 CSS px.
These rules are fixed before candidate residuals and include all chromatic levels.
Channels at >=254.5 are censored when fitting added light, never treated as an
invertible observation. Predictions still face the encoded-channel closure test.

The same forward arithmetic reconstructs shipped web straight-solid means within
0.873/0.847/0.806/0.867 code (1x light/dark, 2x light/dark), without fitting web
residuals. This is a mapping CHECK for the web branch, not a recovered native field.
Known (a,g)=(.07,.23), a baseline shift BEFORE all operators, and a coefficient
shift at fixed baseline recover to <2e-16; quantised prediction error <=.583 code.
encoded-control.json separately adds5 codes AFTER encoding: that passes but is only
a control. Native baseline envelopes, hidden coverage and the unmodelled ramp leave
native identification unresolved. native-law-fits.json and native-grey-law-fits.json
retain native-only solves and g*(L_web-L_native) separately. Web residuals fit nothing.

EXISTING-LEAF FEASIBILITY

Two nominated settings per stratum: a squared rim band with the shipped inner shadow,
and the best tested setting when shadowAlpha may also be0 or negative. Width search
.8..6.5 CSS px in .05 increments, shadowAlpha in {.05,0,-.05,-.1,-.2}; least-squares
coefficients, configuration selected by worst encoded calibration error. This is NOT
a continuous global optimum or a proof against every legal setting. The first solve
uses calibration greys plus five colours and fails; the narrower grey-only solve is
an explicit secondary diagnostic, not removal of colours from the frozen closure.
Validation grey96/cyan is never fitted. All fits are in native forward space.

Eight actual launches,64 deterministic real-Metal captures; two comparer exits1 for
1x light grey255 zero-length contourCurvature, not failed pixel captures. No retry.
Best tested whole-domain residual, with straight-only residual beside:
  1x light 25.50 codes (straight7.00), validation grey96 20.75;
  1x dark  33.75 codes (straight5.00), validation grey96 25.75;
  2x light 36.00 codes (straight9.848684), validation grey96 29.571429;
  2x dark  46.571429 codes (straight6.00), validation grey96 36.142857.
All face tau1. Arcs are the worst bins. The existing diagonal lit law is especially
wrong on them: native grey128's 2x whole-pixel shell-1 reads near-zero excess at
horizontal normals and +20.5/+25 light, +27/+33.33 dark at vertical normals. The
read is in angular-cut.json; it does not identify an Apple light axis uniquely.
A positive ramp alone cannot remove these arc overshoots or the chromatic residual.
A new ramp's required contribution would include the missing5–11-code shallow rise,
but that is not sufficient to close the line's radial, angular and colour response.
No particular one-leaf ramp operator has been identified for G0b by this gate.

UNCHANGED STOPS AND EXPECTED VALUES

There is NO accepted post-fit configuration. A numeric expectation for its canonical
perceptual/chroma statistics would therefore be fabricated. This is a blocked
expectation, not permission to defer declaration until after a successful fit.
The next authorised declaration must supply those expectations before adoption.
The following table distinguishes exact identity expectations from unresolved ones.

  Closure: tau=max(1,bar), population>=4, every channel/shell/part. Required <=1
  on this archive. Actual best tested expected state is the failed values above.
  Complete chromatic validation is not rendered: native-only law failure already
  prevents identifying a transferable scalar response, and cyan stays in the domain.

  Inactive flat: zero operator contribution, expected0 from the composed rim and
  shadow amplitude gates. Actual receded candidate web excess0 at three strata,
  up to0.333333 in 2x light arc bins; native-minus-web excess0.5/2/1/0 across the
  four strata, so 1x dark nominal arc closure fails even with both operators off.
  No second read or domain exclusion repairs that floor.

  Level absorption: no fit to native-minus-web; zero tone/chroma leaf changes.
  Expected coefficient compensation0. Nonzero g*(L_web-L_native) is explicitly
  reported, not fitted away. Effective-only identification stops coefficient transfer.

  Per-cell tables: unchanged, including edgeWeightedMean<=.11 on its owning rows
  and each profile/tier's IoU/contour/SSIM/DeltaE table. No new27 floor. Expected
  post-fit values UNIDENTIFIED: no admitted model of structured boundary body.
  Current recorded failures stay recorded, never widened to admit a candidate.

  W20 conformance: contour<=1 device px, IoU>=.99 where measurable. Expected drawn
  field geometry delta0 if only fully covered pixels change; not a certification
  of the thresholded rendered alpha, which a real changed band may move. These
  candidates have not been rendered on the canonical conformance set.

  Predicate: area ratio .95, actual exclusion list re-derived at adopting gate.
  No numeric exclusion-count expectation is justified before a model survives:
  a brighter edge can create or join luminance-delta silhouettes. Neither the
  current67 list nor zero churn is promised. No predicate edit in G0a.

  M1: median R .80..1.20, per-cell .60..1.40, recorded misses unchanged. Expected
  values UNIDENTIFIED; a white line raises luma spread without restoring chroma.
  A proposed target interval is not a prediction of this ratio.

  M2: |stdDevNew/stdDevPreW35-1|<=.02; G1 reference is the superseded PRE-W35
  generation, not its new result. The26 current values reproduce matrix values
  to <1e-12. Erosion by1/2/3 device px is tabled per cell. Removing1 px lowers
  dark photo capsule std-dev60.03% at1x,48.43% at2x; removing3 lowers68.37%,58.00%.
  This establishes strong edge sensitivity, not the change caused by a correct
  future edge, nor an additive variance share (erosion changes mean/population).
  Expected post-fit M2 is UNIDENTIFIED; keep2% until a viable candidate supplies
  a measured delta. DL3 is a decision draft, not a mask change or waiver.

  X1: expected outside-black lit count0, fraction0, count above1 also0 for a compact
  strictly interior operator; exact identity expectation outside its support.
  Existing squared bands also extend outside d0 and through partial coverage, so
  these renders alone do not certify X1 on every canonical black pixel.

  C1: <=.0042 exterior-shape error per span over admitted3–6 and farther bands;
  B1: sigma law within5% at96/128/160. Expected change0 for a compact boundary
  operator supported inside6 CSS px and with outer-shadow leaves untouched.
  Expected values are the current matrix's values, not a new numerical fit; a
  later operator reaching the admitted exterior invalidates the zero-delta premise.

CSS AND RECEDED COMPOSITION

One CSS px inset becomes two full rows at DPR2 with one alpha. On grey128 the native
2x light rows213/226 cannot both be matched by one constant: the best common output
219.5 misses each by6.5 codes. Dark156/173 gives8.5-code minimax. This is an analytic
straight-edge lower bound, not a CSS render or a completed decline. Derive the
amplitude/integral from the same adopted leaves if one is identified; test on the
bed before carrying or declining it, pin any decline to the actual document bytes.
No CSS browser launch was authorised or performed in G0a.

Receded patches compose OVER the active endpoint. Both shipped receded documents
explicitly zero regular/clear rimAlpha, rimLevelGain, shadowAlpha, plus root
rimCollapsed/rimCollapsedTinted. Width and shape changes stay inert under those
zeros, as the64-capture experiment's nonzero-active receded controls test. An
independent new bodyBoundaryRamp requires explicit0 overrides in BOTH receded
patches: the existing rim gates would not automatically own an independent term.
macos27-profile-export.test.ts pins document composition bytes; an inert operator
case plus posed renders, not that export test alone, must prove flatness.

IDENTITY AND E1

identity-proof.ts applies a proposed flat bodyBoundaryRamp:0 drop in memory only.
Both frozen digests b2b570e4adcea8fb / 874be66ea501621b and all four27 digests stay
exact; a nonzero leaf changes the digest. No nested empty-object workaround, no
material.ts edit. The named future inertLawCase and literal identity assertion are
proposals, not implemented tests or a validated optical law.

Conditional E1 would read absolute RGB inner-edge-shape residual per pose on the
macOS27 standard WebGPU endpoints, using the frozen masks, baseline rule, bars,
calibration/validation roles and population floor. It must rederive from captures
at the adopting documents/source bytes, with their wave-local matrix and canonical
generation binding, not a saved passing cut. W34 holdout is never part of it.
The MATERIAL-axis amendment would need to establish (a) an isolatable inner-edge
estimand on uniform controls rather than a scene-dependent blur fit and (b) a
recoverable, discriminated pre-rim mapping rather than a freely traded body/rim
baseline. This gate supplies(a) only conditionally and does not establish(b), so
E1 is NOT adoptable now. Canonical structured cells remain a separate referee.

WHAT IS NOT CLAIMED

No material improvement, identified native internal state, global optimum over all
existing leaves, prototype operator, canonical read at changed bytes, CSS decline,
post-fit canonical expected table, native capture or holdout read. The next decision
is whether to extend the wave's operator/model budget or close at this finding;
G0b is not started and no src/, profiles/, fixtures/, canonical matrix or scenes move.
