# W27c G1d eye read

Read 2026-09-14 from the six sheets in `sheets/`, native | WebGPU | 8× absolute difference, after the endpoint and holdout were frozen.

- **Dark standard, both scales.** The two bright-solid ends now look like the same material: the large `light-solid` pane differs mainly at its hairline contour, and the dark-solid cells are visually coincident. The remaining misses are structured. On `hc-text__rrect-sm__inactive` WebGPU is visibly brighter and carries a different bar-shaped interior; on the low-contrast checker capsule WebGPU is darker and transmits a different checker amplitude. Large checker/text panes show the rim and lens band in the difference strip even where their mean level is close. The 1x and 2x patterns agree; this is not a scale-only artefact.
- **Light standard, both scales.** The bound's one failing cell is also the clearest eye failure: `hc-text__rrect-sm__inactive` is a pale grey native control and a much brighter WebGPU control. Several large structured panes show different structure transfer and a stronger/differently curved edge band in WebGPU. Uniform `light-solid` is effectively identical and the dark-solid cells are close in level, though `dark-solid__rrect-80__inactive` retains a visible cool/dark body difference. The same families recur at 2x.
- **Increase Contrast.** The body level is close on most cells after the 0.98 fit, but the one-pixel contour is the dominant visible residual on every shape: the difference sheet draws an almost complete bright outline. `light-solid__rrect-ml__inactive` remains visibly too white in WebGPU against the native's greyer panel, and the structured large panes keep a dark interior structure difference despite the low body DeltaE. This is the `strongBorderRim` residual already named by §5.141, not a reason to move the fitted body level after holdout.
- **Reduce Transparency.** The panel is substantially flatter and closer than the shared 0.96 endpoint. The 0.88 fit still leaves a visible level slope across backdrops: WebGPU is darker than native over dark-solid, but brighter over light-solid. Structured text panes retain different edge/structure bands even when the bodies are near-flat. The remaining mismatch is therefore not another single level; it is the residual structure and contour that the existing tracker entry preserves.

The metrics and the eye agree on the disposition: T1 closes the dark bright-solid catastrophe and T2 closes both accessibility bound failures, but neither makes the recede pixel-identical. The two light-standard `hc-text` failures, dark structured transfer, accessibility contour, and Reduce Transparency's residual backdrop-dependent spread remain explicit future work. No floor is adopted from this probe-bar evidence.
