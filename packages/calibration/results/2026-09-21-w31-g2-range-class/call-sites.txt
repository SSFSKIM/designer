W31 G2 — every transcendental call in packages/renderer-webgpu/src/wgsl/, classified.
Generated by scan-report.mjs; claims c9a §5.163 §1.

11 call sites over 9 files: 8 clamped by the source, 3 bounded by a committed proof, 0 STOPPED.

Files with no call of exp/exp2/pow/tanh/sinh/cosh/log/log2/inverseSqrt at all:
  analysis.ts, cross-check.ts, field.ts, index.ts, silhouette-tone.ts

backdrop.ts:164  exp  — clamped — bounded by the source
  exp(ft * ft * inv)
  arg0: ft * ft * inv
          [-f32max, 0.00000]
  argument [-f32max, 0.00000] is at or under ln(f32max) = 88.72284

highlight.ts:153  exp  — clamped — bounded by the source
  exp(-pow(angle_delta(theta, centre) / width, 2.0))
  arg0: -pow(angle_delta(theta, centre) / width, 2.0)
          [-3.94784e+9, 0.00000]
  argument [-3.94784e+9, 0.00000] is at or under ln(f32max) = 88.72284

highlight.ts:153  pow  — clamped — bounded by the source
  pow(angle_delta(theta, centre) / width, 2.0)
  arg0: angle_delta(theta, centre) / width
          [0.00000, 62831.9]
  arg1: 2.0
          [2.00000, 2.00000]
  base [0.00000, 62831.9] to exponent [2.00000, 2.00000] is [0.00000, 3.94784e+9]

optics.ts:285  pow  — clamped — bounded by the source
  pow(x, 1.0 / 2.4)
  arg0: x
          [0.00000, 1.00000]
  arg1: 1.0 / 2.4
          [0.416667, 0.416667]
  base [0.00000, 1.00000] to exponent [0.416667, 0.416667] is [0.00000, 1.00000]

optics.ts:391  tanh  — clamped — bounded by the source
  tanh(t)
  arg0: t
          [-20.0000, 20.0000]
  argument [-20.0000, 20.0000] is within ±ln(f32max)/2 = 44.36142, where the exp(2t) lowering stays finite (claims §5.159b)

optics.ts:529  pow  — clamped — bounded by the source
  pow(1.0 - occ, 1.0 / 2.4)
  arg0: 1.0 - occ
          [0.00000, 1.00000]
  arg1: 1.0 / 2.4
          [0.416667, 0.416667]
  base [0.00000, 1.00000] to exponent [0.416667, 0.416667] is [0.00000, 1.00000]

optics.ts:900  pow  — proven — bounded by a committed range proof
  pow(lensT, ou.lensShape.y)
  arg0: lensT
          [0.00000, 1.00000]
  arg1: ou.lensShape.y
          [-f32max, f32max]
  base [0.00000, 1.00000] reaches zero while the exponent [-f32max, f32max] reaches zero or below: pow(0, e) is exp2(e · log2(0)), which is 0 only for e > 0
  proof arg1: lensProfileExponent, strictly greater than 0 and at most 64
    depends on material leaf `lensProfileExponent` (`ou.lensShape.y`)
    depends on scene: the pixel's depth inside the surface, through `lensT`

optics.ts:947  log2  — clamped — bounded by the source
  log2(max(gainEff, 1e-4))
  arg0: max(gainEff, 1e-4)
          [0.000100000, f32max]
  argument [0.000100000, f32max] is strictly positive

optics.ts:1483  pow  — proven — bounded by a committed range proof
  pow(max(abs(dot(normal, ou.rimLit.xy)) * 1.4142135, 1e-6), ou.rimLit.z)
  arg0: max(abs(dot(normal, ou.rimLit.xy)) * 1.4142135, 1e-6)
          [0.00000100000, f32max]
  arg1: ou.rimLit.z
          [-f32max, f32max]
  base [0.00000100000, f32max] to exponent [-f32max, f32max] is not bounded inside f32
  proof arg0: [1e-6, 2.83] — the floor is the shader's own `max`, the ceiling is 1.4142135 · |rimLitAxis| with |rimLitAxis| at most 2
    depends on material leaf `rimLitAxis` (`ou.rimLit.xy`)
    depends on material leaf `rimLitExponent` (`ou.rimLit.z`), for what the ceiling has to be enough for
    depends on scene: the field pass's surface normal, `field.yz`

prelude.ts:21  pow  — proven — bounded by a committed range proof
  pow((c + vec3f(0.055)) / 1.055, vec3f(2.4))
  arg0: (c + vec3f(0.055)) / 1.055
          [-3.22543e+38, 3.22543e+38]
  arg1: vec3f(2.4)
          [2.40000, 2.40000]
  base [-3.22543e+38, 3.22543e+38] reaches below zero, where WGSL leaves pow undefined
  proof arg0: either inside [0, 1.2e6], or discarded before it is read
    depends on the `select` two lines below, which is the whole of the sign half
    depends on the four callers of `srgb_to_linear` in this package, for the magnitude half

prelude.ts:27  pow  — clamped — bounded by the source
  pow(max(c, vec3f(0.0)), vec3f(1.0 / 2.4))
  arg0: max(c, vec3f(0.0))
          [0.00000, f32max]
  arg1: vec3f(1.0 / 2.4)
          [0.416667, 0.416667]
  base [0.00000, f32max] to exponent [0.416667, 0.416667] is [0.00000, 1.13484e+16]

The instrument can go red — two mutations, each re-introducing a defect a gate fixed:
  angle_delta without W31 G2's floor (highlight.ts)
    pow(angle_delta(theta, centre) / width, 2.0) -> [-f32max, 62831.9]
    UNBOUNDED
    base [-f32max, 62831.9] reaches below zero, where WGSL leaves pow undefined
  outer_shadow_falloff without W30 G3b's ±20 (optics.ts)
    tanh(t) -> [-2.71506e+38, 2.71506e+38]
    UNBOUNDED
    argument [-2.71506e+38, 2.71506e+38] leaves ±44.36142, where a backend that lowers tanh through exp(2t) returns Inf/Inf (claims §5.159b)
