# W33 G2 eye — the floor closes; the contour remains; a display false alarm resolved

2026-09-22, claims §5.173. All 24 native | WebGPU | CSS | OKLab ×8 sheets and all four
native | harness WebGPU | `/laws/` comparisons were viewed. No new capture was taken on
the calibration bed. The source is the main checkout's canonical tree, read-only; both
active and receded document hashes are asserted per cell. `sheet-readings.json` and
`demo-sheets.json` name the pixels and the documents. The eight absent 2x CSS probe panels
are labelled unmeasured, not filled with substitutes. Both schemes and both scales cover
hc-text28/7, checkerboard8 at 128/160, and photo/lg and photo/capsule.

## The material

The black squares and text-stripe exteriors stay black on the WebGPU tier: all sheet
black-floor counts are zero. Every ×8 panel prints its one-byte calibration, **137.10/255
for byte 0→1**, and its actual >0 / >1 counts. The remaining bright contour line is the
unidentified term Decision Log 3 stopped, not a surviving lift. The body/checker and
photo-chroma residuals, especially dark photo, remain conspicuous; they were already
measured and recorded by G1b. There is no new unexplained terrace, second shadow edge or
unamplified banding. Faint amplified level contours do not establish a new material term.

The CSS integer-mask counts remain the previously recorded near-edge gap: 170 on lg,
134 on ml, 165 on hc-text28 at 1x, not the WebGPU lift. The analytic-box convention and
its distinction from the rounded contour are in §5.172's `css-floor.json`. Photo has no
eligible black exterior and is not evidence for that floor. The missing contour and the
receded body's excess structure still stand beside the closed floor, not behind it.

## The page beside the harness

The four rows cover spans 96 and 160, active and receded, at DPR 2. The stage screenshots
are retained in full; the sheet uses a raw 800×500 crop without resampling. This is a
juxtaposition, not equality between different grounds and widths: the 96 harness row is
photo; the 160 harness row is a black/white checker; the page paints a coloured checker.
The active page shadow is broader at 160; the receded page shadow is absent. Its readout
reports the active/receded endpoints, σ 8.96 / 17.37 px, outset 0.50 and offset 7.95;
active depths at 3/12/24 are 6.34/3.01/0.36 % at 96 and 15.69/10.55/4.67 % at 160,
and all receded depths are zero (`laws-readings.txt`). Lengths remain displayed when
amplitude is zero; they are not evidence of a shadow still drawing.

## The stop, and the raw pixels that closed it

**The gate DID stop temporarily.** In a downscaled display of the tall screenshot the
worker read the outer checker as vertical stripes and suspected a page/sampling mismatch.
The coordinator then questioned apparent vertical bands INSIDE the glass. Both readings
also appeared in W32's retained screenshot, and neither is accepted as a diagnosis here.
The chain was stopped after its six non-browser steps; no golden/GPU/e2e case had begun.

`eye-stop.py` and `eye-transects.py` read the stored pixels, without another browser:

- Outside the plate, x=810/842 alternates RGB (214,221,227) and (32,40,48) every **32
  device px in Y**, as it does in X: the declared 16 CSS-px checker, not stripes.
- The requested horizontal transect y=250, x=100…680 crosses the dark **160px label**.
  Its 190.5788-byte luma range and 581px dominant FFT period are not a material reading.
- The label-free vertical x=300, y=120…400 has 7.7874-byte range, dominant FFT period
  70.25px and a fitted 64px component of 2.057936 bytes peak-to-peak. It includes edge
  and lens variation, not a uniform interior alone.
- Matched label-free 161px core lines have **0.981231 horizontal / 1.438915 vertical**
  peak-to-peak components at the painted 64-device-px period. Both axes modulate.
  Their raw ranges are 1.0722 / 2.9278 bytes. Near the top the horizontal FFT period
  is 64.5px; contrast and polarity change with Y and fade into the interior.
- At x=192/224/256/288, active160 red samples are 219/217/220/217 at y128,
  218/219/218/219 at y160, and all 218 at y192…288. A heavily blurred checker remains,
  not a source with only one spatial axis.

**False alarm on both readings; the coordinator closed the stop on those samples.** Its
initial attenuation estimate used the shadow's σ, not the much wider body's blur. No
stride defect was established, no fix wave followed, and W32's `eye.md` is not corrected.
The raw cropped comparison rows make the checker legible again. The lesson is to inspect
an ×8 panel or raw crop and sample both axes before trusting a downscaled patterned image.
Only the not-yet-run browser tail resumed, each suite behind a fresh X6 barrier. No material,
bound, capture or already-green suite was changed or rerun to settle this false alarm.
