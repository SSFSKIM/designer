W36 G0 level cut — §5.178; branch w36-g0-level-cut

Result: reproduction and declaration delivered; the one constrained Part A
scratch candidate fails level-growth and structure stops. Part B is unidentified.
Decision Logs1,2,4 remain the parent's. Nothing is shipped.

README.txt is used because this worker's instructions forbid Markdown reports.
bounds-declaration.md is the required pre-fit contract, frozen before rendering;
bounds-reading.txt records the actual failed trial without rewriting that contract.
The narrative, decisions and remaining work live in §5.178 and the W36 charter.

Replay order (from packages/calibration)
  pnpm exec tsx results/2026-09-24-w36-g0-level-cut/identity-proof.ts
  python3.12 results/2026-09-24-w36-g0-level-cut/cut.py extract
  python3.12 results/2026-09-24-w36-g0-level-cut/cut.py derive
  python3.12 results/2026-09-24-w36-g0-level-cut/cut.py canonical
  python3.12 results/2026-09-24-w36-g0-level-cut/matrix-cut.py
  python3.12 results/2026-09-24-w36-g0-level-cut/fit.py
  python3.12 results/2026-09-24-w36-g0-level-cut/diagnosis.py
  python3.12 results/2026-09-24-w36-g0-level-cut/deep-bars.py

Recorded-output producers refuse overwrite. Replay in a fresh evidence
copy, preserving their sibling paths. identity-proof.ts proves a proposed append in memory, not a shipped shader branch.
All image payloads pass through unchanged W35/W34 readers. Original fixture
membership wins over a scratch probe label. No canonical or W34 holdout is read.

browser.py prepare projects the explicit admitted scenes to a scratch-only probe
manifest; render obtains X6 and launches once per profile. No implicit retry.
A preparation-only failure (canonical per-cell metadata file absent) happened
before launch: the canonical manifest's admitted record supplies that metadata;
the partial native-input projection remains at part-a-preparation-incomplete.
No successful browser work was repeated. prelaunch-corrections.txt names the
invalid profile selector and text probe-role correction before rendering.

read-candidates.py / export-candidate.py / exterior.ts read the actual captures,
check their hashes/determinism, and compare fixed native domains. Four scratch
profile trees and matrices remain under:
  /Users/new/vitrea-w36/scratch/part-a-shared-shifts/
Do not promote these candidates to canonical evidence: they failed the stops.
The main checkout's canonical tree is read-only and was checked before opening.

Key records
  solid-cut.json              104 circular solids, per-channel encoded/linear residuals
  deep-cut.json               336 admitted W34 deep medians, structured evidence included
  deep-median-bars.json       120 grey cells, the deep estimator's own normal repeat bar
  canonical-solids.json       39 admitted canonical/control cells, missing web explicit
  mechanisms.json             48 response checks, black fallback, middle knots, M1 secants
  chroma-cut.json             censor-safe q_native/q_web, q_base and required shader r
  censoring.json              14 censored circular cells (seven at each scale)
  abscissa-comparison.json    both formulas on the SAME silhouette domain
  joint-diagnosis.json        Part B unidentified; no untested operator nominated
  l1-{declaration,baseline}.json  population, misses, absent means and pre-W36 generation
  structured-baseline.json    fixed pre-fit calibration/validation read-only stops
  probe-level-diagnostics.json  separate non-holdout probes, not smuggled into L1
  candidate-fit.json          rank3 shared-shift fit and validation predictions
  candidate-{greys,structured}.json  actual render, all failed stops retained
  exterior-check.json        zero change on the explicitly bounded scratch subset
  browser-runs.txt            X6, commands, status, adapter, repeat checks, captured hashes
  eye-photo-*.png             native/shipped/candidate triptychs, unamplified
  identity-proof.json         both26.5 and four27 digests unchanged at identity
  close-*.txt                 final calibration suite and freeze verification
