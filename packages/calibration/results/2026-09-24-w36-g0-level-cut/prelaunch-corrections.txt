2026-09-24, before any candidate launch (§5.178, clause 2).

bounds-declaration.md calls hc-text-7__rrect-md__rest validation in one sentence.
The canonical declaration calls it PROBE. It is admitted by W35's canonical
reader, read-only and diagnostic; no fitting and no holdout alias. The probe
baseline is in probe-level-diagnostics.json. The exact hc-text/rrect-md remains
refused in both poses. No tolerance changes with this correction.

The first uncommitted least-squares trial selected scheme by searching the full
cell string, so light mid-dark-solid entered the dark objective. It was never
rendered. fit-invalid-profile-selection.{json,txt} retain that invalid arithmetic;
fit.py now selects the profile part only. candidate-fit.json is the corrected
trial, all Jacobians rank3, not an attempted new family or fit to validation.
The lower-dimensional family already misses calibration by 2.52–7.32 codes;
its render prices structured cost, not proof of closure or of global impossibility.
