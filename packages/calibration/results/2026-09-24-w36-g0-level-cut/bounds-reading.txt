W36 G0 — readings beside the pre-fit declaration (§5.178 clauses 1–6)

STATUS: the declared candidate FAILS. No material or bound moves. G1 must not
open on this trial. This is one identified three-parameter family per endpoint,
not an impossibility proof over every uniform tone law or thickness model.

L1: 140 declared calibration/validation standard WebGPU rows, 136 measured.
Two pre-existing absolute misses: 1x/2x light inactive tinted impulse capsule,
0.0660161259 / 0.0660589395 against0.055. Four dark inactive dark-solid rows have
no material means. Baseline growth is0 by definition at the named pre-fit W33
generation; the separate scratch trial is not a canonical L1 regeneration.

W34 deep median bar: re-derived on ALL120 admitted grey cells, seven normal
observations each, maximum median bar0. The previously cited0.5 is W35's shell
bar, not this estimand's bar; neither changes max(1,bar)=1. No zero-noise precision
claim. 40 candidate grey captures:24 meet1 code. Eight black cells intentionally
keep the old fallback because no existing leaf implements the proposed black
branch. Of32 non-black trial cells,24 pass. Failures: dark-active grey64 -2;
light-inactive grey96 -2; light-active white -4; light-inactive white -3, each at
both scales. Receded deep-body max-min is0 on every trial grey. Thick validation
remains untouched: no measured candidate claim is made there.

Structured stops:12/24 trial rows fail level growth. Light photo, rest/inactive:
  1x growth +0.02936709 / +0.02971698;
  2x growth +0.03370576 / +0.02974768; stop +0.005.
Light active checkerboard growth +0.05381791 / +0.05505393; the2x absolute miss
0.05586064 also crosses0.055. All four dark checkerboard rows miss growth too.
The admitted text probe and light impulse pass the declared level stops.
The exact hc-text/rrect-md holdout was never read or rendered.

M1: all eight rendered photo/rrect-md rows stay inside[0.8,1.2], range
0.86345688–1.10829053. This is NOT the full M1 cohort, so no bed-wide pass is
claimed. Retention is unchanged. Part B old-tone extrapolations remain outside
the median band:1.9848/1.76938/2.317745/3.5216 at operator r=.9/.9/1/1.
The per-cell converted r ranges imply M1 about1.855–1.957,1.647–1.752,
2.240–2.368,3.349–3.613. A new-tone Part B candidate was not nominated.

M2: dark active photo/rrect-md changes +4.129586% at1x and +3.139822% at2x,
outside2%. Its improved LEVEL does not excuse that structure growth. Other
rendered photo rows stay inside2%. The reference is the G0 pre-W36 generation,
not W31's or W32's older reference; full-cohort re-baselining is not performed
for a refused, non-shipping scratch trial.

Exterior: 24 canonical scratch cells, 2,866,176 pixels >=3 CSS px outside both
fixed native and declared silhouettes, changed0, max encoded delta0. Of these,
1,052,658 native-and-backdrop black pixels have candidate nonzero count0. This
shows the expected zero movement on this subset, not a replacement C1/X1 cut.
B1's leaves are byte-identical because only tone-response arrays were tuned.
Full C1/X1/B1 would be regenerated only at a permitted G1 material read.

Predicate, full per-cell/MISSED_27_ROWS, tinted shade and CSS coherence are NOT
re-adopted here. The trial records standard per-cell fields on its admitted
subset; no holdout or tinted scratch capture was authorised. L1's current
baseline records all tinted calibration/validation shade fields. Their shade
model gap persists as a stop, not a fitted compensator. CSS is an implementation
and re-read plan, not an unperformed browser measurement. The body-only trial
cannot certify the complete conditioning predicate or the historical eight
missed rows. Existing bounds remain unchanged.

Eye: the committed1x photo triptychs were inspected. Lowering light tone makes
the candidate platter visibly darker while retaining a subdued chromatic field;
the dark candidate is brighter than shipped but still markedly greyer than
native. The contour mismatch remains visible and is outside this level fit.
No contrast-amplified panel or synthetic enhancement was used.

Operational: five launches including opening isolation; all preflights RT0,
IC0,slider0.5,Show Borders0,foreign processes0,idle>=31895.513s. Isolation17/17;
64/64 deterministic Apple/Metal-3 candidate captures. One1x-light comparer exits1
because native grey255 has zero contour length for curvature; its image is kept
and the deep median is still read. No rerun, no native capture, no holdout read.
