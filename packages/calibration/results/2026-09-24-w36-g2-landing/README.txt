W36 G2 landing — c9a §5.180, clause 8

Prepared at0.24.0, unpublished. Review belongs to the parent; no merge or push.
The narrative is §5.180, the eight-clause retrospective is the W36 charter, and
c9d has the prepared release row. Material/profile/matrix/frozen evidence stayed
unchanged. Main's canonical and superseded capture trees were read-only inputs.

L1: l1-cut.py/json, four owner cases at the foot of adopted-thresholds.test.ts.
140 rows/136 measured, four UNMEASURED, two unfloored absolute misses, growth0.
discriminate.py and three discrimination logs exercise the real owner assertions
with scratch matrices/cuts; l1-green.txt is the untouched four-case result.
confirm-evidence.py/json re-derives M2 from current/W33/W31 and checks125 protected
files. final-audit.py/json adds the13 unchanged golden PNGs and chain/X6 totals.

Chain: chain.sh, chain-status.txt, chain-*.txt. React is173 pass/3 skip/1 standing
Firefox morph-release red, retained without rerun; react-first-failure-context.txt
is the raw driver report, not an instruction to fix or rerun it. All other full
suite steps pass; units2761/196 files, goldens34, GPU49, platform410, demo61.
Browser preflights and completion receipts are browser-runs.txt. Both eye foreign
counts1 are preserved and classified beside in final-audit.json. No process census
was silently normalised. Demo suite used free5177; live eye used owned5197.

Demo consumer correction: demo-black-control-red.txt precedes min0/prose repair;
demo-laws-green.txt has all10 law cases. Demo build/lint/46 units and root eslint
were run after it. laws-reference.ts resolves all four sealed endpoints using the
app's tsconfig; its first alias-less import refusal is retained. The CLI first
refused --filename before executing a screenshot, then the inline session produced
the actual hardware, byte-identical pair. Both sessions closed; Vite was stopped.

Eye: sheets.py and sheet-readings.json produce24 guarded triptychs. W34 black's
third panel is G1's pre-seal price proven patch-equal to the shipped law, explicitly
not a sealed canonical capture. Canonical panels check both document hashes and
matrix paths. Every x8 panel prints the LSB calibration. All24 and demo-black-pair.png
were viewed; eye.txt and demo-black-reading.json keep the qualifications.

Release: changeset-version.txt; dry-run.sh/.txt; pack-check.py/json. Three tarballs
are under this worktree's ignored .vitrea-tmp/w36-g2-pack. Core586228 /web569814 /
React188606 bytes,0.24.0 and internal ranges^0.24.0. No changeset publish invoked.

Recorded-output producers use exclusive output files. Do not replay into these
paths and overwrite a reading. The chain/one-browser runner refuse repeated log
names. Ordinary package tests are repeatable; browser runs still need X6, and the
standing React timing red is not a reason to rerun. No canonical/W34 holdout or
native capture is part of this landing's replay.
