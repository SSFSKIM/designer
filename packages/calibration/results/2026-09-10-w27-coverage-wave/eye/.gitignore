# The raw device-pixel captures the sheets are composed from. Regenerated by
# apps/demo/eye-capture.mjs (see index.md); the composed sheets are the evidence,
# the way packages/calibration/web-captures/ is not committed either.
panels/
