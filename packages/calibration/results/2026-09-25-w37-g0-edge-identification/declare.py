"""Pre-score population and no-change stop snapshot; replay only into a fresh copy."""
from pathlib import Path
import json,gzip,hashlib
HERE=Path(__file__).resolve().parent; R=HERE.parents[3]; C=R/'packages/calibration'; E=C/'results/2026-09-25-w37-g0-edge-identification'
for name in ['canonical-stop-baseline.json.gz','e1-declaration.json']:
 if (E/name).exists():raise FileExistsError('refuse to rewrite declaration evidence')
spec=json.loads((R/'apps/reference-apple/scenes.json').read_text()); cv=set(spec['split']['calibration']+spec['split']['validation']); matrix=json.loads((C/'results/matrix.json').read_text())
rows=[r for r in matrix['cells'] if r['key']['profileKey'].startswith('apple-macos-27.0-') and 'standard' in r['key']['profileKey'] and r['key']['sceneId'] in cv]
base={'sourceMatrixSha256':hashlib.sha256((C/'results/matrix.json').read_bytes()).hexdigest(),'meaning':'Expected post-gate values if no material is adopted: exactly the current values. A nonidentity operator needs a prospective price before rendering; neither interval nor baseline is a predicted pass.','rows':rows}
(E/'canonical-stop-baseline.json.gz').write_bytes(gzip.compress(json.dumps(base,sort_keys=True,separators=(',',':')).encode(),mtime=0))
ids=['light-solid__capsule-button__rest','dark-solid__capsule-button__rest','light-solid__rrect-md__rest','dark-solid__rrect-md__rest','light-solid__rrect-ml__rest']
cells=sorted(r['key']['profileKey']+'/'+r['key']['sceneId'] for r in rows if r['key']['web']['renderer']=='webgpu' and r['key']['sceneId'] in ids)
e1={'schema':1,'claims':'c9a §5.181; W37 DL4 draft, not ruled','cells':cells,'renderer':'webgpu','backend':'gpu-texture','sets':['calibration','validation'],'pose':'active','schemes':['light','dark'],'scales':[1,2], 'populationRule':'Standard macOS 27 current matrix rows intersected with five named untinted uniform scenes and declared profile membership; no predicate drop. Fourteen rows at declaration.','scenes':ids,'shellsCss':[-6,0], 'deep':'per-channel median of the same image at centered supplied-shape signed distance d<=-6 CSS px', 'straightDomain':'Centred nominal supplied body. Pixel centres x+.5/y+.5; all four pixel corners inside. Top/bottom x excludes 1.6 radius from each corner; left/right y likewise; no arc pooling, empty straight interval UNMEASURED. Capsules use radius=min(size)/2. Shells [s,s+1) in device px. Native path/isoresponse uncertainty remains separate.', 'estimator':'For each side, shell and RGB channel, mean(abs((web pixel - web deep median) - (native pixel - native deep median))). Maximum across measured bins/channels; populations >=4. Native reference fixed by fixture PNG SHA; no image-dependent mask refit.', 'boundCodes':1,'minimumPixels':4,'reason':'max(1 code, normal repeated per-bin bar); all admitted W34 bars <=0.5 code. One-code allowance covers encoded quantisation, not path uncertainty; canonical repeat bar not newly measured.', 'noReach':'Native excess <=1 is NOT dropped: same bound tests no spurious light. Missing captures, deep or row population -> UNMEASURED, never pass. No inactive row is called passed E1; inactive byte identity is independent.', 'generationBinding':'At adoption read matrix-named capturePath and matching cell metadata, active+receded document hashes, renderer/backend and deterministic state; source receipt; fixed native PNG hashes. If tree absent report UNMEASURED. Recompute from images every adoption/test, never trust a stored verdict.', 'materialAxisAmendment':'Direct encoded RGB excess on a fixed uniform reference separates deep level additively and samples a known spatial boundary, not an inferred blur sigma or structured plate. Native-conditioned coefficient identification plus per-cell web-conditioning transfer stays separate: E1 is actual composite fidelity, not a test of Apple internal layers. One-code errors are resolvable versus observed <=0.5 repeat bar. Body-conditioned transfer can fail E1 with correct shader: record failure, do not refit on web or exempt it.', 'outOfPopulation':'Tints, structured beds, grouped/stacked, accessibility, 26.5, inactive, probes and holdout: no E1 claim; retain their existing independent stops. Continuous arcs lack nominal-whole identification and remain diagnostic.'}
import sys
sys.path.insert(0,str(C/'results/2026-09-23-w34-g0-contour-bed'))
from w35_readers import CanonicalNativeReader
reader=CanonicalNativeReader()
e1['nativeReferences']={cell:hashlib.sha256(reader.read(cell)).hexdigest() for cell in cells}
(E/'e1-declaration.json').write_text(json.dumps(e1,indent=2)+'\n')
print(len(rows),'stop baseline rows;',len(cells),'E1 cells')
