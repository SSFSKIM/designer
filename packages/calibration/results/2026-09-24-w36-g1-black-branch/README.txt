W36 G1 — black branch, sealed and read once (c9a §5.179)

Outcome
-------
Black-only branch shipped to the four macOS 27 endpoint documents. Identity gate0
keeps frozen macOS 26.5 and the renderer default exactly inert. At strength1 the
branch blends both response target and authority below encoded0.003, with a
smoothstep rejoin and no changed Hermite slopes above support. The lowest admitted
packed impulse input is0.0031948897521942854. Thin and thick black ordinates are
explicitly equal by rule; thick black and the open near-black interval are not
identified on this bed.

Endpoint ordinates: light active0.23074004852434915 / receded0.23455058216100522;
dark active0.014443843596092545 / receded0.006995410187265387. Native and GPU deep
black codes are132/133 and32/20 at both scales. CSS active retains+1/+2 from its
existing boundary projection; receded is exact. The middle, chroma and rim are not
claimed closed. No native capture, native app build, merge, push or release occurs.

Order and main evidence
-----------------------
identity-proof.ts/json, join-inputs.json, identity-captures.json: actual digest
path and six endpoint byte comparisons at identity. Identity goldens and on-state
GPU tests precede fitting; no golden hash changes.
fit.py, black-native.json, candidate-ordinates.json: guarded native black reader,
join declaration, ordinates. Candidate documents remain in ~/vitrea-w36/scratch/.
price-final.json is the qualified decision table; the earlier raw tables remain.
512 GPU and512 CSS scratch cells price all admitted non-holdout standard cells,
the black/grey controls, impulse/tint rows and frozen cuts. Frozen stops all hold.
price-gpu-closure.json explains the active-only C1 aggregate and missing scratch
alpha fields, not a changed metric. The final read supplies all786 alpha images.
css-boundary-isolation.json and coherence-mask-decomposition.json preserve the
CSS residual and existing predicate's rim-only exclusions, not new exceptions.

formula-declaration.txt precedes the separable receded colour-formula experiment.
110 inactive cells/tier:74 achromatic GPU controls identical,16 L1 growth failures
and three M2 failures. formula-outcome.json proves both source files restored;
formula-candidate.patch and formula-unit-case.ts.txt retain the declined experiment.
Two impulse/lg probe coherence gaps are inherited, not caused by that formula.

sealed-manifest.json records the four old/new digests and full file hashes.
Seal2f49d390 precedes configuration registration26be8ede. The cross-gate ledger
results/holdout-configuration/configuration-log.json holds configuration5, BEFORE
any canonical pass. canonical-read.py has exclusive start receipts, sealed hashes
and committed-registration guards; browser-runs.txt records each X6 preflight,
foreign-process count, command and exit. Completed read:268 calibration,
64 validation,330 ladder,124 holdout =786. The holdout is SPENT at this configuration.
Do not invoke the canonical runner again or reread W34's holdout.

Split, integrity and cuts
-------------------------
The split retired509 light /277 dark W33 rows to superseded/6e509c7f76cc.json and
eab099cc6698.json. Working matrix1893 =1107 frozen +786 new. split-apply.txt is
the initial atomic refusal; split-apply-shared.txt is the successful apply after
the additive sharedReceded fix. Old aliases are never repointed. index-history.py
proves13 old entries,24 aliases and three metadata fields byte-identical.
shared-receded-{red,green}.txt records failing-before/passing-after synthetic apply
cases; the existing seven classifier cases pass too. A premature read-witness.txt
is empty because the first refused split had no retired files; successful output
is read-witness-completed.txt. No recorded number is replaced.

append-witness.py reconstructs the pre-split2679-row matrix from actual raw row
slices, checks its SHA against before-manifest.json, and compares the read against
the seal's1893-row matrix blob, whose SHA equals before-read.json. All six read
clauses pass. W33's append-check.py runs IN PLACE with this directory's explicit
--manifest and --json paths; all six split clauses pass, including raw reconstruction.
These are immutable-data checks, not browser runs.

chroma-cut.py/json, exterior-cut.py/json, black-cut.py/json and l1-cut.py/json name
the new shipped hashes. M1 medians unchanged; M2 all26 cells0% growth against W33,
cumulative drift -2.774796% to+1.965563% retained in m2-rebaseline.json. C1 maximum
0.0038247143 against0.0042. X1 all218 pass:0 positive pixels of12,681,980 integer
or12,593,524 analytic eligible pixels. L1 population140/measured136, same2 absolute
misses/4 missing means, growth0. B1's six windows unchanged. Existing coherence
referee holds. Gated230/786, PREDICATE_EXCLUDES67 and MISSED_27_ROWS8 unchanged;
formal L1 adoption is G2. read-calibration-tests.txt:47 files/678 passed, no skip.

read-witness.json and read-pixel-witness.json compare all786 cells to W33:785
normal PNGs and all786 alpha PNGs identical. holdout-difference.py/json decomposes
the sole CSS holdout difference:five exterior pixels +1 grey code, identical inputs
above branch support, shape and alpha exact, cause UNIDENTIFIED. All adopted stops
pass. No retry or fit follows it.

Eye and closing tests
---------------------
sheets.py, sheet-readings.json and sheets/:18 raw native/shipped/candidate
triptychs, with absolute OKLab×8 panels and the137.097415/255 one-byte-at-black
check beside each. eye.txt records all18 viewed: black body closes; angular inner
line/contour and photo chroma/grey level gaps remain. No new eye-only regression.
A deployed demo comparison belongs to the parent's G2 landing, not this worker.

final-verification.json preserves the first closing chain, including one stale
platform black-ramp oracle failure. pixel-oracle-before.ts.txt and the diagnostic
logs retain its decomposition: evaluated rim amplitude and the selected anchor
were missing from the oracle. The corrected production mirror and removal of the
obsolete >20-code black overshoot assertion make monotonicity include step0.
final-platform-e2e-corrected.txt:410 passes. No sealed material changed. Initial
post-diagnostic lint failed only on console.log; final-platform-lint-clean.txt
passes after its removal. closing-verification.json consolidates the actual logs.
Build/lint pass; units2757; goldens34; GPU49; platform410; React174 pass/3 declared
unsupported-engine skips; freeze1818; worktree capture tree786/786 matches.

Landing and replay boundaries
-----------------------------
The main checkout's old capture tree was verified and read ONLY. New pixels are
in this worktree's packages/calibration/web-captures/ and are gitignored. Before
removing this worktree, the parent must archive the old canonical generation by
active document hash, copy this tree to the canonical location, then run
pnpm --filter @vitrea/calibration --fail-if-no-match run check-capture-tree there.

The ledger §5.179 is the decision record; the charter Tracking Map is delivery,
not merge approval. CPU producers generally write exclusive evidence outputs;
replay their readers into fresh scratch outputs, never overwrite these readings.
The standalone shared-receded selftest and package suites are ordinary repeatable
tests; any live browser run still needs the X6 launcher. Do not rerun final-verify.py
under its already used log names. No extra holdout is a verification step.
